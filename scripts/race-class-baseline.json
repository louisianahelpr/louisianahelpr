{
  "_doc": "Grandfathered hits of scripts/check-race-class.mjs, as of 2026-09-12. May only SHRINK: a key that stops matching fails src/test/raceClassGuard.test.ts until deleted here. Never add a key to get green — lock the read or add the status predicate instead.",
  "allow": {
    "sql:public.notify_on_application": "Reads only title + customer_id (never change after posting) to address a notification; no status decision.",
    "sql:public.flag_suspicious_review": "Fraud heuristic over budget/timestamps; a stale read mis-scores one flag, never a lifecycle or money state.",
    "sql:public.enforce_open_job_limit": "COUNT across a poster's open jobs (a set, not a row lock); a race lets one extra open job through, cap is soft abuse control.",
    "sql:public.notify_helpers_on_job_post": "Fan-out of job-match notifications on the INSERTed job's own NEW values; jobs read is a candidate filter only.",
    "sql:public.notify_helper_on_tip": "Reads job title for notification copy after payment_status flips; no decision on job state.",
    "sql:public.enforce_review_validity": "Gates a review INSERT on job completed + participant; completed is terminal, so a concurrent cancel cannot un-complete it. Not yet re-audited under this class.",
    "sql:public.sweep_job_start_reminders": "Cron reminder sweep; worst race outcome is one reminder on a job cancelled in the same instant.",
    "sql:public.sweep_no_show_alerts": "Cron alert sweep; worst race outcome is one alert on a job that changed status in the same instant.",
    "sql:public.sweep_daily_job_digest": "Daily digest notification over open jobs; informational only.",
    "sql:public.detect_suspicious_user_patterns": "Aggregate fraud-flag cron over jobs; writes only fraud_flags for admin review.",
    "sql:public.detect_stuck_payments": "Cron detector writing error_logs + admin notifications; no money movement.",
    "sql:public.reject_other_applications_on_accept": "Ownership/membership EXISTS over applications JOIN jobs; caller must already own the job. Not yet re-audited under this class.",
    "sql:public.rpc_decide_dispute": "Reads customer_id/helper_id/title (participants, immutable once hired) for notifications; dispute row itself is the decision. Not yet re-audited under this class.",
    "sql:public.notify_helper_application_viewed": "Reads job title for notification copy; no decision on job state.",
    "sql:public.respond_to_direct_offer": "Main status read is FOR UPDATE (raced 0/20 on prod 2026-09-12); remaining hit is the INSERT ... SELECT FROM jobs for the decline notification after the lock is held.",
    "sql:public.sweep_dayof_confirm_reminders": "Cron reminder sweep; worst race outcome is one reminder on a job that changed in the same instant.",
    "sql:public.enforce_application_credential_tier": "Reads credential_tier to gate the application INSERT; tier is set at posting. Not yet re-audited under this class.",
    "sql:public.sweep_release_last_chance": "Cron notification sweep; informational only.",
    "sql:public.apply_to_job": "Status read is already FOR SHARE; remaining hit is the self-apply EXISTS on customer_id, which never changes on a live job.",
    "sql:public.reject_pending_job": "Admin rejection of a pending post; reads business_id + notification copy. Not yet re-audited under this class.",
    "sql:public.notify_user_on_review": "Reads title + customer_id for notification copy; no decision on job state.",
    "sql:public.purge_user_data": "Account deletion; the jobs read selects the deleting user's own unfunded, unhired jobs. Not yet re-audited under this class.",
    "sql:public.settle_dispute_record": "Closes the dispute row from the job's settled money state; called after the money write. Not yet re-audited under this class — candidate for FOR SHARE.",
    "sql:public.notifications_fill_job_id": "EXISTS check that a job id is real before stamping notifications.job_id; no lifecycle decision.",
    "sql:public.snapshot_application_job_point": "Copies rounded job coordinates onto the application at INSERT; coordinates are not lifecycle state.",
    "sql:public.open_dispute_as": "Primary job read is FOR UPDATE; remaining hit is a velocity/fraud-flag read in the same body. Not yet re-audited under this class.",
    "client:src/components/JobConfirmation.tsx::opaque:{ [field]: new Date().toISOStr": "Computed-key day-of confirmation stamp; field set is helper_dayof/poster confirmations. Not yet re-audited under this class.",
    "client:src/components/JobTracking.tsx::opaque:patch": "stampJob(patch) helper for poster tracker stamps; payload typed TablesUpdate<\"jobs\">. Not yet re-audited under this class.",
    "client:src/components/PhotoProof.tsx::opaque:updateField": "Payload is proof_before_urls / proof_after_urls only (not lifecycle); opaque to the parser because it is a variable.",
    "client:src/components/activity/CompletionChoiceSheet.tsx::status": "Legacy revision_requested write kept for pre-RPC read paths. Not yet re-audited under this class.",
    "client:src/components/activity/EditJobDialog.tsx::opaque:updateData": "Poster edit of job details (title, schedule, expires_at); lifecycle columns not in the object. Opaque because it is a variable.",
    "client:src/components/activity/appliedJobCard/DisputedSection.tsx::opaque:patch": "Payload is dispute_helper_response (+ dispute_status), not job status; opaque because it is a variable.",
    "client:src/components/admin/AdminDisputes.tsx::status": "Admin-only dispute close; admin is the arbiter of that job's status. Not yet re-audited under this class.",
    "client:src/components/admin/AdminJobs.tsx::status": "Admin-only removal (status cancelled); intended to win over any concurrent state. Not yet re-audited under this class.",
    "edge:supabase/functions/create-payment/index.ts::payment_status+status#2": "admin_refund_general full-refund flip (status cancelled, payment_status refunded) matched on id only. Audited 2026-09-14: UNSAFE (a concurrent Quick Release on a disputed job is overwritten). Fix pending in the dispute-races branch, deliberately not edited here.",
    "edge:supabase/functions/execute-dispute-split/index.ts::opaque:jobPatch": "Settlement flip is a CAS on .in(payment_status, [escrow, payout_pending, released, refunded]) writing payment_status + fee columns only; the resume states let a release and a refund overlap. Audited 2026-09-14; fix (settlement claim) pending in the dispute-races branch, not edited here."
  },
  "_doc_safe": "`safe`: edge lifecycle writes AUDITED 2026-09-14 (docs/audit/lifecycle-writes-audit-2026-09-14.md) and proved safe, or fixed with a guard the scanner cannot read, each with its reason. Same rules as `allow`: exact keys, may only shrink, and a new hit beside one of these still fails. The usual reason is a compare-and-set on payment_status, which the scanner does not read as a predicate (it only reads .eq/.in on `status`).",
  "safe": {
    "edge:supabase/functions/_shared/releaseFlip.ts::opaque:{ payment_status: \"released\", ": "SAFE: CAS .in(payment_status, [payout_pending, released]) with .select(id), zero rows reported and not retried; writes payment_status + caller commission columns, never status. Opaque only because of the ...extraFields spread.",
    "edge:supabase/functions/create-payment/index.ts::opaque:{ stripe_session_id: newSessio": "FIXED 2026-09-14: stampSession is a CAS on stripe_session_id AND payment_status IN (null, unpaid, abandoned, failed); the second predicate closes redeem_gift_card flipping unpaid -> escrow without touching the session id. Opaque only because of the ...extra spread; the payload never carries status.",
    "edge:supabase/functions/create-payment/index.ts::payment_status+status": "FIXED 2026-09-14: cancel_escrow's fallback flip, reached only after the status-pinned flip matched zero rows AND a re-read shows our 'cancelling' claim still held (status moved mid-refund, e.g. open_dispute_as). CAS .eq(payment_status, cancelling) + .select(id), paged to ops: the refund already went out, so cancelled must win over a dispute that would otherwise let Quick Release pay the Helpr.",
    "edge:supabase/functions/stripe-webhook/handlers/chargeDisputeClosed.ts::payment_status": "SAFE: warning_closed unblock is a CAS .eq(payment_status, chargeback) + .or(dispute_status as read) -> the pre-chargeback state with .select(id); disputed_at is cleared only when a card-dispute status owns it and no unexecuted dispute or reversed transfer exists (2026-09-14). The outcome write is markers only (dispute_status, dispute_resolved_at), CAS on a card-dispute status. Stripe event-id dedupe in stripe_webhook_events.",
    "edge:supabase/functions/stripe-webhook/handlers/chargeDisputeCreated.ts::payment_status": "FIXED 2026-09-14: the chargeback block is a CAS .in(payment_status, [payout_pending, escrow]) with .select(id), payment_status only, so a payout that settled or a cancel_escrow mid-refund is never overwritten. The markers (dispute_status, disputed_at) are a separate CAS .or(no markers | card-dispute status) with .select(id), so an internal dispute or reversal hold is never overwritten either (2026-09-14).",
    "edge:supabase/functions/stripe-webhook/handlers/chargeRefunded.ts::payment_status": "SAFE: fires only on Stripe's own FULL-refund confirmation (amount_refunded >= amount), deduped per event id; 'refunded' is ground truth for the charge and overwriting any prior state only ever stops further money movement. No status write.",
    "edge:supabase/functions/stripe-webhook/handlers/checkoutSessionCompleted.ts::opaque:updateData": "SAFE: records a CAPTURED checkout (Stripe ground truth), deduped per event id, and must win over local state: a guard would drop the only record of captured money. A job cancelled while paying lands cancelled+escrow, which void-cancelled-payments refunds; two completable sessions are prevented upstream by create-payment's expire + Stripe check. Payload is PI id, payment_status, tax; never status.",
    "edge:supabase/functions/stripe-webhook/handlers/paymentIntentPaymentFailed.ts::payment_status": "SAFE: CAS .or(payment_status is null, eq unpaid) -> failed with .select(id); a stale event against a funded job matches zero rows and is acked.",
    "edge:supabase/functions/stripe-webhook/handlers/transferCanceled.ts::payment_status": "SAFE: CAS .eq(payment_status, released) -> payout_pending, behind the payout_transfers ledger flip for this transfer id; never regresses a refunded or charged-back job.",
    "edge:supabase/functions/stripe-webhook/handlers/transferCreated.ts::payment_status": "SAFE: a ledger CAS (payout_transfers pending|paid -> paid) gates it, then CAS .in(payment_status, [payout_pending, escrow, released]) -> released with .select(id); a refunded or cancelled job matches zero rows.",
    "edge:supabase/functions/stripe-webhook/handlers/transferFailed.ts::payment_status": "SAFE: CAS .eq(payment_status, released) -> payout_pending after the ledger row for this transfer id is marked failed; never regresses a refunded or charged-back job.",
    "edge:supabase/functions/stripe-webhook/handlers/transferReversed.ts::payment_status": "SAFE: the freeze is a CAS .eq(payment_status, released) -> payout_pending + reversal_hold markers; never regresses a refunded or charged-back job.",
    "edge:supabase/functions/void-cancelled-payments/index.ts::opaque:patch": "SAFE: settleCancelledJob is a CAS .eq(payment_status, escrow) with .select(id) and a loud zero-row path, on rows selected at status = cancelled, which has no outgoing transition in enforce_job_status_transition. Patch is payment_status + cancellation_fee_status only.",
    "edge:supabase/functions/void-cancelled-payments/index.ts::payment_status": "SAFE: markAbandoned is a CAS .eq(payment_status, unpaid) -> abandoned with .select(id); a checkout that completed since the read matches zero rows. No status write."
  }
}
