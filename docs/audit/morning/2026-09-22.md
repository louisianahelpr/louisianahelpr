# Morning report: 2026-09-22 (run 07:31 PDT)

**8 things need attention**

- **The prod database is down for real users right now.** At 07:35 PDT, two direct reads of `open_jobs_browse` returned HTTP 503 `PGRST002` ("Could not query the database for the schema cache"). The auth health endpoint still returns 200. It has been failing since at least 04:03 PDT: Uptime at 04:03 PDT found no database answer within 10 s, and the nightly backup's `pg_dumpall` got "authentication query failed". This is the same outage `docs/OPEN.md` recorded in 44c8cede1. Check the Supabase dashboard (project `fncmgoasalhdgfwzhsqa`, status and resource use). No code change can fix it.
- **Most of today's red CI runs come from that outage, not from code.** These failed because the database never answered:
  - **E2E real backend:** the anon surface contract got a 503 from `open_jobs_browse`. This is issue #1637.
  - **E2E user journeys:** minting a poster token timed out 3 times, on both the Chromium and WebKit jobs. The last run on main at 08:32Z was cancelled, and no newer run has finished. This is issue #1595.
  - **DB drift detector:** type generation got `PGRST002`.
  - **DB backup (nightly):** the pooler could not authenticate. **There is no backup from last night.**
  - **Uptime:** reported DOWN.
- **Prod audit is red, and some of it may not be the outage.** In run 35705632232 (08:35Z), all 11 `/admin?view=…` screens timed out after 1.5 min without rendering, which is probably the outage. But two failures ran for 0 ms, which usually means a setup or fixture problem rather than a slow backend: `activity-loading-reserve` (my-jobs) and `activity-tabs-visible` (my-posts @320). Re-run once the database is back to tell them apart. This is issue #1618 (marked stale).
- **Press every control is red.** The latest run on main (07:49Z, shard 4/4 at 375) failed, and the log does not show a clear assertion; the build-step warnings look unrelated. Re-run it after the database recovers. This is issue #1582 (marked stale).
- **"Nightly reds nobody read" fails on every push**, because issues #1618, #1595 and #1582 have been open longer than the grace period. It will stay red until they are fixed or acknowledged.
- **One migration push failed last night.** db-deploy run 35657538818 (21:30Z, 13fad2aa1) failed with "Direct deletion from storage tables is not allowed. Use the Storage API instead" (SQLSTATE 42501). The next db-deploy (21:35Z, 3f4679be6) was green, but a later green does not clear an earlier red. Check that the migration which deletes storage rows was reworked and not just skipped.
- **New Sentry errors in the last 24h all look like the outage.** Each affected 1 user:
  - JAVASCRIPT-27 "Loading notifications timed out" (9 events, first seen 8h ago)
  - JAVASCRIPT-2A "Xr" (8 events, 5h)
  - JAVASCRIPT-29 and JAVASCRIPT-1P "realtime channel CLOSED" (4 events each)
  - JAVASCRIPT-2B and JAVASCRIPT-2C "AuthRetryableFetchError: HTTP 504" (3h)
  - JAVASCRIPT-1H "socket closed: 1006" (24 events, 21h)

  "Xr" is a minified name that tells us nothing. Open PR #1639 (normalise plain Supabase error objects before sending them to Sentry) is aimed at exactly this kind of unreadable title.
- **20 old "URGENT: main broken" issues are still open, from July and August** (#922 to #1557). Several are duplicates, such as #1252/#1254/#1255/#1256 and #1319/#1321. They look stale, because Test, Vitest and E2E smoke are green on main today. Closing them is the owner's call.

## Production
- **Production is current.** It serves `fad35f45d`, which is the newest commit that ships (06:50 PDT). The site itself answers; only the database behind it is down.

## GitHub Actions (since 2026-09-21 07:30 PDT)
- This window had roughly 1,600+ runs. The repo runs about 400 in 7 hours, and the scan fully covered 11:27Z (Sep 21) to now.
- **Green now on main:** Test, Vitest, Vacuity, E2E happy-path smoke, Nightly WebKit, Race Runner, Prod freshness, Prod errors, UI sweep, Mobile viewport, Bundle size, Broken links, Sentry release.
- Test, Vitest and Vacuity were red many times overnight (Test 70+, Vitest 20+, Vacuity 20+). All of them have been green since 13:09Z or earlier, and no red remains on the latest commit.
- **Red on main right now:** E2E real backend, E2E user journeys (last run cancelled), Prod audit, Press every control, DB drift detector, DB backup, Uptime, Nightly reds nobody read. The causes are in the list above.
- PR #1639 (`fix/sentry-…`) has red E2E real backend and Nightly-reds checks. Both come from the same outage.

## Issues
- **nightly-red:**
  - #1637 e2e-real-backend (new today)
  - #1618 prod-audit (stale)
  - #1595 e2e-journeys (stale)
  - #1582 press-every-control (stale)
- **prod-errors:** none open.
- **URGENT in the title:** 20 open, all from 2026-07-01 to 2026-09-07. See the last bullet at the top.

## Cloud routines
- **Not fully checked.** The routines API returned only its first page: 20 finished one-off PR reminders, all disabled, the last from 2026-09-16. The list tool cannot load later pages, so the 8 daily routines were not visible.

## Pull requests
- **Need the owner:**
  - #1639 fix(sentry): normalize plain Supabase error objects (today; red only because of the outage)
  - #1624 and #1623: two overlapping package-lock resyncs from 2026-09-20, both red. Probably only one is wanted.
  - #1622 docs: db-drift schedule (red)
  - #1621 perf: lazy-load Sentry (red)
  - #1607 role-neutral copy (draft; green, waiting on an owner look at 375)
  - #1529 fix(e2e) Playwright fixes (since 2026-09-03; 6 failing checks)
- **Dependabot:** #1630, #1620, #1599, #1550, #1549 are all red.
- **Merged in the last day:** none. All work lands directly on main.
- **Closed by this report: none.** No automated PR was both older than 3 days and failing. #1621 and #1622 match the automated-branch pattern but are only 2 days old.

## Open list (docs/OPEN.md)
- 205 unchecked and 275 checked.
- About 30 new unchecked items in the last 24h. Most are about the vacuity and guard work:
  - 49 guards strip the code they inspect
  - `error-state-sweep` is still unproven
  - 7 route names render one screen
- Launch-relevant items:
  - No guard asserts that a Stripe outflow has a ledger row (`cash-out-credits`).
  - `verification-webhook` tests only its Checkr branch.
  - The App Store screenshots should be captured from the finished app.
  - An iPad screen shows "0 jobs" above 7 job cards (not reproduced yet).
  - The Notifications panel sometimes shows an error screen instead of its content.

## Main (last 24h)
- 244 commits:
  - About 70 vacuity and test-strength work (test(vacuity), vacuity, fix(vacuity), ci(vacuity))
  - 26 test and 5 test(class)
  - 14 fix(e2e)
  - About 23 docs and OPEN updates
  - 3 fix(money), 2 fix(migration), 2 fix(map), 2 fix(prod-audit)
  - A few ci and ui commits, including "the loading mark breathes instead of spinning"
