- [ ] **Q299 Two declared journey outcomes are still not driven on the real backend: "cancelled" and "disputed" (found by the Q253 guard, 2026-09-23).** e2e/journeys/scenarios.ts OUTCOMES declares both; no e2e/journeys spec's scenarioTitle/title call names either (02-marketplace's J6 dispute leg is gone; nothing cancels a funded job as a journey). Build each on prod with the shared accounts (Stripe TEST), is_seed, cleaned up; a cancel_with_helper strike must never land on poster-e2e (use a throwaway via e2e/journeys/throwaway.ts if the leg records one). GUARD: src/test/journeyOutcomesDriven.test.ts holds both in KNOWN_UNDRIVEN_OUTCOMES (two-way: driving one fails the test until its entry is removed).
- [ ] **Q300 One full admin refund sends the poster TWO notifications (measured by the Q226 journey, 2026-09-23).** admin_refund_general writes "Refund issued" (create-payment/index.ts), then stripe-webhook's charge.refunded handler writes "Refund processed" for the same refund (handlers/chargeRefunded.ts: every FULL refund of a job PI notifies, whoever issued it). Run 2 of e2e/journeys/04-admin-safety.spec.ts: poster notifications on the job = Partial refund issued, Refund issued, Refund processed. Same handler also upserts a payment_refunds row with source 'stripe_dashboard' and amount_cents = charge.amount_refunded (CUMULATIVE); create-payment's upsert won the race in both runs (rows: admin_refund_general 500 + 2300), but if the webhook wins, the ledger row is mislabelled and over-states that refund (code read, NOT measured). Likely also on cancel_escrow (NOT checked). Fix: the webhook skips the notification (and the ledger write) when a payment_refunds row for that stripe_refund_id already exists or the refund's metadata names an app source; guard: the journey's notification assertion tightened to exactly two titles (delete the Q300 tolerance lines in 04-admin-safety.spec.ts) plus an edge test on chargeRefunded.
