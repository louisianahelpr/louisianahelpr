10 things need attention

Morning report for 2026-09-24, written about 14:40Z. This file stays local and is not committed.

## Needs attention, most important first

1. **Press every control has been red for 11 days.** Its nightly-red issue #1582 dates from 09-13. The last run (35976390920) sent 1240 backend requests per minute, against a budget of 400. Its clean-up step also failed with "no .env" while it was setting up the incomplete-e2e test account, and 1 leftover test row was not cleaned up.
2. **The quota monitor is red** (issue #1770, run 36007401257). It says the connection pools may hold 58 database connections but only 57 are usable, so pg_cron would be refused under load (Q317). Commit 5f021a5e5 ("count PostgREST's LISTEN connection") landed at 13:45Z. That was after the last monitor run at 13:41Z, so it has not been re-measured yet.
3. **The E2E user journeys are red** (issue #1719, run 36003048625), for two reasons:
   - 747 backend requests in the busiest minute, against a ceiling of 400.
   - One per-test budget is now too loose: 65.8 is under half of 135.3, so the budget has to drop to 65.8.
4. **The prod audit is red** (issue #1754, run 35999386803). Four checks failed:
   - The messy-input login sweep timed out after 30 seconds.
   - The layout-shift check failed at 1440px.
   - The phone-width section-gap check failed.
   - 684 requests per minute, against a budget of 400.
5. **Loading states refresh is red** (issue #1654, run 35970927275). It found 14 loading-state problems on a customer profile page and on /rules (media, rows and jumps). It also found 9 stale entries that must be removed from docs/audit/loading-states/baseline.json.
6. **"Nightly reds nobody read" keeps failing** (issue #1657). It is still red at 14:17Z, because several of the issues above are past their grace period. It clears only when they are fixed or closed.
7. **A11y in WebKit (prod) did not finish.** It was cancelled three times in a row (04:23, 04:39 and 04:54Z). The last completed run was a success at 09-23 17:29Z, so there is no result after that. A cancelled run is a hidden red.
8. **Two nightly-red issues are open for workflows that are now green.** #1742 (e2e-real-backend) and #1731 (vacuity) should close on their next green run. If they don't, the auto-close is broken.
9. **20 old URGENT issues are still open:** #922, #966, #1130, #1252–1256, #1319, #1321, #1367, #1387–1390, #1447–1449, #1457, #1517 and #1557. Most name workflows that are green today (Test, Vitest, E2E smoke, DB and functions deploy), so they look stale. I did not verify each one.

10. **The ops alert list has 25 open alerts** (as of 14:35Z): 1 critical, 20 errors and 4 warnings. Read them with `node scripts/ops-alert-ledger.mjs list`. The top ones:
    - A critical user report from 08-30 that is still unread (/admin?view=health).
    - The Stripe test balance is below the payout threshold. It has fired 8 times, most recently at 13:42Z, and it needs a test-mode top-up before payouts fail.
    - Sentry session replays are over quota.
    - The quota monitor cannot read edge function invocations.

## Everything else

- **Production is current.** The live site serves 35a96fa06, which contains the expected deploy commit ae1378c92.
- **Recovered since yesterday and green at their latest run:** Test, Vitest, Prod errors, Race Runner, Edge Function Smoke, E2E real backend, Supabase DB Deploy, Expiry monitor, Staleness watch and Vacuity. No workflow is disabled.
- **Coverage gap in my check:** GitHub returned at most 1000 runs per query, and one slice was full. So runs between 09-23 14:30Z and 09-24 03:30Z were not all read. The red list above comes from each workflow's latest run.
- **No open prod-errors issues.**
- **Sentry (org helpr-4m):** no new issues in the last 24 hours and no regressed issues.
- **Cloud routines (RemoteTrigger):** the only scheduled routine is "Mac cleanup", which is enabled and next runs 09-28. There were 19 one-off cloud routines, fired 09-23 between 17:31 and 19:00Z, and all show "fired".
- **Cloud branches:** 42 of 43 cloud/ branches have commits whose content is not an exact match on main. Some were landed by hand with edits, so this is an upper bound. I did not check each branch.
  - Newest: cloud/q272-press, cloud/q232-admin, cloud/q274-q244-q235, cloud/q223-q255-q182 and cloud/q275-sentry.
- **Pull requests:** there are 2 open, both bot refresh PRs: #1740 (write-contract) and #1722 (scoreboard). Neither has a failing check, and neither needs you. No PRs were merged since yesterday.
- **Stale automated PRs:** none qualified, so I closed nothing.
- **Open work list (docs/OPEN.md):**
  - The file's own summary says 227 done, 27 partly done and 104 open.
  - A raw count of every checkbox, including sub-items, gives 229 checked, 27 partly done and 394 unchecked.
  - Added in the last 24 hours: 192 new or rewritten unchecked lines. Among them are:
    - owner steps: Q207 and Q209 (subscribe the Stripe webhook to charge.dispute.funds_withdrawn), Q192a (Auth URL allow-list) and Q144 (the launch email switch)
    - the card-dispute protection work: Q202, Q208 and Q210
    - WCAG debt: Q211 and Q212
    - spacing and layout: Q190, Q191 and Q213–Q215
    - a skeleton-shape cleanup: Q201
- **Main in the last 24 hours:** 564 commits.
  - By type: 229 fixes, 143 docs, 79 chores, 51 tests, 37 features, 8 perf, 8 WIP, 3 CI, 2 copy and 1 refactor.
  - Money and disputes: the ME-009 to ME-017 fixes, plus the chargeback evidence deadline (AM-002).
  - Ops alerts and monitors: seed disputes go to the digest, log queries were moved to the new logs table, and expiry-monitor Vercel and Apple date handling was fixed.
  - Cron: review-nag and stalled-completion sweeps now run 8 at a time, and pg_net response ids were fixed.
  - Test harness: prod-audit fixtures, press-sweep cleanup and Stripe Checkout retries.
  - Performance: zod moved off the boot path (−23 KB gzip per route), and social login is now lazy-loaded.
  - Native: the privacy shield on pause, keyboard inset, and the inert background mode was dropped.
