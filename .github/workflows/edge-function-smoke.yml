name: Edge Function Smoke Test

# Verifies that critical edge functions boot and respond after deploy.
# Catches Deno import errors, missing env vars, and runtime crashes that
# only surface when functions are actually invoked.
#
# Runs:
# - After every push to main that touches edge functions (post-deploy check)
# - Once daily on schedule (catches secret expirations / Supabase outages)
# - Manually via workflow_dispatch
#
# Note: Reduced from hourly to daily to conserve GitHub Actions minutes.
# Real outages will be caught by Sentry / user reports much faster than CI.

on:
  # DISABLED auto-triggers — daily cron + post-deploy push were failing
  # noisily because CRON_SECRET isn't configured as a repo secret. Sentry +
  # user reports catch real edge-function outages much faster anyway.
  #
  # To re-enable, add CRON_SECRET under Settings → Secrets → Actions, then
  # uncomment the `push:` and `schedule:` blocks below.
  #
  # push:
  #   branches: [main]
  #   paths:
  #     - "supabase/functions/**"
  # schedule:
  #   - cron: "0 13 * * *" # daily at 13:00 UTC (08:00 CT)
  workflow_dispatch:

concurrency:
  group: edge-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: Health-check edge functions
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Wait for deploy to propagate
        # Edge functions deploy automatically but take a few seconds to be live.
        run: sleep 30

      - name: Hit health-check endpoint
        # health-check requires server-to-server auth via CRON_SECRET (Bearer token).
        # If CRON_SECRET isn't configured as a repo secret, the step is skipped
        # rather than failing — the hourly schedule will still catch real outages
        # once the secret is added.
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN0ZWlnZHdycGtvc2JpeWNzaHd6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzMxODUxOTUsImV4cCI6MjA4ODc2MTE5NX0.OzTZGTj86ZZyudkGVp8go0b0ORpg--h2dVh2qxKLxQI
        run: |
          set -e
          URL="https://steigdwrpkosbiycshwz.supabase.co/functions/v1/health-check"
          if [ -z "$CRON_SECRET" ]; then
            echo "⚠️  CRON_SECRET repo secret not set — skipping health-check probe."
            echo "    Add it under Settings → Secrets and variables → Actions to enable this check."
            exit 0
          fi
          echo "Pinging $URL"
          HTTP_CODE=$(curl -s -o /tmp/response.json -w "%{http_code}" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$URL")
          echo "HTTP $HTTP_CODE"
          cat /tmp/response.json
          echo ""
          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ health-check returned $HTTP_CODE — edge functions are unhealthy"
            exit 1
          fi
          echo "✅ Edge functions are responding"

      - name: Verify Stripe public key endpoint
        # get-stripe-public-key validates the caller's JWT, so we just confirm
        # the function boots (anything other than 5xx means the runtime is alive).
        env:
          SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN0ZWlnZHdycGtvc2JpeWNzaHd6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzMxODUxOTUsImV4cCI6MjA4ODc2MTE5NX0.OzTZGTj86ZZyudkGVp8go0b0ORpg--h2dVh2qxKLxQI
        run: |
          set -e
          URL="https://steigdwrpkosbiycshwz.supabase.co/functions/v1/get-stripe-public-key"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            "$URL")
          echo "get-stripe-public-key: HTTP $HTTP_CODE"
          # 401/403 = function booted and rejected unauth call (expected, healthy)
          # 5xx    = function crashed on boot (unhealthy)
          if [ "$HTTP_CODE" -ge 500 ]; then
            echo "❌ get-stripe-public-key returned $HTTP_CODE — function crashed on boot"
            exit 1
          fi
          echo "✅ Stripe public key endpoint healthy (HTTP $HTTP_CODE)"
