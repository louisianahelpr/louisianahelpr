# Louisiana Helpr morning report — 2026-09-13

**8 things need attention.**

1. **The production backend is DOWN.** At 14:40 UTC, every request to the prod Supabase project (`fncmgoasalhdgfwzhsqa`) returned 504 Gateway Timeout after about 5 seconds, both sign-in (`/auth/v1/health`) and data (`/rest/v1/open_jobs_browse`). The website loads, but nobody can sign in or load jobs. The database pooler has been refusing connections since at least 11:05 UTC (drift detector), and error screens reached real users by about 04:00 UTC (Sentry: "Loading jobs timed out", "We couldn't load jobs."). Supabase's status page lists no matching outage, so this looks specific to this project: it could be paused, out of resources, or unhealthy. Check the project in the Supabase dashboard first. Every red nightly run below follows from this.
2. **Almost every nightly run against prod is red, because of the outage.** These runs couldn't sign in (504) or couldn't reach the database (timeout): e2e-real-backend (money loop and anon contract), e2e-journeys, e2e-abuse-notifications, a11y-webkit-prod, prod-errors, DB backup, write-contract-refresh and db-drift-detect. None of them tested the app itself, so re-run them once prod answers.
3. **Last night's database backup did not happen.** `pg_dumpall` timed out connecting to the pooler, so there is no nightly backup for 2026-09-13.
4. **Press every control found 5 failures on prod.** Four are the Apple and Google sign-in buttons on /login and /signup, which got a 504 from `/authorize` (the outage again). The fifth is real and unrelated: on the 404 page, "Go Back" logs 404s for `speed-insights/script.js` and `insights/script.js`, so the Vercel analytics scripts are missing on prod.
5. **The mocked happy-path smoke is still red** (run 34749614576, the latest on main). `/dashboard` fails the axe colour-contrast check at 375-dark and 1440-dark. The nightly WebKit run fails the same two, plus `activity-card-density.spec.ts:676` (the tap-toggle count) and `apply-dialog-fit.spec.ts:190` at 375 and 320. These are the mocked specs, so reproduce on prod before fixing. This is already on OPEN.md.
6. **The seeded a11y axe sweep is broken** (run 34749614456). Every variant fails with "No tests found", probably because the mocked visual sweep spec it pointed at was deleted in 605df3d6f. This isn't on OPEN.md yet.
7. **A cloud routine pushed straight to main, against its own rules.** The Edge Functions Health routine committed `5ab35263f chore: update package-lock.json` directly to main, bypassing branch protection ("Bypassed rule violations"). Its prompt says never commit to main. The change was only lockfile noise from `npm install`, but that routine's stop hook keeps pushing it to commit.
8. **The Supabase advisor routine did not run.** Its Supabase connector is unauthorized, so no security or performance advisors were checked today. The owner needs to reconnect Supabase (and Slack and Canva) in claude.ai connector settings.

Also worth knowing:
- Production is current: it serves `ef18b5af1`, which includes the newest shipping commit `f5bf06ec8`.
- Vitest and Test went green again on main at 09:30 UTC, after being red earlier this morning.
- 21 old "URGENT: main broken" issues are still open, some from July. Most look stale and should be checked and closed.

---

## 0. Is production current?
Yes. The live build stamp is `ef18b5af16181983b278e728a52863b30e131d52`, and the newest commit that ships is `f5bf06ec8`, an ancestor of it. The frontend is current, but the backend is down (see above).

## 1. GitHub Actions since 2026-09-12 07:30 local
There were 100 runs in the window, all on main except one dependabot branch.

**Latest run is red:**
| Workflow | Run | Cause |
|---|---|---|
| Press every control | 34760940917 | 4 presses failed on Apple/Google sign-in (504 from auth); 1 on the 404 page "Go Back" (Vercel `speed-insights/script.js` and `insights/script.js` 404) |
| A11y in WebKit (prod) | 34760803024 | Sign-in for the poster failed with 504 Gateway Timeout, so the Chromium and WebKit sweeps both stopped at test 1 |
| E2E abuse & notifications | 34760792812 | Couldn't mint a poster token (504 three times) |
| Nightly WebKit happy-path | 34759867360 | Mocked spec failures: /dashboard dark contrast (375, 1440), activity-card-density:676, apply-dialog-fit:190 at 375 and 320 |
| E2E user journeys | 34759828803 | Couldn't mint a poster token (504 three times), in both Chromium and WebKit |
| Prod errors | 34758193362 | Management API SQL query: "Connection terminated due to connection timeout" |
| DB backup (nightly) | 34758146946 | `pg_dumpall` to the pooler: `FATAL: Failed to connect to database: {:error, :timeout}` |
| E2E real backend | 34757946539 | Money loop couldn't mint a poster token (504); the anon contract says the signed-out marketplace doesn't answer correctly (outage) |
| Write contract snapshot refresh | 34756053709 | `supabase db query --linked` failed. The log also shows "not supported on your current network… IPv4", so it may not be only the outage |
| Supabase DB Drift Detector | 34753529614 | `supabase migration list --linked` couldn't connect to the pooler (timeout) |
| E2E happy-path smoke | 34749614576 | 2 failed: device-pass-measure /dashboard at 375-dark and 1440-dark (axe contrast) |
| A11y (axe) seeded sweep | 34749614456 | "No tests found" in every variant |

**Cancelled with no newer run:**
- Prod audit (messy input, deep links, interruptions), run 34760802899, cancelled at 13:47 UTC with 0 jobs.

**Green on its latest run:** Race Runner, Prod freshness, Lighthouse CI, Test, Vitest, Bundle Size, UI sweep, Mobile viewport spot-check, Sentry Release, Push on main and Supabase DB Deploy (07:29 UTC).
**Did not run in the window:** functions-deploy.

Earlier reds on main this morning (07:26–07:58 UTC) came from the Vitest naming guard, the recurring-visits test and the Test asset guard. They were fixed by `5164198cb` and have gone green since.

## 2. Open issues
**nightly-red:** #1597 a11y-webkit-prod · #1596 nightly-webkit · #1595 e2e-journeys · #1592 e2e-real-backend · #1582 press-every-control
**prod-errors:** none open. The prod-errors workflow itself couldn't query the database, so this proves nothing today.
**URGENT in title (21 open):** #1557 is_seed trigger resets · #1517 knip · #1457 health-check 401 · #1449 /dashboard 320 overflow · #1448 empty-state sweep · #1447 /business/team realtime errors · #1390 /my-posts contrast · #1389 activityFilters off-by-one · #1388 /my-posts titles · #1387 and #1367 smoke auth 401s · #1321 and #1319 edge deploy SchemaError · #1256, #1255, #1254 and #1252 stripe-webhook deploy · #1130 post-job CTA · #966 and #922 migration drift. Most are weeks old and likely already fixed. They need a triage pass.

## 3. Cloud routines (last 24h)
I only list routines that found something, changed something or failed.
- **Edge Functions Health (07:03 UTC):** found nothing in the audit, but then **pushed `5ab35263f` (a package-lock.json change) straight to main**, bypassing branch protection.
- **Supabase security + performance advisor (11:06 UTC):** **did not run**, because its Supabase connector is unauthorized.
- **Weekly codebase health (10:03 UTC):** posted a comment on #1425. Typecheck "fails" in the cloud only because Deno isn't installed. Lint has 1 warning (unused eslint-disable at `scripts/state-review.mjs:408`). Files over 600 lines went from 12 to about 50, the largest being `JobTracking.tsx` at 2187 lines.
- **Bundle-size budget check (04:06 UTC):** opened PR #1581 (lazy-load Sentry in ForgotPassword). It was closed without merging.
- Clean runs: Stripe Webhook Health, Sentry triage, Docs Drift, iOS/Capacitor Config Drift.

## 4. Prod errors (Sentry, helpr-4m)
8 new issues in the last 24h, all now marked resolved, none regressed:
- JAVASCRIPT-1V "ZIP 99999 resolved to no Louisiana parish": 39 events, 2 users, first seen 13h ago (likely a test harness)
- JAVASCRIPT-1W "Edge Function returned a non-2xx status code": 12 events, 1 user, 11h ago
- JAVASCRIPT-1X "Xr": 11 events, 1 user, 10h ago
- JAVASCRIPT-21 "dashboard.ctx sub-query failed: applications: JWT expired": 9 events, 10h ago
- JAVASCRIPT-22 "JWT expired": 6 events, 10h ago
- JAVASCRIPT-1Y "Vv": 6 events, 10h ago
- JAVASCRIPT-20 "Error screen shown: We couldn't load jobs.": 1 event, 10h ago
- JAVASCRIPT-1Z "Loading jobs timed out": 1 event, 10h ago

Sentry shows little activity after that, even though prod is down now. That may be because the outage stops the app before its error reporting starts, so no events arrive.

## 5. Pull requests
**Need the owner (not dependabot, not routine):**
- #1529 "fix(e2e): resolve 4 Playwright failures from Sept 2026 suite run" (`e2e/playwright-fixes-sept-2026`, opened 2026-09-03). Checks are failing: Lint/type-check/build/test and 5 A11y axe variants. It is 10 days old and probably stale.

**Dependabot (for reference):** #1580 and #1579 (npm groups, red), #1550 upload-artifact v7 (no failures), #1549 cache v6 (red).

**Merged in the last day:** none. Everything went to main directly.

## 6. Stale routine PRs closed
None. No open PR matched the rules: no routine-authored branch prefix or title that was also more than 3 days old and failing.

## 7. docs/OPEN.md
100 unchecked, 103 checked. Items added in the last 24h, in brief:
- Main CI reds: happy-path smoke contrast and card density, the write-contract refresh SQL, the Vitest naming guard and recurring-visits test, and the Test asset guard in a shallow checkout. Several of these were fixed later in the day.
- Test poster account strikes, the "Under a minute left" chip squeezing location at 375, and tightening fixtureSchemaContract.
- Two agents tripped the safety classifier, the Accept/Release in-flight guard, the RichMessageInput double send, and chunkReload follow-ups. The last three landed as `a33298034`, `22f4df0c0` and `f5bf06ec8`.
- Hallie Helper's avatar on prod storage, cleaning up about 20 finished agent worktrees and 16 wip/ branches, and a Vercel deploy budget warning (landed as `34ffd973d`).
- Owner steps: the STRIPE_TEST_SECRET_KEY secret, reconnecting the Supabase, Slack and Canva connectors, and deleting Stripe endpoint #1586.
- Git history rewrite, button-height false positives, race fixes and combobox WIP, types.ts drift, the Stripe webhook CI check, and a final independent re-check by sonnet.
- Duplicates: the untracked audit doc and write-contract items each appear twice.

## 8. Main (163 commits in the last 24h)
- **Money and races:** synchronous in-flight guards on Apply, money handlers, Release/Accept keyed by id, and the Send button; job row locking; weekly hours made atomic; create-payment conditional writes.
- **No-mock move to prod:** press-every-control and prod-audit suites now drive prod; mocked visual and messy-input specs deleted; a no-mock ratchet guard; journeys J1–J8 on the real backend; prod seed accounts.
- **Gift card rename:** the database and code halves, with a naming guard.
- **Dead code:** removed dead DB objects, owner-approved unread columns, unused routes and scripts, and Instant Book.
- **Helpr naming:** 43 copy strings, the signed-in heading "Home", and /saved-helprs.
- **CI and guards:** release gate, nightly-red issue sync, oversized-binary guard, Vercel deploy-budget pre-push warning, gate lock, and pre-push sweeps of changed screens.
- **UX fixes:** avatar crop and zoom, ZIP check, DOB wheel keyboard, contact-leak rejection, time zones and DST, and the chunkReload retry.
- **OPEN.md and CLAUDE.md:** owner standing orders (no mock mode, 2–3 agents at most, never fable).
