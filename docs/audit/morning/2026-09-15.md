# Louisiana Helpr morning report — 2026-09-15

**9 things need attention**

1. **Race Runner is red on main (latest 13:49Z).** The race fixtures still use the old rules. They fail on `completion_requires_confirmed_arrival` (the VN-33 arrival gate) and on `permission denied for function open_dispute_as`, so race 3's control step says "fixture invalid". The harness needs updating to the new server rules. This is not a proven race bug. Issue #1609.
2. **The DB Drift Detector is red (10:08Z) on two real findings, not ledger drift.** (a) `rpc_add_dispute_evidence` is client-callable, SECURITY DEFINER, and does `UPDATE jobs` from the caller's `_evidence_urls` argument. The guard says that gets around the dispute-marker trust trigger. This needs a money/authz review. (b) `src/integrations/supabase/types.ts` is stale against the live schema; the fix is `npm run db:types`. Function-body drift was OK.
3. **The "We couldn't load your account" error screen shows up on prod for the helper test account.** a11y-webkit-prod (#1597) captured it on /my-posts, /complete-profile, /settings, /profile?tab=str_settings and /user/…dead. press-every-control (#1582) hit the same error on /user/<id> and /jobs/<id>, with `[auth] admin role lookup failed`. Shard totals were 95 + 50 + more failed presses. It may be timeouts under load, but it is the same error on two separate suites.
4. **E2E happy-path smoke is red on main (10:00Z).** `earnings-views.spec.ts` "earnings tab renders one view at a time" can't find its element. This probably comes from VN-3 (the Earnings reorder in 98635843e). The spec runs on mocked Supabase, which is against the no-mock rule.
5. **Nightly WebKit is red (14:20Z, #1614).** The same earnings-views test fails, plus `apply-single-sheet.spec.ts` ("apply form is on the job detail surface"). The messages-thread back-gesture test was flaky.
6. **E2E user journeys is red (08:41Z, #1595).** On Chromium, the "do-the-job" step of the marketplace chain can't find an element at tablet-768. On WebKit, Stripe Checkout never showed a payment field.
7. **E2E abuse & notifications failed yesterday (15:51Z) and has not run since.** The cause was "could not mint a poster token" on both browsers.
8. **Prod audit was cancelled yesterday (15:51Z) and has not run since.** It most likely got cancelled in the shared `prod-load` queue, which is a hidden red.
9. **20 old "URGENT: main broken" issues are still open (#922–#1557).** Most of them describe failures that have since been fixed. They need triage or closing, so a real URGENT doesn't get lost among them.

## Production
- Production is current. The live build stamp is `c989800c3`, which is the newest shipping commit on main.

## GitHub Actions (since 2026-09-14 07:30 PDT)
- **Green now after earlier reds:** Test, Vitest, Supabase DB Deploy (red at 06:05Z and 07:15Z, green at 09:41Z), UI sweep, Edge Functions Deploy, E2E real backend, Prod freshness, Prod errors, Uptime, DB backup and Write contract refresh (last run 00:36Z).
- **Still red at the latest run:** Race Runner, DB Drift Detector, E2E happy-path smoke, Nightly WebKit, E2E user journeys, press-every-control (latest 02:29Z), a11y-webkit-prod (latest 05:26Z) and E2E abuse & notifications.
- **Cancelled with no newer run:** Prod audit (2026-09-14 15:51Z).
- **Branch `sec-hardening-v2` (PR #1612):** several runs were cancelled when newer pushes replaced them, and E2E smoke failed at 10:51Z.
- The run list covers the last 300 runs, which reach back to 05:59Z today. Earlier runs were checked for each workflow separately.

## Open issues
- **nightly-red:** #1614 nightly-webkit, #1609 race-runner, #1597 a11y-webkit-prod, #1595 e2e-journeys and #1582 press-every-control.
- **prod-errors:** none.
- **URGENT in title:** 20 open, all dated 2026-07-01 to 2026-09-07. None were opened in the last 24h.

## Cloud routines
- Two routines are enabled. Both are one-shot PR check-in reminders (#1607 and #1612), and neither shows a run in the last 24h. Nothing to report.

## Sentry (last 24h)
- JAVASCRIPT-24: "MapKit falling back to the unrestricted build-time token: signal is aborted". 1 event, first seen about 13h ago, already resolved.
- JAVASCRIPT-23: "Downtime detected for www.louisianahelpr.com". 1 event, about 15h ago, resolved. This matches the Vercel account block that OPEN.md says is now fixed.
- No regressed issues.

## Pull requests
- **Needs the owner:**
  - #1612 sec-hardening (no default client grants on new relations): 1 failing check.
  - #1607 role-neutral copy: draft, checks green, waiting for someone to look at 375.
  - #1529 fix(e2e) Playwright fixes from 2026-09-03: 6 failing checks, looks stale.
- **Dependabot:** #1599, #1598, #1580, #1550 and #1549 are open, and all have failing checks.
- **Merged since yesterday:** #1611 (partial refund of exactly the budget stays partial), #1610 (VN-52 group jobs plan), #1608 (offer privacy), #1606 (discarded PostgREST builder calls) and #1604 (Stripe refund check).
- **Closed by this report:** none. No open PR matched the routine branch or title prefixes.

## docs/OPEN.md
- 132 open and 197 checked, counting nested items.
- **About 40 open items were added in the last 24h.** The main ones:
  - Re-run the nightly reds to green, one at a time. Queued prod workflows cancel each other.
  - Race proofs are queued for the fa107a92f fixes and for completion-race (2608b2585).
  - A native build is needed to pick up the contact-scanner client changes.
  - Owner action: set Vercel Deployment Retention to shortest.
  - Owner question: can hired Helprs and roster members still read `offered_to_helper_id`?
  - Decide what to do about the 9 auth-timeout presses and the Browse header count.
  - VN-52: the group-jobs flag stays false.
  - Work through the visual-notes tracker.

## Main (last 24h)
- 280 commits, including merges.
- **Visual notes:** about 60 commits fixed and confirmed items, including VN-3, 4, 14–16, 18–35, 37, 41, 44–47, 49, 51–55.
- **Money and authz:**
  - The dispute-settlement race fixes.
  - The table door for "done is final".
  - Completion columns are now server-owned.
  - An `open_jobs_browse` view was writable by clients (critical); client writes are now revoked.
  - Exposed views are now read-only for clients.
  - Excess anon grants were revoked.
  - Offer privacy.
  - Six edge endpoints were hardened.
  - Storage size and type caps were added.
  - Partial-refund fixes (MS-6).
  - Payout double-pay fix (fresh ledger read).
- **CI and ops:**
  - A function-body drift check.
  - Crons spread out behind one `prod-load` queue.
  - Test suites now build locally instead of loading the live site.
  - Alerts for Vercel, Supabase usage and uptime.
  - 182 remote branches deleted.
  - knip dead-code cleanup, which turned Test green.
