# Open items — the definitive "what's left to fix"

**Compiled 2026-09-02** from `COVERAGE_2026-08-31.md` (§1–§10), `COVERAGE_LEDGER.md`,
`FULL-SURFACE-2026-08-31.md`, `STATE_MATRIX.md`, `IOS_COVERAGE.md`, the
`launch-2026-09/` findings bus, and every commit body since 2026-08-30.

**What this supersedes.** The "Known-open", "Found, NOT fixed", "Open — for other
owners" and "UNVERIFIED" sections of all five documents above. Those were written
over three days by eleven concurrent lanes and roughly **40% of their entries were
already fixed by a sibling lane** by the time they were read. Every row below was
re-verified against the working tree at HEAD `8ce9c04c` before it was written down;
everything rejected is listed at the bottom with the reason, so the next person does
not re-derive it.

**The single most important correction.** Every ledger entry of the form *"the
migration is written but uncommitted/undeployed, so this is still live in prod"* is
**stale**. Local `main` is at `8ce9c04c`, `origin/main` at `5fd3686b`, and all 17
migrations dated `20260831232513` → `20260902024735` are ancestors of `origin/main`,
so `db-deploy.yml` has applied them. There are zero pending migrations. That single
fact retired nine separate "still live in prod" claims.

**Counts:** 30 LIVE DEFECT · 12 LATENT · 14 UNVERIFIED · 12 OWNER-ONLY · 17 HYGIENE,
plus 4 contested findings filed by the in-flight launch fleet after this sweep
closed — see **Amendment** at the end. One of them (O-002, `/my-posts` renders no
job card) would move to the top of §1 if it holds; settle it first.

> **Read LATENT #1 first anyway.** The group-jobs cluster is filed as LATENT
> because production has never held a group job — but the feature is shipped,
> reachable from the post-job segmented control, and broken in five independent
> places. It is the largest coherent block of work in this document.


## STATUS STAMP — 2026-09-02, after the ten-lane fix pass

**This document is a snapshot of what was open when it was compiled, and most of
§1 has since been fixed.** Do not read the tables below as current. Ten lanes ran
against this list and landed 23 commits, all on local `main` and gated
(`tsc -b` clean, full `vitest` green). Read `git log origin/main..HEAD` for the
authoritative state.

Closed since compilation, by lane:

| Was | Outcome |
|---|---|
| LIVE #1 rate limiter | Real, and worse — 18 importers, not 17. Now DB-backed, keyed on the JWT subject. |
| LIVE #2 `money-reconciliation` | **Not the stated defect.** It has been dispatching all along; it was never *recorded* — it answered a bare 200 with no `fn` key, invisible to both watchers at once. A 65-minute total pg_cron blackout on 09-01 was found by differencing another job's timestamps. |
| LIVE #3 `payment-confirm-reminder` | Real. Schedule and constant now derived together. |
| LIVE #4 portfolio public bucket | Real. Stable random keys; deletion restated as an invariant that fails closed. |
| LIVE #5 client-supplied storage keys | **Five sites, not four — and not an orphan bug.** Path traversal in `avatarExt` escaped the user's folder and overwrote another member's live avatar, proven on prod by SHA. Unauthenticated. Fixed and deployed. |
| LIVE #6 orphaned rows | Real, 122 rows. Four brief corrections: no IPs anywhere; the 109 `admin_audit_log` rows are an all-zeroes sentinel, never a person; two `reports` rows were dangling *job* pointers in a polymorphic column; 8 of 9 departed accounts are our own test lanes. Also found `analytics_events.properties` embeds the actor uuid a second time, making `purge_user_data()`'s anonymisation cosmetic. |
| LIVE #7 `url.hash` | Real, but **not** the unblocker for claiming `/reset-password` — the session is constructed before `appUrlOpen` fires, so claiming it would fail *after* the user types a new password. Both stay excluded. |
| LIVE #8 AASA 404s | **Six paths, not five** — `/legal/*` was missed. All now served on the web. |
| LIVE #11 review nag | Partly wrong: only the *held* branch was a dead end; the revealed branch was already correct. |
| LIVE #12 parish matching | Writer was never built. **The gate is not `get_ranked_open_jobs`** (parishes never reach its WHERE) — it is the fan-out trigger. Fan-out now reaches 7 accounts instead of 0. |
| LIVE #13 realtime | **14 sites, not 11**; four of the listed paths were wrong. All adopted. |
| LIVE #14–17, #21, #26, #27 | Fixed. `click_action` brief was half wrong — `/jobs/:id` had 28 rows, not zero; the genuinely dead branch was `JOB_ACCEPTED`. |
| LIVE #18–20, #22–24, #28–29 | Fixed. `/analytics` contrast was **14 serious nodes, not 2**, and in a different file than named. `heading-order` was on **three** routes, not two. |
| LIVE #25 senior `.truncate` | Six sites, not 27 problems; 24 losses → 8. The mid-word amputation was the absence of `overflow-wrap: anywhere`, not clamping. |
| LATENT #1 group jobs | All five real. **Gated, not fixed** — widening the RLS policy would be worse than the lockout, because two triggers early-return on `auth.uid() IS DISTINCT FROM OLD.helper_id`. |
| LATENT #2 payout NULL | Real and armed. Fixed fail-closed. |
| LATENT #3 saved-search NULL | Real. The sibling direct-offer trigger is **not** a defect — the CI warning is a false positive of a shape heuristic. |
| LATENT #4 marketing blast | Fixed. |

**A defect this pass introduced and caught:** the `prefers-reduced-transparency`
blur fix reported as "18 → 0" was measured on a dev server and was **false in the
shipped bundle** — Vite's minifier collapses `backdrop-filter` and its `-webkit-`
twin and keeps only the last. Verified in `dist/` after the real fix.

**Still open and unchanged:** everything under OWNER-ONLY, the UNVERIFIED section,
and HYGIENE. Plus: no browser could be launched on this host for the last third of
the pass (`mach_port_rendezvous` failures under load), so several UI fixes are
verified by measurement, axe and jsdom but **not by a fresh pixel** — those need a
re-render, and none has been near the iOS simulator.

---

## 1. LIVE DEFECT — currently broken in production for real users

Ordered by user impact, highest first.

| # | What | Where (file:line or surface) | Evidence it is still open | Effort |
|---|---|---|---|---|
| 1 | **`_shared/rate-limit.ts` is not a rate limit.** It keys on the client-supplied *first* `x-forwarded-for` hop and stores counters in a per-isolate in-memory `Map`, so any caller sets their own key and any cold start resets the count. Its own doc comment claims it uses Supabase; it makes no DB call. | `supabase/functions/_shared/rate-limit.ts:28-32` (`.split(",")[0]`), `:36-41` (`globalThis.__rateLimitStore = new Map()`) | 17 importers, including `create-payment`, `instant-payout`, `cash-out-credits`. Verified in tree. | M |
| 2 | **`money-reconciliation` has never completed a single run.** 0 rows in 2,493 `cron_run_log` rows; its entire `error_logs` trace is one pg_net timeout on 2026-08-28. No HTTP request has been dispatched since. | cron `20 8 * * *`, `supabase/migrations/20260828230000:36-38` — schedule unchanged since | Function *is* deployed (`OPTIONS` → 200). Root cause is UNVERIFIED (see U-11) but the outcome is established: money reconciliation is not running and no watcher can see it. | S–M |
| 3 | **`payment-confirm-reminder` has never once fired.** Cron is `15 15 * * *` (daily) against a 12-hour eligibility window, so ~half of all submissions can never land in it, on either pass. The upper bound is `AUTO_COMPLETE_HOURS` — when money moves. | `supabase/migrations/20260829010000:71` (`alter_job`, still `15 15 * * *`); function names its own fix at `index.ts:50`, `:219`, flag `SCHEDULE_LEAVES_A_HOLE` | Prod: `jobs.payment_confirm_notif_sent = true` → **0 rows, ever**. No `15 */6 * * *` migration exists anywhere in the tree. | S |
| 4 | **Portfolio photos go to the PUBLIC bucket and have no delete path.** "Remove" only rewrites the `profiles` column; the storage object stays anonymously fetchable forever. Same defect class as the avatar orphan, on a surface that accepts N files. | `src/components/profile/profileEditForm/usePortfolio.ts:69` (`.from("avatars")`), `:75` (`getPublicUrl`), `:83-86` (`removePortfolioAt` → `persistPortfolio(next)` only) | No `storage.remove` anywhere in the file. `avatarStorage.ts`'s sweep explicitly excludes portfolio sub-folders. | M |
| 5 | **Four storage keys built from a client-supplied file extension**, with only a size check between parse and use. Any suffix is a reachable permanent key — public for the avatar, private for the other three. | `supabase/functions/complete-signup/index.ts:31-37` (destructures `avatarExt`), `:213-214`, and the same pattern at `:233` (id-document), `:254` (license), `:271` (insurance) | Fix is the content-type-derived key + sweep that `src/lib/avatarStorage.ts` already implements for the client paths. Also `src/pages/Profile.tsx:414` (`file.name.split(".").pop()`) into `id-documents`, private bucket, lower risk. | S |
| 6 | **173 rows in prod point at users who no longer exist**, two of them carrying literal PII: `notification_logs.recipient_email` (their email address) and `login_history.ip_address`. `purge_user_data()` is deployed but only governs *future* deletions. | prod census, `COVERAGE_2026-08-31.md` §8.2; purge in `supabase/migrations/20260902014651` | `analytics_events.user_id` 63 rows/3 users, `notification_logs.user_id` 6/4, `referral_codes.user_id` 3/3, `notifications.user_id` 1/1, `admin_audit_log.admin_id` 100/1 (deliberate). Needs a one-off backfill; nothing in the tree performs one. | M |
| 7 | **`normalizeDeepLinkUrl` drops `url.hash`.** This is what blocks claiming `/reset-password` and `/account-pending` in the AASA — both carry their session in the fragment. On iOS a reset link therefore opens Safari, and the user ends up signed in *in Safari* while the app still holds the old session. | `src/lib/deepLinkRoute.ts:102, :109, :125, :142, :148` — every branch returns `` `${path}${search}` `` | The AASA file now documents the exclusion as gated on this fix. Verified: `url.hash` appears nowhere in the module. | S |
| 8 | **Five AASA-claimed path patterns 404 on the web**, and two real routes are still unclaimed. Anyone without the app installed who taps a share link dead-ends. | `public/.well-known/apple-app-site-association` claims `/j/*`, `/u/*`, `/m/*`, `/messages/*`, `/post-job/*`; `src/App.tsx` registers only `/jobs/:id`, `/user/:userId`, `/messages`, `/post-job` | Unclaimed but real: `/forgot-password` (`App.tsx:185`) and `/rules` (`App.tsx:297`). `deepLinkRoute.ts` maps all five *inside* the app, so this is web-only. | S |
| 9 | **Every job `str-ical-sync` creates is invisible to every helper.** The insert omits `payment_status`, so the column default `'unpaid'` applies, and all three browse surfaces filter `payment_status = ANY('escrow','payout_pending','released')`. | `supabase/functions/str-ical-sync/index.ts:241-255`; predicates at `supabase/migrations/20260901035245:347, :426, :491` | Knowingly deferred — the run counts them as `jobs_created_unfunded` — but the STR feature produces nothing a helper can see. | L (product call) |
| 10 | **`str-ical-sync` drops today's checkout** — the most urgent cleaning job is the one never created. `checkoutDate` is built with `Date.UTC(y,m,d)` and compared against `new Date()`. | `str-ical-sync/index.ts:67-75` vs `:166`, compared at `:189` (`if (checkoutDate < now) continue`) | Cron is `44 */6 * * *`, so every run is past midnight UTC and today's turnover drops on **every** run. | S |
| 11 | **Every review nag links to a screen with no review-writing affordance.** `/profile?tab=reviews` is read-only (it shows reviews *received*) and `Profile.tsx` never reads `?job=`. | `supabase/functions/review-nag-cron/index.ts:269`; same dead link in trigger `supabase/migrations/20260831162131:71` | The real write surface is `src/components/activity/ActivityDialogs.tsx:130,137` on `/my-posts` + `/my-jobs`, which already parse `?job=` (`Activity.tsx:147`). | S–M |
| 12 | **Parish matching does nothing: `helper_preferred_parishes` has no writer.** Zero rows across all 32 accounts, and no INSERT/upsert anywhere in `src/`. | Only non-generated reference is a *read* at `src/components/admin/adminHealth/useHealthData.ts:118` | The table is consumed as a filter by `get_ranked_open_jobs` (`20260901031421:112-116`) and by `notify_helpers_on_job_post`, so the whole fan-out loops zero times. | M |
| 13 | **11 realtime channels observe no status and nothing ever resubscribes.** After a socket drop the UI goes silently stale — the app renders fine and simply never hears about anyone else's writes. | `NotificationPanel.tsx:198`, `DesktopSidebarNav.tsx:159`, `JobTracking.tsx:604`, `useMessageReactions.ts:133`, `AdminNotificationLogs.tsx:209`, `useNavUnreadCount.ts:89`, `useCurrentUser.ts:165`, `useActivityBadgeCounts.ts:162`, `useRealtimePush.ts:62`, `Admin.tsx:378`, `useMessagesRealtime.ts:191` | Only `useActivityData.ts:682,691` pass `observeStatus`, and that reports to Sentry rather than to the user. No `resubscribe`/`reconnectChannel` helper exists in `src/`. | L |
| 14 | **`admin-update-email` rewrites a third party's login email** with no audit row and no notification to the person whose email changed. | `supabase/functions/admin-update-email/index.ts:97-121` (`freeDeniedAccountEmail`) | The audit insert (`:258`) and the notification (`:243`) are both scoped to `userId`, never `deniedUserId`. Only side effect on the third party is a `console.log`. | S |
| 15 | **`admin-user-actions`: five discarded notification inserts, no rate limit, and a non-idempotent ban.** A banned user may never be told; a double-tap duplicates the ban row, the audit row and the notification. | Bare inserts at `:187, :250, :293, :516, :648`; `:604-610` plain `.insert` into `user_bans` with no unique constraint (`20260311015019:28-37`) and no pre-check; no `checkRateLimit` import | Each bare insert sits directly beside an audit insert that *is* checked. The `:628` "double-tap is harmless" comment covers only the later close step. | S |
| 16 | **`process-email-queue` can double-send and drops two errors.** Ordering is send → mark → delete, and the sole guard is a `status='sent'` read. `vt: 30` applies to the whole batch from read time, and `_shared/resend.ts:233-282` has no timeout/`AbortSignal`. | `:249` (drops `error`), `:256` (discards result), `:104-108` (`vt: 30`), guard at `:181` | `:265`'s delete error is now caught but only `console.error`'d — it never reaches `cronResult`, so a run that failed to dequeue still answers clean. | S–M |
| 17 | **`set_available_now` / `clear_available_now` report success unconditionally.** No `GET DIAGNOSTICS`, so 0 rows updated is indistinguishable from 1 at the client — on the toggle poster-side applicant cards read. | `supabase/migrations/20260612430000_helper_availability_status.sql:14-25` and `:29-37` | Latest definitions. `20260618140000` only pins `search_path` via `ALTER`; it does not redefine the bodies. | S |
| 18 | **`/wrapped` — six defects on one shareable card.** (a) spend tile suppressed whenever earnings exist, so a user who both posts and helps never sees their spend; (b) `bestRating` is `Math.max`; (c) `~N hrs` is `earned / 15` with the $15/hr assumption undisclosed; (d) `reviewsGiven` fetched and never rendered; (e) the share string and the tile format the same number differently; (f) `handleShare` discards `shareNative`'s outcome with no `isSharing` guard, so a double-tap opens two sheets. | `src/pages/HelprWrapped.tsx:324`, `:179`, `:23/:185/:321`, `:52/:195/:33`, `:272` vs `:319`, `:263/:288` | All six confirmed at those lines. | M |
| 19 | **`/home-history` — three defects on a screen billed as a permanent service record.** (a) infers the helper from `applications.status='accepted'` rather than the canonical `jobs.helper_id`, so instant-book and direct-offer jobs lose the "done by" line and group jobs collapse to one name; (b) prints `budget` beside the words "what it cost"; (c) no export, on the same `.doc-card` surface as `/work-record`, which ships "Share Record (PDF)". | `src/pages/HomeHistory.tsx:116-118` + `:136`; `:307-309` under the framing at `:184`; no Export/Share control in the file | A homeowner has nothing to hand a buyer or insurer. | M |
| 20 | **A `?job=` deep link on the *posted* tab neither highlights nor scrolls.** The notification says "your job X" and the user lands on a list with the target card 4th of 5. | `src/pages/Activity.tsx:268-270` (posted branch resolves only the bucket) vs `:274-281` (applied branch also calls `setHighlightAppId`) | `useHighlightPulse.ts:16` (`scrollIntoView`) exists only under `appliedJobCard/`; `PostedJobsTab.tsx` / `PostedJobCard.tsx` accept no highlight prop. | M |
| 21 | **`SeriesStrip` quotes the wrong funding date on a money line** — "next funds \<visit date\>" where funding actually happens `FUND_LEAD_DAYS` (3) earlier. | `src/components/activity/SeriesStrip.tsx:78`, `next` resolved at `:55-56`; real lead time at `charge-recurring-visits/index.ts:97, :251` | The file's own header at `:17` says "funds automatically 3 days ahead" — the rendered line dropped the clarifier. | S |
| 22 | **axe `heading-order` (moderate) on two profile tabs** — `h1` then `h3` with no `h2`. | `/profile?tab=referral`: `PageHeader.tsx:300` `<h1>` then `src/components/profile/ReferralExtras.tsx:68` `<h3>`. `/profile?tab=subscription`: `SubscriptionTab.tsx:163` then `:465` `<h3>` | Present in base *and* senior mode. | S |
| 23 | **axe `nested-interactive` (serious, 5 nodes) on `/profile?tab=saved_helpers`** — `div[role="button"]` cards containing focusable children. | `src/components/profile/savedHelpersTab/SavedHelperCard.tsx:51-53`, children at `:158` (`<textarea>`) and `:168, :177, :227, :258, :269` (`<Button>`) | `stopPropagation` fixes the click behaviour but not the violation. | M |
| 24 | **axe `color-contrast` (serious) on `/analytics`** — `hsl(var(--olivewood) / 0.62)` at `text-ds-11`. | `src/components/.../AnalyticsUpgradePanel.tsx:77` rendered by `AnalyticsPanel.tsx:32`; same token/size at `NotEnoughYet.tsx:26-27` | Route `App.tsx:345`. | S |
| 25 | **Senior mode silently amputates visible characters via `.truncate`** — measured `St. Martinville` −48px, `Opelousas` −43px, `Baton Rouge` −39px on `/dashboard`, `/jobs`, `/browse`, plus a 40px hard clip (no ellipsis) on a job card at 320. | 193 `.truncate` occurrences in `src/**/*.tsx`; the blanket fix is refused at `src/index.css:2731-2753` | **Only per-component work is acceptable.** A blanket `.truncate`→`line-clamp` rule was tried, measured, and reverted on 2026-09-01 (it produced silent mid-word amputation with no ellipsis); the CSS carries a "do NOT re-add" note. | L |
| 26 | **The ops Slack alert for a filed dispute always 401s.** `fireSlackAlert` runs in the browser with a user JWT; the function accepts only `CRON_SECRET` or the service-role key. | Caller `src/components/DisputeDialog.tsx:206` via `src/lib/slackAlerts.ts:33`; gate at `supabase/functions/slack-ops-alert/index.ts:122-134` | Severity reduced but not removed: in-app admin notifications now work server-side (`20260901032007`), so ops are not blind — only the Slack channel is. | S |
| 27 | **`send-push-notification`'s `click_action` inference is dead code.** It tests `link.startsWith('/jobs/')` and `'/dashboard'`. | `supabase/functions/send-push-notification/index.ts:118-120` | Live link inventory: `/my-posts` 16, `/profile` 15, `/admin` 12, `/my-jobs` 9, `/earnings` 6, `/dashboard` 6, **`/jobs/` zero**. Both `JOB_*` branches are unreachable; ~25 lifecycle pushes resolve to no APNs category. | S |
| 28 | **`/help` has no primary CTA and its FAQ anchors link to nothing.** The page's only route to a human is a plain text link in a flat card. | `src/pages/HelpCenter.tsx` — `grep -c btn-grad-primary` → **0**; anchors stamped at `:72` (`id={\`faq-${topicSlug(...)}\`}`) with no `href="#faq-…"` anywhere in `src/` | Sections also default closed and open/closed states are byte-identical apart from the chevron. | S |
| 29 | **A successful password change is silent.** `handleSubmit` toasts on failure and shows nothing on success — it just navigates to `/dashboard` after 800 ms. | `src/pages/ResetPassword.tsx:101` (error toast) vs `:104-107` (bare `setTimeout` → `navigate`) | The adjacent comment describes "confirmation without making the user stare at a toast" — but no confirmation is rendered. (The *loading-state* half of this ledger entry is fixed; see rejected #12.) | S |
| 30 | **`JobDetailDialog`'s three corner icons are 32×32**, below the 44px HIG floor the shared close X was just raised to. | `src/components/dashboard/JobDetailDialog.tsx:118-166` passes `compact`; `dashboard/IconActionButton.tsx:92` (`minHeight/minWidth: "32px"`), `:117` (`h-8 w-8`) | Owner-requested (comment at `JobDetailDialog.tsx:121-124`: 44px icons "made a large gap above title"), so this needs a design answer, not just a size bump. | S |

---

## 2. LATENT — will break under a condition that has not occurred yet

| # | What | Where (file:line or surface) | Evidence it is still open | Effort |
|---|---|---|---|---|
| 1 | **Group jobs are broken in five independent places.** Every surface assumes `jobs.helper_id` is the only helper and none consults `group_job_helpers`: (a) helper #2 **cannot message** — they hold the street address and have no channel; (b) helper #2 **cannot accept, confirm, mark arrival or mark complete**; (c) the job **vanishes from helper #2's Activity** once the roster fills; (d) **reviews are impossible in both directions**, which silently feeds the trust ladder and tier; (e) `admin_release_dispute` pays **1/N of escrow** and flips the job to `released`. | (a) `can_message_in_job`, `20260820063000:19-48`. (b) `20260312010219:2-6` `USING (auth.uid() = helper_id)`. (c) `get_jobs_for_my_applications`, `20260901033219:248-256`. (d) `can_review_job` + its RLS policy, `20260823200000:70-91`, `:97-115`. (e) `supabase/functions/create-payment/index.ts:1091-1101`, flip at `:1117-1123` | Condition: **any poster taps "Group" in the post-job segmented control.** Prod has never held a group job, which is why this reads LATENT — but the control ships today. Widening (b) alone is unsafe: see `COVERAGE_2026-08-31.md` §4.1 on the arrival/completion triggers. | L |
| 2 | **`process-scheduled-payouts` counts a departed helper as "paid".** `:164` filters nulls out of the roster (`.filter(Boolean)`); `:633` does not (`new Set((paidRows ?? []).map(r => r.helper_id))`), so a NULL from a departed member counts as one distinct payee. A roster of 3 with one stale NULL transfer reaches `distinctPaid >= rosterSize` and **releases the job with a real helper unpaid**. | `supabase/functions/process-scheduled-payouts/index.ts:164` and `:633`; roster size set at `:175-176` | **This became reachable on 2026-09-02.** `20260901033011:88-108` and `20260902014651:98` drop `NOT NULL` on `payout_transfers.helper_id` and `group_job_helpers.helper_id`, and both FKs are now `ON DELETE SET NULL`. An earlier verification that called this inert was reading the pre-2026-09-01 column definitions. Condition: any paid helper deletes their account. | S |
| 3 | **The saved-search funded trigger has a NULL `payment_status` hole.** Its `WHEN` uses a bare `OLD.payment_status = ANY (…)`, which is NULL for a NULL row, so Postgres does not fire — a job sitting at NULL that then funds notifies nobody. | `supabase/migrations/20260901035245:793-803` (`trg_notify_saved_searches_funded_update`) | Latest definition; no `COALESCE`. The sibling `notify_helpers` trigger uses `COALESCE(OLD.payment_status,'')` (`20260902015059:268-286`) and explicitly defers this one "to that lane". Prod holds 0 NULL rows. | S |
| 4 | **`send-marketing-blast` reads suppression and opt-out lists unbounded** against a 1000-row PostgREST cap, defeating the fail-closed guard written to prevent exactly this. | `supabase/functions/send-marketing-blast/index.ts:156-158` and `:239-241` — no `paginate` import at all, error-only checks | `engagement-automations` is **fixed** (branches on `error` at `:231` *and* `!complete` at `:247`). `notification_preferences` is one row per user, so it crosses 1000 first: at 1000 users the 1001st opt-out gets mailed. Prod holds 5 rows. | M |
| 5 | **`charge-recurring-visits` charges an arbitrary saved card.** It resolves the customer by email and takes the first record holding a payment method, rather than the method the poster authorised at checkout. | `supabase/functions/charge-recurring-visits/index.ts:632` (`customers.list({email})`), `:645` (`paymentMethods.list`) | Improved (scans 100 in parallel batches, deterministic winner) but still email-resolved. Condition: duplicate Stripe customers on one email, or several cards. Prod has 0 series parents. | M–L |
| 6 | **`charge-recurring-visits` Stripe idempotency keys expire inside the funding window.** Keys are retained 24 h; a visit date sits in the window for up to three daily runs (`FUND_LEAD_DAYS = 3`). | `:702` (key `recurring-visit:${parent.id}:${visitDate}`), `:97`; acknowledged at `:691-694` | Mitigated by the `jobs_one_visit_per_series_date` unique index and the 23505 refund branch. Residual: charge succeeds, child insert fails for a reason other than 23505. | M |
| 7 | **No FK protects the ten no-FK tables.** A deletion performed outside the three delete functions (e.g. the Supabase dashboard) still orphans rows; the rule lives only in `purge_user_data()`. | `supabase/migrations/20260902014651` contains no `ADD CONSTRAINT … FOREIGN KEY`, only `ALTER COLUMN … DROP NOT NULL` (`:126`) | **Deliberately deferred** — header `:30-38` records that prod already holds pre-existing orphans, so the `ALTER` would fail on deploy. Closing this needs the backfill in LIVE #6 first. | L |
| 8 | **Three edge functions have no `verify_jwt = false` block in `config.toml`.** Correct defence for the day legacy JWT keys are disabled. | `supabase/config.toml` — all 36 `[functions.*]` blocks enumerated; `saved-helper-availability-push`, `str-ical-sync`, `slack-ops-alert` absent, and `[functions]` sets no default | **Severity downgraded from the ledger:** the implied "the cron is being 401'd today" is wrong. Callers send the legacy-JWT service-role key, the gateway accepts it, and `cron_run_log` shows 200s. This is config inconsistency, not a live outage. | S |
| 9 | **`GroupJobHelpers` types `helper_id` as non-null while the column is now nullable.** | `src/components/GroupJobHelpers.tsx:13` (`helper_id: string;`), `:94` (`nameMap.get(h.helper_id) \|\| "Helpr"`) | The `\|\| "Helpr"` fallback is the tell that null is reachable while the type denies it. `src/integrations/supabase/types.ts` also wants regenerating after `20260902014651`. A departed member degrades to the generic label "Helpr". | S |
| 10 | **`OfflineBanner` is `lazy()`** — on a cold offline boot its chunk can fail to load and the banner never appears. | `src/App.tsx:23`, rendered `:645` inside `<Suspense fallback={null}>` | Deliberate (comment `:20-22`: keeps `lucide-react` out of the main chunk) and mitigated by the precached PWA shell, but the failure mode is real and silent. | S |
| 11 | **Twelve bare `<Navigate>` redirects still drop `search`/`hash`.** | `src/App.tsx:207, 252, 253, 262, 271, 297, 305, 306, 307, 389, 390, 391` | Sanctioned at `App.tsx:152-155` for destinations that hard-code their own params, and none is currently reached with a query (verified against `notifications.link` and `error_logs.url`). Eight need *merge* semantics, not the plain `PreserveQueryRedirect`. | S |
| 12 | **Ten retired marketing routes 404 with no redirect stub** — `/enterprise`, `/how-it-works`, `/parishes`, `/parish/:slug`, `/impact`, `/local-guide`, `/community`, `/browse-jobs`, `/evacuation`, `/become-a-partner`. | `src/App.tsx` code comments document the absence as intentional | Filed today as RW-002 by the in-flight launch fleet; not independently re-verified here. Intentional, but any external link or indexed URL still pointing at them now 404s. | S |

---

## 3. UNVERIFIED — could not be tested; unknown state

Each row names what would settle it.

| # | What | Where (file:line or surface) | Evidence it is still open | Effort |
|---|---|---|---|---|
| 1 | **The entire native surface.** Nothing in any lane was verified on iOS. The whole bug class this week was WKWebView-specific (inert `<a download>`, no `window.print()`, `data:` URLs with no UTI, missing `PushManager`, the dropped APNs token). | all of `src/`, `ios/` | **Settled by:** a TestFlight or simulator pass over the screens changed since 2026-08-30. This is the largest single gap in the product. | L |
| 2 | **Universal-link association on a device.** The AASA is served correctly (200, byte-identical to the repo copy) and the entitlement matches, but whether iOS hands `https://www.louisianahelpr.com/jobs/<id>` to the app depends on Apple's CDN having fetched and cached the file for this bundle ID. | `public/.well-known/apple-app-site-association`, `ios/App/App/App.entitlements` | **Settled by:** `swcutil dl` on a device, or tapping a link, after the next TestFlight build. Cannot be established from a workstation at all. | — |
| 3 | **Cold-start push tap.** The plugin source shows `retainUntilConsumed: true` and a replay when a JS listener attaches, which is the right kind of evidence — but it is a read of `node_modules`. | `nativePush.ts:150-204`, `:225` | **Settled by:** a real APNs push to a real device with the app force-quit. | — |
| 4 | **The `helpr://` custom-scheme branch** (the Stripe return path). Bypasses the host allowlist and has **zero** test coverage — `deepLinkRoute.test.ts` never constructs a `helpr:` URL. | `src/lib/deepLinkRoute.ts:80-86` | **Settled by:** a unit test constructing a `helpr:` URL, plus one drive from `SFSafariViewController`. | S |
| 5 | **A group job end to end** (post → accept ×3 → work → complete → payout). Blocked *upstream* by LATENT #1b: helper #2 cannot accept the offer at all. | — | **Settled by:** widening the `jobs` UPDATE policy first (with the trigger hazard handled), then three authed helper accounts. This is the finding, not a gap in the attempt. | — |
| 6 | **A recurring series end to end** (create → a visit charges → cancel one). Funds from a live saved card off-session; the seeded test poster has no Stripe customer. | `charge-recurring-visits` | **Settled by:** a test-mode Stripe customer on a seeded poster, or the cron's next real run. | M |
| 7 | **A dispute end to end on live accounts** (file → evidence → escalate → admin resolve) with money at each step. The *data layer* was executed in PGlite (31 assertions), which is strictly narrower. | `DisputeDialog`, `DisputeTimelineDialog`, `DisputedSection` — none has ever been rendered at any breakpoint | **Settled by:** a funded escrow (a real Stripe checkout), a second authed helper account, and an admin role. | M |
| 8 | **`cancelStripeSubscription` on a real subscription.** `STRIPE_SECRET_KEY` is an edge secret and is not in `.env`, so the local run took the "skipped" branch. | `delete-own-account` | **Settled by:** a test-mode subscription attached to a test account, then a deletion. The dialog promises "membership stops billing"; that sentence is proven in code only. | S |
| 9 | **No Stripe call was made in either mode by any lane.** Duplicate customers per email, portal scoping, and `accounts.del` refusing a non-zero balance all rest on recorded findings and Stripe's docs. | `pro-customer-portal`, `stripe-connect`, `charge-recurring-visits` | **Settled by:** one test-mode call per claim. | M |
| 10 | **No cron handler was ever driven over HTTP.** `.env`'s `SUPABASE_SERVICE_ROLE_KEY` is the 219-char legacy JWT; deployed handlers compare the bearer against `SECRET_KEY` (the 41-char `sb_secret_*`). All answer 401 to it. | six cron functions | **Settled by:** the `sb_secret_*` value, or `gh workflow`-side invocation. Behaviour was proven through the harness against real source instead. | S |
| 11 | **`money-reconciliation`'s root cause** — "not firing" vs "firing and never answering". | `cron.job` / `cron.job_run_details` | **Settled by:** `SELECT jobname, schedule, active FROM cron.job WHERE jobname='money-reconciliation';` and `SELECT status, start_time, return_message FROM cron.job_run_details d JOIN cron.job j USING (jobid) WHERE j.jobname='money-reconciliation' ORDER BY start_time DESC LIMIT 10;` No SQL path exists from this workstation. | S |
| 12 | **Three write paths never driven:** the `/availability` weekly-hours SAVE, the "available now" toggle, and `/str-settings` end to end (Connect / Sync Now / Remove). | `HelperAvailability.tsx`, `AvailabilityTab.tsx`, `StrSettings.tsx` | **Settled by:** the snapshot/restore harness in `scripts/audit-capture.mjs`. Pressing Save rewrites all 7 `helper_availability` rows, and a prior sweep left the seeded account wrong for days by doing exactly this. | M |
| 13 | **The `/complete-profile` avatar UPLOAD path.** The submit test used an account that already had an avatar, so `uploadProfileFiles` → Storage was never exercised: the 5 MB/type rejection copy and the upload-timeout path are unverified. | `src/pages/completeProfile/uploadProfileFiles.ts` | **Settled by:** a signup round trip with a fresh account and an oversized file. | S |
| 14 | **Whether GoTrue's own rate limit backs the login lockout.** The 5-strike lockout is client-side in `safeStorage`; measured, `localStorage.clear()` + reload resets it and the next attempt reaches the network. | `src/pages/Login.tsx:26-67, :194-233` | Correct for its stated threat model (a stolen phone driving the UI). **Settled by:** reading the Supabase Auth dashboard's rate-limit settings — not readable from this repo. | S |

Also unverified, and recorded once rather than as rows: **content-process jetsam**
(the app-lock bug's actual cause — needs a physical device under memory pressure);
**the software keyboard covering a sheet** and **tapping anything in the simulator**
(needs XCUITest/Maestro or Accessibility permission); **landscape safe-area insets**
(rotation is not a `simctl` verb); **`prefers-reduced-transparency` in WKWebView**
(emulated via CDP only); **offline on the native surface** (all offline driving used
Playwright's `context.setOffline()`, i.e. the `navigator.onLine` path, not
`@capacitor/network`); and **the 25 manual boxes in `docs/qa/ACCESSIBILITY_AUDIT.md`**
(VoiceOver, Dynamic Type, Reduce Motion, 44pt targets, Color Filters — ~1 hour on a
real device, per the doc's own estimate). See `IOS_COVERAGE.md` for why each is
unreachable rather than merely undone.

---

## 4. OWNER-ONLY — needs a human (secrets, App Store, Stripe live mode, a product decision)

| # | What | Where (file:line or surface) | Evidence it is still open | Effort |
|---|---|---|---|---|
| 1 | **Set `RESEND_WEBHOOK_SECRET` in prod.** Until it is set, `resend-webhook` refuses every unsigned delivery by design → `suppressed_emails` stays at 0 → all three fail-closed suppression guards are no-ops, and hard-bounced and complained addresses keep being mailed. | secret; reader at `supabase/functions/resend-webhook/index.ts:90`, 503 at `:94`; sole writer `:193-195` | Highest-value one-line action in the whole document. Readers at `engagement-automations:229`, `send-marketing-blast:157`, `send-notification-email:181`. `email_send_log` already holds 162 real sends. | S (config, 0 code) |
| 2 | **The CAN-SPAM physical postal address is empty**, so every commercial email footer currently renders **no address at all**. Live on every drip and marketing send. | `supabase/functions/_shared/resend.ts:96` — `const POSTAL_ADDRESS_LITERAL = "";`, fallback at `:109`; guard at `email-templates/components.tsx:164` | The plumbing landed; the value did not. The file's own comment admits it renders nothing rather than a placeholder. One string, or one `supabase secrets set HELPR_POSTAL_ADDRESS`. | S |
| 3 | **Switch Stripe from test to live mode.** | Stripe dashboard | Established; not re-investigated. | — |
| 4 | **Native iOS rebuild + TestFlight** — required for push to work at all (the AppDelegate fix is in the tree but needs a native build), and the only way to close UNVERIFIED #1–#3. | `ios/App/App/AppDelegate.swift` | Established; not re-investigated. | — |
| 5 | **Archive 3 stale Stripe products and 9 prices**, including a $15 Elite set that is a latent under-charge if Elite is ever resolved by lookup key. Plus 6 orphan `STRIPE_PRICE_SEAT_*` edge secrets. | Stripe dashboard | Established; nothing in the repo reads them. | — |
| 6 | **Resolve the App Store ID.** `apps.apple.com/us/app/helpr/id6754470134` → 404 and `itunes.apple.com/lookup?id=6754470134` → `resultCount: 0` in all four storefronts. **Do not raise `min_supported_build` above 0 until it does** — the block screen's only escape would point at a dead listing. | `src/components/ForceUpdateGate.tsx`, `src/lib/minSupportedBuild.ts:124-135` | Established. The gate itself is built, wired above `AppLockGate`, fails open on every read failure, and `min_supported_build` is 0 in prod. | — |
| 7 | **Decide the two stuck disputes.** Both `status='open'`, `execution_status IS NULL`, deadlines now 9 and 10 days past. | `public.disputes`, 2 rows | Both jobs are `is_seed=true` with no `stripe_payment_intent_id` and no `payout_transfers`, so no real money is held — but they pin the escalated-reminder path. | — |
| 8 | **The seed-job cold start.** `seed_jobs_hidden_publicly()` now gates all three browse surfaces *and* the saved-search trigger from one DB flag — but 100% of the browsable board is fixture data (20 open seeds, 0 open non-seed), so flipping it at launch empties the marketplace. | `public.seed_jobs_hidden_publicly()`, `supabase/migrations/20260901035245` | The engineering is done and proven (136 PGlite assertions, applied 3×). What is left is purely the decision of *when* to flip, and what fills the board. | — |
| 9 | **Purge three orphaned avatar objects** from the prod `avatars` bucket, all anonymously fetchable at 200 today. | `…888/avatar.jpg`, `…147/avatar.png`, `…044/avatar.png` | All three on clearly-marked test accounts; no real member account holds an orphan. The teardown call was refused by the permission classifier. Needs one approved `DELETE /storage/v1/object/avatars`. They also self-clear on that account's next upload. | S |
| 10 | **Early Access delays a free helper past their own match alert.** All three browse surfaces gate on `created_at <= early_access_cutoff()` — up to 20 minutes for a free viewer — so a correctly-timed alert can arrive before the job is openable. | predicates at `20260901035245:355, :431, :506`; documented as unfixed at `20260902015059:145-166` | Measured in prod: same job, same helper, live `created_at` → 0 rows from both surfaces; backdated one hour → 1 and 1. The honest repair is either a per-recipient delay or accepting it as the free tier's shape — identical for `notify_saved_searches_on_new_job`, `instant-job-match` and the digest crons. | M |
| 11 | **Enable HaveIBeenPwned in Supabase Auth** (`F-SEC-08`). | Supabase dashboard | Dashboard toggle, nothing in-repo. Still correctly listed open in `TODO.md`. | S |
| 12 | **Decide the Android question.** `send-push-notification` carries a complete, maintained FCM v1 branch — OAuth2 token exchange with 55-min caching, `messages:send`, error-status handling, an `fcmConfigured` env gate — with no client to serve. | `supabase/functions/send-push-notification/index.ts:227-266, :312, :327, :61, :466-467`; `capacitor.config.ts:41` | No `android/` directory and zero `assetlinks.json` anywhere in the repo. Either ship Android (**L**) or park/delete the branch (**S**) — but maintaining a dead one is the current state. | L or S |

---

## 5. HYGIENE — dead code, stale test, doc drift; no user impact

| # | What | Where (file:line or surface) | Evidence it is still open | Effort |
|---|---|---|---|---|
| 1 | `e2e-happy-path` is the only red workflow, and it is a **required** check on `main`. 5 failed / 845 skipped / 96 passed. **See the amendment below — this may not be hygiene.** | run `33585488724`; specs `activity-card-density`, `customer-post-job`, `customer-sees-application`, `earnings-views`, `zz-runtime-probe.spec.ts:718` | The 5th failure is `zz-runtime-probe` asserting an AASA `paths` array that was just rewritten — that one is genuinely a lagging test. The other four are **contested**: those spec files are dirty in the working tree (a session is editing them), but the launch fleet reads the same failures as a product defect. **In flight — do not duplicate.** | S |
| 2 | **645 of 946 e2e tests never run in CI**, gated behind `RUN_VISUAL_SWEEP` / `RUN_STATE_SWEEP` / `RUN_ERROR_SWEEP` / `RUN_EMPTY_SWEEP` / `RUN_OVERLAY_SWEEP`, which `e2e-happy-path.yml` sets. So a green "E2E smoke" is 96 tests, not full-surface coverage. | `.github/workflows/e2e-happy-path.yml`, 5 of 32 spec files | Partially mitigated: `a11y-axe.yml` runs `RUN_VISUAL_SWEEP` separately across four variants. | M |
| 3 | **The axe workflow exists but is not a required check.** Branch protection on `main` lists only `Playwright happy-path smoke`, `Playwright mobile viewports`, `Vitest unit tests`. | `.github/workflows/a11y-axe.yml` (latest main run: success) | Every one of the 51 contrast defects in the last full-surface audit was machine-findable in seconds. Promoting it is a settings change. | S |
| 4 | **Dead `avatarBroken` / `setAvatarBroken` prop chain**, six files deep. `useProfileLandingDerived`'s `hasPhoto = !!avatar_url && !avatarBroken` is now just `!!avatar_url`. | `profileEditForm/types.ts:20-21`, `ProfileEditForm.tsx:48-49,180-181`, `profileLanding/types.ts:31-32`, `ProfileTabPanels.tsx:89-90,144-145,192-193`, `Profile.tsx:90,646-647,719-720`, `PhotoNameSection.tsx:53-54` | The only writer in the chain is `Profile.tsx:495 setAvatarBroken(false)` — nothing ever sets `true`. `PhotoNameSection` declares both props and destructures neither. **Do not touch `CompleteProfile.tsx:72/576`** — that is a separate, live local state with a real `onError`. | S |
| 5 | **`handleIdUpload` and the `idUploading` / `onIdUpload` prop chain are now dead** — the manual ID-upload card was removed when Stripe Identity took over verification. | `Profile.tsx:407-439`, state at `:230`; `ProfileEditForm.tsx:54,59` takes them as `_idUploading`, `_onIdUpload` | **This supersedes the ledger's "the ID picker does not say the file is private" item — there is no ID picker left to fix.** What remains is dead code threaded through three files. | S |
| 6 | **Inline translucent fills survive `prefers-reduced-transparency`.** Blur is fully neutralized; the fills on the two inline-styled nav surfaces are not, and the CSS says so. | Nav pill `MobileNav.tsx:690` (`var(--nav-pill-bg)` = `hsla(0,0%,100%,0.40)` light / `hsl(220 14% 12% / 0.72)` dark, `index.css:349`, `:570`); curtain `MobileNav.tsx:673` (`var(--nav-curtain-top)` = `hsla(40,28%,99%,0.6)`, `:354`, `:575`) | Neither div carries a `.glass*` class, so the opaque-fill list at `index.css:2838-2845` misses both. Fixable by redefining the two tokens inside the `:2798` media block. **The "88px avatar disc" half of this ledger entry is not a defect** — see rejected #14. | S |
| 7 | **`compactClose` is dead.** Its own doc comment says so. | `src/components/ui/dialog.tsx:73, 81, 83, 230, 286, 304, 306` | `:73` reads "NOTHING PASSES THIS as of 2026-09-02". `JobDetailDialog`, the sole `topRightSlot` caller and the reason the prop exists, does not pass it. | S |
| 8 | **`helpr-pass-wallet` is unreferenced and 501s on both terminal branches.** | `supabase/functions/helpr-pass-wallet/index.ts:180, :190` | Zero references outside itself and docs; `scripts/check-dead-edge-functions.mjs:32` already lists it as known-dead. | S |
| 9 | **`submit-partner-application` does not exist, but the write path was revoked on its promise.** Partner applications are structurally undeliverable. | `supabase/migrations/20260618160000:5-11, :19-20` (drops the insert policy, `REVOKE ALL … FROM anon`) | Directory absent; `/become-a-partner` is one of the ten retired routes that now 404 (LATENT #12). Dead end on both sides — either build it or drop the table and amend the migration comment. | M |
| 10 | **`.text-display-eyebrow` is `display: none` while 15 call sites still emit it and 7 doc passages still mandate it.** | CSS `src/index.css:1811`; emitters incl. `EmptyState.tsx:194`, `ReviewsTab.tsx:201`, `EarningHistory.tsx:116`, `AccountDenied.tsx:51`, `AccountBanned.tsx:153`, `AccountPending.tsx:225, :262`; mandates at `AGENTS.md:33, :63` and `SKILL.md:455, 468, 475, 480, 565` | Two files have already noticed and left comments (`HowItWorksSection.tsx:134`, `ApplicantsStates.tsx:406`). Docs and CSS disagree outright — pick a direction, then fix either 15 call sites or 7 doc passages. | M |
| 11 | **`platform_settings.feature_flags` carries four keys nothing reads** — `referrals_enabled`, `boosts_enabled`, `subscriptions_enabled`, `ai_helpr_assistant`. | prod `platform_settings` row | `grep -rn` across `src/` and `supabase/` → **zero** code hits for all four; the only mentions are in the audit ledger itself. `adminHealth/useConfigChecks.ts` already warns about orphan keys. Data-only cleanup. | S |
| 12 | **`TODO.md` carries two entries whose code has moved on.** `F-MONEY-01` (retire `process-scheduled-payouts`) — the racing cron is already gone (`20260618130000` unschedules it; `20260831190419` refuses to re-create it), only the function directory remains. `F-DISC-01` (street-address leak via `open_jobs_safe`) — the view is dropped (`20260618120000:64`) and the RPC masks (`:53`, preserved at `20260901031421:171`). | `TODO.md` | `F-DISC-01`'s one genuinely missing piece is the regression test it names: no test asserts that an anon open-jobs surface never returns a street number. `F-SEO-01` is correctly ticked. | S |
| 13 | **`CLAUDE.md` says AccountPending uses `AppShell`; it uses `AuthShell`.** | `CLAUDE.md:47-48` vs `src/pages/AccountPending.tsx:9, :212` | The code is deliberate (comment at `:208` explains the unification); the doc is stale. One-line fix. | S |
| 14 | **`FULL-SURFACE-2026-08-31.md:492` asserts `scripts/test-signin-link.mjs` does not exist.** It does — 12,177 bytes, executable, documented as verified end to end. | that doc line | One stale sentence in an audit report that another agent will otherwise trust and stall on. | S |
| 15 | **`Login.tsx`'s `?redirect=` is written and never spent.** `ProtectedRoute.tsx:200` encodes it; `Login.tsx:145` reads it only to pick banner copy; `Login.tsx:139` hardcodes `postLoginDest = "/dashboard"`. | `src/pages/Login.tsx:139, :145, :310` | Documented as the owner's explicit "log in → home" choice at `:133-144`, so this is dead plumbing rather than a bug. **Correction to the ledger:** `postAuthDestination` *does* spend a stored redirect — from `localStorage["helpr.signupRedirect"]`, written only by `Signup.tsx:57`. It is the *login query param* that is never spent, not the mechanism. | S |
| 16 | **`/str-settings` route comment and code disagree.** The comment says the page renders read-only for guests so the footer Plans link resolves logged-out; the Route wraps it in `ProtectedRoute` with no guest bypass and the page has no guest branch. | `src/App.tsx:311`, `src/pages/StrSettings.tsx` | Filed today as RW-001 by the in-flight launch fleet. I confirmed the `ProtectedRoute` wrap; I did **not** re-verify the footer link half. Either the docs are stale or every logged-out visitor bounces to login. | S |
| 17 | **`src/integrations/supabase/types.ts` needs regenerating** after `20260901033011` / `20260902014651` made six previously-`NOT NULL` actor columns nullable. | that file | Drives LATENT #9 and any other consumer that trusts the generated non-null types. | S |

---

## Checked and found ALREADY FIXED — do not re-derive these

Every item below appears as "open" in at least one audit document. Each was checked
against the current tree and is closed. The evidence is given so the next person can
refute it in one command rather than re-running the original investigation.

**The blanket correction first:** every "the migration is uncommitted, so this is
still live in prod" claim in `COVERAGE_2026-08-31.md` §3, §4.2, §6.2, §7.4, §8.7,
§9.4, §10 and in `COVERAGE_LEDGER.md`'s deletion, force-update and seed-visibility
lanes. All 17 migrations `20260831232513` → `20260902024735` are ancestors of
`origin/main` and have been applied by `db-deploy.yml`. Verify with
`git merge-base --is-ancestor <sha> origin/main`.

| # | Rejected claim | Why it is closed |
|---|---|---|
| 1 | The street-address leak is live — a helper who merely tapped Apply can read the full address and 7 dp GPS | `20260831232513_address_only_when_offered.sql` (commit `ad315368`) plus its re-application `20260901033219_readdress_only_when_offered.sql` (`c6dd9915`) are both pushed and deployed |
| 2 | **The pending trap** — `ProtectedRoute` evaluates `approval_status === 'pending'` before the profile gate, bouncing incomplete users to `/account-pending` from every route | Reordered. `ProtectedRoute.tsx:291-307` documents the fix; the profile gate is now answered *before* the `pending` bounce at `:334` |
| 3 | `time_credits` self-mint — RLS lets any authenticated user insert their own ledger rows | Table **dropped**. `20260901035602:88-130` asserts 0 rows then `DROP TABLE IF EXISTS public.time_credits`. Also retires fleet finding SI-002 |
| 4 | `platform_settings.platform_fee_percent = 15` is returned to `anon` by a SECURITY DEFINER RPC | `20260901035602:180-206` rebuilds `get_public_platform_settings()`; the column is not in the returned set |
| 5 | `payout_transfers` FKs are `ON DELETE RESTRICT`, so any user ever paid can never be deleted (32% of prod accounts) | `20260901033011:135-140` re-points `helper_id` RESTRICT → SET NULL and `jobs.customer_id` CASCADE → SET NULL |
| 6 | `reviews.reviewer_id` and `disputes.opener_id` are `ON DELETE CASCADE`, so one departure destroys others' records | Both in the SET NULL spec list of `20260901033011`. (`reviewee_id` stays CASCADE deliberately, documented `:41-45` — that is policy, not the reported defect) |
| 7 | `Profile.tsx:505` carries a dead `arrived`/`awaiting` enum list that throws 22P02, so in-app self-deletion is 100% broken | Pre-check deleted. `Profile.tsx:512-534` is now a comment explaining why there is none; `:537` invokes `delete-own-account` directly |
| 8 | `/warnings` is a 404 and two live prod notifications point at it | `src/App.tsx:271` — `<Route path="/warnings" element={<Navigate to="/profile?tab=warnings" replace />} />` |
| 9 | Every gift-card claim link is dead — the `?claim=` token is dropped by a bare `<Navigate>` | `App.tsx:156-159` `PreserveQueryRedirect` (forwards `pathname`, `search`, `hash`), used at `:329` |
| 10 | Offline mutations are silently lost — `queryClient` needs `networkMode: "always"` | `src/lib/queryClient.ts:93` `networkMode: "always"`, `:98` `retry: 0`, inside the `mutations:` block |
| 11 | `requireOnline()` reads `navigator.onLine` only and never consults `@capacitor/network` | `src/lib/requireOnline.ts:57-59` reads both, `onlineManager` pinned to Capacitor in `appLifecycle.ts` |
| 12 | `/reset-password` has no loading state, so the wrong copy paints on a valid link | `ResetPassword.tsx:33, :67-70` — `ready` now distinguishes three cases. (The *silent success* half is still open — LIVE #29) |
| 13 | Senior mode turns every `Switch` into a 51×48 blob | `index.css:2873-2875` — the rule now carries `:not([role="checkbox"]):not([role="radio"]):not([role="switch"])`; the unguarded duplicate is gone, with a verification bench recorded at `:2870-2872` |
| 14 | The 88px profile avatar disc keeps a translucent inline background under reduced transparency | Not a defect. `IdentityHeader.tsx:144` sets only `boxShadow`; its wrapper card `:95` uses `liquid-glass`, which the fill list already opacifies; `UserAvatar`'s fallback uses opaque gradient stops |
| 15 | 28×28 status pips on `/my-jobs` cannot be lifted by senior mode | `JobTracking.tsx:1490-1508` adds an invisible 44×44 overlay child; non-tooltip steps are non-interactive `<div>`s, so no tap target is undersized |
| 16 | Re-submitting `/complete-profile` overwrites `accepted_terms_at`, destroying the original consent | `20260901035252:139-145` pins it: `IF OLD.accepted_terms_at IS NOT NULL THEN NEW.accepted_terms_at := OLD.accepted_terms_at;` |
| 17 | Consent is displayed at signup and never recorded — `CompleteProfile.tsx` is the only writer | `Signup.tsx:275` sends `termsAccepted`; `complete-signup/index.ts:465-467` writes `terms_version_accepted`, `terms_accepted_at` **and** `accepted_terms_at` |
| 18 | `IdentityHeader` paints the ID-verified shield over a blank/monogram avatar | `IdentityHeader.tsx:166` now passes `onPhotoRejected={setPhotoRejection}`; `PhotoNameSection.tsx:74` does the same |
| 19 | `statusBadge` is missing `pending_approval` and its test hardcodes the same 7 | `activityConstants.ts:100` is `Record<JobStatus, string>` with `:108 pending_approval`, guarded by `activityConstants.test.ts:100-101` |
| 20 | `SHOW_SEED_JOBS_PUBLICLY` reaches only 1 of 3 browse surfaces | Constant removed from `src/`; authority moved to `public.seed_jobs_hidden_publicly()` (`20260901035245`), read by all three surfaces **and** the saved-search trigger. What remains is the owner's flip decision (OWNER-ONLY #8) |
| 21 | `enforce_open_job_limit` charges recurring posters then refuses the visit | `20260901030422:169-171` — `IF NEW.status IS DISTINCT FROM 'open' THEN RETURN NEW; END IF;` |
| 22 | `group_job_helpers` has no DELETE policy and its UPDATE policy is a theft path | `20260901030422:79-93` adds the poster-scoped DELETE policy; `:69-77` makes UPDATE poster-only with a matching `WITH CHECK`; `:95-119` adds a `freeze_group_roster_identity` trigger |
| 23 | `notify_helpers_on_job_post` fires on a job nobody can see, and carries the NULL `payment_status` hole | `20260902015059` replaces it with two triggers that fire on the funded transition, both using `COALESCE(…, '')` |
| 24 | `instant-job-match` fans out push for unfunded jobs | Verified fixed in the current function source |
| 25 | `weekly-helper-report` discards its notification insert and always reports a clean run | Verified fixed |
| 26 | `auto-release-payment` answers 500 on every run from one stuck test row | Verified fixed |
| 27 | `_shared/paginate.ts:157` `scanAll` discards pages already read when a later page errors | Fixed by the avatar lane — returns the accumulated prefix with `complete:false` + `error`; 17 tests |
| 28 | `admin-delete-user`'s active-work guard uses non-member enum values → 500 for every admin deletion | Fixed; now byte-identical to `delete-own-account`'s corrected list |
| 29 | `slack-ops-alert` requires a `kind` field no SQL watcher sends, so every automated ops alert 400s | Fixed — `kind` optional, severity normalised. (The *browser* caller is a separate, still-open issue: LIVE #26) |
| 30 | `str-ical-sync` fetches a user-supplied URL with no scheme or private-IP restriction and no size cap (SSRF + OOM) | New `str-ical-sync/safeFetch.ts`: scheme+port allowlists, IPv4/IPv6 private-range classification, DNS all-address checks, `redirect:"manual"` re-validated per hop, `Content-Length` **and** streamed cap at `MAX_FEED_BYTES = 2MB` |
| 31 | `engagement-automations` reads suppression and opt-out lists unbounded | Now branches on `error` (`:231`) **and** `!complete` (`:247`), same for opt-out (`:286`/`:296`). Only `send-marketing-blast` remains (LATENT #4) |
| 32 | `review-nag-cron`'s `[24,36)` / `[72,84)` windows miss half of all completed jobs | Windows are now `[24,48)` / `[72,96)`, matching the daily cron. Only the function's header narrative still says otherwise. (The *destination* is still wrong — LIVE #11) |
| 33 | `ios/App.entitlements` claims the apex domain and drops `webcredentials:` | Both entitlement files now carry `applinks:` **and** `webcredentials:www.louisianahelpr.com`; the root template's `:66` documents the old defect |
| 34 | AASA is missing 15 live notification link shapes (~53% of links open Safari) | Rewritten with per-path row-count justifications; 12 of the 15 now claimed. Two are deliberately excluded pending the hash fix (LIVE #7) and two remain genuinely missing (LIVE #8) |
| 35 | `knip` has been failing all along (16 unused files, 1 unused dependency) | `npx knip` → exit 0. Remaining output is `warn`-level only |
| 36 | `db-smoke` and `test.yml` are red on every run | Both green. `29dde7eb`'s allowlist realignment and `ac3a8edc`'s `runner`-context fix both worked |
| 37 | 7 KNOWN RED tests in two untracked files (`ProtectedRouteGates`, `auto-resolve-disputes`) | Both files are now tracked and green — 2 files, 36 tests, 0 failed |
| 38 | The five contradictions between audit documents (UNVERIFIED allowed vs forbidden; gloss vs no-gloss; branch+PR vs commit-to-main; MCP `apply_migration` vs NEVER; key handling) | All five reconciled in-file — `SKILL.md:181-183` and `:1659-1661`, `SKILL.md:574-575` vs `AGENTS.md:13-19`, `.claude/commands/audit.md:94` and `:99`, `FULL_SURFACE_AUDIT_PROMPT.md:37` |
| 39 | `sitemap-drift` has no CI check; nothing regenerates the sitemap from the route table | `.github/workflows/sitemap-drift.yml` runs `generate-sitemap.mjs --check`, deriving URLs from `src/App.tsx`. Deliberately not a required check |
| 40 | Fleet findings **SI-001** (`worker_protection_credits` never issued) and **SI-004** (`helper_circles` dead tables) | Both tables were **dropped on 2026-08-30** by `20260830072801_drop_unused_scaffold_tables.sql:34-43` (pushed). SI-001's sole `src/` "reference" is a code comment in `CancellationDialog.tsx:405-420` explaining that the credit does not exist |
| 41 | Fleet findings **SI-003 / SI-005** (`create_business_api_key` + `business_*` objects still a live attack surface) | `20260828004538:39-69` explicitly `DROP FUNCTION IF EXISTS public.create_business_api_key(uuid, text)` plus 17 sibling functions and 3 tables; `20260828011811:265-319` drops the rest. A prior lane confirmed both approval functions absent from prod |
| 42 | The four prod accounts trapped behind the Big-7 gate with no signal about what was missing | `CompleteProfile.tsx` now renders the checklist ("Still needed → City · 5 of 6 done"), previews an existing avatar, keeps preview and gate in sync, and guards the silent write with `unwrapMutationRow` |
| 43 | `process-scheduled-payouts` under-filled group jobs can never be released (`allRosterPaid` needs `helpers_needed`) | Now counted against the actual roster (`:637`), with a Slack page naming the unallocated escrow share |
| 44 | The `?ref=` referral handoff needs a client change nobody owns | Addressed by `20260901035252_signup_consent_referral_integrity.sql` together with the `complete-signup` write path |
| 45 | A blanket `.truncate` → `line-clamp` rule will fix senior-mode text loss | **Won't-fix as stated.** Tried, measured better, and rejected on the screenshots (silent mid-word amputation with no ellipsis); `index.css:2731-2753` carries a "do NOT re-add" note. Only per-component work is acceptable — LIVE #25 |

---

## Amendment — filed after this document was compiled (2026-09-02T03:35Z)

The `launch-2026-09` fleet is running concurrently and filed four findings after
this sweep closed. They are recorded here unresolved rather than silently folded
in, because two of them **contradict** a verdict above and the contradiction is
the useful part.

| ID | Claim | How it bears on this document |
|---|---|---|
| **O-002** (blocker) | `/my-posts` renders **no job card for an authed customer** — the single cause behind three of the five red e2e specs (`customer-post-job` "shows a posted job", `customer-sees-application` "surfaces the applicant count", `activity-card-density` "No-Show appears once start time passed"). Element not found on both attempts and retries. | **Directly contests HYGIENE #1.** If O-002 holds, three of those failures are a **LIVE DEFECT on the poster's primary screen**, not a lagging test, and belong at or near the top of §1. Settle this before triaging anything else in §5 — it is the highest-stakes open question in the document. |
| **O-004** | The `earnings tab renders one view at a time` spec fails on main; two earnings views may render simultaneously or none does. `?tab=payment` aliases `?tab=earnings`, a plausible double-render source. | Same contest, for the fourth red spec. |
| **O-003** | The two `zz-runtime-probe` AASA assertions are failing — "AASA is served over HTTPS with the claimed paths" and "apex serves AASA without a redirect". | Consistent with my read (the file was rewritten and the expectation lags), **but** the apex-redirect assertion is a different claim from the paths one and is worth confirming independently: a genuinely broken AASA means universal links do not open the app at all, which would promote LIVE #7 and #8. |
| **RW-003** | `/admin?view=map` and `/admin?view=parishtax` are referenced in `src/` but are not keys in `VIEW_LABELS`, so `Admin` cannot render them. A stale `?view=parishtax` link previously rendered the home dashboard with an `AdminSectionHeader` whose title was `undefined` — an empty `h1` stacked on home's own. | New; not covered above. Confirm the `isRealView` guard covers both and that nothing still links to them. Admin-only, so HYGIENE-to-LATENT. Effort **S**. |

Fleet findings **SI-001, SI-003, SI-004, SI-005** are answered in the rejected
table (#40, #41) — all four objects were dropped by migrations that are already
deployed, and the fleet's own repro notes say they need live `pg_proc`/`pg_tables`
verification, which the migration files supply.

---

### TODO/FIXME/HACK/XXX sweep — nothing to report

`src/`: **10 hits, zero real gaps.** Six are assertions in `proTiers.parity.test.ts`
that TODO placeholders are *absent*; one describes a test's own flagging behaviour;
two are `XXX` format placeholders (`$X,XXX.XX`, `(XXX) XXX-XXXX`); one notes a
live-tier price ID is a deliberate, env-gated placeholder until Basic ships live.

`supabase/functions/`: **zero matches** across 124 `.ts` files. This repo records
known gaps as prose header comments rather than inline markers, and every such gap
found is already a row above.
