# Louisiana Helpr morning report — 2026-09-12 (run 21:40 PDT)

7 things need attention

- **Main's core check is red.** The "Test" job (lint, types, build) failed on the latest finished push. The dead-code step (knip) found 132 unused exports and 18 unused types. A run on the newest commit ("Dead-code gate green") is still in progress, so this may already be fixed. Run 34737883595.
- **The user-journeys suite is red, with three failures.** (1) In the marketplace journey, the helper never sees the funded job in Browse (waited 60s). (2) The IDOR test asks for `payout_transfers.amount`, a column prod does not have, so the test is broken, not the security. (3) The keyboard journey for a new account failed; the log excerpt does not say why. Run 34736754204.
- **The abuse and notifications suite is red on wording only.** The server correctly rejects a job post that mentions an off-platform payment service. But the test expects the phrase "detected in the job description", and the server now says "mentioned in the job description". A commit named "Contact-leak rejection match covers mentioned" may fix it, but nothing has re-run since. Run 34736718685.
- **The DB drift detector has been red for three nights in a row.** `src/integrations/supabase/types.ts` no longer matches the live schema. The fix is `npm run db:types`, then fixing the type errors that surfaces. Run 34687532189.
- **The write-contract snapshot refresh has never passed.** Both runs so far failed because the Supabase CLI in CI cannot reach the database ("IPv6 is not supported… run supabase link"). Run 34737567728.
- **The prod accessibility sweep is red on its "every screen rendered" gate.** In WebKit, the Wrapped screen navigated away mid-check. In Chromium, Admin Export did the same. Axe itself found nothing, but those screens went unchecked. Run 34736755077.
- **The nightly WebKit run failed on its only run so far.** The apply-dialog fit tests at 375 and 320 failed, and so did the one-sheet apply form test. Run 34724281422.

## Also worth knowing
- **Production is current.** The live site serves 2816d5ebc, which is the newest commit that ships.
- **About 370 runs were cancelled in the last day.** Nearly all were superseded by rapid pushes to main. A commit named "CI: runs on main finish instead of cancelling each other" landed tonight to stop this.
- **These went red and are green again on their latest run:** Race Runner (5 fails), Prod freshness (3), Vitest (8), Press every control, UI sweep, A11y axe, E2E real backend and E2E smoke. Also DB deploy, Sentry release and Bundle size.
- **No open `nightly-red` or `prod-errors` issues exist,** even though the reds above are real. The label-sync fix only landed tonight, so these reds have no issues yet.
- **Sentry shows 8 new issues and 1 regressed issue in the last 24 hours.** They are low user counts (1–2 users each), probably the test accounts:
  - "ZIP 99999 resolved to no Louisiana parish": 37 events, first seen 3h ago. Likely the messy-input audit.
  - "Edge Function returned a non-2xx status code": 12 events, 1h ago.
  - "Xr": 11 events, 47m ago, still firing. The minified name hides what it is.
  - "dashboard.ctx sub-query failed: JWT expired": 8 events, 11m ago. There is also a plain "JWT expired" with 5 events, 11m ago.
  - "Vv": 3 events. "We couldn't load jobs." error screen: 1 event. "Loading jobs timed out": 1 event.
  - Regressed: JAVASCRIPT-1C "Cannot read properties of undefined (reading 'default')" in the lucide chunk, 1 event.
- **docs/OPEN.md has 66 open items and 82 done.** About 40 were added in the last 24 hours. The biggest themes:
  - Journey-lane defects: Log Out signed out every device (a fix commit landed), saving weekly availability could wipe the week (fix landed), two payout buttons on one card, a stale avatar after "Use Photo", and "ID verified" shown beside "Verification in progress".
  - Time-travel findings: an expired direct offer never tells the poster, and "Expired" shows for the last 59 seconds of a live listing.
  - The move off mock mode onto prod.
  - Cleanup candidates.
  - The launch checklist: switch Stripe to live, and hide seed jobs.
- **160 commits landed on main in the last 24 hours.** In broad groups:
  - Audits moved off mocks onto prod: journeys J1–J8, press-every-control on prod, a prod seed script, a prod a11y sweep, and messy-input and deep-link audits.
  - Real fixes: contact-leak rejection in posts and bios, row locks on application judging (race rate 14/20 → 0/20), atomic weekly-hours save, device-only Log Out, the DOB wheel keyboard, the TrackingMap crash, and DST-correct times.
  - CI plumbing: a race runner, a stale-prod issue opener, the release gate, nightly-red issue sync, and pre-push guards.
