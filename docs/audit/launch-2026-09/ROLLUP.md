# Launch audit — findings rollup

_Generated from findings.jsonl as of its newest entry (2026-09-24T04:17:29.676Z). Do not hand-edit — run `node scripts/audit-bus.mjs rollup`._

**162 open findings** · 5 open launch blockers · 208 fixed · 0 wontfix · 17 obsolete · 14 retracted · 28 duplicate · 429 filed all time

## HIGH (114)

| ID | Blocker | Status | Surface | Claim | Agent | Evidence |
|---|---|---|---|---|---|---|
| O-001 |  | fixed | `.claude/commands/audit.md` | The /audit command still instructed every audit that there is 'no meaningful native code' - the exact sentence CLAUDE.md identifies as the cause of push being dead for the life of the project. FIXED during fleet setup. | lh-orchestrator | 1 |
| O-002 | **YES** | fixed | `/my-posts (Activity posted tab)` | E2E happy-path is RED on main: three independent specs fail because /my-posts renders no job card for an authed customer - customer-post-job ('shows a posted job'), customer-sees-application ('surfaces the applicant count'), and activity-card-density ('No-Show appears once start time passed', timing out on boundingBox). Element(s) not found, both attempts and retries. | lh-orchestrator | 4 |
| SI-006 | **YES** | fixed | `purge_user_data / account deletion (GDPR + CCPA + Apple in-app deletion)` | Account deletion leaves user rows behind in at least 5 tables that have NO foreign key to auth.users, so neither the purge RPC nor any FK cascade touches them. Proven by executing the LIVE purge_user_data() in prod against a seeded synthetic user: 5 of 6 seeded rows SURVIVED. The survivors include job_tracking (helper GPS latitude/longitude history), legal_acceptances (consent record incl. ip_address + user_agent), helper_availability (schedule), favorite_helpers (the deleted helper stays in other users' saved-helper lists), and reports.reporter_id. purge_user_data's own returned report has no counter for any of them -- it reports 22 counters and none names these tables. Residual PII after deletion is a compliance finding and Apple requires in-app deletion to actually work. | lh-schema-integrity | 17 |
| SI-007 |  | fixed | `Supabase STAGING project okpxtpfvwtmbuxugqsws (what the local CLI is linked to)` | Staging is 65 migrations behind prod and still holds NINE tables that were deliberately dropped from prod as dead/unsafe -- including time_credits, whose INSERT policy the 2026-09-01 security lane proved let any signed-in user mint their own currency (HTTP 201 on a self-authored 999999-minute ledger row). Because supabase/.temp/project-ref points at staging, 'supabase migration list --linked' and 'supabase db push' from any dev machine or agent target this stale project, so CLAUDE.md's 'zero migration drift is the standing requirement' is being checked against the wrong database. db-drift-detect.yml only diffs repo-vs-PROD (via secrets.SUPABASE_PROJECT_REF), so nothing in CI has ever looked at staging -- its drift is invisible by construction. | lh-schema-integrity | 2 |
| SI-012 | **YES** | fixed | `src/integrations/supabase/types.ts vs live prod nullability (13 columns)` | The account-deletion retention migrations dropped NOT NULL from 13 columns so that a deleted user's rows could be anonymised via ON DELETE SET NULL -- but the generated TypeScript types were never regenerated. All 13 are nullable in prod and all 13 are still declared as non-null 'string' in types.ts. This is worse than an untyped field: the compiler now actively ASSERTS these can never be null, so every consumer is entitled to dereference them and no guard can be forced by typecheck. The columns are the core money and trust surface -- jobs.customer_id, jobs.helper_id, reviews.reviewer_id, payout_transfers.helper_id, disputes.opener_id, gift_cards.donor_id, skill_endorsements.endorser_id, group_job_helpers.helper_id, referral_codes.user_id, referrals.referrer_id, job_disputes.opened_by, job_revisions.requested_by, jobs.location. The values are all non-null TODAY, which is exactly why this is invisible: the first real account deletion is what turns them null, and it does so on jobs, reviews, payouts and disputes simultaneously. This is the 'crashes on real data that never appears on seeded data' class, and account deletion is a shipping Apple requirement, so it will be exercised. | lh-schema-integrity | 1 |
| SF-001 | **YES** | fixed | `/my-posts — src/lib/jobDate.ts + supabase/functions/_shared/cancellationFee.ts:53` | jobLocalMidnightMs() throws an unguarded RangeError on any date_needed string that is not bare YYYY-MM-DD, from inside a useMemo in the Activity filter path — so ONE unparseable job date kills the ENTIRE /my-posts route with the generic 'This page hit a problem.' boundary instead of degrading one card. cancellationFee.ts:54-61 does dateNeeded.split('-').map(Number) -> Date.UTC(...) -> NaN -> new Date(NaN) -> Intl.DateTimeFormat.formatToParts(Invalid Date) which THROWS. jobDateMs() (src/lib/jobDate.ts:61) guards null/undefined/empty-string but NOT a malformed non-empty string, and there is no Number.isFinite check on utcMidnight before formatToParts. The same function is the money path's cancellation-fee clock in supabase/functions/_shared/cancellationFee.ts, where CancellationFeeJob.date_needed is typed string\|null. | lh-silent-failure | 3 |
| SF-002 |  | fixed | `e2e/happy-path/customer-post-job.spec.ts:105 + customer-sees-application.spec.ts:24 (O-002 root cause)` | O-002 ROOT CAUSE, and it is NOT a product silent-failure. The two element-not-found specs seed date_needed as a full ISO timestamp (new Date(...).toISOString()) into a mocked jobs row, but prod jobs.date_needed is a Postgres 'date' NOT NULL (verified: information_schema.columns on fncmgoasalhdgfwzhsqa), so PostgREST only ever returns bare YYYY-MM-DD. The malformed value crashes jobLocalMidnightMs (see SF-001) and /my-posts renders the RouteErrorBoundary, so the job title is genuinely absent. The repo ALREADY documents the correct shape and ships the helper: e2e/happy-path/seedData.ts:72 says verbatim 'date_needed is a Postgres date, NOT a timestamptz' and exports DATE(n); activity-card-density.spec.ts:76 uses localDate() correctly. Only these two specs bypass it. Fix = use the DATE()/localDate() helper in both. Zero prod RPCs return date_needed as a timestamp (checked pg_get_function_result across all public functions), so the crash is NOT reachable from the real backend today — the E2E red is a fixture defect, and the product exposure is the missing guard filed as SF-001. | lh-silent-failure | 1 |
| SF-003 |  | fixed | `src/components/TermsReconsentDialog.tsx:84 (consent capture)` | Zero-row-write, trust/legal. The profiles UPDATE setting terms_version_accepted + terms_accepted_at has no .select() and no unwrapMutation() — its only check is 'if (error) throw error', and a zero-row match returns { data: [], error: null } so it falls straight through. Worse, the legal_acceptances INSERT on line 95 succeeds REGARDLESS, so the audit table records a consent event for a terms version the profile never pinned — two systems permanently disagree about whether this user accepted. What the user sees: hapticSuccess(), setAcceptedVersion(LATEST_TERMS_VERSION) closes the non-dismissible gate locally, refresh()+invalidateQueries run, no error. The re-consent modal then returns on the next cold launch with no explanation. On a real-money, age-restricted platform, unrecorded consent is a compliance exposure. | lh-silent-failure | 3 |
| AR-001 | **YES** | fixed | `PostgREST /rest/v1/rpc/apply_consequence_ladder (prod fncmgoasalhdgfwzhsqa)` | apply_consequence_ladder is SECURITY DEFINER, takes target user as the p_user parameter, sets app.trusted_ladder_write to bypass prevent_self_escalation, and has NO authorization check (no has_role/admin, no auth.uid gate, no CRON/service check). EXECUTE is granted to anon AND authenticated. Any unauthenticated caller holding only the public anon key can permanently ban, temp-ban, or final-warning ANY user by uuid, insert arbitrary user_violations + user_bans rows, and spam notifications. Banning every admin locks the team out of /admin. It is an internal core meant to be called only by sibling SECURITY DEFINER wrappers (report_helper_no_show etc.); the default GRANT EXECUTE TO public was never revoked. | lh-authz-rls | 6 |
| ME-001 |  | fixed | `revision flow → escrow auto-release` | The 72h post-revision auto-release promised to BOTH parties is enforced by nothing: no cron, RPC, or edge-function path ever releases escrow when revision_acceptance_deadline lapses. create-payment resolve_revision notifies the poster 'If you do nothing, payment auto-releases' and ActiveJobSection.tsx:313-319 shows the helper 'Poster didn't respond — payment auto-releasing', but auto-release-payment Phase 1 filters .is('revision_requested_at', null) (index.ts:123) and resolve_revision never clears revision_requested_at or changes status, so a revision job is excluded forever. Escrow strands until a human acts. | lh-money-escrow | 4 |
| ME-005 |  | fixed | `post-job checkout summary → service-fee percent source` | The displayed poster service-fee percent has THREE sources that only coincide by luck: the client seeds it from platform_settings.customer_fee_percent via RPC and keeps it when the poster-profile read errors (useJobFormEffects.ts:175-213), falls back to a legacy 10 (legacyFeeFallback.ts:30) when that column is NULL, while the server charges posterFeePercentForTier(tier) with DEFAULT_TIER_FEE_PERCENT=12 fallback and explicitly refuses to bind customer_fee_percent (create-payment/index.ts:251-302). Additionally useJobDerived.ts:118 and CheckoutStep.tsx:454 carry hardcoded 12s instead of deriving from subscriptionTiers.ts. Editing the admin-writable DB column, or a NULL, shows the poster one percent and charges another (shown $220, charged $224 on a $200 job) with no error. | lh-money-escrow | 6 |
| ME-006 |  | verified | `Terms → tip fee disclosure` | TermsSection.tsx:132 states '100% of tips go to the Helpr — no platform fee on tips', but create-payment's tip action takes application_fee_amount = stripeProcessingCostCents(tipCents) (2.9%+30c) on every tip (index.ts:816,834) — a $5 tip nets the helper $4.55 (91%). TipDialog's own copy is honest about the card-processing deduction; the Terms sentence is a false binding fee disclosure. | lh-money-escrow | 2 |
| CJ-001 | **YES** | fixed | `db: sweep_silent_cron_failures() / cron_run_log` | The app's own silent-cron detector CRASHES daily at 08:47 UTC and its crash rolls back the run-log ingest for a whole hour, permanently. sweep_silent_cron_failures() casts (body->>candidate_key)::numeric, but money-reconciliation's registered candidate_key 'scanned' is a JSON OBJECT, not a number, so the cast raises 22P02. Because the ingest INSERT and the detection loop are one transaction, the raise rolls back the ingest of every cron response in that 60-minute window; the next run's lookback is only 60 minutes, so that hour is lost forever. Consequences observed in prod: (a) money-reconciliation and subscription-reconciliation have ZERO rows in cron_run_log ever, so their registered work expectations can never be evaluated; (b) 2026-09-01 07:47-08:47 is a permanent hole in cron_run_log for EVERY http cron; (c) the cron_work_expectations note for money-reconciliation asserts 'the numeric cast yields 0 candidates', which is the opposite of what the code does. | lh-cron-jobs | 1 |
| CC-001 |  | fixed | `22 sites across 18 files — apply flow, review form, security tab, signup, 8 admin queues` | Raw database/API error text is shown directly to users. 8 sites do toast.error(error.message) with NO fallback; 14 more do toast.error(err.message \|\| "friendly copy") where the RAW message wins and the human copy only renders if the raw one is empty — backwards. A Supabase failure surfaces text like 'new row violates row-level security policy for table "reviews"' or a unique-constraint name to the user, leaking schema internals and reading as a crash. | lh-copy-content | 4 |
| GD-001 |  | fixed | `src/integrations/supabase/types.ts vs prod schema (fncmgoasalhdgfwzhsqa)` | types.ts is stale again: legal_acceptances.user_id and reports.reporter_id are NULLABLE in prod but the committed types assert non-null. Migration 20260902051631 landed TODAY and prod already holds 8 anonymised legal_acceptances rows (user_id IS NULL). This is the SI-012 incident recurring within hours of it being fixed. | lh-generated-drift | 5 |
| GD-002 |  | fixed | `CI / git hooks / cron — types.ts freshness enforcement` | NOTHING enforces types.ts freshness. Zero references to 'gen types', 'db:types' or 'types.ts' exist across all 24 .github/workflows files, .husky/pre-commit, or the lint-staged config. This is the root cause of SI-012 and of GD-001: a migration can drop NOT NULL, deploy green via db-deploy.yml, and leave the compiler asserting a withdrawn guarantee indefinitely. db-drift-detect.yml does NOT close this gap - it compares migration VERSION LISTS (repo filenames vs prod schema_migrations), so a migration present in both sides reports zero drift while types.ts is arbitrarily stale. Notably, every OTHER generated/mirrored artifact in this repo already has a guard: sitemap (generate-sitemap.mjs --check in sitemap-drift.yml), iOS metadata (ios-metadata.yml regenerates+commits), iOS icons (ios-icon-sync.yml), the migration ledger (db-drift-detect.yml, fails the run), the legalVersions mirror (legalVersions.parity.test.ts), and the Stripe type shim (typecheck-edge.mjs asserts the import mapping is live). types.ts is the only one with no guard, and it is the one that gates type safety for all of src/. | lh-generated-drift | 4 |
| NB-004 | **YES** | filed | `iOS push notifications, end to end (every installed build)` | Push is STILL 100% dead in production. The AppDelegate APNs fix exists only in main and has never been shipped: it landed in ad315368 on 2026-08-31 20:24 PDT, and the most recent TestFlight build ran on 7b057572, committed 2026-08-31 15:21 PDT — five hours EARLIER. 'git merge-base --is-ancestor ad315368' is false for both of the last two builds, and 'git show 7b057572:ios/App/App/AppDelegate.swift \| grep -c capacitorDidRegisterForRemoteNotifications' returns 0. So every build any user or tester has installed still drops the APNs device token on the floor. Live prod confirms it: push_tokens has 0 rows and max(created_at) is NULL — not one token has EVER been persisted — while notification_logs shows 168 sends status='skipped' error='no_registered_devices' between 2026-09-01 00:57Z and 2026-09-02 16:26Z. 86 commits now sit between the last shipped build and origin/main. This is not a code defect any more; it is an unshipped fix, and the only remedy is cutting an iOS build. IMPORTANT POSITIVE: the two token_deleted rows show APNs answered a seeded fake token with HTTP 400 BadDeviceToken, which proves the server-side APNs stack (ES256 JWT, key, topic, endpoint) authenticates correctly and rejects only the token — the build is the sole missing link. | lh-native-bridge | 4 |
| DH-001 |  | filed | `/jobs/:id (public job page + Share link) → /dashboard?quickApply=` | The app hands the poster a share link to their own job and then refuses them when they open it. JobDetail.tsx:105 hard-redirects EVERY signed-in visitor to /dashboard?quickApply=<id> with no owner branch, so the job's own poster is routed into the apply flow. open_jobs_browse deliberately whitelists customer_id = auth.uid(), so the fallback read succeeds and QuickApplyHandler.tsx:141 fires 'You can't apply to your own post.' For a non-open own job the same redirect yields 'This task isn't available to open yet - if you just got the alert, try again in a few minutes.' - false on all three counts for the owner. The link is manufactured by the poster's own Share chip (ShareJobButton.tsx:177 via PostedJobActions.tsx:322) and Share is the PRIMARY empty-state CTA when a job has no applicants (ApplicantsStates.tsx:247). Missing branch belongs at JobDetail.tsx:105: if the viewer is job.customer_id, route to their own posted-job view, never the apply flow. | lh-design-holes | 2 |
| NB-008 | **YES** | fixed | `biometricGate.requireBiometric — 21 call sites incl. app lock, instant payout, admin grant/ban/refund/bulk-payout` | requireBiometric() fails OPEN on biometry LOCKOUT, not just on 'no biometry enrolled'. src/lib/biometricGate.ts:44 reads only info.isAvailable and returns true when it is false. In the plugin, isAvailable is literally context.canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics) (BiometricAuthNative.swift:38), which iOS returns FALSE for with LAError.biometryLockout after five consecutive failed biometric attempts. So the documented and intended fail-open for 'this device has no Face ID enrolled' also fires for 'someone just failed Face ID five times in a row' — and the gate opens with no credential presented at all. That defeats the control precisely in its threat model: a stolen or borrowed phone. Worst instance is AppLockGate.tsx:152 ('Unlock Louisiana Helpr') — the app lock itself is bypassed by failing Face ID five times. The other 20 sites include instant-payout and referral cash-out, payout account change/remove/reset, and the admin actions: change a user's login email, permanently delete a user, settle a dispute, issue a refund, run individual AND bulk payouts, ban a user, GRANT ADMIN ACCESS and remove an admin. The fix is available and unused: checkBiometry already returns code ('biometryLockout') and deviceIsSecure (canEvaluatePolicy(.deviceOwnerAuthentication), which stays TRUE during biometry lockout because passcode still works), so the gate can distinguish the two cases and fall back to the passcode via the allowDeviceCredential:true it already passes. NOT FIXED BY ME: this is auth plus money and belongs behind a reviewer. | lh-native-bridge | 7 |
| WD-001 | **YES** | fixed | `Global — every .liquid-glass/.glass-* frosted surface (login/signup card, top nav, sticky headers, chat composer dock, every Dialog/AlertDialog via .glass-modal, form field glass, skeleton loaders)` | The app's entire frosted-glass design language renders with ZERO backdrop blur for every Chromium-based visitor (desktop Chrome/Edge, and the Android Capacitor WebView) in the PRODUCTION build. Only WebKit/Safari/iOS gets the intended blur. This is the mirror image of the already-fixed reduce-transparency minifier bug documented in CLAUDE.md: that fix special-cased ONE rule pair behind an @supports guard, but every OTHER backdrop-filter pair in src/index.css (11 rules: .glass, .glass-nav, .glass-nav.is-scrolled, .liquid-glass, .tracker-merged>.liquid-glass, .glass-header, .glass-dock, .glass-modal, .glass-field, the :has() adornment override, .skeleton-glass — src/index.css:1290,1732,1751,1876,1929,1942,1965,1979,2006,2026,2343) still writes 'backdrop-filter: X; -webkit-backdrop-filter: X;' as a plain sequential pair with no @supports boundary. Vite's CSS minifier collapses the pair and keeps only the LAST-declared one — and in source the unprefixed comes first, -webkit- second, so the minifier drops the unprefixed 'backdrop-filter' and ships '-webkit-backdrop-filter' alone. Confirmed empirically that Chromium does NOT apply blur from a bare -webkit-backdrop-filter declaration with no unprefixed twin (getComputedStyle reports the effective 'backdrop-filter' as literal 'none'), while WebKit still applies it because WebKit aliases the two. On the DEV SERVER (unminified) both properties survive and both engines render correctly — this is a build-only defect, invisible unless you check dist. | lh-webkit-differ | 14 |
| EF-004 | **YES** | fixed | `PROD edge function 'boost-job' — deployed, ACTIVE, zero source in the repo` | boost-job is live and callable in prod but its source was deleted from the repo on 2026-08-29, so no review, lint, typecheck, CI or grep can see it. It is a mass push-notification amplifier whose rate limiting is now entirely inoperative. The deployed code (verified below to be the deleted source) fires up to MAX_NOTIFICATIONS=200 real push notifications per invocation to approved, non-banned helpers within 80km, via the still-live send-push-notification + push_tokens. Its two documented rate limits were (a) a query guard on public.job_boosts and (b) 'the unique index on job_boosts (DB-level guarantee)'. public.job_boosts NO LONGER EXISTS in prod - to_regclass returns NULL - which removes (b) entirely and makes (a) FAIL OPEN by explicit design: 'if (boostCheckErr) { console.warn(...); }' with the comment 'table may not be deployed yet - Log and proceed rather than blocking the poster'. That branch was a reasonable deploy-lag accommodation while the table was arriving; with the table permanently gone it is a permanent bypass. Remaining guards are only: caller owns the job, job status='open', job age>=24h. So any authenticated user with one open 24-hour-old job can call it in a loop and blast 200 Helpr-branded push notifications to real users' phones per call, unbounded and unlogged (the job_boosts write at the end also fails). Note npm run deadcode:functions cannot detect this - it scans the repo, and this function is not in the repo. FIX IS OUTSIDE MY TERRITORY AND OUTSIDE THE REPO: it is 'supabase functions delete boost-job --project-ref fncmgoasalhdgfwzhsqa', an owner/orchestrator action against prod. I have not run it. | lh-edge-functions | 4 |
| EF-006 |  | fixed | `resend-webhook → suppressed_emails (bounce + complaint suppression)` | RESEND_WEBHOOK_SECRET is NOT SET in the deployed prod environment, so resend-webhook refuses EVERY Resend delivery with 503 and has never ingested a single bounce or complaint. The platform is actively sending mail (171 emails all-time, 160 in the last 30 days, most recent 2026-09-02 18:24Z) into a suppression list that has 0 rows. Consequence: hard-bounced addresses keep being mailed and spam complaints are never honoured, which is precisely how a sending domain's reputation degrades - and the mail that then starts landing in spam includes password resets, email verification and payment receipts, i.e. the auth channel. Resend will also retry a 503 and can disable a persistently failing endpoint outright. Nothing surfaces this: the missing-secret branch does console.error and returns 503 with NO postSlackOpsAlert, unlike stripe-webhook's equivalent branch which does alert. This is the SECOND time this exact function has been silently dead - its own header comment documents the first (a Fetch Headers vs plain-object bug that made it 401 every delivery, found only after the fact), and config.toml's comment names the empty suppressed_emails table as 'the exact state that made both suppression guards no-ops'. The code itself is correct and fails closed by design; the defect is purely that the secret was never provisioned. FIX IS OUTSIDE THE REPO: supabase secrets set RESEND_WEBHOOK_SECRET=whsec_... for project fncmgoasalhdgfwzhsqa, taken from the Resend webhook endpoint's signing secret. That is an owner action - I cannot mint the secret. Filed HIGH but NOT --blocker: it degrades deliverability progressively rather than breaking a launch-day flow, though a verifier who weights the auth channel may reasonably upgrade it. | lh-edge-functions | 2 |
| NB-013 |  | filed | `Password reset + signup confirmation on native — /reset-password and /account-pending cannot be claimed as Universal Links` | RESTATES the retracted OPEN_ITEMS item 7 (see NB-012) as the defect that is actually live. The hash is no longer the blocker; the session-construction ORDER is, and it is a worse bug because it dead-ends the user at the last possible moment. supabase-js reads the URL fragment only inside detectSessionInUrl, which runs ONCE at client CONSTRUCTION. On native the client is built at app boot from capacitor://localhost/ with no fragment — long before appUrlOpen delivers the link. So if /reset-password were claimed in the AASA, the recovery tokens would arrive in window.location.hash, ResetPassword.tsx:79-80 would read type=recovery and set ready=true, the form would render, the user would type a new password, and ONLY THEN would supabase.auth.updateUser({password}) fail with 'Auth session missing'. Every signal up to the final tap says it is working. /account-pending is the same shape: Signup.tsx sets emailRedirectTo=<origin>/account-pending and the confirmation carries its session in the fragment, so a brand-new user would land on the gate screen signed OUT with tokens sitting unread — a broken signup funnel. CURRENT STATE IS SAFE: both paths are explicitly excluded in the AASA and the reset works in the browser, so nothing is broken for users today. This is filed HIGH because it is a live constraint on the auth surface, the exclusions are the only thing preventing it, and the AASA comments are the sole place the reasoning is recorded — a future lane 'helpfully' claiming those two paths would ship an account-recovery dead end. PRECONDITION FOR CLAIMING THEM: ResetPassword must parse access_token/refresh_token out of the fragment and await supabase.auth.setSession() (or verifyOtp) BEFORE rendering the form, and AccountPending must do the same. Neither does today. | lh-native-bridge | 5 |
| EF-008 | **YES** | fixed | `slack-ops-alert + _shared/slack-alerts.ts → ALL operational alerting` | Slack ops alerting is NOT CONFIGURED in prod, so every operational alert in the system is a silent no-op that reports SUCCESS to its caller. slack-ops-alert answers HTTP 200 {"skipped":true,"reason":"slack_not_configured"} (index.ts:142), and the shared _shared/slack-alerts.ts path returns quietly after a console.warn when neither SLACK_WEBHOOK_URL nor the Lovable key pair is set (slack-alerts.ts:64-71). Because the response is 200 and callers are fire-and-forget, nothing anywhere registers that the alert did not land. This is the meta-failure that silences every other guard the project has built: stripe-webhook's three CRITICAL alerts (STRIPE_SECRET_KEY missing = 'payments, subscriptions and payouts are broken', signature verification failed, idempotency insert failed), the stripe-idv-webhook equivalents, verification-webhook's alerts, and sweep_dead_crons' 'N cron(s) are not running as scheduled'. In the last 7 days prod wrote 124 rows to error_logs tagged source in (cron-dead, cron-http) - 124 alerts that were raised, routed, and delivered nowhere. The durable error_logs rows do survive, which is the saving grace and why this is recoverable, but nothing pages: someone has to already suspect a problem and go read a table. Launching a real-money marketplace whose entire alerting layer answers 200-and-does-nothing is the definition of a blocker. NOTE the code is not wrong - both paths deliberately fail soft so an alerter can never take down the money path it is reporting on, which is correct. The defect is purely that the secret was never provisioned, plus the fact that the no-op is indistinguishable from success at the call site. FIX IS OUTSIDE THE REPO: supabase secrets set SLACK_WEBHOOK_URL=... --project-ref fncmgoasalhdgfwzhsqa (owner action). In-repo follow-up available in my lane: make slack-ops-alert's skipped path write an error_logs row so an unprovisioned alerter is itself visible. | lh-edge-functions | 4 |
| RW-004 |  | fixed | `/signup (About You step)` | Intermittent silent data loss on account creation: phone, location (city), date_of_birth and avatar_url can be silently dropped even though the client validated them (green checkmarks) and the account is created successfully. User is bounced to /complete-profile on next login to re-enter the same data, no error shown at signup time. | lh-route-walker | 1 |
| CV-001 |  | fixed | `audit coverage registry — scripts/audit-coverage.mjs` | The coverage registry was checking itself. It maps surface CLASSES to lanes ('?tab= variants -> lh-route-walker + lh-state-matrix'), which renders green across all 23 tabs and is true — every tab IS walked and state-forced. It answers the wrong question: nothing asked which lane checks what the earnings tab COMPUTES. Class owned, instances orphaned. Measured across all 39 lane definitions: ?tab=saved_helpers and ALL SIX post-a-job entry paths (start fresh, draft, repost, template, AI builder, offer to saved helpr) were named by ZERO lane. /payment-success — the Stripe checkout RETURN page — was named only by lh-native-bridge, which owns the native handoff and not what the page asserts about the money; lh-money-escrow's scope contained neither it nor ?tab=earnings. | main | 2 |
| NT-001 | **YES** | fixed | `push notifications — fan_out_push_on_notification() + notification_type_pref_map (LIVE PROD)` | 43% of all notifications IGNORE the user's notification preferences. The trigger gates push with: SELECT pref_column INTO pref_col FROM notification_type_pref_map WHERE type = NEW.type; IF pref_col IS NOT NULL THEN <check the user's toggle> END IF. A type with NO ROW in that map yields a NULL pref_col, so the category check is SKIPPED ENTIRELY and the push sends on the master push_enabled toggle alone. Three types are sent in prod with no map row: 'warning' (525 sent, most recent 2026-09-02), 'info' (222, most recent 2026-09-02 18:24) and 'success' (16) — 763 of 1773 notifications ever sent, and 'warning' is the single most-sent type in the entire system. The cause is that these three are SEVERITY labels while the map keys on CATEGORY, so the sender picked the wrong axis. Every one of them has a category the user CAN turn off and which is silently not honoured: '📋 New job offer!' is type info while a new_offers toggle exists; 'Helpr is On the Way'/'Arrived'/'Working' are info while transit_updates and work_status exist; 'Application accepted!'/'Application declined' are success/info while job_applications exists; 'Job auto-cancelled' is warning while job_updates exists. A user who turns a category OFF keeps receiving those pushes, with no error and nothing in the UI to suggest the toggle did nothing. | main | 3 |
| SC-001 | **YES** | fixed | `profiles.boost_credit_used_month · create-boost-payment (Pro free-boost ledger)` | The Pro 'one free Job Boost per month' ledger is a CLIENT-WRITABLE column. authenticated holds a column-level UPDATE grant on profiles.boost_credit_used_month and prevent_self_escalation() does NOT pin it, so a Pro member can PATCH their own profile to reset the ledger and mint unlimited free $3 boosts. The 2026-09-01 tier-lockdown migration derived the writable set as the complement of profiles_locked_update_columns(), which lists only the six subscription/Stripe columns — this perk ledger shipped 2026-08-24 and was never added to the locked set. | lh-subscriptions-credits | 1 |
| ST-001 |  | fixed | `Cancellation → CancellationDialog / poster_cancel_job / void-cancelled-payments` | The cancellation fee ladder anchors on MIDNIGHT of date_needed and ignores start_time, so the tier charged is systematically one step harsher than the disclosed policy. job_hours_until_start(date,timestamptz) computes (date_needed::timestamp AT TIME ZONE 'America/Chicago') - now(), never adding start_time. Because start_time >= 00:00 always, the error is ALWAYS against the poster: a job 41 real hours away (disclosed '24+ hours before: 0% free') is charged 25% of budget. 61 of 64 live jobs carry a start_time, up to 18:00, so this is live for essentially every job. Client CancellationDialog.tsx:98 uses the same midnight anchor via jobLocalMidnightMs, so the dialog agrees with the charge but BOTH contradict the ladder copy at CancellationDialog.tsx:301-303 and :338 ('more than 24 hours until the job starts'). | lh-scheduling-time | 3 |
| OA-001 | **YES** | verified | `/signup → complete-signup edge function` | ROOT CAUSE CORRECTION for RW-004. The signup failure is not 'four profile fields dropped' — the complete-signup edge function was NEVER INVOKED, and nothing anywhere recorded that. The account is left with approval_status='pending' (never flipped to approved), terms_version_accepted='', terms_accepted_at NULL, accepted_terms_at NULL, ZERO legal_acceptances rows, and all profile fields NULL. So the 18+ attestation and the Terms/Privacy/Platform-Rules acceptance the user actually ticked are NEVER PERSISTED — that is a consent-capture compliance gap on a real-money platform (lh-audit S4: 'Consent capture'), not just data loss. Signup is not atomic: auth.signUp() creates the account irreversibly, then a second, unguarded network call is responsible for every profile field, the approval flip and the entire legal record. When that second call does not happen there is no compensating write, no retry, no reconciliation sweep, and no error surfaced to the user or to error_logs. | lh-onboarding-auth | 2 |
| N-001 | **YES** | fixed | `send-notification-email → every transactional email` | Transactional email is dead for 86% of accounts. notification_preferences rows are created LAZILY (only when a user opens Profile→Notifications and flips something); handle_new_user never creates one and no migration backfills. send-notification-email/index.ts:152 reads 'if (!prefs \|\| !prefs[prefColumn]) skip' — a MISSING row is treated identically to an explicit opt-out, and logged as reason 'preference_off' for a preference the user never set. | lh-notifications | 6 |
| N-002 |  | fixed | `fan_out_push_on_notification (DB trigger)` | The same missing notification_preferences row makes PUSH fail OPEN while email fails CLOSED. fan_out_push_on_notification wraps its ENTIRE preference gate in 'IF prefs.user_id IS NOT NULL THEN ... END IF', so for an account with no row neither push_enabled NOR the per-category toggle is consulted and the push is sent unconditionally. One missing row, two opposite failure modes. | lh-notifications | 1 |
| N-003 |  | filed | `/profile?tab=notifications → Quiet Hours` | Quiet Hours is enforced in UTC against a UI that presents local time, so the muted window lands ~5-6 hours off. The picker defaults to 22:00-07:00 and reads 'Mute non-critical pushes overnight'; QuietHoursClock documents its inputs as '24hr local'. send-push-notification evaluates isInQuietHours(start,end,new Date()) with no timezone conversion — its own comment says 'no per-user timezone is stored, so we evaluate the window in UTC'. In Louisiana (CDT, UTC-5) a 22:00-07:00 setting actually silences 17:00-02:00 local: pushes are suppressed all evening and resume at 2am — inverted from the stated intent. | lh-notifications | 2 |
| ED-001 |  | fixed | `purge_user_data() → email_tracking (open/click pixel tracking)` | email_tracking (user_id, email_type, event_type, ip_address, user_agent) has NO foreign key to auth.users and is NOT touched anywhere in purge_user_data() — confirmed by reading the live function body (pg_proc.prosrc) and by seed-then-purge on prod. Same defect shape SI-006/the 2026-09-02 migrations fixed for job_tracking, helper_availability, favorite_helpers, legal_acceptances and reports (all five are now handled in the current purge_user_data), but email_tracking was missed because it currently holds 0 rows in prod, so the empirical census method (delete a test account, look for its uuid) can never have found it. It records literal IP address + user agent per email open/click, which is the same PII class as login_history (which IS purged). Currently latent (0 live rows) rather than actively leaking, but it will start accumulating real PII the first time any open/click-tracked template fires, with no deletion path. | lh-email-delivery | 4 |
| SC-002 |  | fixed | `auto-tip-charge (cron, off-session) · Stripe customer resolution` | auto-tip-charge resolves the poster's Stripe customer with customers.list({email, limit:1}) and gives up with 'no_saved_card' if that ONE record has no card — but one email routinely holds several Stripe customer records and only one carries the saved card. This is the SAME bug this repo already diagnosed and fixed in charge-recurring-visits (the only other off-session charger) and in pro-customer-portal, where its documented symptom was 'a paying member could not reach the billing portal to cancel'. Because Stripe's customer list is newest-first, limit:1 favours the MOST RECENT record — very often the empty one. Consequence, unique to this function: it is the only remaining off-session (confirm:true, no user present) charge path still on limit:1, so there is no interactive Checkout to collect a card as a fallback. The poster is told 'Your tip didn't go through', the helper never receives the tip, and auto_tip_candidates() then excludes the job forever because it tests only the EXISTENCE of a source='auto' tips row, never its status. A stale comment at index.ts:322-323 asserting 'there is no stripe_customer_id column on profiles' is why the fix was not applied here — that column has existed since migration 20260901011254. | lh-subscriptions-credits | 4 |
| SC-003 |  | fixed | `auto-tip-charge · /profile?tab=auto_tip — off-session charge with no success notification` | Auto-tip is the only path in the app that charges a saved card OFF-SESSION with the user absent, and it is the one charge that tells the payer NOTHING when it succeeds. auto-tip-charge notifies the poster ONLY on failure ('Your tip didn't go through', index.ts:258-268). On success it writes the tips row and returns — there is no poster notification, no email, and paymentIntents.create sets no receipt_email (index.ts:350-376), so whether Stripe emails anything depends on an account-level dashboard setting the repo cannot assert. The only notification a successful tip produces goes to the HELPER (notify_helper_on_tip trigger, verified live in prod). So the poster's card is debited on a cron schedule, days after they last opened the app, and the app's own record of that charge is invisible to them: the notification asymmetry is exactly backwards, since the failure they can act on is announced and the successful debit they might dispute is silent. This is the classic off-session chargeback pattern, and the /profile?tab=auto_tip screen sets the preference ONCE with no per-charge confirmation, so consent is durable but the exercise of it is unannounced. | lh-subscriptions-credits | 9 |
| SC-004 | **YES** | fixed | `iOS · /profile?tab=subscription → create-pro-checkout (Apple guideline 3.1.1)` | OWNERSHIP NOTE: this lane owns establishing WHAT the Pro/Basic/Elite membership unlocks; lh-compliance-store owns the App Review conclusion. Finding: on the native iOS surface the membership is bought by sending the user OUT of the app to Stripe Checkout (SubscriptionTab.tsx:133-146 handleSubscribe -> openExternalUrl(data.url) with native:true; create-pro-checkout buildRedirectUrl(..., isNative)), and the perk inventory it unlocks is majority DIGITAL-ONLY IN-APP FUNCTIONALITY, which is the exact shape guideline 3.1.1 requires IAP for. Measured from TIER_PERKS (src/lib/subscriptionTiers.ts:121-214), the perks split: IN-APP DIGITAL (the 3.1.1 exposure) — Advanced Analytics (pro/elite, gated server-side by helper_has_advanced_analytics(), verified live in prod), early access to the job feed at 5/10/20 minutes (gated server-side by early_access_cutoff(), verified live), Priority Placement in the applicant list, Portfolio Showcase, Helpr Badge and Featured Crown Badge, and the Reliability Shield strike forgiveness. REAL-WORLD/FINANCIAL (defensible under 3.1.3(e)) — the platform commission ladder 12/11/10/8 percent, Instant Payouts, boost discounts and the free monthly boost, Priority Support. Bundling does not cure it: Apple can require IAP if the subscription unlocks ANY in-app functionality, and here that is five of roughly ten advertised perks. There is a stranded feat/apple-iap branch, so this is a known-open question rather than a settled decision. Anti-steering is the second exposure — the iOS purchase button links straight out to an external web checkout with no IAP alternative. | lh-subscriptions-credits | 5 |
| CC-001#lh-concurrency-cache | **YES** | fixed | `app boot (every route, web + WKWebView)` | If localStorage access throws (Safari 'Block All Cookies', WKWebView/embedded-browser storage disabled, enterprise/kiosk policy), React NEVER MOUNTS. Two unguarded module-scope reads on the boot path: (1) @capgo/capacitor-social-login's oauth-popup-bridge.js:53 shouldAutoFinishOAuthRedirect(), auto-run on import and pulled in eagerly by the static import at src/lib/socialAuth.ts:15; (2) src/integrations/supabase/client.ts:26 'storage: Capacitor.isNativePlatform() ? keychainStorageAdapter : localStorage'. The user gets index.html's #boot-loader forever with no error, no retry, no message. Identical failure in Chromium AND WebKit. Everything downstream is already guarded (safeStorage.safeGet), so the whole failure is those two lines. | lh-concurrency-cache | 4 |
| VC-001 | **YES** | fixed | `/post-job (credential tier picker) -> applications INSERT -> trg_application_credential_gate` | The 'Licensed' and 'Licensed + Insured' job gate can NEVER be satisfied by any user. get_user_credential_tier() reads ONLY helper_credentials (trade_license/insurance/bond, status=verified); that table has 0 rows in prod and NO writer exists for those credential types in src/ or in any edge function. The real license/insurance flow (/profile?tab=credentials -> get_pending_credentials -> admin -> review_credential) writes ONLY profiles.license_status / profiles.insurance_status, a different store the tier function never reads. Result: a poster picking Licensed (tier 2) or Licensed+Insured (tier 3) on a handyman/painting/moving/assembly job creates a job NO HELPER ON THE PLATFORM CAN APPLY TO, including helpers whose license AND insurance an admin has verified. The UI offers those helpers a 'Get Verified to Apply' CTA pointing at /profile, where the only thing they can do is re-upload the document that already got approved - an unescapable loop. | lh-verification-credentials | 4 |
| VF-001 | **YES** | fixed | `/post-job (credential tier picker) -> applications INSERT -> trg_application_credential_gate` | The 'Licensed' and 'Licensed + Insured' job gate can NEVER be satisfied by any user. get_user_credential_tier() reads ONLY helper_credentials (trade_license/insurance/bond, status=verified); that table has 0 rows in prod and NO writer exists for those credential types in src/ or in any edge function. The real license/insurance flow (/profile?tab=credentials -> get_pending_credentials -> admin -> review_credential) writes ONLY profiles.license_status / profiles.insurance_status, a different store the tier function never reads. Result: a poster picking Licensed (tier 2) or Licensed+Insured (tier 3) on a handyman/painting/moving/assembly job creates a job NO HELPER ON THE PLATFORM CAN APPLY TO, including helpers whose license AND insurance an admin has verified. The UI offers those helpers a 'Get Verified to Apply' CTA pointing at /profile, where the only thing they can do is re-upload the document that already got approved - an unescapable loop. | lh-verification-credentials | 4 |
| AS-001 | **YES** | fixed | `CI gate: .github/workflows/a11y-axe.yml + e2e/happy-path/visual-audit-sweep.spec.ts` | The accessibility gate reported green on colour-contrast without ever running it. It asserted only on axe.violations, and axe files a color-contrast result under axe.incomplete (not violations) whenever it cannot resolve the backdrop — which on this app is always, because the page canvas is a gradient. Every contrast result on every screen therefore went to a bucket nothing read. Four real WCAG AA failures on the public surface sat behind that green check. | lh-a11y-sensory | 4 |
| LTF-001 |  | fixed | `STR iCal sync → auto-create-cleaning (Profile ?tab=str_settings, str-ical-sync edge fn)` | Enabling 'Auto-creates cleaning jobs' promises a cleaning job will be posted at checkout, but str-ical-sync creates it with payment_status='unpaid'. All three browse RPCs (get_ranked_open_jobs, open_jobs_browse, get_open_jobs_for_map) require payment_status IN ('escrow','payout_pending','released'), so the job is invisible to every helper. No client entry point funds an already-created job (is_auto_created has zero references outside types.ts). The host sees the job in their own list and believes it worked; nothing tells them it needs funding, because nothing lets them fund it. | lh-long-tail-features | 3 |
| BR-001 | **YES** | fixed | `.github/workflows/deploy.yml (App Store / TestFlight release lane)` | deploy.yml builds the shipped iOS bundle with NO VITE_* env, so the .ipa it produces hangs forever on the boot-loader. It is ACTIVE and auto-triggers on any 'v*.*.*' git tag, which is the App Store 'release' lane. The repo secrets it needs (VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY, VITE_APPLE_MAPKIT_TOKEN) exist since 2026-08-20 but the workflow never references them. ios-beta.yml (the pipeline actually used for TestFlight) was fixed and has both an env block and two loud fail-fast guards; deploy.yml never received that fix and has zero VITE_ references. | lh-build-release | 4 |
| VC-002 | **YES** | fixed | `helper_credentials RLS -> get_user_credential_tier() -> trg_application_credential_gate` | ANY signed-in user can self-grant the top 'Licensed + Insured' credential tier with two INSERTs and no document, no admin and no review. helper_credentials has policy 'Users can insert own credentials' WITH CHECK (auth.uid() = user_id) and 'Users can update own credentials' USING (auth.uid() = user_id) - neither constrains the 'status' or 'credential_type' columns, and authenticated holds column INSERT/UPDATE on them. get_user_credential_tier() trusts helper_credentials.status='verified' as the sole source of truth, so a user who inserts (me,'trade_license','verified') and (me,'insurance','verified') jumps from tier 0 to tier 3 and satisfies enforce_application_credential_tier for jobs a poster restricted to licensed-and-insured pros. On an app whose entire premise is sending strangers into people's homes, the licensed-and-insured gate is the safety system, and it is self-attested. | lh-verification-credentials | 4 |
| VF-002 | **YES** | fixed | `helper_credentials RLS -> get_user_credential_tier() -> trg_application_credential_gate` | ANY signed-in user can self-grant the top 'Licensed + Insured' credential tier with two INSERTs and no document, no admin and no review. helper_credentials has policy 'Users can insert own credentials' WITH CHECK (auth.uid() = user_id) and 'Users can update own credentials' USING (auth.uid() = user_id) - neither constrains the 'status' or 'credential_type' columns, and authenticated holds column INSERT/UPDATE on them. get_user_credential_tier() trusts helper_credentials.status='verified' as the sole source of truth, so a user who inserts (me,'trade_license','verified') and (me,'insurance','verified') jumps from tier 0 to tier 3 and satisfies enforce_application_credential_tier for jobs a poster restricted to licensed-and-insured pros. On an app whose entire premise is sending strangers into people's homes, the licensed-and-insured gate is the safety system, and it is self-attested. | lh-verification-credentials | 4 |
| BR-002 |  | fixed | `.github/workflows/sentry-release.yml (web sourcemap upload)` | Every production WEB stack trace is unsymbolicated for application code. sentry-release.yml builds with no VITE_SUPABASE_* env, so the VITE_* values that Vite inlines are absent and 165 of 393 chunks get a DIFFERENT content hash than the bundle Vercel actually serves. Sentry matches maps by filename (url_prefix '~/assets') and there is no Sentry Vite plugin injecting debug ids at build time, so the uploaded maps are keyed to chunk filenames production never serves. The workflow reports success on every run, and vendor chunks (the 228 that do match) still symbolicate, so it looks like it is working. This is the SAME class as the 2026-08-27 'Sentry upload never ran' bug, but a different mechanism: the upload now genuinely runs, it is just uploading maps for the wrong build. The iOS path is NOT affected -- ios-beta.yml builds with env and uploads from that same build. | lh-build-release | 4 |
| DR-001 | **YES** | fixed | `Supabase project fncmgoasalhdgfwzhsqa (prod) — backup configuration` | Prod has ZERO restorable backups and no point-in-time recovery. The Supabase Management API reports pitr_enabled:false, an EMPTY backups array, and empty physical_backup_data for the prod project. The worst-case data-loss window is not measured in hours — it is TOTAL: there is no artifact from which any prior state of this database could be reconstructed. This database is the only record of escrow state, dispute outcomes, payout ledgers and the credit currencies; Stripe knows what it charged but not what the platform promised whom. NOTE: walg_enabled is true and archive_mode=on with 87,037 WAL segments shipped and 0 failures, which looks reassuring and is NOT — that is Supabase's own platform infrastructure, and with pitr_enabled:false and earliest/latest restore timestamp both 0 there is no customer-invokable restore window against it. This is a consequence of the accepted free-tier decision, filed so the decision is informed rather than assumed, not as a demand to spend money. | lh-data-recovery | 4 |
| DR-002 | **YES** | fixed | `.github/workflows/db-deploy.yml + migration-lint.yml — migration auto-deploy to prod` | Migrations auto-deploy to prod on merge to main with no destructive-DDL gate, no human approval, and (per DR-001) no backup to fall back on. migration-lint.yml has exactly FOUR rules: RLS on new tables, non-immutable CHECK, DROP-without-IF-EXISTS, and structural DDL on reserved schemas. Only the first two and the fourth set FAIL=1. Rule 3 is explicitly annotated 'Warning only, not a fail'. The word TRUNCATE appears ZERO times in the file, and no rule matches an unqualified DELETE FROM or UPDATE without a WHERE clause. So a migration containing 'TRUNCATE payout_transfers;', 'DELETE FROM tips;', or 'DROP TABLE IF EXISTS jobs;' passes lint cleanly, merges, and db-deploy pushes it straight to prod — the 'environment: production-db' required-reviewer gate exists in the file but is COMMENTED OUT, so no human sees it. Combined with DR-001 that is an unbounded, unrecoverable data-loss vector on the only record of the platform's money. | lh-data-recovery | 2 |
| DR-003 |  | fixed | `public.jobs — RLS DELETE policy + ON DELETE CASCADE fan-out` | Any authenticated poster can hard-DELETE their own job in ANY state, including one holding escrow, and that single row-delete CASCADEs to 21 child tables — destroying money and trust records that have no backup behind them (DR-001). The RLS policy 'Customers can delete their own jobs' is FOR DELETE USING (auth.uid() = customer_id) with NO status or payment_status predicate; the authenticated role holds the DELETE grant on public.jobs; and there is NO BEFORE DELETE trigger on the table. The CASCADE set includes tips (real Stripe-charged money), disputes AND job_disputes (the dispute OUTCOME — who won, decided_by, resolution — which exists nowhere else, since Stripe records only that a transfer happened), reviews (both directions, destroying the helper's reputation record), messages and message_reactions (a two-level chain), applications, job_tracking, job_checkins, group_job_helpers, job_revisions and recurring_visit_releases. The asymmetry is the tell that this is unintended: payment_refunds.job_id and helper_w9_records.job_id were deliberately given ON DELETE SET NULL so those money records SURVIVE a job delete — the author reasoned about record survival for some money tables and tips was not one of them. 28 jobs are in payment_status='escrow' in prod right now. | lh-data-recovery | 2 |
| DR-004 |  | filed | `Restore completeness — Storage buckets, edge-function secrets, cron schedules` | A database restore would resurrect rows pointing at objects and configuration that live entirely outside Postgres, so a partial recovery would read as a total one. Three stores are outside any DB backup: (1) STORAGE — 10 buckets holding 24 live objects (~19MB), including id-documents (1 object, 791kB: a government ID), user-documents (7, 8.9MB: licence/insurance credentials), avatars (13, public), application-attachments (2), proof-photos (1). storage.objects rows would come back in a physical restore but the underlying S3 objects are a separate lifecycle; and account deletion removes them irreversibly (supabase/functions/_shared/accountPurge.ts deletes every object under <userId>/ across the identity buckets, verified by re-list). Restoring profiles.avatar_url / id_document_url after such a purge yields rows pointing at objects that no longer exist. (2) EDGE-FUNCTION SECRETS — 37 secrets incl. STRIPE_SECRET_KEY, RESEND_API_KEY, APNS_AUTH_KEY, SEND_EMAIL_HOOK_SECRET, CRON_SECRET and 24 STRIPE_PRICE_* ids live in Supabase's function-secret store, NOT in Postgres. Only three values are in the DB (vault.secrets: supabase_url, service_role_key, legacy_service_role_key). Nothing in the repo exports or records the other 37, so if the project were lost rather than merely rolled back, the STRIPE_PRICE_* ids in particular are unrecoverable from the codebase. (3) CRON — 44 active schedules in cron.job survive a physical restore but not a rebuild-from-migrations, and nothing reconciles the two. | lh-data-recovery | 1 |
| OBS-001 | **YES** | verified | `AdminHealth admin-alert push path: 13 admins, 0 push tokens` | Every admin-facing push alert (fraud flags, auto-restrict reverses, dispute escalations, stuck-payment alerts per useHealthData.ts comment) fans through push_tokens filtered to admin user_ids, and in prod right now ZERO of the 13 admin accounts has a registered push token. Combined with EF-008 (slack-ops-alert SLACK_API_KEY unset, silencing the DB-side cron/dispute alert path) and CJ-001 (sweep_silent_cron_failures crashes daily and cannot detect anything), this is a THIRD independent alerting channel that is also dead right now. All three fail silently: a push send to zero recipients is not an error, a Slack POST that gets skipped returns 200, a crashed detector just does not insert a row. The only place any of this surfaces today is AdminHealth.tsx's own warning banner (line ~154), which itself requires an admin to already be looking at /admin?view=health -- exactly the failure mode its own comment warns about (alerts only show up if you happen to refresh this dashboard). Net effect: a fraud flag, auto-restrict reversal, dispute escalation, or stuck-payment alert generated today has no live channel to a human except an admin proactively opening that one dashboard tile. NOTE: this finding was originally mis-filed as O-001/O-002 due to an audit-bus.mjs ID-prefix collision between lh-observability and lh-orchestrator (both reduce to prefix O); refiled here under OBS-001 to avoid clobbering the orchestrator's existing O-001/O-002 records in the folded view. Flagged to team-lead. | lh-observability | 5 |
| VC-003 |  | fixed | `/profile?tab=credentials -> profiles.license_status / insurance_status` | A verified license or Certificate of Insurance NEVER EXPIRES. The live credential store is profiles.license_status / insurance_status, and prod has no expiry column of any kind on profiles for either (only subscription_expires_at, unrelated). review_credential() sets license_status='verified' with a reviewed_at stamp and nothing else; no re-verification, no lapse, no warning. helper_credentials.expiration_date exists and IS honoured by get_user_credential_tier, but that table is empty in prod and has no writer for license/insurance (see VC-001), so the expiry logic guards a store nobody populates. Consequence: a COI that lapses the day after an admin approves it keeps showing the public 'Insured' badge on that helper's profile forever, and CredentialBadge.tsx:42-43 renders it purely from is_insured && insurance_status==='verified'. Insurance is the single credential most certain to expire - policies are annual - and this app's whole premise is sending strangers into people's homes. The code knows: CredentialsTab.tsx:360-362 says 'The schema doesn't track an explicit expires_at on credentials yet'. | lh-verification-credentials | 13 |
| EJ-001 |  | filed | `/signup (whole funnel)` | Signup is capped project-wide at ~3-4 accounts/hour by Supabase's auth email rate limit, and mailer_autoconfirm=false means a throttled user can never confirm. The 4th signup in one hour returned HTTP 429 'email rate limit exceeded'; the user sees only a transient toast and is left on a fully-valid form with an enabled Create Account button, with no indication the account was not created. | lh-e2e-journeys | 1 |
| AM-001 |  | filed | `supabase/functions/auto-resolve-disputes/index.ts:279,284 → /admin?view=disputes` | A past-deadline dispute the auto-resolver cannot settle is silently abandoned with escrow HELD: the two 'continue' paths (no payment intent; PI not succeeded) send NO admin reminder, record NO cron defect, and surface nowhere. The comment at :535 asserts 'both leave the dispute for an admin, which is the designed behaviour' — but no admin is ever told. The sibling branches DO notify: the escalated branch calls remindAdmins at :250 and the stuck-split branch at :512. Both parties keep seeing a frozen 'disputed' job; the only trace is a console.error in edge logs. | lh-admin-moderation | 2 |
| OA-002 |  | verified | `/signup (About You step) → Create Account button` | DOUBLE-SUBMIT RACE: one user gesture fires TWO concurrent auth.signUp() calls. The Create Account button is disabled only by `loading` (SignupStep2.tsx:335), but `loading` is set inside createAccountAndFinish() — which runs AFTER the awaited validateAboutYouStep() (Signup.tsx:490-495). That validator performs a network round-trip: the phone-duplicate lookup against profiles (Signup.tsx:178-184). For the whole duration of that query the button stays ENABLED, so a second tap starts a second, fully independent createAccountAndFinish(). Both call supabase.auth.signUp with the same email. Exactly one can win; the loser takes the 'already registered' branch at Signup.tsx:337-347, which calls navigate('/login') and returns SILENTLY — no toast, no report(), no error of any kind. That branch exists for privacy (not confessing whether an email is registered) and is correct for a real duplicate signup, but here it fires for the user's OWN in-flight signup and yanks them off the signup screen mid-flight. This is a plausible mechanism for OA-001/RW-004's 'account created but complete-signup never ran', though I have NOT closed the last link (see repro caveat) and am not claiming it as the proven cause. | lh-onboarding-auth | 2 |
| AL-001 | **YES** | fixed | `purge_user_data() / account deletion — residual PII (CCPA + GDPR + Apple in-app deletion)` | SI-006's five tables are now fixed but the CLASS is still open: ELEVEN more (table.column) pairs reference a user by a bare uuid with NO foreign key and are NOT touched by the live purge_user_data(). Proven by seeding a synthetic user into each and running the REAL production RPC: 11 of 11 seeded rows SURVIVED. Four carry literal PII. helper_w9_records keeps typed_signature (the helper's legal signature on a W-9) plus the signing ip. email_tracking keeps ip_address + user_agent (the same PII class as login_history which IS purged). saved_searches keeps location_keyword which is a street address. admin_user_notes keeps free-text staff notes about the person. The other seven are user_bans / user_violations / helper_shadowbans / user_blocks (both directions) / helper_preferred_parishes / instant_payouts. purge_user_data returns 28 counters and none names any of the eleven so the caller reports a clean deletion. | lh-account-lifecycle | 1 |
| AL-002 |  | fixed | `/admin?view=payouts — Payout Ledger` | The whole 50-row admin Payout Ledger fails to a query-error state as soon as ANY helper deletes their account. payout_transfers.helper_id is nullable and IS set NULL by the retention policy; AdminPayoutBatches.tsx:171 maps every row's helper_id into a PostgREST .in('user_id', helperIds) with no null filter inside unwrap(), so the null reaches the in.() list as the literal string 'null' and PostgREST rejects the whole request. unwrap() rethrows so the admin sees an error state rather than a ledger. LedgerList.tsx:30's intended 'Unknown Helpr' fallback is unreachable. The hardened twins at GroupJobHelpers.tsx:97 and useActivityData.ts:272 filter the nulls out first. | lh-account-lifecycle | 1 |
| OA-003 | **YES** | fixed | `/signup → Create Account (auth email rate limit)` | Signup's hardest failure shows the user a raw backend string. When Supabase's auth email-send rate limit trips, supabase.auth.signUp returns 429 {code: over_email_send_rate_limit, message: 'email rate limit exceeded'} and the signup screen renders that message VERBATIM as its error toast: the person tapping Create Account is told, in lowercase, 'email rate limit exceeded'. It names an internal quota, does not say the failure is temporary, does not say to wait, and offers no next step. The cause is that Signup.tsx:400 routes through userFacingError(), whose INTERNAL_PATTERNS (src/lib/userFacingError.ts:37-54) do not match this string and whose length gate (160 chars) passes it, so it is trusted as human copy. The app ALREADY OWNS the correct sentence — friendlyAuthError() maps /rate/+/limit/ to 'Too many attempts just now. Give it a moment and try again.' (src/lib/authErrors.ts:13-15) — but Signup.tsx does not call it; ResetPassword.tsx:116 does. So the good copy exists and the signup screen cannot reach it. Flagged --blocker not for the copy alone but because the underlying quota is a launch-capacity question nobody has answered: this is the auth email budget shared by signup confirmations, password resets AND resends, and it is reachable in ordinary use. | lh-onboarding-auth | 1 |
| TS-001 | **YES** | fixed | `chat → scan_message_content → enforce_ban_gate` | The 7-day auto-suspension for repeated off-platform contact is COSMETIC. scan_message_content sets only profiles.auto_suspended_until and never touches ban_status; is_caller_banned() gates exclusively on ban_status, so the ban trigger on jobs/messages/applications never fires. The user is told '🚫 Account temporarily suspended' and remains fully able to post jobs, send messages and apply. | lh-trust-safety | 4 |
| AL-004 | **YES** | fixed | `/account-banned + delete-own-account — a banned user has NO in-app deletion, and the endpoint has no ban check` | Two halves of one gap. (a) APPLE COMPLIANCE: a banned user cannot delete their account in-app at all. ProtectedRoute.tsx:258-263 runs the ban gate BEFORE the allowUnapproved branch, so a user with ban_status in (banned, temp_banned, permanently_banned) is redirected to /account-banned from every protected route INCLUDING /profile, which is the only place the delete flow lives (Profile.tsx:534). AccountBanned.tsx offers only 'appeal' and 'sign out' — grep for delete/remove/erase/close account returns nothing. Apple 5.1.1(v) requires in-app deletion for users who can sign in, and a banned user can still sign in. (b) BAN EVASION: delete-own-account/index.ts never reads ban_status or approval_status — it checks only the auth token, the confirmation string and findActiveWork — so a banned user holding a valid session can call the endpoint directly. Deletion frees their email (auth.admin.deleteUser) while user_bans / user_violations / helper_shadowbans stay keyed to the OLD uuid and are not purged (see AL-001), so re-signup with the same address produces a clean account and the ban does not follow. | lh-account-lifecycle | 1 |
| AL-005 |  | fixed | `Direct offers — a job offered to a helper who then deletes is stranded forever with escrow funded` | LATENT but fully reachable: 0 instances in prod today. jobs.offered_to_helper_id was added as a bare uuid with NO foreign key, so the retention policy's ON DELETE SET NULL never fires on it, and purge_user_data does not touch it either. When the targeted helper deletes, the job keeps a DEAD uuid in offered_to_helper_id with direct_offer_status='pending'. Every browse surface gates on the same predicate — offered_to_helper_id IS NULL OR direct_offer_status IN ('declined','expired') OR offered_to_helper_id = auth.uid() — and none of those three disjuncts can ever become true again. The job is therefore invisible to every helper and every browse surface, permanently, while status stays 'open' and the escrow stays funded. The only escape, expire_pending_direct_offers(), requires direct_offer_expires_at IS NOT NULL, and that column is nullable — an offer made without an expiry is unreachable by it. | lh-account-lifecycle | 1 |
| TS-002 |  | fixed | `/profile?tab=saved_helpers — favorite_helpers.private_note` | A poster's PRIVATE note about a Helpr is readable by that Helpr. RLS policy 'Helpers can see who favorited them' grants row SELECT where helper_id=auth.uid() with no column restriction, and authenticated holds SELECT on private_note. SavedHelperCard.tsx:162-165 asserts the opposite: 'Never shown to the helpr (RLS scopes reads/writes to customer_id)'. | lh-trust-safety | 1 |
| TS-003 |  | fixed | `BlockUserDialog → applications` | Blocking does NOT stop a blocked user from applying to your jobs, despite BlockUserDialog.tsx:142 promising 'They won't be able to ... apply to your jobs'. Enforcement is a client-side filter only; the row still lands in the database. | lh-trust-safety | 1 |
| TS-005 |  | fixed | `post-a-job entry:offer-saved-helpr → /post-job?offerTo=<id>` | The direct offer respects blocks in NEITHER direction, and offered_to_helper_id is entirely unvalidated server-side. A poster who has been blocked can push a private job offer plus a push notification to the person who blocked them - an unblockable contact channel that routes around the one place blocking is enforced (messages). | lh-trust-safety | 1 |
| TS-006 |  | filed | `applicants panel / bid — report+block friction` | There is NO report and NO block control anywhere on a bid or application - the highest-volume stranger-to-stranger contact in the marketplace and the first place a scam appears. The only route is out to /user/:id (3 taps plus a full route change), which loses the application context and files the report as type 'user' rather than against the application. | lh-trust-safety | 2 |
| TS-007 |  | filed | `active job — safety escalation` | The Helpr, the party who physically travels to a stranger's address, has NO safety control of any kind on an active job, and no 911/emergency path exists anywhere in the app. SosShareButton has exactly ONE mount site and it is on the POSTER's card. SOS also performs no escalation: it reads one GPS fix into the OS share sheet, writes nothing to Supabase, notifies no admin and no counterparty, and records nothing. | lh-trust-safety | 4 |
| S-001 | **YES** | fixed | `/signup (SignupStep2) → profiles.parish → notify_helpers_on_job_post (LIVE PROD)` | The supply-side notification spine is structurally dead for 72% of accounts because signup collects City, never ZIP, and parish is only ever derived from ZIP. notify_helpers_on_job_post matches helpers on profiles.parish; 28 of 39 prod profiles have parish NULL, 35 of 39 have zip_code NULL, 38 of 39 have latitude NULL. The trigger's own comment asserts 'profiles.parish is derived from the ZIP for EVERY account' - the prod data disproves it. A helper who signs up and never hand-edits their profile is notified of nothing, forever, with no error. helper_preferred_parishes (rung 1 of the ladder) has 0 rows. | lh-suggester | 1 |
| S-002 |  | filed | `supply-side reachability, all channels at once (parish fanout + push + transactional email)` | COMPOSITE, no single lane owns it: all three channels that can tell a helper a job exists are dead simultaneously, so at launch a posted job reaches nobody by any route. (1) parish fanout is blind for 28/39 profiles (S-001); (2) push_tokens holds 0 rows in prod - not 1, zero - so no device can be reached, and min_supported_build is still 0 so existing installs are never forced onto the AppDelegate fix (NB-004); (3) transactional email is dead for 86% of accounts because notification_preferences holds 5 rows for 39 profiles (N-001). Each owning lane sees one dead channel and can reasonably assume the others carry the load. None of them do. | lh-suggester | 6 |
| CC-003#lh-concurrency-cache | **YES** | fixed | `sign-out on a shared device (src/main.tsx:189-236)` | The SIGNED_OUT handler that wipes the persisted React Query cache is registered INSIDE a try whose catch is empty, AFTER five dynamic imports and two analytics init calls. If any of those fails — most commonly an ad blocker refusing the PostHog chunk, which uBlock Origin, Brave and Safari content blockers all do by default — supabase.auth.onAuthStateChange is never registered, so queryClient.clear() and removePersistedClient() never run on sign-out. The previous user's entire persisted cache survives in IndexedDB under 'helpr-rq-cache' for the full 24h PERSIST_MAX_AGE_MS and is rehydrated by the next person to open the app on that device. The auth token IS removed from localStorage either way, so the app looks completely signed out and nothing signals the leak. The leaked payload is exactly what queryClient.ts's own header comment says must not survive sign-out. This is the fail-open-catch class: the comment '/* analytics + error tracking must never break the app */' is true of its intent but the block it guards also contains the privacy control. | lh-concurrency-cache | 4 |
| AM-008 | **YES** | fixed | `public.scan_message_content() BEFORE INSERT ON messages · public.is_caller_banned()` | The off-platform-contact scanner's repeat-offender auto-suspension is a COMPLETE NO-OP, and it tells the user they are suspended while they are not. On the 3rd flag in 24h the trigger runs 'UPDATE profiles SET auto_suspended_until = now() + interval 7 days' and sends a notification titled 'Account temporarily suspended'. It never sets ban_status. But is_caller_banned() — the only thing enforce_ban_gate consults, and it gates messages, jobs, applications and every other write — tests 'ban_status IN (banned, temp_banned, permanently_banned)' and treats auto_suspended_until ONLY as a modifier on an already-temp_banned row. So ban_status stays 'active', the gate returns false, and the user keeps messaging, posting and bidding for the full 7 days they were told they were suspended. Second-order: sweep_expired_auto_bans() requires ban_status='temp_banned', so it never clears the stale auto_suspended_until either — the column stays dirty forever. This is the platform's primary anti-disintermediation enforcement and it does not enforce. | lh-admin-moderation | 4 |
| CC-006 | **YES** | fixed | `/signup → completeProfile (src/pages/signup/signupHelpers.ts:70-78, src/pages/Signup.tsx:241)` | RW-004's missing mechanism: an awaited promise that NEVER SETTLES between account creation and the profile write. fileToBase64() attaches only onload and onerror. FileReader has THREE terminal events — load, error and abort — so an aborted read resolves nothing and rejects nothing, and the await hangs forever. This is the FIRST await inside completeProfile (Signup.tsx:241), and completeProfile runs AFTER supabase.auth.signUp has already created the account. Every consequence matches RW-004's evidence exactly: the auth user exists (account created); supabase.functions.invoke('complete-signup') is never reached, so the function is never ENTERED and writes no edge_rate_limit_log row — which is precisely the dispositive absent artifact lh-onboarding-auth used to prove it was never invoked; the catch at Signup.tsx:398 never runs, so there is no report() and no toast (no error); the finally never runs, so setLoading(false) never fires and the button spins forever; and the profile keeps only what handle_new_user inserted (user_id, full_name, email), leaving phone, location, date_of_birth and avatar_url blank — exactly the four fields RW-004 names. Intermittent because it depends on the image and the device. A SECOND path produces byte-identical evidence: supabase.functions.invoke is called with no AbortSignal and no timeout anywhere in the signup path, so a stalled connection on a flaky mobile network hangs the same way with the same silence. Note the avatar is a HARD requirement of the signup form ('Add a profile photo' blocks submit), so every single signup passes through fileToBase64. The codebase already learned this exact lesson elsewhere and did not apply it here: keychainStorageAdapter.ts's HARD CAP comment says verbatim 'The try/catch below is NOT sufficient on its own. It catches a REJECTION; it does nothing for a Capacitor bridge call that never settles at all.' | lh-concurrency-cache | 5 |
| BR-005 | **YES** | filed | `prod GoTrue auth config (fncmgoasalhdgfwzhsqa) — signup confirmation / password reset / resend` | ANSWERS OA-003. Two separate questions, two different answers. (1) SENDER: custom SMTP is NOT configured, but it does not matter — auth mail does NOT go through Supabase's built-in sender. A GoTrue Send Email Hook is wired to the auth-email-hook edge function, which sends via Resend on the verified apex domain louisianahelpr.com. So the 'built-in sender is not for production' deliverability blocker does NOT apply. (2) THROUGHPUT: the blocker is real and is a config value, not a sender. GoTrue applies rate_limit_email_sent BEFORE it invokes the hook, so the hook never gets the chance. That limit is EXACTLY 2 emails per hour, project-global, on a FIXED clock-hour window — Supabase's built-in-sender default, left unraised when the hook was added. At launch the platform can confirm at most 2 new signups per hour in total, shared with every password reset and resend; the 3rd user in any clock hour gets a 429 and no email. FIX IS CONFIG-ONLY, NO CODE: raise Auth > Rate Limits > 'Rate limit for sending emails' in the Supabase dashboard (or PATCH rate_limit_email_sent via the Management API). Nothing in the repo needs to change. This also RESOLVES the anomaly OA-003 could not explain ('one rolling hour 03:40-04:05 carried 4, so it is not a flat 2/hour'): the window is fixed, not rolling — 2 landed at the end of hour 03 (03:40:52, 03:40:54) and 2 at the start of hour 04 (04:04:09, 04:05:56). Four in 25 minutes, still exactly 2 per clock hour. | lh-build-release | 6 |
| BR-006 | **YES** | fixed | `prod platform_settings.min_supported_build — ForceUpdateGate / native force-update lever` | The force-update gate has never been armed and is the ONLY remote lever that can retire a broken native build. Live prod: platform_settings.min_supported_build = 0 AND latest_build = 0, updated_at 2026-05-02, updated_by NULL — never touched by an operator. 0 is the documented GATE_OFF value (src/lib/minSupportedBuild.ts), so the gate is fully disabled. WHAT VALUE IT NEEDS: the build number of the FIRST TestFlight build cut from a commit at or after 6d3cf8f19 (2026-09-01, 'Every APNs action button the server has ever sent was dropped on the floor'), which is the first commit whose AppDelegate posts .capacitorDidRegisterForRemoteNotifications. Fastlane's next_build_number takes max(TestFlight)+1 and the last delivered build was 7101, so that build will be >= 7102. Set min_supported_build to that exact number the moment it ships — not before, or the gate locks out every existing install with no replacement available to update to. WHAT BREAKS UNTIL THEN: every build <= 7101 in the wild — which is EVERY build that exists, since no build containing the APNs fix has ever been produced — silently drops the APNs token iOS hands it on every launch. push_tokens never fills; those users receive zero push notifications, forever, with no error, no log and no prompt to update. Because the gate reads 0 they are never turned away, so they keep running the broken binary indefinitely. This compounds NB-004 (lh-native-bridge): the fix exists in main and has never been in a build anyone has run, and the one mechanism that could force those users off it is switched off. Note the gate correctly fails OPEN on every read failure by design (minSupportedBuild.ts) — that is right and must not be changed; the defect is purely that the value was never set. | lh-build-release | 12 |
| NB-017 | **YES** | filed | `helpr:// deep links / the native Stripe return — dead whenever the app was opened from the home screen` | OBSERVED ON DEVICE, reproducible: the appUrlOpen listener never delivers when the app process was started WITHOUT a URL — which is every normal launch from the home screen. Deep links are then dead for the entire life of that process. Controlled experiment on the iOS 26.1 simulator, app built from origin/main: terminate, plain 'simctl launch' with no URL, settle 25s, then 'simctl openurl helpr:///legal?tab=privacy'. Result: the app foregrounds and stays on the guest browse feed, and error_logs receives ZERO rows from the HelprApp user agent after the marker timestamp. That zero is the proof the handler never ran, because handleIncomingUrl's FIRST act on a helpr: URL is 'await Browser.close()', which on this device always rejects with 'No active window to close!' and is reported to error_logs by the catch at nativePush.ts. Whenever the handler runs, that row appears. Contrast, same build, same session: after cold-launching the app FROM a URL, two subsequent warm URLs both navigated correctly (a guest was bounced to /login with 'That page needs an account' and the destination preserved) and each produced its browserClose row at 22:05:35 and 22:05:43. Tally: 5 failures in the launched-without-URL condition, 3 successes in the launched-with-URL condition. WHY THIS IS A BLOCKER: the production Stripe flow is exactly the failing condition. The user opens the app from the home screen (no URL), taps Pay, Browser.open() presents SFSafariViewController, Stripe redirects to the native=1 page, that page fires helpr:///payment-success — and nothing happens. No Browser.close(), no navigation. The user is stranded looking at the website inside a sheet they must dismiss by hand, which is precisely the defect nativeReturnBounce.ts was written to eliminate. MECHANISM — HYPOTHESIS, NOT OBSERVED: useNativePushSetup guards on a MODULE-level 'listenersAttached' flag but tracks cancellation in an effect-local 'cancelled' variable, and its deps are [navigate]. If the effect re-runs once (plausible on a plain launch, where NativeLaunchRouter navigates from '/' to the default route and changes router state), the cleanup sets cancelled=true on run 1 while run 2 returns immediately at the listenersAttached guard; run 1 then hits 'if (cancelled) return' and never reaches either App.addListener('appUrlOpen') or App.getLaunchUrl(). Launching WITH a URL would avoid the extra navigation and let the chain finish. Someone should confirm that before fixing. | lh-native-bridge | 6 |
| OA-009 | **YES** | fixed | `/signup -> legal_acceptances / profiles.terms_version_accepted` | CONSENT CAPTURE, stated separately from the data loss in OA-001 because it has a different owner and a different remedy. A live auth account exists whose holder ticked BOTH the "I am 18 or older" attestation and the "I agree to the Terms, Privacy Policy and Platform Rules" checkbox, and neither was ever persisted. helpr-audit-routewalker@mailinator.com (auth.users 9a31db25-5089-4789-8adf-e5c737586166, created 2026-09-02T18:18:42Z, email CONFIRMED at 18:21:52Z, signed in at 18:41:36Z) has terms_version_accepted='' (empty string, not the 'Jun 2026' its successful sibling carries), terms_accepted_at NULL, accepted_terms_at NULL and ZERO rows in legal_acceptances. The app told this person their agreement was recorded; it has no record of it. On a real-money, 18+-gated marketplace that is a compliance gap, not a data-quality one: if this user later disputes a charge, is injured on a job, or turns out to be a minor, the platform cannot produce the consent it asserted at signup.

The structural cause is that consent is written by a SECOND, unguarded network call. auth.signUp() creates the account irreversibly; every profile field, the approval flip AND the entire legal record then depend on a separate supabase.functions.invoke('complete-signup') that has no retry, no queue, no compensating write and no reconciliation sweep. Nothing anywhere notices when it does not happen.

lh-verification-credentials independently established the same mechanism from the other side and it is worth stating as one sentence, because neither lane could say it alone: complete-signup/index.ts:465-470 sets approval_status='approved' UNCONDITIONALLY once validation passes ("Auto-approve - there is no manual admin review step anymore"), handle_new_user inserts without it, and the profiles INSERT policy hard-pins 'pending' so it cannot be forged. So 'pending' is purely the pre-completion state, and APPROVAL TO USE THE PLATFORM IS A SIDE EFFECT OF AN EDGE FUNCTION THAT CAN SILENTLY NOT RUN - the same single point of failure that loses the consent record also decides whether the account is approved. RW-005 is therefore NOT an approval-queue bypass (it is intentional) but it confirms this finding's mechanism. | lh-onboarding-auth | 3 |
| OA-011 |  | fixed | `/signup -> Create Account (complete-signup network failure)` | At the single worst moment in the funnel the user is shown a raw Supabase SDK internal string. When the complete-signup invoke fails on the network - i.e. exactly when the auth account has just been created irreversibly and the profile, the approval flip and the entire legal record are about to be lost - the toast reads, verbatim: "Failed to send a request to the Edge Function". That is the supabase-js FunctionsFetchError message. It names an internal component ("Edge Function"), does not say what failed from the user's point of view, does not say their account WAS created, and offers no next step.

This is the SAME defect class as OA-003 but a different string on a different path, so fixing OA-003 does not fix it: userFacingError() (src/lib/userFacingError.ts:37-57) has no INTERNAL_PATTERNS entry matching "Edge Function", and the string is 44 chars so the 160-char length gate passes it through as human copy.

The state it leaves behind is what makes this HIGH rather than a copy nit. Measured: the user is returned to /signup with the form still populated and the Create Account button ENABLED, while a live, email-confirmable auth account for that address already exists in prod with no profile, no approval flip and no legal_acceptances row. The only affordance offered is to press Create Account again - which now takes the already-registered branch at Signup.tsx:337-347 and redirects them to /login telling them the address is already registered. The recovery path from a failure that stranded their account is to be told the account they just failed to create already exists. | lh-onboarding-auth | 5 |
| OA-012 |  | fixed | `src/lib/biometricGate.ts -> 21 money and admin step-up gates` | CONFIRMS NB-008 and widens it. requireBiometric() fails OPEN on biometry LOCKOUT, not merely on "not enrolled", and the passcode fallback it appears to offer is unreachable in exactly that case.

biometricGate.ts:42-43 reads checkBiometry() and returns true when info.isAvailable is false. Verified against the PLUGIN OWN iOS SOURCE rather than its docs: BiometricAuthNative.swift:38 computes isAvailable from canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics) - BIOMETRICS ONLY. Apple returns false from that call with LAError.biometryLockout after five consecutive failed biometric matches, and the plugin maps that exact code at :27. So isAvailable === false conflates four very different states: biometryNotEnrolled, passcodeNotSet, a missing NSFaceIDUsageDescription, and biometryLockout. The gate treats all four as pass.

biometryLockout is the one that inverts the security property. It is the strongest signal the device has that someone just failed five biometric attempts, and that is precisely when the check is removed. Line 47 passes allowDeviceCredential:true to authenticate(), which WOULD present the iOS passcode sheet - but the early return at :43 means authenticate() is never called, so on a locked-out device the user is never asked for anything at all.

The fix needs no new API. The plugin already returns deviceIsSecure at :39, computed from .deviceOwnerAuthentication which is biometrics OR passcode, plus a code string - and biometricGate reads neither. When code is biometryLockout and deviceIsSecure is true, calling authenticate with allowDeviceCredential:true anyway yields the passcode sheet, which is the secure fallback this file own doc comment implies already exists.

BLAST RADIUS - 21 call sites, and they are not minor: instant payout cash-out (InstantPayoutDialog.tsx:66), referral cash-out (ReferralSection.tsx:120), add/change/remove/reset payout account and Stripe dashboard access (PayoutSetupForm.tsx:111,145,172,196), change a user login email (admin/EditEmailDialog.tsx:61), dispute settlement (admin/AdminDisputes.tsx:193,247), refunds (admin/AdminJobs.tsx:303), permanent account deletion (admin/DeleteUserDialog.tsx:44), single AND BULK payout runs (admin/AdminPayoutBatches.tsx:197,296), bans (admin/BanDialog.tsx:121), granting and removing ADMIN ACCESS (admin/AdminSettings.tsx:303,340), and the app lock itself (AppLockGate.tsx:152).

Scoped honestly: this is not privilege escalation and does not defeat server-side authorization - the Supabase session and RLS still apply - and the file stated intent, never lock a user out of their own money just for not having Face ID, is defensible for the not-enrolled case. What is lost is the step-up re-authentication on a stolen, already-unlocked phone, which is the entire threat this gate exists to cover. HIGH, but NOT a launch blocker: native-only, and it requires physical possession of an unlocked device. | lh-onboarding-auth | 4 |
| OA-017 |  | filed | `/account-pending` | /account-pending is a terminal screen that describes a process which no longer exists. It tells the user "Our team is reviewing your credentials", renders a four-step progress ladder - Email confirmed / ID uploaded / Professional check / Final admin review - at 25%, and promises "Reviews usually finish in under 2 hours during business hours (8a-6p CT). Overnight signups clear next morning."

There is no team and no review. complete-signup/index.ts:465-470 auto-approves unconditionally the moment validation passes, and its own comment says so: "Auto-approve - there is no manual admin review step anymore" (independently established by lh-verification-credentials, and this lane confirms it from the writer side). So an account that lands here is waiting for a step that was deleted.

Worse, nothing the screen offers can change the state it is describing. Its four affordances are Explore Jobs While You Wait, Sync Status, Contact support and Sign Out. handleSync (AccountPending.tsx:145-171) only scrubs cached keys and re-runs the currentUser query - it re-READS approval_status and never re-invokes complete-signup or anything else that could write it. So the button offered as the remedy polls a value that nothing on this path will ever change. The five writers of approval_status='approved' across the whole codebase are complete-signup:470, stripe-webhook/handlers/accountUpdated.ts:88, stripe-idv-webhook:275, admin-user-actions:203 and adminusers/useAdminUserActions.ts:38 - the last two are admin-only, and neither Stripe path is reachable or linked from this screen. The ladder even shows an "ID uploaded" step while offering no way to upload an ID.

Reachability is real and is caused by this lane's other findings. ProtectedRoute.tsx:335-336 bounces any account with a COMPLETE profile and approval_status='pending' to this screen, and the repo's own test asserts it ("a COMPLETE profile that is still pending keeps its /account-pending bounce", ProtectedRouteGates.test.tsx:102). A complete-but-pending profile is exactly what CompleteProfile.tsx:399-401 produces when its recovery invoke of complete-signup fails: it writes the profile and the legal_acceptances row FIRST and only then invokes the function that flips approval, so the same unguarded second network call behind OA-001 / OA-009 / OA-011 strands the recovery too - and deposits the user on a screen that tells them to wait for a review nobody will perform.

Net effect: a user in this state waits indefinitely, is given a false ETA, has no self-service escape, and the only real exit is a support ticket that lands an operator in the admin console. That is the exact failure mode this lane's brief asks about - a screen with no way forward - made worse by an explanation of what happens next that is not true. | lh-onboarding-auth | 3 |
| IB-001 | **YES** | fixed | `/post-job (Budget step, Urgent bonus) + jobs table + create-payment` | Urgent-bonus fee has NO server-side ceiling — only the client toast (useJobSubmit.ts) enforces MAX_URGENT_FEE_DOLLARS (5000). The live jobs_urgent_fee_required CHECK constraint only enforces the $5 floor, and create-payment/index.ts:308,369-379 charges Math.round(job.urgent_fee*100) verbatim with no re-validation. A job inserted directly against PostgREST (bypassing the post-job form) with an oversized urgent_fee reaches live Stripe checkout uncapped — the exact incident documented in src/lib/moneyLimits.ts's own comment (2026-08-31, $99,999 fee reached a live $103,088.88 Stripe checkout), except that fix only patched the client. | lh-input-boundary | 2 |
| IB-002 |  | verified | `AI Job Builder (post-job EntryChoice, entry path 5) + jobs table title/description` | Job title/description/special_requirements have ZERO content-moderation or off-platform-contact scanning at insert time, unlike chat messages which have several dedicated scan functions (scan_message_offplatform_phrases, strengthen_server_phone_scan). The AI Job Builder happily embeds a phone number and street address directly into a job description on request, and that text is stored and published to the PUBLIC open-jobs feed with no gate at all -- a lower-friction disintermediation path than chat, reachable by a poster who never even typed the contact info themselves (the AI wrote it in). | lh-input-boundary | 2 |
| IB-003 |  | fixed | `jobs.urgent_fee + create-payment (charge side) + _shared/stripeFees.ts netUrgentFeeDollars (payout side)` | Found during lh-money-escrow review of IB-001's fix: a NEGATIVE urgent_fee also bypasses every existing guard. jobs_urgent_fee_required is vacuous when is_urgent=false, so a direct-insert with is_urgent=false, urgent_fee=-500 satisfies it. create-payment/index.ts:308 charges Math.round((job.urgent_fee ?? 0)*100) unsigned into the checkout total (undercharging the poster by $500 vs budget), while netUrgentFeeDollars in _shared/stripeFees.ts clamps the PAYOUT side's cents to >= 0 -- so the helper still gets paid the full per-helper budget and the platform silently eats the $500 delta. | lh-input-boundary | 2 |
| V-002 | **YES** | fixed | `/browse + /jobs + browse map + landing RPC — get_ranked_open_jobs / get_open_jobs_for_map / get_public_open_jobs` | LIVE RIGHT NOW: prod Postgres runs TimeZone=UTC, so CURRENT_DATE rolls over at 19:00 CDT. Three discovery RPCs gate on 'date_needed >= CURRENT_DATE', so every evening from 7pm to midnight Louisiana time, jobs needed TODAY silently vanish from the public job board, the browse map and the landing feed while remaining visible on the signed-in dashboard. The client already knows this is wrong: useDashboardFilters.ts:196-201 deliberately builds todayLocalDate from LOCAL parts with a comment warning against exactly this mistake, and the open_jobs_browse view applies no date predicate at all - so the same job is visible on one surface and gone from another at the same instant. This is the peak-usage window for same-day work in a same-day-labour marketplace. | lh-verifier | 4 |
| V-003 |  | fixed | `audit process — docs/audit/launch-2026-09/lanes/lh-browse-discovery.md vs findings.jsonl` | lh-browse-discovery reports as a ZERO-FINDING all-clear to every downstream consumer, but it is not one: its lane report contains a five-row findings table (BD-001..BD-005) under the heading 'Findings filed (bus: findings.jsonl)', and NONE of the five was ever appended to the bus. The lane did real work with real live-DB reproductions and then ended mid-run ('Awaiting the typecheck gate from team-lead before committing'), so its findings exist only as prose in a file nothing reads programmatically. Its self-declared fix is also fictional - dedupeById does not exist anywhere in src/. This is worse than a lane that found nothing, because the ledger positively asserts a clean discovery surface. BD-004 in particular is a live defect: useOpenJobsFeed.ts:89-97 paginates by p_offset with a flatMap append, no de-dup, over an ORDER BY whose rank_score contains a now()-decaying recency term and a boost term that flips on expiry, with no unique tiebreaker - and because Jobs.tsx:383 keys per-row inside chunked parent arrays, a duplicated id produces NO React duplicate-key warning, it just renders the same card twice. | lh-verifier | 1 |
| V-004 |  | fixed | `CI a11y gate — e2e/happy-path/visual-audit-sweep.spec.ts:251 + .github/workflows/a11y-axe.yml` | A GREEN a11y-axe RUN DOES NOT MEAN CONTRAST WAS CHECKED - on this app's page canvas it means contrast was SKIPPED. The gate asserts on axe.violations and never reads axe.incomplete, and axe classifies color-contrast as INCOMPLETE (not a violation) whenever it cannot resolve the background - which is always here, because the page canvas is a gradient. Measured against live prod with the gate's own tag set: / = 0 violations / 24 incomplete; /help = 0 / 40; /browse = 0 / 22, with axe's own message 'Element's background color could not be determined due to a background gradient'. Two further gaps: ALL_VARIANTS sweeps NO dark mode above 375px, so desktop-dark and small-dark are never covered; and auditCatalogRoutes.test.ts asserts catalog->route but NOT route->catalog, so /messages/:id, /j/:id, /m/:id, /u/:id, /post-job/*, /warnings and /legal/:tab are silently unswept forever. This is why the four AA contrast failures filed alongside this finding are green in CI. | lh-verifier | 1 |
| V-005 |  | fixed | `src/components/AppLockGate.tsx:409 — app-lock Unlock button (native, App Lock enabled)` | The app-lock screen can render perfectly and be completely dead. AppLockGate deliberately keeps children mounted (:314, 'Children stay mounted so unlocking restores the exact prior screen'), so if a Radix dialog was open when the app backgrounded it stays open and portalled to <body> — and an open Radix modal sets body{pointer-events:none}, which INHERITS. The lock panel renders inside #root and sets no pointer-events:auto, unlike PhotoLightbox.tsx:310 and MessageAttachment.tsx:344, which both carry that exact override because this trap was already hit there. Paint order is fine (panel z-100 beats the overlay z-50), so the user sees a normal, correct-looking, entirely inert unlock screen. Recovery is force-quit. ForceUpdateGate is NOT affected — it returns the gate INSTEAD of children (:47), so Radix unmounts and restores body. | lh-verifier | 3 |
| V-006 |  | fixed | `src/hooks/useDashboardData.ts:294-296 — dashboard feed infinite scroll` | One blocked poster permanently caps the dashboard feed. .range(offset, offset+PAGE_SIZE) fetches 26 rows, but hasMore = rawAll.length > PAGE_SIZE is computed AFTER the blocked-poster filter runs. A single blocked user with a single open job anywhere in the window makes 25 > 25 false, sets nextOffset = null, and infinite scroll never fires again for the rest of the session. Silent — no error, no empty state, no indication the feed was truncated. | lh-verifier | 1 |
| N-009 |  | fixed | `notify_on_application (DB trigger) — email branch, both call sites` | notify_on_application carries the SAME fail-closed email gate as N-001, twice, in SQL. Both branches read 'SELECT email_job_applications INTO v_email_enabled FROM notification_preferences WHERE user_id = v_user_id' and then gate on 'IF v_email_enabled IS TRUE'. A missing prefs row makes the variable NULL and 'NULL IS TRUE' is FALSE, so the email is silently skipped. 34 of 40 prod accounts have no prefs row, so neither the 'New application' email (fires when someone applies to YOUR job — the poster's core loop) nor the 'Application update' decline email has ever been sent to 85% of accounts. This is a SECOND, independent instance of N-001's root cause: N-001 is in the edge function, this one is in the trigger that calls it, so fixing only the edge function would not fix this path. Every other prefs-reading producer (notify_helper_on_tip, notify_on_payment_escrowed, notify_poster_on_status_change, notify_user_on_review) uses 'IF COALESCE(v_pref, true)' — this one is the outlier. | lh-notifications | 1 |
| BD-001 |  | fixed | `/browse (guest) + /dashboard — Nearby radius filter` | The 'Nearby' radius filter on the browse feed is a total no-op for guests, and degraded to a crude string match for signed-in users. open_jobs_browse (the view BOTH feeds read) exposes no latitude/longitude, so useDashboardFilters.ts:230 can never take its haversine branch and falls to a profile-location string match; a guest has profile=null so NOTHING is filtered. Aggravating factor: BrowseTasksToolbar.tsx:74 prints the heading 'Filtered Results' the whole time, so the UI positively asserts a filter that is not running. | lh-browse-discovery | 1 |
| BD-002 |  | fixed | `public.get_public_open_jobs (anon RPC)` | The paid Early Access perk leaks through get_public_open_jobs. The other three browse surfaces gate on public.early_access_cutoff() -- get_ranked_open_jobs, get_open_jobs_for_map and the open_jobs_browse view all carry 'created_at <= cutoff.ts'. get_public_open_jobs has NO created_at predicate at all and orders 'boosted DESC, created_at DESC LIMIT 6', so it returns precisely the newest jobs -- exactly the rows the 20-minute perk is meant to withhold from free and logged-out viewers. It is EXECUTE-granted to anon, so the public VITE anon key is the whole exploit: a free user gets Elite-tier early access from one curl. | lh-browse-discovery | 1 |
| GD-007 |  | fixed | `src/integrations/supabase/types.ts` | types.ts on origin/main is stale against prod in 26 places: 2 columns nullable in prod but asserted non-null (legal_acceptances.user_id, reports.reporter_id), 16 columns and 2 tables missing (edge_rate_limit_log, retained_bans, profiles.license_expires_at, profiles.insurance_expires_at), 7 RPCs missing. The nightly guard is ALREADY RED on this. | lh-generated-drift | 3 |
| GD-008 |  | fixed | `supabase.rpc() — all call sites` | tsc cannot check RPC argument NAMES, so types.ts is not the early-warning system it is assumed to be. postgrest-js types args as an inferred generic (Args extends Schema['Functions'][FnName]['Args'] = never), so excess-property checking never runs. AdminCredentialQueue.tsx:110,154 pass _expires to review_credential whose committed Args lacks it, and CI's npx tsc -b --noEmit passed GREEN. A misspelled or removed arg name is equally invisible and becomes a runtime PGRST202 that fails the whole call. | lh-generated-drift | 2 |
| NB-018 |  | filed | `Push permission / push_tokens observability` | Push is still 0 tokens on a build that carries the APNs fix, and nothing anywhere records whether a user granted OS push permission — so '0 rows' is permanently undiagnosable. Build 7103 (ce4d38d5, TestFlight 2026-09-02T18:25Z) DOES contain ad315368 (git merge-base --is-ancestor = true; git show ce4d38d5:ios/App/App/AppDelegate.swift lines 141/148 post both notification names). A real signed-in device IS running 7103: user 76b07824-9b41-4741-a4c4-4f8de362f682 emitted error_logs rows tagged 'realtimeRecovery' at 2026-09-03 05:29Z, 05:36Z and 15:26Z from capacitor://localhost/my-jobs — and that tag was introduced by ede9db4c (2026-09-01 22:15 PDT), which is an ancestor of ce4d38d5 but NOT of 7b057572 (build 7101) or any earlier build. There is no OTA plugin in package.json, so the bundle is baked into the IPA. Yet push_tokens = 0 rows, max(created_at) NULL, and notification_logs shows 199 push sends skipped for no_registered_devices in the last 48h (latest 2026-09-03 15:25:55Z). The cause is NOT a residual native bug: PushNotifications.register() is called only when checkPermissions().receive === 'granted' (nativePush.ts), and if it had been called, exactly one of three traces would exist — a push_tokens row, an error_logs row tagged push.registrationError, or one tagged persistPushToken.upsert. All three are empty, so register() was never called, so permission was never granted on that device. RLS is not the blocker either (push_tokens policy users_manage_own_tokens is ALL with check user_id = auth.uid()). The permission gate is correct-by-design (contextual, per Apple) but unreachable for a tester who only browses: PushNotificationPrompt requires recordJobActionForPermissionPrompt (post or apply) AND a 30-day snooze miss AND a 14-day nudge-cooldown miss AND OS state 'prompt'; the only other path is the first-application / first-accept toast. So push will stay unproven through launch, and there is no server-side signal to tell anyone why. | lh-native-bridge | 1 |
| PD-005 |  | filed | `src/hooks/useAuthReady.ts:32 (every route, every visit)` | The first-interaction analytics gate in main.tsx is defeated for Sentry: the 214KB/68.8KB-gz sentry-B_-f9OWS.js chunk is fetched on EVERY passive page load at ~3.8-4.7s, inside Lighthouse's page-load network window, from useAuthReady.ts:32's markColdLaunchPhase import. PostHog correctly stays out until interaction (9.7s). The gate's own comment says these chunks must stay off the load-time dependency chain (~4.6s longest path on slow 4G); Sentry is on it. | lh-perf-deps | 1 |
| ME-039 | **YES** | fixed | `prod Supabase fncmgoasalhdgfwzhsqa → STRIPE_SECRET_KEY / _shared/proTiers.ts resolvePrice()` | Production runs on a Stripe TEST-mode secret key, and the six STRIPE_PRICE_* secrets that exist to support that test mode will keep WINNING over the hardcoded live Price ids after the key is flipped to live — resolvePrice() reads env first — so the go-live key swap alone silently breaks every membership checkout (test price id posted to live Stripe => opaque 500). Flipping the key and unsetting the six overrides is ONE atomic step, and nothing in CI or the launch checklist couples them. | lh-money-escrow | 1 |
| ME-040 | **YES** | fixed | `stripe-webhook payment_intent.payment_failed → /payment-success + poster notification` | A DECLINED card on the escrow Checkout flow tells the poster NOTHING and is indistinguishable from a slow webhook. handlePaymentIntentPaymentFailed finds the job by jobs.stripe_payment_intent_id = pi.id, but on a Checkout-Session escrow that column is only written by the SUCCESS path — on a decline it is still NULL. So failedJob is null, the 'Payment failed' notification is never inserted, and payment_status is never set to 'failed'. It stays 'unpaid', which is not in PaymentSuccess.tsx NOT_HELD_STATUSES, so the return page can never reach its 'Your payment didn't go through' branch for the single most common failure. Instead it polls 4x and prints 'Your payment hasn't been confirmed on our side yet. It usually lands within a minute. Please don't pay again' — to someone whose card was declined and who should be retrying with another card. | lh-money-escrow | 1 |
| ME-042 |  | fixed | `/profile?tab=earnings → Earned card 'total earned'` | The headline figure a helper reads to learn what they are owed is derived from jobs.status='completed', NOT from the payout_transfers ledger — so a job whose transfer never happened, failed, or was reversed still counts as money earned. EarningsTab.tsx:151 completedJobs = earningsJobs.filter(j => j.status === 'completed'), then :158 totalEarnings = sumHelperTakeHomeDollars(completedJobs). Nothing consults payout_transfers, nothing checks payment_status, and nothing filters is_seed — even though auto-release-payment and money-reconciliation both scope to is_seed=false. The same hook file ALREADY loads the ledger (useEarningsData.ts:39-59, commented 'the authoritative record of every stripe.transfers.create()'); the Earned card just doesn't use it. | lh-money-escrow | 2 |
| AR-011 | **YES** | fixed | `messages table / PostgREST PATCH /rest/v1/messages` | A message RECEIVER can silently rewrite the CONTENT of a message the other party sent them. Policy 'Users can mark messages as read' (UPDATE, USING/WITH CHECK auth.uid()=receiver_id) is column-blind and the 'content' column is UPDATE-granted to authenticated, so it permits arbitrary column writes; it ORs with the 15-minute sender-only edit policy and defeats it. Chat is dispute evidence on a real-money marketplace. | lh-authz-rls | 3 |
| CC-019 | **YES** | fixed | `Membership: Plus tier () vs every entitlement gate` | Plus tier (restored 2026-09-05, $15/mo, sold as 'Everything in Pro' → 'Everything in Basic' → 'Instant Payouts', priorityPlacement:true) is REFUSED by every gate that lists tiers by name: deployed instant-payout v79 line 'entitled = basic\|\|pro\|\|elite' 403s a Plus member with 'Upgrade to Basic, Pro or Elite' (an upgrade to a cheaper tier); EarningsTab.tsx:99 same client gate; applicantScoring.ts:92-93 gives plus 0 placement points; JobPosterCard.tsx:45-47 omits plus badge; AdminDisputes.tsx:159 ranks plus as 0; Terms 'Membership tiers' section (TermsSection.tsx:174-186) lists Free/Basic/Pro/Elite only; HelpCenter 'When do I get paid?' (helpCenterContent.ts:211) says instant payout is 'Basic, Pro or Elite'. Plus members pay more than Pro and get less. | lh-copy-content | 5 |
| AR-013 | **YES** | fixed | `applications INSERT / POST /rest/v1/applications` | Two discovery filters that gate a PRIVATE and a PAID thing are enforced only in the feed, never at the apply path. (1) A job under a live direct offer is withheld from every discovery surface but a non-targeted helper may still APPLY to it given the job id -- the targeted-offer privacy is cosmetic. (2) early_access_cutoff() is the Pro/Elite early-access perk; a free-tier helper may apply inside the window, ahead of the subscribers who paid for the head start. The applications INSERT policy carries NO job-state condition at all (only helper_id, status=pending, are_users_blocked), and of the five BEFORE INSERT triggers only enforce_application_credential_tier looks at the job. | lh-authz-rls | 1 |
| CC-021 |  | fixed | `ErrorBoundary + every userFacingError() caller (23 sites) on WebKit/WKWebView` | ErrorBoundary.tsx:124 renders the raw render-error message to the user — reproduced live: /profile?tab=home_history showed 'Something went sideways. Can't find variable: subscribeToOwnProfile'. And userFacingError() (the sanitizer behind 23 toast sites) passes through every WebKit-shaped engine string verbatim: 'Load failed', 'Network request failed', 'Can't find variable: x', 'undefined is not an object (evaluating …)', 'x is not a function', 'The request timed out.', 'The network connection was lost.' — proven by executing the module (only Chromium's 'Failed to fetch' maps to the fallback). The app ships in WKWebView, so on the real surface the sanitizer is a no-op for network failures. | lh-copy-content | 4 |
| BD-007 | **YES** | fixed | `/dashboard + /browse — Nearby radius with no coordinates` | Choosing a radius and declining the OS location prompt leaves a lit filter chip over an unfiltered feed, and there is NO fallback to coordinates the account already has. useDashboardFilters.ts:132 feeds the radius solely from useUserLocation, which is browser/Capacitor geolocation ONLY. On denial userLoc.status!=='ready', nearbyUnavailable goes true and every job is deliberately kept (correct — a denied prompt must not empty the feed), but locationFilter still counts toward activeFilterCount (:158), so the chip and the filter badge keep asserting a filter that is provably not applied. Two coordinate sources the app already holds are never consulted: profiles.latitude/longitude, and the signup ZIP via louisiana_zip_parishes (which carries lat/lng). On prod only 2 of 8 approved accounts have profile coords but 6 of 8 have a resolvable ZIP, so the ZIP tier is the load-bearing one and it is entirely unwired. | lh-browse-discovery | 11 |
| BD-008 | **YES** | fixed | `/dashboard — 'N jobs match your filters' header count` | The feed header states a count that the list underneath it contradicts, and the code documents the cause as acceptable. useDashboardJobsCount runs a separate count:'exact', head:true query against open_jobs_browse and deliberately reproduces only the filters that 'translate cleanly to SQL' — the radius/haversine branch and matchAvailability are skipped by design. So with a 5-mile radius applied the list renders 1 card while Dashboard.tsx:538 prints '3 jobs match your filters', and removing a card by filtering does not move the number. The hook's own comment calls this a 'KNOWN GAP (documented, not a bug)' that 'can run slightly HIGH... never low' — but 3 over 1 is a 3x overstatement, and the words 'match your filters' make it a false claim about the very set on screen rather than a rounding gap. The count must be the rendered set. | lh-browse-discovery | 1 |
| SC-013 | **YES** | fixed | `Plus tier ($15/mo) — every entitlement gate written as a tier list` | CONFIRMED and FIXED. The Plus tier, restored 2026-09-05, was excluded from ELEVEN separate hand-written tier lists of the shape `tier === "basic" \|\| tier === "pro" \|\| tier === "elite"`. A paying Plus member was: 403'd from Instant Payouts by supabase/functions/instant-payout with copy telling them to 'Upgrade to Basic, Pro or Elite' (a CHEAPER plan than the one they hold), and shown no button at all client-side (EarningsTab.tsx:99); scored ZERO priority-placement points on both the applicant list (applicantScoring.ts:92) and the poster feed (smartSort.ts:141) despite TIER_PERKS.plus.priorityPlacement === true; charged FULL price for a Job Boost their plan discounts (productPrices.ts:41 + create-boost-payment/index.ts:214) and denied the monthly free boost entirely (create-boost-payment:138, JobBoostDialog.tsx:51); shown no membership badge on FOUR surfaces (JobPosterCard, HelperBadges, IdentityHeader, ApplicantsPanel) and given the free-account avatar ring; denied the Portfolio Showcase perk (UserProfile.tsx:780); sorted BELOW Basic in the admin dispute queue (AdminDisputes.tsx:159); counted as $0 of MRR and absent from every chip, pie slice and filter on the admin analytics surfaces; and their REFERRER was paid nothing when a Pro upgrade would have paid $10 (check-pro-subscription/index.ts:130). None of it errored. Two edge functions additionally inverted the house null-expiry convention (`subActive = expiresAt ? … : false`), so a comped/lifetime member lost their included boost. This is the registries-checked-against-themselves pattern for the 9th time: src/lib/tierNames.parity.test.ts's own ALL_TIERS literal omitted 'plus', so the test could not fail for the member it lacked. | lh-subscriptions-credits | 10 |
| PD-018 |  | fixed | `/my-posts, /my-jobs, every ProtectedRoute page` | Every protected page's JS chunk download was serialized BEHIND the auth round-trip it does not depend on. ProtectedRoute returns its fallback (never children) while the session is unknown, and React only starts a lazy() import when the element renders, so the route chunk was not even requested until the profile response landed. | lh-perf-deps | 1 |
| PD-020 |  | filed | `/my-posts (representative of every authed route)` | Rendering My Posts costs 2.49 MB across 252 HTTP requests. The module graph is split so finely that the browser walks a deep dependency tree one round-trip per level, and the boot path pulls chunks the page cannot use — twitter-provider (31KB), forms (80KB), usePersistedBrowseView (34KB), select (21KB), TimePickerWheel, DatePickerField, CityAutocomplete, currency-input, stripeFees, PhotoLightbox, BlockUserDialog, ReportDialog, useMapKitJs — all downloaded BEFORE the posted-jobs response. This, not any single query, is the dominant cost of the reported ~12s blank. | lh-perf-deps | 1 |
| EJ-019 |  | fixed | `/my-posts → PostedJobCard (job abandoned at Stripe checkout)` | A poster who abandons Stripe checkout is left with a job that says it is live and is not. The row persists at status='open' with payment_status 'unpaid' or 'abandoned'; all four browse surfaces filter on a FUNDED payment_status, so NO helper can ever see it. The card nonetheless asserted the opposite in three places: the job-tracking rail lit 'Posted' as a COMPLETED step ~40px above the notice reading 'Payment not finished'; an Applicants button offered applicants for a listing nobody could see (its '0' reads as weak demand, not invisibility); and Boost offered to CHARGE for promoting reach that does not exist. Separately the notice predicate keyed on payment_status='unpaid' only, so live 'abandoned' rows showed nothing at all. The job then sits at 0 applicants until date_needed passes and auto-expire-jobs cancels it as 'Job listing expired — scheduled time passed with no helper assigned', blaming the schedule for a non-payment failure. | lh-e2e-journeys | 4 |
| EJ-021 | **YES** | fixed | `.github/workflows/e2e-real-backend.yml → e2e/prod-lifecycle.spec.ts (the nightly real-backend money loop)` | The nightly real-backend money loop — the ONE job that covers escrow/hire/release/review unmocked — has never passed, and cannot pass as written. It fails at its first assertion on every run: 'is_seed must be true or the money sweeps will pick this up'. The spec INSERTs the job as the poster with is_seed:true, but public.enforce_jobs_insert_column_lock unconditionally sets NEW.is_seed := false on every poster INSERT, by design and with its reason stated in the function ('a poster must not be able to hide a job from the admin money figures'). So the assertion is unsatisfiable through the path the spec uses. Two consequences. (1) COVERAGE: the loop dies ~1.4s in, before post/fund/apply/hire/complete/release/review run at all, so the gap this workstream exists to close is still fully open while a green-looking scheduled workflow implies otherwise — and the failure is a red run nobody reads, not a skip that announces itself. (2) SAFETY: is_seed=true is one of the FOUR blast-radius controls the spec's own header documents ('THE MONEY SWEEPS SKIP IT — the job is still marked is_seed = true, which is what auto-release-payment, payment-confirm-reminder, money-reconciliation, process-scheduled-payouts and subscription-reconciliation filter on'). That control does not exist and cannot for a poster-inserted row, so the documented safety model is a quarter false. The same root cause explains why all 8 current fixture jobs on the two test accounts carry is_seed=false. | lh-e2e-journeys | 1 |
| TC-008 | **YES** | fixed | `release-payout edge function / jobs.payment_status` | release-payout sends the Stripe transfer, then fails its own job-status update and returns 500 — money leaves the platform while the job stays payout_pending forever. Proven live on 2026-09-08: transfer tr_3UDDZQKp2H4b7tEC13IlnQEy ($22.00) was created, payout_transfers row for job 0021b7d3-7294-4871-89a2-f1a48891e10e reads status='paid' with that transfer id, and jobs.payment_status is STILL 'payout_pending'. The function's own error text names the state: 'transfer sent but job status update failed - manual reconciliation needed' with zero_row_match:false. This is exactly the split money-reconciliation:287 exists to page about, and the caller (admin Release payout button, and auto-release-payment Phase 2 over HTTP) sees a 500 - so a human or a cron will retry a payout that already went out. Not a test defect: the transfer is real and the ledger row is real. | lh-test-ci | 4 |
| VD-001 |  | fixed | `TermsReconsentDialog (app-wide)` | Terms re-consent dialog could never open: eligibility required !profile.ban_status, but profiles.ban_status defaults to 'active' (every prod row is 'active'), so isEligible was false for all users regardless of terms_version_accepted. Separately, the dialog body naming the Terms/Privacy links was stripped in f6a21b44a, so once open it read title + 'I Agree' with no statement of what is agreed. | visual-dialogs | 2 |
| ME-044 | **YES** | fixed | `/admin?tab=payouts (get_payout_batches / release-payout)` | A dispute decided but not yet settled was offered for FULL payout: get_payout_batches()/get_payout_batch_job_ids() included it and release-payout's dispute guard allow-lists dispute_status='resolved', so Bulk Approve would have transferred 88% of a job whose decision awards the helper 50% — and a later Retry settlement would refund+transfer on top (double-settle). | lh-money-escrow | 2 |
| BR-022 | **YES** | fixed | `.github/workflows/functions-deploy.yml → Supabase edge functions (prod fncmgoasalhdgfwzhsqa)` | functions-deploy reports SUCCESS over edge-function deploys the platform silently discards. Run 35756822730 (2026-09-22, deploy-all) printed 'Deploying Function: X (script size: N)' + 'Deployed Functions on project ***: X' + '✓ X deployed' for all 73 functions and concluded success; 8 of those uploads never landed. The workflow's only success test is the CLI exit code (line ~239 'if supabase functions deploy ...; then'), which is a statement about the client, not about prod. | lh-build-release | 2 |
| BR-023 | **YES** | fixed | `prod edge function claim-gift-card (gift card claim)` | PROD IS RUNNING STALE CODE for claim-gift-card. The deployed source still selects 'id, recipient_id, recipient_email, status, expires_at' with NO payment_status, and has no funding gate — so a gift card whose charge was refunded or charged back can still be CLAIMED and bound to an account. The gate landed on main at 2026-09-22T16:50:37Z in 61fe256d2 and the 16:51Z deploy-all reported deploying it. | lh-build-release | 1 |
| SC-016 |  | filed | `supabase/functions/create-pro-checkout + .github/workflows/functions-deploy.yml` | Production is running the 2026-09-07 bundle of create-pro-checkout (version 87). The GitHub Actions edge-function deploy reports 'deployed' and exits 0 for this slug while the deployed artifact never changes, so every merged change to this money-path function for the last 15 days is absent from prod — including today's duplicate-purchase guard fix. | lh-subscriptions-credits | 12 |
| SC-017 |  | filed | `supabase/functions/create-pro-checkout` | create-pro-checkout wrapped its active-subscription guard in 'if (billing_cycle !== "one_time")', so a member holding a live Stripe subscription was refused a second monthly/annual but was allowed to buy a one-time pass. The pass grants the same tier perks for ONE_TIME_PASS_DAYS=30 the subscription is already granting — tiers do not stack — and is written through oneTimePassLinkage() with a now+30d expiry while check-pro-subscription rewrites subscription_expires_at from the subscription's current_period_end on every dashboard load, so the paid-for window can be silently overwritten. | lh-subscriptions-credits | 4 |

## MEDIUM (177)

| ID | Blocker | Status | Surface | Claim | Agent | Evidence |
|---|---|---|---|---|---|---|
| RW-003 |  | fixed | `/admin?view=map, /admin?view=parishtax` | Two ?view= values are referenced in src/ but are not keys in VIEW_LABELS, so Admin cannot render them. Code comments record that a stale ?view=parishtax link previously rendered the home dashboard WITH an AdminSectionHeader whose title was undefined - an empty h1 stacked on home's own, caught by the UI sweep as 'expected exactly 1 h1, found 2'. Confirm the isRealView guard covers both and that nothing still links to them. | lh-route-walker | 1 |
| O-003 |  | filed | `AASA / universal links` | zz-runtime-probe AASA tests failing: '3a AASA is served over HTTPS with the claimed paths' and '3a-defect apex louisianahelpr.com serves AASA without a redirect'. If AASA is not served correctly, universal links do not open the app. | lh-orchestrator | 1 |
| O-004 |  | fixed | `profile earnings tab` | e2e 'earnings tab renders one view at a time' failing on main - suggests two earnings views render simultaneously or none does. Note ?tab=payment aliases ?tab=earnings, which is a plausible source of a double render. | lh-orchestrator | 1 |
| SI-009 |  | fixed | `money-column CHECK constraints: instant_payouts, referral_credits, jobs fee columns, applications.stake_amount` | Several live money columns have NO CHECK constraint at all, so the database will accept a negative amount, and instant_payouts has no constraint tying net to gross minus fee. instant_payouts (gross_amount, fee_amount, net_amount -- all numeric(10,2) NOT NULL) carries ZERO check constraints; referral_credits.amount (numeric, NOT NULL, default 5) has none; applications.stake_amount has none; and on jobs the fee columns platform_fee_amount, customer_fee_amount, commission_tax_amount, sales_tax_amount, sales_tax_rate, cancellation_fee, protection_fee and helper_fee_percent are all unconstrained numeric. Compare the tables that DO get this right: payout_transfers.amount_cents CHECK (> 0), payment_refunds.amount_cents CHECK (>= 0), gift_cards.amount CHECK (> 0), tips.amount CHECK (> 0 AND <= 1000), disputes CHECK (COALESCE(execution_helper_cents,0) >= 0 AND COALESCE(execution_refund_cents,0) >= 0). The guard is the house style; these columns are the gap in it, and instant payout is the one money system with a documented history of stranding money (reap_stranded_instant_payouts exists). NOTE: no float-typed money column exists anywhere -- every money column is numeric or integer, so the HIGH float check in the lane definition passes clean. | lh-schema-integrity | 3 |
| SI-013 |  | filed | `text-CHECK pseudo-enums vs real Postgres enums (exhaustiveness coverage)` | The schema uses two different mechanisms for closed value sets and only one of them can be checked at compile time. FIVE real Postgres enums (app_role, application_status, auto_tip_mode, job_category, job_status) generate into types.ts as string unions PLUS a runtime Constants array, which is what lets src/test/jobStatusExhaustive.test.ts enumerate job_status from its source of truth and assert every label/colour map covers every member. TWENTY-TWO other closed sets are text columns with CHECK (x = ANY (ARRAY[...])) instead, so they generate as bare 'string' -- no union, no Constants entry, no assertNever possible, and no equivalent guard test. The two most consequential are jobs.payment_status (10 values: unpaid, escrow, payout_pending, released, refunded, cancelled, abandoned, failed, chargeback, cancelling -- the escrow state machine) and notifications.type (17 values). The jobStatusExhaustive test exists precisely because pending_approval once fell through every branch and rendered an empty box, twice; the same failure mode is currently unguardable for payment_status. | lh-schema-integrity | 1 |
| SI-014 |  | filed | `RLS predicate evaluation cost: 14 auth_rls_initplan policies + 54 multiple_permissive_policies (prod)` | Unindexed/re-evaluated RLS predicates are the lane's named 'performance cliff that only appears at scale', and the project is on the Supabase free tier. Prod's performance advisor reports 103 lints: 54 multiple_permissive_policies, 31 unused_index, 14 auth_rls_initplan, 3 unindexed_foreign_keys, 1 table_bloat. The 14 auth_rls_initplan policies call auth.uid()/current_setting() PER ROW instead of once via (SELECT auth.uid()), across 8 tables: subscription_cancel_reasons (3), recurring_visit_releases (3), group_job_helpers (2), messages (2), jobs (1 -- 'Customers can create jobs'), cron_run_log (1), cron_work_expectations (1), profile_search_rate_log (1). Separately, jobs is the worst permissive-overlap target on the money surface: FOUR permissive SELECT policies and FOUR permissive UPDATE policies, every one of which is evaluated for every row of every query. Three tables (helper_skills, job_pets, verification_exceptions) have policies with no TO authenticated restriction, so they multiply across all six roles including anon. FK indexing itself is clean -- only 3 unindexed FKs exist and all 3 are on tables with 0 live rows (subscription_cancel_reasons_user_id_fkey, thread_archives_job_id_fkey, thread_archives_other_user_id_fkey), so the earlier add_missing_fk_indexes passes did their job. | lh-schema-integrity | 1 |
| SI-015 |  | fixed | `public.user_roles RLS: 'Deny all deletes to user_roles' is a PERMISSIVE no-op` | user_roles carries a policy named 'Deny all deletes to user_roles' with USING (false), but it is declared PERMISSIVE, not RESTRICTIVE. Permissive policies are OR-ed together, so a permissive USING(false) contributes nothing at all: it can never subtract access. DELETE on user_roles is therefore governed entirely by the sibling 'Admins can delete roles' policy, exactly as if the deny policy did not exist. The current net behaviour is probably the intended one (admins may delete roles), so this is not an open hole -- it is a security control that reads as a hard stop, is named as a hard stop, and enforces nothing. That is the 'configured but does nothing' class, and on the table that grants admin. Note the contrast with its twin: 'Deny all updates to user_roles' is also permissive-false, but it happens to be the ONLY permissive UPDATE policy on the table, so updates really are denied -- by the absence of any passing policy, not by the deny. Two policies written the same way, one working by accident and one inert. Routed to lh-authz-rls as the owning lane. | lh-schema-integrity | 2 |
| SF-004 |  | filed | `src/components/profile/CredentialsTab.tsx:397 (licensed-and-insured credentialing)` | Zero-row-write, trust. The profiles UPDATE setting business_name is the ONLY unguarded write in its own file — the other three profiles writes in CredentialsTab.tsx (271, 341, 480) all end .select('user_id') and pass through unwrapMutation(); this one stops at .eq('user_id', userId). On a zero-row match the UI still runs patchCache({ business_name, license_status:'pending', insurance_status:'pending' }), fires hapticSuccess(), and toasts 'Saved — your badge is back in review while we check the new name.' Neither thing happened. The invalidateQueries on line 414 then refetches, the server wins, and the verified badge silently flips back with the old name and no explanation. Credentialing is explicitly in scope (PROTOCOL 6d B2B carve-out). A BEFORE UPDATE trigger (trg_auto_pending_credentials) is live on this column, and profiles.id vs profiles.user_id confusion has bitten this codebase before. | lh-silent-failure | 2 |
| SF-005 |  | fixed | `src/components/admin/adminusers/useAdminUserActions.ts:158 (unban) + src/components/admin/AdminUserNotes.tsx:152,165` | Two admin-write zero-row exposures. (1) UNBAN: the user_bans UPDATE { is_active:false } carries no row-count guard and its justification lives in a TEST FILE (src/test/mutationRowGuard.test.ts:178 ALLOWLIST: '.eq(is_active,true) legitimately matches zero rows when there is no active ban'), not in a comment at the call site as CLAUDE.md requires. The residual risk is real and not what the allowlist reasons about: RLS denying an admin UPDATE on user_bans produces zero rows IDENTICALLY to 'no active ban' — verbatim the AdminExceptionQueue bug whose own comment (AdminExceptionQueue.tsx:191) records that the only non-owner policy was auth.role()='service_role', which never matches an admin JWT, so writes affected zero rows while the queue reported success. The paired profiles.ban_status write immediately below IS guarded and would succeed, producing a HALF-LIFTED BAN: the app treats the account as active while the user_bans row stays is_active=true. (2) AdminUserNotes UPDATE (:152) and DELETE (:165) have neither guard nor comment; on a refused write cancelEdit()/setDeleteNote(null) closes the editor and loadNotes() silently restores the old text, so a refused moderation-record write reads as a UI glitch. | lh-silent-failure | 4 |
| SF-006 |  | fixed | `src/pages/postjob/useJobSubmit.ts:511 (geocode attach) + src/pages/postjob/useJobMediaUpload.ts:221 (scope video attach)` | Both sites drop the Supabase error and then check ONLY the zero-row case — which means the case they were written to catch is the one they miss. Both do: const { data: rows } = await supabase.from('jobs').update(...).select('id'); if (rows && rows.length === 0) report(...). When the write genuinely FAILS (RLS denial, network, PGRST202) supabase-js returns { data: null, error: <err> }, so rows is null, the guard 'rows && rows.length === 0' is FALSE, and nothing is reported at all. The comment at useJobMediaUpload.ts:220 says 'ignore any error, but still surface a silent zero-row rejection so it's visible in logs' — the intent is right, the implementation reports the benign half and swallows the real half. Consequence for the geocode one: a job silently gets no lat/lng and never appears on /browse?view=map, with no trace — the exact defect class that already shipped once when the CSP blocked nominatim and both call sites swallowed it with catch { return null }. | lh-silent-failure | 2 |
| SF-007 |  | fixed | `e2e/happy-path/activity-card-density.spec.ts:416 (O-002 third spec)` | The third O-002 spec fails for a THIRD, unrelated reason, and it is also spec drift, not a product defect. The No-Show chip renders correctly — the captured DOM shows button 'No-Show — report that the Helpr never turned up' present and expanded — but line 416 queries getByRole('button', { name: 'Report the Helpr as a no-show' }), which is not a substring of the current accessible name, so the locator never resolves and boundingBox() times out. src/components/activity/postedJobCard/PostedJobActions.tsx:568-576 documents WHY the name changed: JobActionRow sets aria-label which REPLACES rather than augments the name, so the old 'Report the Helpr as a no-show' made the visible word 'No-Show' something voice control could not say (WCAG 2.5.3 Label in Name). The product change is correct; the spec was never updated. Note line 407 of the same spec uses /no-show/i and passes — only the exact-string assertion at :416 is stale. Fix = assert /no-show/i at :416 too. | lh-silent-failure | 3 |
| SF-008 |  | fixed | `src/hooks/useActivityData.ts:633-697 — realtime on /my-posts + /my-jobs (native)` | The Activity realtime socket fails repeatedly inside the WKWebView and the only user-visible consequence is that the feed goes SILENTLY STALE. Confirmed in prod error_logs: 12+ CHANNEL_ERROR rows on capacitor://localhost/my-posts and /my-jobs, each event producing exactly two rows with identical created_at and tags.channel of 'activity-core' and 'activity-reviews'. This DISPROVES the reused-channel-name hypothesis for O-002: it is one component opening two deliberately-split channels (useActivityData.ts:663 and :689), one socket-level transport failure fanning out to both, batched into a single error_logs INSERT by errorLogger.ts. Every channel in src/ was audited — 14 channel() sites, 26 postgres_changes bindings, 13/14 use channelNonce(), 14/14 clean up on unmount, 21/26 bindings user-filtered and all 5 unfiltered ones deliberate and commented. So the mechanism is correct and the instrumentation is correct; the defect is that when the transport dies the poster's cards stop moving on the other party's writes with NO user-facing signal and no automatic recovery path — the warning is logged to a table the user never sees. Related: useActivityData is the ONLY channel in the codebase that passes a status callback to subscribe(); the other 13 call bare .subscribe(), so an identical transport failure anywhere else is invisible even to us. | lh-silent-failure | 5 |
| O-005 |  | fixed | `e2e/happy-path/activity-card-density.spec.ts:698` | After the build fix, one spec still fails: 'the active bucket is the selected tab, not a caption beside the title' times out at waitForSelector('h1') while looping /my-posts?filter=needs_you\|scheduled\|waiting\|done with seed:true. Reproduces in isolation, so it is not port contention or flake. | lh-orchestrator | 2 |
| AR-002 |  | fixed | `reviews + reports tables (prod) — banned-user write path` | The server-side ban gate (enforce_ban_gate trigger) is installed on jobs (INSERT+UPDATE), applications (INSERT) and messages (INSERT) only. reviews and reports have NO ban gate. A user who is banned mid-relationship but still holds a valid pre-ban JWT can INSERT a 1-star review on any completed job they were a party to (review-bombing after a ban) and file reports. The reviews INSERT RLS policy correctly checks job party+completion but does not consult ban_status; is_caller_banned() is never called on these two tables. | lh-authz-rls | 2 |
| ME-002 |  | fixed | `stripe-webhook checkout.session.completed → escrow funding write` | The write that marks a job funded (payment_status→escrow, stripe_payment_intent_id) at checkoutSessionCompleted.ts:727 is a bare .update().eq('id', jobId) with no .select('id'): a zero-row match (job row gone between session creation and payment) returns {error:null}, logs 'Stored payment_intent and escrow status on job', 200-ACKs, and commits the dedupe row — a captured charge with no funded job and no alert, unrecoverable by Stripe retry. | lh-money-escrow | 1 |
| ME-004 |  | fixed | `post-job checkout window → captured amount vs jobs.budget` | jobs.budget stays poster-writable while payment_status='unpaid' (enforce_poster_jobs_money_lock only locks once funded — verified live via pg_get_functiondef on prod), and the Stripe Checkout line items are frozen at session creation. A poster who edits budget between create-payment's session creation and paying is charged the OLD amount while every downstream payout computes from the NEW jobs.budget. Inflation is caught only because payouts link source_transaction (transfer > charge is rejected by Stripe) — but that link is best-effort inside a try/catch (process-scheduled-payouts:539-550 'Could not link charge' warn-and-continue), so a transient Stripe read failure turns the cap off and the inflated transfer draws from the platform balance. Deflation (lower budget after session creation) silently over-charges the poster with no reconciliation. Nothing at funding time compares pi.amount_received against budget+fees. | lh-money-escrow | 3 |
| ME-007 |  | verified | `TipDialog → tip idempotency` | TipDialog never sends the tipAttemptId the server's idempotency design depends on (TipDialog.tsx:39 body is {action,jobId,amount,native}), so production always uses the 10-minute time-bucket fallback key. A deliberate second identical tip within the same bucket silently replays the first Checkout session (helper paid once, poster believes twice, no error); conversely a retry straddling the bucket boundary can double-create. The server comment 'the attempt id is stable for as long as the tip prompt is open' describes behavior that does not exist client-side. | lh-money-escrow | 2 |
| ME-008 |  | verified | `helper first payout → $2 onboarding deduction invisible` | process-scheduled-payouts and release-payout deduct the one-time onboarding fee (200c) from a helper's FIRST payout, but no client take-home surface (JobPrice.computeNet, helperEarnings.helperTakeHomeDollars, ApplyEarningsBreakdown) subtracts or mentions it — a first-time helper is shown 'You earn $72' and receives $70, violating JobPrice.tsx's own stated rule that a displayed payout figure may never read higher than the payout. Sole disclosure is one Help Center sentence. | lh-money-escrow | 4 |
| ME-009 |  | verified | `payment_status chargeback/failed → user-facing rendering` | Edge functions write payment_status values 'chargeback' and 'failed' (plus 'cancelling','abandoned','pending'), but paymentStatusLabel is consumed only by admin components; zero non-admin src/ hits for 'chargeback'. A charged-back or failed-payment job renders to both parties identically to a healthy escrowed job (useDashboardData.ts filters only 'abandoned'), and the helper's apply sheet keeps promising 'Held securely…' on funds already pulled back. Neither party is ever told the money left. | lh-money-escrow | 2 |
| ME-010 |  | verified | `EditJobDialog → category edit after funding changes tax classification` | Jobs are escrow-funded at post, category remains editable until a helper is assigned (EditJobDialog.tsx:87,179 gates only on hasHelper), and category is the sole input to isLaborTaxable. Post as exempt 'cleaning' (0 tax charged) then edit to taxable 'assembly': job is now taxable labor on which LA sales tax was never collected (or the reverse: tax paid on a now-exempt job with no refund path). No trigger locks category; remittance exposure. | lh-money-escrow | 3 |
| ME-011 |  | verified | `auto-tip-charge → ambiguous-failure handling` | auto-tip-charge's catch treats EVERY throw from paymentIntents.create as a decline: giveUp() marks the tip failed and tells the poster 'Your tip didn't go through… send it in a tap' (index.ts:413-419). A lost response/socket reset after the charge succeeded leaves: poster charged, helper paid via transfer_data, tips row 'failed', and the poster actively invited to tip again — the exact ambiguous-outcome class charge-recurring-visits handles with DEFINITIVE_CHARGE_FAILURES + an 'unknown' outcome (charge-recurring-visits/index.ts:168-232). No same-key retry, no alert on this path. | lh-money-escrow | 2 |
| ME-013 |  | verified | `cash-out-credits → unsalted idempotency key + no ledger` | Two defects: (a) idempotencyKey cashout-sha256(creditIds) has no failure salt, and on transfer failure the claim is rolled back so the same creditIds are re-claimable — every retry inside Stripe's ~24h window replays the cached failure as a silent no-op (verbatim the bug _shared/payoutClaim.ts:26-39 was built to fix; this function never adopted it). (b) The transfer carries no metadata and no ledger row is written anywhere — referral_credits.redeemed=true stores neither transfer id nor timestamp, so a cash-out is unreconcilable by money-reconciliation or support. | lh-money-escrow | 2 |
| ME-014 |  | verified | `charge-recurring-visits → Stripe Tax transactions never registered + fee floor excludes tax` | (a) Tax on recurring visits is computed via stripe.tax.calculations.create and charged inside totalCents, but tax.transactions.createFromCalculation is never called anywhere in the repo (0 grep hits) — LA sales tax collected on recurring assembly/handyman visits never reaches Stripe Tax's filing reports, and calculations expire (~90d) so they cannot be committed retroactively. (b) posterServiceFeeCents(budgetCents, pct, 0) passes otherChargeCents=0 while taxCents is added to the same charge afterward, so on taxable visits the Stripe-cost floor base is short by taxCents — systematic ~2.9%-of-tax under-collection whenever the floor branch wins. Fix is ordering: compute tax first, pass as otherChargeCents. | lh-money-escrow | 2 |
| ME-015 |  | verified | `instant-payout → fee-taken-but-payout-failed does not page` | When the 3% fee transfer succeeded but the subsequent payouts.create failed, the only trace is 'IMPORTANT: fee of Nc was already transferred — reverse…' written into instant_payouts.error_message (index.ts:383-394); Slack fires only if the DB write ITSELF fails. The symmetric platform-favoring case (fee_uncollected) DOES alert. So the one path where the HELPER is out money relies on someone reading a DB column. Also: the two fee-marker UPDATEs at :218-221 and :269-272 have no .select()/zero-row check and OVERWRITE error_message rather than append (the SQL reaper deliberately appends with COALESCE to preserve exactly this marker). | lh-money-escrow | 2 |
| ME-016 |  | fixed | `subscription-active convention → three money paths disagree on NULL expiry` | A profile with a paid tier and subscription_expires_at NULL is ACTIVE to instant-payout (index.ts:92, null⇒active) and to _shared/helperFees.ts:86 (null⇒active), but INACTIVE to create-boost-payment (index.ts:97, null⇒false): a lifetime/comped Elite gets the 8% rate and instant payout but is denied their free boost and charged $3. Conversely a null-expiry residue row is granted paid features by instant-payout. One convention should own this (helperFees' null⇒active), in a shared helper. | lh-money-escrow | 3 |
| CJ-002 |  | verified | `db: cleanup_observability_tables() / cleanup_stripe_webhook_events()` | Two retention functions exist, are correct, and are scheduled NOWHERE — they have never run once. cleanup_observability_tables() prunes analytics_events at 90 days and error_logs at 30; cleanup_stripe_webhook_events() prunes stripe_webhook_events at 30 days. Neither appears in cron.job, in any edge function, in any workflow, or in any script. Worse, three deployed edge functions carry comments citing the prune as the reason growth is not a concern — stripe-webhook/index.ts:269, stripe-idv-webhook/index.ts:205 and verification-webhook/index.ts:269 all say 'cleanup_stripe_webhook_events() prunes at 30 days. So no...' — an invariant asserted in a comment that does not hold. On the Supabase FREE TIER this is unbounded growth with a documented policy nobody enforces. | lh-cron-jobs | 1 |
| CJ-004 |  | verified | `db: sweep_dead_crons() failure detection` | A pg_cron job that fails INTERMITTENTLY alerts nobody. sweep_dead_crons() only emits an 'erroring' verdict when the last 3 recorded runs ALL have status <> 'succeeded'; a job that fails once a day out of 24 hourly runs never trips it, and pg_cron's own failure record is written to cron.job_run_details, which nothing else reads. Proven live by the very failure this lane found: sweep-silent-cron-failures raised on 2026-09-01 08:47 and the app produced no signal at all — no error_logs row, no Slack alert, nothing. It also means a SQL cron that raises every day at the same hour is indistinguishable from a healthy one on every dashboard the app has. | lh-cron-jobs | 1 |
| CC-002 |  | fixed | `browse map + /jobs feed (get_open_jobs_for_map, get_ranked_open_jobs)` | RESOLVED same-day: neither RPC excluded the viewer's own jobs, so a poster saw their own listing with an apply affordance and hit 'You can't apply to your own post.' BrowseMap.tsx documented the workaround rather than fixing it. Fixed in 20260902173900. The toast itself is NOT removable: /dashboard?quickApply=<id> and a direct /jobs/:id URL still reach the state, so the guard remains correct. | lh-copy-content | 2 |
| GD-003 |  | fixed | `src/ — stale 'as any' / 'as never' casts compensating for a types.ts that has since been regenerated` | Four call sites cast away type checking to work around a stale types.ts, and all four are now obsolete: their target RPCs are live in prod AND present in the committed types.ts. Three of the four comments literally say 'Drop the cast once types.ts is regenerated' - it was, and they survived, because a regeneration event has no follow-up step. Two sit on trust-critical paths: rpc_open_dispute (DisputeDialog) and rpc_check_application_rate (applyRateLimit, which fails OPEN on error). The arguments happen to match prod today, so nothing is broken right now - the defect is that these are permanently-open holes where a future signature change would compile clean and fail at runtime as PGRST202, which applyRateLimit.ts:105 converts into 'allow the apply'. | lh-generated-drift | 4 |
| GD-004 |  | fixed | `src/integrations/supabase/types.ts — objects live in prod but absent from the generated types` | Six database objects exist in prod but are missing from the committed types.ts: the table edge_rate_limit_log, and the functions cron_dispatch_health, notify_ops_dispute_filed, prune_edge_rate_limit_log, rate_limit_hit and sweep_cron_blackouts. Any src/ code calling these must cast around the type system (see GD-003 for what that costs long-term). Currently no src/ consumer references them - they are edge-function / cron-side objects - so the runtime blast radius today is zero; the finding is that the generated file no longer describes the database. | lh-generated-drift | 1 |
| NB-001 |  | filed | `Stripe Connect onboarding / Express dashboard / AwardGate payout fix (native iOS)` | The native Stripe return bounce is wired for 8 checkout functions but NOT for stripe-connect. getPublicReturnUrl() never appends native=1 on a Capacitor origin, and safeReturnUrl() (unlike buildRedirectUrl) has no native branch, so all 4 stripe-connect return_url call sites hand Stripe an untagged https URL. main.tsx:151 only bounces a page carrying ?native=1, so after finishing Connect onboarding the user is left staring at the marketing website inside the SFSafariViewController sheet and must dismiss it by hand. AwardGateDialog.tsx:71 additionally passes no onReturn callback, so even the manual Done refetches nothing. | lh-native-bridge | 6 |
| NB-002 |  | filed | `iOS springboard app-icon badge / APNs payload` | The app-icon badge can never change while the app is closed, so it diverges from server unread state in exactly the case a badge exists for. Three independent causes, all verified: (a) no caller anywhere sets payload.badge, so aps.badge is never present in any push APNs delivers — send-push-notification/index.ts:161 emits it only when 'typeof payload.badge === number', and a grep for 'badge' across all of supabase/functions/ returns zero hits outside that file; (b) appBadge.ts drives the badge from the running app only (useNavUnreadCount), so it is frozen at whatever value the last foreground session left; (c) no PushNotifications.presentationOptions is configured, so PushNotificationsHandler.willPresent returns [] and even a foreground push updates nothing. | lh-native-bridge | 4 |
| CJ-007 |  | filed | `db: cron_run_log / cron_work_expectations coverage across all 44 pg_cron jobs` | HALF THE CRON FLEET HAS LIVENESS MONITORING ONLY — NO WORK VISIBILITY AT ALL. Of the 44 scheduled pg_cron jobs, 22 are SQL crons (`SELECT public.fn()`) and NONE of them has ever produced a single row in cron_run_log — that table is fed exclusively by sweep_silent_cron_failures ingesting net._http_response, so a SQL cron's return value is written nowhere and read by nothing. pg_cron's own job_run_details records only 'succeeded' and '1 row'. Correspondingly, every one of those 22 jobs is registered in cron_work_expectations with candidate_key = NULL, and sweep_silent_cron_failures filters `WHERE c.candidate_key IS NOT NULL` — so the app's silent-work detector skips all 22 BY CONSTRUCTION. The app can prove these jobs FIRED; it cannot in any way tell whether they DID ANYTHING. That is exactly the failure mode cron_work_expectations was built to catch. The affected set includes money- and safety-adjacent sweeps: sweep-release-last-chance (the last-chance-before-escrow-auto-releases warning), detect-stuck-payments, reap-stranded-instant-payouts (which exists because instant payout has stranded money before), auto-start-due-jobs, sweep-dayof-confirm-reminders, sweep-no-show-alerts, sweep-expired-auto-bans and sync-profiles-update-grants. Each of these returns a work count (integer or jsonb) that is discarded at the call site. | lh-cron-jobs | 1 |
| CJ-008 |  | filed | `supabase/functions/auto-tip-charge + public.auto_tip_candidates()` | AUTO-TIP IS THE ONE MONEY CRON WHOSE BACKLOG DOES NOT DRAIN — a >24h outage loses every tip in the gap, permanently and silently. auto-tip-charge runs hourly and calls auto_tip_candidates(_since_hours := 24), whose predicate is 'j.updated_at > now() - make_interval(hours => 24)'. That is a hard exclusion, not a page window: a job whose completion falls outside the last 24 hours can never be picked up again by any run, and no other code path charges an auto-tip. So if the cron (or pg_cron itself — a 65-minute total dispatch blackout already occurred on 2026-09-01) is down for more than a day, every poster who opted into auto-tip silently does not tip and every helper is silently under-paid, with no queue, no retry and no record that anything was skipped. The window buys nothing: idempotency is already provided by the separate, permanent guard 'NOT EXISTS (SELECT 1 FROM tips t WHERE t.job_id = j.id AND t.source = ''auto'')', so widening _since_hours cannot cause a double-charge. Contrast the other money crons, which are all unbounded and drain correctly: auto-release-payment uses '.or(poster_completed_at.lte.cutoff, helper_completed_at.lte.cutoff)' with no lower bound and no .limit(); process-scheduled-payouts uses '.lte(payout_scheduled_at, now)' with no lower bound; charge-recurring-visits looks back SERIES_LOOKBACK_DAYS = 371. auto-tip-charge is the outlier at 24 hours. | lh-cron-jobs | 1 |
| CJ-009 |  | filed | `.github/workflows/schedule-heartbeat.yml` | THE WATCHER OF SCHEDULED WORKFLOWS DOES NOT WATCH ITSELF, AND MISSES TWO OTHERS. schedule-heartbeat.yml exists to catch scheduled workflows that have silently stopped — its own header documents the incident (broken-links, lighthouse and security-audit accumulated 1, 2 and 1 scheduled runs in four months and nothing surfaced it) and names the likely cause: GitHub disables schedule triggers on repos idle for 60 days and does not re-enable them. It states the rule 'Only workflows with a real cron are listed' and 'When you add or remove a schedule: block, update WATCHED below.' The rule is not met. EIGHT workflow files carry a real cron; WATCHED lists FIVE. Unwatched: (a) prod-freshness.yml (daily 07:45 UTC), (b) ui-sweep.yml (three weekly crons: Sun 05:30, Wed 05:00, Fri 05:00), and (c) schedule-heartbeat.yml itself (daily 11:23). (c) is the load-bearing one: if GitHub disables the heartbeat for inactivity — the exact policy its own comment names — then all five workflows it protects become silently unwatched at the same instant, and the guard's disappearance is itself unobservable. This is the same shape as CJ-001 in the database half: the detector is the single point of failure and nothing detects the detector. NOT currently firing-broken — all 27 workflows report 'active' and prod-freshness/ui-sweep/heartbeat all have recent successful scheduled runs — so this is a gap in the guard, not live breakage. The workflow-id ordering shows ui-sweep (336583180) predates schedule-heartbeat (344221362): it was omitted at authoring time, not left behind by a later addition. | lh-cron-jobs | 2 |
| NB-006 |  | filed | `iOS app-icon badge on foreground (appLifecycle -> removeAllDeliveredNotifications)` | appLifecycle.ts clears delivered banners on EVERY foreground and its comment states it 'leaves the numeric icon badge alone — MobileNav owns it'. The plugin does the exact opposite: PushNotificationsPlugin.removeAllDeliveredNotifications sets UIApplication.shared.applicationIconBadgeNumber = 0 unconditionally. So every return to the app force-zeroes the springboard badge. Nothing puts it back: useNavUnreadCount.ts:115 only re-runs setAppIconBadge when  or  CHANGES, and coming back to the foreground changes neither — so the badge stays 0 until the unread count itself moves. SECOND, CHAINED FAILURE: the same plugin method rejects outright when appDelegateRegistrationCalled is false, which is the state of every currently-shipped build (see NB-004), and appLifecycle.ts swallows it in a bare  — so on shipped builds delivered banners are never cleared either, silently. Two comments in two files assert a badge behaviour that neither the plugin nor any sender supports. | lh-native-bridge | 3 |
| NB-007 |  | filed | `Native photo picker / camera — all 3 call sites (RichMessageInput, PhotoProof, ReviewForm)` | All three native photo call sites collapse three distinct plugin outcomes into one toast, 'Couldn't open your photos. Please try again.' Read from the plugin's own iOS source, Camera rejects with THREE different messages: 'User cancelled photos app' (CameraPlugin.swift:315,319,323,342,499), 'User denied access to photos' (:534,547) and 'User denied access to camera' (:515,525), plus genuine failures like 'Error processing image' (:331). Consequences: (1) a user who simply BACKS OUT of the picker is shown an error toast for something that did not fail; (2) a user who DENIED the permission is told to 'try again', which is wrong advice — iOS prompts once per install and every retry silently no-ops, so the toast sends them into a loop with no path to re-enable. Scope item 'permission denial is graceful, with a path to re-enable' is not met on any photo surface. nativeCamera.ts's own docstring says callers should 'fall back gracefully'; none of the three inspect the rejection message. | lh-native-bridge | 5 |
| GD-006 |  | fixed | `scripts/check-types-fresh.mjs — the GD-002 freshness guard itself` | The new types-freshness guard has two proven coverage gaps and one unvalidated input. (1) A column TYPE change with unchanged nullability passes with the GREEN message: nullabilityMap stores a boolean (/\bnull\b/.test(type)) and discards everything else, so text->uuid or string->number reports 'types.ts matches the live schema'. That is a false green on real drift. (2) Functions/RPCs are not covered: run against the known real drift set it catches the new edge_rate_limit_log table but zero of the five new RPCs. (3) generateFresh() trusts SUPABASE_PROJECT_REF without validating it, so a wrong ref silently compares types.ts against staging - the trap CLAUDE.md and PROTOCOL section 4 both name, and .temp/project-ref points at staging today. The guard is otherwise correct: verified 5/5 on real drift, removed column, empty file, garbage file and a matching pair, and its parse coverage is exactly 100% (958 columns / 82 objects parsed vs 865+93=958 columns across 80 tables+2 views in information_schema). | lh-generated-drift | 3 |
| EF-001 |  | fixed | `supabase/config.toml → verification-webhook (Checkr / Certificial / Stripe Identity callbacks)` | verification-webhook is the ONLY inbound webhook with no verify_jwt=false entry in config.toml, so the Supabase gateway rejects every vendor callback with 401 before the handler runs. Its own signature verification (HMAC + Stripe constructEventAsync, fail-closed, timing-safe) is correct and can never execute. Latent today because no background-check vendor is live (function header says so), but it is wired for three vendors and will fail silently and unattributably the day one is switched on: the vendor sees 401, no row is written, nothing is logged, and the correct-looking in-function code invites the wrong diagnosis. Sibling webhooks resend-webhook / stripe-webhook / stripe-idv-webhook all carry verify_jwt=false, so this is drift, not a design choice. | lh-edge-functions | 1 |
| EF-002 |  | fixed | `money-reconciliation + subscription-reconciliation → cron outcome observability` | Both reconciliation crons have produced ZERO outcome observations in their entire life, and no alert exists for that state. pg_cron liveness is fine (sweep_dead_crons reads cron.job_run_details and correctly sees them firing), but OUTCOME is observed only via public.cron_run_log, which sweep_silent_cron_failures populates by parsing the 'fn' key out of net._http_response bodies. Both functions DO call cronResult (so they would emit 'fn'), yet neither has ever produced a single cron_run_log row. So nobody has ever seen a response from the money reconciliation: whether it completes, what it scanned, or whether it found a discrepancy is unobserved. The two watchers each read green because each is monitoring the half the other covers - enqueue succeeds, so liveness passes; no body is ever ingested, so the silent-failure detector has nothing to evaluate and reports nothing rather than reporting an absence. Most likely mechanism (stated as hypothesis, not verified - I did not invoke a money function per the live-Stripe rule): pg_net's default 5000ms timeout, which is already demonstrably firing on lighter crons in this same project. | lh-edge-functions | 1 |
| EF-003 |  | filed | `create-notification → in-app notification bell (platform-branded spoofing)` | Any authenticated user can push an arbitrarily-worded, platform-branded notification into any job poster's notification bell, including type='payment' / 'financial_alerts' / 'verified' / 'system_alert', with a same-origin deep link. create-notification's authorization (index.ts:105-154) allows a caller to notify a target if EITHER shares a job with them OR has an application on the other's job. The application branch is the problem: it authorizes the caller's CHOICE OF WORDS, not just their right to be heard. The 17 lifecycle call sites this rule was widened for (see the comment at :100-111) all send TEMPLATED copy; the endpoint accepts free text (title<=200, message<=1000) plus a caller-chosen type from a 17-value allowlist. The notification panel renders type with authoritative chrome and NO sender attribution: 'payment' and 'financial_alerts' render a primary-coloured DollarSign, 'verified' a ShieldCheck, 'system_alert' a Megaphone (notificationPanelHelpers.tsx:4-22). So 'Payment released - confirm your payout details' with link '/profile?tab=payment' is indistinguishable from a real platform notice. Filed MEDIUM not HIGH because it moves no money by itself and needs no privilege escalation; it is a phishing primitive on a money app, so the verifier may reasonably upgrade it. The rest of this function is genuinely well-hardened (UUID pinning before .or() string interpolation, same-origin link sanitiser, type allowlist, .select('id').single()) - the defect is only that content is unconstrained once the party check passes. | lh-edge-functions | 3 |
| DH-002 |  | filed | `/messages?userId=<self>&jobId=<own job> — built by JobDetailDialog.tsx:108` | The app builds, and the database permits, a message thread a user has with THEMSELVES. JobDetailFooter.tsx:109 renders 'Ask a question' when viewerUserId === job.customer_id (its own comment names 'the poster themselves' as an intended audience), and JobDetailDialog.tsx:108 handles it with navigate('/messages?userId=' + job.customer_id + '&jobId=' + job.id) — the poster's OWN id. The messages page renders that as a real thread ('Conversation with Audit W.', the signed-in user) with a working composer and the prompt 'Say hello. Send the first message to get the job moving.' Nothing refuses it: the live INSERT policy 'Users can send messages' is WITH CHECK (auth.uid() = sender_id AND can_message_in_job(job_id, auth.uid())) with NO sender_id <> receiver_id predicate, no CHECK constraint on messages, and can_message_in_job returns true for the poster on their own job. This is the mirror of a design hole — not an affordance offered then refused, but a nonsense state the product accepts. The missing predicate belongs in loadConversations.ts buildDeepLinkPlaceholder (drop deepLinkUserId === uid) and in the messages INSERT policy as defence in depth. | lh-design-holes | 2 |
| NB-010 |  | filed | `useKeyboardInset — keyboard avoidance on Messages, PostJob, ProfileEditForm, BrowseSearchBar, FilterSheet` | The hook reads as an either/or and behaves as a both: on NATIVE it attaches the Capacitor Keyboard listeners AND the visualViewport listeners at the same time. The Capacitor branch lives inside an async IIFE that is never awaited, and its early 'return' exits only that IIFE — not the useEffect callback — so the visualViewport block below it runs unconditionally, on every platform, plus a synchronous update() call. Both then write the same piece of state. The comment on the catch says 'plugin not available — fall through to visualViewport', which is the intended behaviour and is not what the code does. The conflict is real rather than merely redundant because capacitor.config.ts sets Keyboard.resize = 'body': when the keyboard opens the WebView body is resized, so window.innerHeight shrinks with it and update() computes diff ~= 0 and calls setInset(0) — overwriting the true keyboardHeight the Capacitor listener just set. Which value survives depends on event ordering. Five keyboard-critical surfaces consume this hook. It survived because the unit tests mirror the INTENDED split: there are native-subscribes tests and web-subscribes tests, and not one asserts that the native path does NOT also subscribe to visualViewport. | lh-native-bridge | 8 |
| DH-003 |  | filed | `Activity → posted/applied job card → Review chip (PostedJobActions.tsx:781, AppliedJobCard.tsx:511)` | The Review chip is offered forever; the server stops accepting reviews after 30 days, and the client has no idea. The live INSERT policy 'Users can create reviews for eligible jobs' on public.reviews requires COALESCE(poster_completed_at, helper_completed_at, updated_at) > now() - interval '30 days' AND (has_active_dispute = false OR dispute_resolved_at IS NOT NULL). The client gate is only payment_status: canReview = job.payment_status === 'released' \|\| 'payout_pending' (PostedJobActions.tsx:725-726) and the same on the helper side (AppliedJobCard.tsx:511). grep of src/ finds NO 30-day review constant anywhere and no has_active_dispute check on the helper side. So a job that crosses 30 days keeps a live Review chip: the user taps it, writes the review, submits, and gets 'We couldn't post your review — this job may no longer be open for reviews. Refresh and try again.' (ReviewForm.tsx:206-208). Refreshing can never help — the window is closed permanently. Every completed job crosses this line eventually, so the population of offer-then-refuse jobs only grows. The missing filter belongs in the client gate at PostedJobActions.tsx:725 and AppliedJobCard.tsx:511, mirroring the RLS predicate, with the chip swapped for an explanatory 'Review window closed' state rather than a dead tap. | lh-design-holes | 1 |
| DH-006 |  | filed | `Activity → posted job card (in_progress) → No-Show chip (PostedJobActions.tsx:562)` | One no-show per JOB, not per helper — so the second no-show on a re-hired job is refused with a message about somebody else. report_helper_no_show ends by reopening the job: 'UPDATE public.jobs SET status = 'open', helper_id = NULL WHERE id = p_job_id' (confirmed live via pg_get_functiondef). So the normal recovery path is: report no-show, job reopens, poster hires a second helper, job returns to in_progress. GUARD 3a inside that same function is scoped to the job alone — EXISTS (SELECT 1 FROM user_violations WHERE job_id = p_job_id AND violation_type = 'no_show') — with NO user_id predicate. If the SECOND helper also fails to show, the poster taps a fully-rendered No-Show chip and is told 'A no-show has already been reported for this job.' (lifecycleErrors.ts:25). The second helper is never recorded, never laddered, and the poster has no other route. The chip's render gate (PostedJobActions.tsx:490-494) checks status/completion/arrival/clock only — it has no knowledge of an existing report, so it cannot warn either. Two halves to the fix and they are NOT the same lane: the UI half (do not offer a chip that will be refused) is a client filter; the server half (scope GUARD 3a to (job_id, user_id) so a different helper can be reported) is a data-model and consequence-ladder change and is QUEUED FOR THE OWNER per the standing rule — it changes ban-ladder behaviour and needs a migration. | lh-design-holes | 2 |
| CJ-011 |  | filed | `db: the four cron detectors (sweep_cron_http_failures / sweep_silent_cron_failures / sweep_dead_crons / sweep_cron_blackouts)` | THE FOUR CRON DETECTORS DO NOT DUPLICATE EACH OTHER — but sweep_dead_crons is the sole watcher of the other three and has no watcher of its own. Answering the orchestrator's question directly: there is NO overlap. The four read different inputs, emit different verdicts, write different error_logs sources, and are deliberately staggered. sweep_cron_http_failures (*/15) reads net._http_response status codes and catches an edge function answering non-2xx -> 'cron-http'. sweep_silent_cron_failures (:47) reads cron_run_log bodies against cron_work_expectations.candidate_key and catches a 2xx that found candidates and dispositioned none -> 'cron-silent'. sweep_dead_crons (:53) reads cron.job_run_details against expected_max_gap and catches unscheduled / inactive / never-ran / dead / erroring -> 'cron-dead'. sweep_cron_blackouts (:57) differences the AGGREGATE cron.job_run_details dispatch stream for holes >15 minutes and catches pg_cron itself dispatching nothing -> 'cron-blackout'. Four distinct failure modes, no redundancy. THE REAL RISK IS TOPOLOGICAL, NOT DUPLICATION. All four detectors are themselves pg_cron jobs, and only ONE thing can notice a detector failing: sweep_dead_crons, which is the only function in the entire 254-function catalog besides the read-only reporter cron_dispatch_health() that reads expected_max_gap. So sweep_dead_crons watches the other three — and nothing watches sweep_dead_crons. If it stops firing or starts raising, the database emits no signal of any kind, and the failure of every other detector becomes simultaneously unobservable. Compounding it, sweep_dead_crons is blind to intermittent failure (CJ-004), which is the LIVE state of sweep_silent_cron_failures: 3 raises, 0 cron-dead rows. This is the exact shape of CJ-009 in the CI half — schedule-heartbeat.yml also watches everything except itself — so the same structural gap exists independently on both sides of the stack. | lh-cron-jobs | 1 |
| EF-007 |  | fixed | `mapkit-token → unauthenticated minting of UNRESTRICTED Apple MapKit JS tokens` | mapkit-token mints a real, Apple-signed, one-hour MapKit JS token to anyone who asks, with no authentication (verify_jwt=false), NO RATE LIMIT of any kind, and no origin binding whenever the caller simply omits the Origin header. originClaimFor() returns null when there is no Origin header and the payload is built as 'if (origin) payload.origin = origin' (index.ts:89-91, :136), so the claim is absent rather than defaulted. Omitting a header is the trivial case for any non-browser client. The result is that an unauthenticated script can mint unlimited domain-unrestricted tokens against this Apple developer account (team P85MCK558V), each usable on any site for an hour, consuming the account's MapKit JS daily map-view quota - which when exhausted breaks the map for real users and is billable past the free tier. The code comment at :82-88 is honest about WHY the no-Origin path exists (the native WebView sends no web origin), so the omission is deliberate and the trade-off is understandable; the defect is that nothing bounds it. What is NOT honest is the security justification in supabase/config.toml, which argues verify_jwt=false is safe because 'The token this mints is locked to the requesting origin ... so a token obtained without a session only works on this app's own domain'. That is false for exactly the easiest path to take, so the stated basis for making the endpoint public does not hold. Suggested fix, entirely in my lane: bound it (rate limit per IP, or require an authenticated caller when Origin is absent), and correct the config.toml comment to describe what the code actually does. NOTE this function does NOT import _shared/rate-limit.ts, so this is not a duplicate of the already-filed rate-limit defect - it is the absence of any limiter at all. | lh-edge-functions | 4 |
| EF-009 |  | fixed | `calculate-tax → unauthenticated, unmetered Stripe Tax API spend` | calculate-tax is public (verify_jwt=false, deliberately - it is the post-a-job pricing preview shown before a session exists) and has NO rate limiting of any kind, yet every accepted request performs a BILLED third-party call: stripe.tax.calculations.create (index.ts:66). Stripe charges per tax calculation, so an unauthenticated caller can drive unbounded spend on the account's Stripe Tax bill by looping the endpoint with a valid-shaped body. Reaching the Stripe call is cheap: the only guards are budgetCents finite and > 0 (index.ts:48), zip present and a string (:51), and isLaborTaxable(category) (:60) - no session, no origin check, no throttle. The function's header is right that it creates nothing and holds no money, and config.toml's reasoning for making it public is sound on the data-exposure axis ('the worst a caller can do is learn the sales tax on an amount in a ZIP, which is public information'). But that reasoning only covers information disclosure; it does not cover COST, and cost is the exposure here. Same defect class as EF-007 (mapkit-token) - a public endpoint whose per-request work is billed by a third party, with the security argument written only about data. Note this is NOT a duplicate of the already-filed _shared/rate-limit.ts defect: calculate-tax does not import that module at all, so this is the absence of a limiter rather than a weak one. Fix is in my lane: add a per-IP throttle, and/or cache calculations by (budgetCents, zip, category) since the same three inputs always yield the same answer - a cache would cut both the bill and the abuse ceiling. brand-asset, contact-support and ai-job-builder, the other public endpoints, DO rate-limit; calculate-tax and mapkit-token are the two that do not. | lh-edge-functions | 2 |
| NB-015 |  | filed | `iOS app-switcher snapshot — chat, payments, earnings and ID documents are captured unredacted by default` | The privacy shield that redacts the iOS app-switcher snapshot only runs for users who have opted into the app lock, so by default the snapshot captures whatever screen the user was on. AppLockGate's appStateChange handler ends with 'if (isAppLockEnabled() && userRef.current) setCovered(true)', and the pause handler opens with 'if (!isAppLockEnabled() \|\| !userRef.current) return'. isAppLockEnabled() reads safeStorage for APP_LOCK_ENABLED_KEY === '1' and returns FALSE when unset, and the only writer is the Profile > Security toggle (SecurityTab.tsx:297). So the default for every account is an unredacted snapshot. That matters more here than in a typical app because iOS writes those snapshots to disk under the app container and they persist across termination, and the screens in question are an open chat thread, payout and earnings figures, and the ID-document upload surface. The usual bar for an app handling money and identity documents is to redact unconditionally, which is what banking apps do precisely because the setting cannot be relied on. The mechanism is already built and correct — it is simply behind an opt-in most people will never find. PRECISE FIX, cheaper than it looks: the snapshot is taken around backgrounding, so gating the shield on the 'pause' event alone (rather than on appStateChange) covers it for everyone WITHOUT reintroducing the Control-Centre / notification-shade flash the current design was rewritten to avoid — that flash is the appStateChange path, and the header comment explains at length why it was made conditional. Only the isAppLockEnabled() check on 'pause' needs to drop. NOT FIXED BY ME: this is session-privacy and lh-onboarding-auth owns app-switcher redaction. | lh-native-bridge | 3 |
| NB-016 |  | filed | `Live en-route tracking — stops the moment the helper leaves the app, and the poster is not told` | Live position sharing is driven by a 45-second setInterval inside the WKWebView (JobTracking.tsx:673), and Info.plist declares only 'remote-notification' in UIBackgroundModes — there is no 'location' background mode. iOS suspends a suspended app's JS timers within seconds of backgrounding, so the interval stops firing as soon as the helper leaves the app. That is not an edge case: it is the single most common thing a helper does after tapping 'on my way' — switch to Apple Maps or Google Maps for driving directions. So the feature works precisely while the helper is staring at Helpr and not driving, and stops while they are driving. The poster is given no signal that it has stopped. JobTracking.tsx:1639 renders 'Updated {toLocaleTimeString}' — an ABSOLUTE clock time with no relative framing and no staleness state — which is the format least likely to be read as stale: nobody computes that 'Updated 10:09' means thirty-two minutes ago. The pin sits at the helper's starting point and reads as current. This is the same defect class the 2026-08-31 fix was written to solve (the comment at :620-624 records a frozen updated_at proven by moving the helper 1.5 km); that fix made the interval exist, but only while foregrounded. RECOMMENDED FIX IS THE CHEAP ONE, NOT THE BACKGROUND MODE: declaring a location background mode invites App Review scrutiny, costs battery, and contradicts this feature's own stated restraint at :627-632 about not tracking someone for a whole job. Instead make the UI truthful — render relative time ('Updated 3 min ago') and show an explicit paused/stale state once updated_at exceeds roughly twice the 45s cadence, so the poster knows the position is not live rather than being quietly misled. | lh-native-bridge | 2 |
| EF-010 |  | filed | `pro-customer-portal → status semantics on the membership self-serve exit` | READ THIS FIRST: this is NOT an authorization bypass. pro-customer-portal correctly refuses an unauthenticated caller - index.ts:20 'if (!authHeader) throw new Error("No authorization header")' and :24-26 reject a bad token - and no billing portal session can be created without a valid user JWT. I probed it with no credentials as part of the sanctioned money-endpoint auth sweep, got a 500 rather than the expected 401, stopped immediately per the orchestrator's rule, and did not probe it again; the 500 turned out to be a status-code defect, not a hole. THE ACTUAL DEFECT: the single catch at :80-86 converts EVERY throw into an opaque 500 {"error":"Internal server error"}, so three completely different conditions are indistinguishable to the client and to monitoring: (1) not authenticated / expired session - should be 401; (2) 'No Stripe customer found' at :50, which is the ROUTINE state for any member who has never subscribed - should be 404 or a 200 with a clear 'no membership to manage'; (3) an actual Stripe or runtime failure - correctly 500. User-facing consequence on a money surface: a member whose session lapsed, and a user who simply has no subscription, both click 'Manage membership' and are told the server broke. The second case is not an error at all. Observability consequence: genuine Stripe outages are buried in a stream of 500s generated by routine auth and no-customer cases, which matters more than usual given EF-008 - the cron-side alerting that would otherwise notice a 500 spike is itself unprovisioned. This function is the OUTLIER among its siblings: create-payment answers 401 {"error":"Missing authorization header"} and stripe-connect answers 401 {"error":"Not authenticated"} to the identical probe. That is the same drift the file's own comment at :35-40 describes for a different bug - 'Both siblings were fixed; this one was not.' Minor secondary point: req.json() is awaited at :19 BEFORE the auth check at :20, so an unauthenticated request's body is parsed before it is rejected. Not exploitable (it is .catch()-guarded and nothing is done with the value before auth) but the ordering is backwards. Fix is small and entirely in my lane: throw typed errors or return early with the right status, and let the catch handle only the genuinely unexpected. | lh-edge-functions | 5 |
| TC-001 |  | filed | `vitest suite — src/components/admin/FormalWarningDialog.test.tsx (symptom, not cause)` | A SECOND, UNDOCUMENTED SHAPE of the known shared-tree vitest flake. CLAUDE.md records the flake as findBy* TIMEOUTS on I/O-bound specs; this one is a module-interop failure instead: 'TypeError: ReactDOM.createPortal is not a function', thrown 6 of 7 times in FormalWarningDialog.test.tsx. Neither that test nor its component references ReactDOM — it comes from Radix's Portal — and the file passes 7/7 in isolation every time. Measured non-determinism on IDENTICAL code, three consecutive full runs: fail, pass, pass. It is worth writing down because it does not LOOK like a flake: it is a hard TypeError with a precise message, not a timeout, so the natural reading is a real module-resolution regression in whatever you just changed. I nearly chased it as one — the trigger was an unrelated one-line import edit, which changes module graph ordering and nothing else. The tell is the same as the documented shape: identical code, differing results. | main | 3 |
| NT-002 |  | fixed | `docs/audit/launch-2026-09/SURFACE.md — notification type inventory` | The audit surface inventory reported 5 notification types with a '⚠︎' marker. The live map holds 14 and prod has actually sent 16. The count was derived by grepping src/ for `type: "..."` next to a `link:` key, but notifications are created by edge functions and DB triggers, so the enumerator was reading a source that does not contain the data. Its own fallback text already said 'enumerate from notification_type_pref_map in the database' — the doc knew, and shipped 5 anyway. An inventory row that under-reports by 3x is worse than a missing row: it reads as covered. | main | 2 |
| N-004 |  | fixed | `notification_type_pref_map vs send-notification-email TYPE_MAP` | One notification type is mapped to a preference by THREE independent, disagreeing sources. Push uses the DB table notification_type_pref_map (14 rows). Email uses a hardcoded TypeScript TYPE_MAP in send-notification-email/index.ts:13-30 (13 entries). The producer notify_helpers_on_job_post hardcodes a third choice in SQL. They disagree: email handles info/success/warning (the exact three types the DB map omits — that omission IS the root cause of NT-001), and job_match maps to job_updates in the DB map, to email_new_offers in the email map, and is gated on new_offers by the producer. | lh-notifications | 1 |
| CC-003 |  | filed | `Post Job → Urgent bonus toggle copy` | BudgetSection.tsx:360 tells the poster the urgent bonus 'goes straight to the Helpr who takes it — no platform fee applied,' implying the Helpr receives 100% of it. In fact create-payment/_shared/helperEarnings.ts nets the bonus through netUrgentFeeDollars(), which deducts Stripe's 2.9% marginal processing cost before it reaches the helper (a $5 bonus nets ~$4.86, a $20 bonus nets ~$19.42) — same shape as ME-006's tip claim: 'no platform fee' is technically true (platform takes no cut) but 'goes straight to the Helpr' overclaims, since card processing still eats a slice. This is a money/legal-adjacent copy claim — propose wording, do not ship per standing constraint. | lh-copy-content | 3 |
| N-005 |  | fixed | `/profile?tab=notifications → the 7 category switches` | The preference UI shows a switch that controls nothing and hides the switches that matter. The screen renders 7 rows, but the maps use 10 pref columns. In the map but with NO switch: job_updates (types job_match/job_update/job_updates/expired = 579 of 1773 prod rows, the largest block), system_alerts (76), job_applications (90), payments (27) — 772 rows / 44% of all notifications are uncontrollable. In the UI but mapped to NO type: promotions — a Promotions switch that gates no push at all. | lh-notifications | 2 |
| N-006 |  | filed | `iOS app-icon badge vs in-app bell` | The iOS springboard badge counts only unread MESSAGES and never unread notifications, so the app icon and the in-app bell disagree by up to 108. useNavUnreadCount.ts:116 is the ONLY caller of setAppIconBadge in the codebase and derives its number from the messages table. Separately, fan_out_push_on_notification never sends a 'badge' value even though send-push-notification supports one (index.ts:93,161) — so once push is live an arriving notification will not increment the icon badge either. | lh-notifications | 1 |
| N-007 |  | filed | `notify_helpers_on_job_post (parish fan-out)` | The parish fan-out is unbounded and unthrottled, and can be driven into a storm by a post/cancel/repost loop. The function loops every approved, non-banned helper matching the parish with NO LIMIT and NO cooldown, and issues one net.http_post to send-notification-email PER RECIPIENT synchronously inside the job INSERT transaction. Its sibling notify_saved_searches_on_new_job has a 1-hour per-search throttle; this one has none, and the UPDATE trigger's WHEN clause re-arms on every transition back to open+funded, so repeatedly unfunding/refunding or cancelling/reposting a job re-alerts the entire parish each cycle. | lh-notifications | 1 |
| ED-002 |  | filed | `auth-email-hook (signup confirmation, password reset, magic link, email change, reauthentication)` | auth-email-hook has NO Slack ops alert on any of its failure branches (missing SEND_EMAIL_HOOK_SECRET, bad webhook signature, unknown email_action_type, or the enqueue_email RPC failing), while every comparable webhook in the repo (stripe-webhook, stripe-idv-webhook, verification-webhook, execute-dispute-split, instant-payout, send-push-notification, contact-support, subscription-reconciliation, create-payment) imports and calls postSlackOpsAlert on failure. auth-email-hook does not import _shared/slack-alerts.ts at all. A failed call IS loud to the one user mid-signup/reset (Supabase Auth surfaces the non-2xx as a client-visible error), but a SYSTEMIC failure -- e.g. SEND_EMAIL_HOOK_SECRET rotated/misconfigured, or enqueue_email failing repo-wide -- silently blocks EVERY signup confirmation and password reset in the app with zero ops visibility beyond console.error, which nobody is paged on. This is the auth channel: it would be discovered only when users start complaining that confirmation/reset emails never arrive. | lh-email-delivery | 7 |
| ST-002 |  | filed | `/profile?tab=availability → HelperAvailability editor; Dashboard browse feed 'match my availability'` | helper_availability accepts an INVERTED time range (start_time > end_time) with no guard at any layer, and live prod already contains one. There is no CHECK constraint on the table (only a PK), no validation in the editor (HelperAvailability.tsx updateSlot writes through unchecked), and none in TimeRangeField (it passes start/end straight to onChange). The consequence is silent and inverted from what the user sees: HelperAvailabilityDisplay renders the day as AVAILABLE with hours '9:00 PM - 5:00 PM', while the browse filter at useDashboardFilters.ts:276-278 keeps a job only when job.start_time >= slot.start_time AND <= slot.end_time — unsatisfiable for an inverted range — so EVERY job on that day is silently hidden from the helper's feed. A helper who mis-taps the wheel appears bookable all day and receives nothing. | lh-scheduling-time | 2 |
| TC-001#lh-test-ci | **YES** | fixed | `CI: e2e/happy-path/apply-single-sheet.spec.ts` | E2E happy-path smoke has been RED on every push to main since commit a46d6bdc (2026-09-02 12:29 PT) — 'applying stays on ONE top-anchored sheet' asserts JobDetailDialog sits at top-[7vh], but a46d6bdc intentionally re-centered it per owner request ('yes it was my call but I don't like it'). The spec was never updated, so a real design decision reads as a broken deploy on every CI run since. | lh-test-ci | 1 |
| ST-003 |  | fixed | `/profile?tab=availability → HelperAvailability.handleSave` | Saving weekly hours is a non-transactional DELETE-then-INSERT from the client, so a failure between the two statements leaves the helper with ZERO availability rows — their whole published schedule wiped — while the toast only says the save failed. Both statements are individually guarded with unwrapMutation (good), but they are two separate round trips with no transaction and no restore-on-failure: the delete removes all 7 weekly rows, and if the insert then fails (network drop, tab close, RLS refusal, app backgrounded on native) the catch fires an error toast and the rows are gone. HelperAvailabilityDisplay then renders 'Availability not set' on the helper's public profile, and useDashboardFilters' matchAvailability path sees helperAvailability.length === 0 and stops filtering entirely. The correct shape is one RPC that deletes and inserts in a single transaction. | lh-scheduling-time | 3 |
| TC-003 |  | fixed | `JobDetailDialog.tsx apply flow (dashboard job-detail sheet, apply step)` | Fixing the stale E2E assertion (TC-002) uncovered a REAL, live regression: the job-detail sheet's top edge moves 66px between the detail step and the apply step, reproducing exactly the 'opens small then gets bigger' bug the owner complained about pre-2026-08-31. JobDetailDialog.tsx:331-333 claims 'the panel now reserves its poster row and note field from the first frame (see min-h below)' but there is NO min-h class anywhere in the file (grep confirms) -- the comment describes a fix that is not actually present in the code, likely dropped when a46d6bdc removed the top-[7vh] anchor that used to hide this symptom. | lh-test-ci | 1 |
| SC-005 |  | filed | `/gift-card · gift_cards · create-gift-card-checkout / claim-gift-card — never once executed in production` | The Gift Card feature (formerly gift card; table and functions deliberately keep the gift_card_ names) has NEVER run end to end in production, and the three rows that exist are hand-seeded rather than evidence that it works. Every one of the 3 gift_cards rows carries a sequential hand-written UUID (a5eedca0-0000-4000-8000-00000000000{1,2,3}), NULL stripe_session_id, NULL stripe_payment_intent_id, NULL recipient_email and NULL claim_token — so no row was created by create-gift-card-checkout (which is the only writer that sets a Stripe session) and no row was ever claimed through claim-gift-card (the only writer that sets claim_token). One row is worse than merely unexercised: it reads status='redeemed' with job_id NULL, a combination redeem_gift_card CANNOT produce — that RPC sets job_id and redeemed_at in the same UPDATE that writes status='redeemed'. So the seed data asserts a state the code has no path to, and any surface or reconciliation that reasons 'redeemed implies a job' is wrong on it. This is a readiness statement, not a defect in the logic: I audited redeem_gift_card against live prod and it is genuinely well built (FOR UPDATE locks in a stable job-then-credit order, ownership check, funded check, expiry check, state gate, leftover split into a fresh gift, so double-redeem and concurrent-redeem are both impossible). The risk is that a money path with real Stripe checkout behind it is going to launch having never processed a single real donation, claim or redemption. | lh-subscriptions-credits | 6 |
| ST-005 |  | fixed | `/my-posts → PostedJobActions No-Show control (src/lib/dateUtils.ts jobStartDateTime/hasJobStarted)` | The client and the server disagree about which timezone a job's scheduled start is in, and the disagreement gates a trust/safety control. Server-side, date_needed + start_time is always resolved in America/Chicago (job_expires_at_for_schedule uses AT TIME ZONE 'America/Chicago'; helper_cancel_booking computes v_starts_at the same way). Client-side, dateUtils.jobStartDateTime builds the instant with parseLocalDate + setHours, i.e. in the DEVICE's zone — its own docstring states the intent as 'A 9:00 AM job is 9:00 AM where the user is standing.' The two agree only while the viewer's device is in Central. hasJobStarted() is the sole gate on the No-Show report at PostedJobActions.tsx:489-495, so for a poster whose device is west of CT the No-Show control stays HIDDEN for hours after the job really started, and for one east of CT it appears EARLY — which is the exact behaviour the owner's rule quoted in that comment forbids ('A 9:00 AM job must not offer No-Show at 8:00 AM'). | lh-scheduling-time | 2 |
| PD-001 |  | filed | `CI: bundle-size.yml / vite.config.ts build.modulePreload` | bundle-size.yml's per-chunk (400KB) and total (1500KB) budget checks measure ONLY the entry chunk (38KB) against a true critical-path JS payload of ~913KB raw / 292KB gzip across 75 chunks, because vite.config.ts sets build.modulePreload.resolveDependencies to an empty-array callback for EVERY module, not just dynamic-import branches as the adjacent comment claims — so dist/index.html ships zero <link rel=modulepreload> tags at all, and the CI script's 'grep assets/*.js from index.html' finds only the single <script> tag. | lh-perf-deps | 1 |
| SW-001 |  | verified | `every public route except /jobs/:id, /signup?ref=, /user/:userId` | Server-rendered (non-JS) title/description/canonical/OG are IDENTICAL homepage boilerplate on every route except the 3 vercel.json share rewrites. A non-JS crawler or link-unfurler hitting /jobs, /browse, /legal, /legal?tab=privacy, /legal?tab=community, /help, /support, /login, /signup (no ref) all get the exact same 28,728-byte document as '/', including a canonical href of https://www.louisianahelpr.com on every one of them. usePageMeta only updates tags via a useEffect after JS runs, so any consumer that does not execute JS never sees the route-specific values it sets. | lh-seo-web | 3 |
| PD-002 |  | filed | `/profile?tab=posted_jobs and ?tab=applied_jobs (Activity → PostedJobsTab/AppliedJobsTab via ActivitySectionedView)` | The poster's posted-jobs feed and helper's applied-jobs feed are fetched with an UNBOUNDED query (no .limit()/.range()) and rendered with a plain .map() with NO virtualization in ActivitySectionedView.tsx, unlike the Dashboard browse feed (VirtualizedJobList, @tanstack/react-virtual) and the /browse route + message thread list (both use VirtualList). Currently low-impact (prod max is 16 jobs for one customer_id) but the query has no cap, so it is unbounded growth risk rather than a currently-visible bug. | lh-perf-deps | 3 |
| SW-003 |  | verified | `robots.txt + every ProtectedRoute/AdminRoute/account-state path` | robots.txt is 'Allow: /' with zero Disallow rules, and every authenticated/admin/account-state route (e.g. /admin, /dashboard, /profile, /my-jobs, and any nonexistent path) returns HTTP 200 with the SPA shell — the ProtectedRoute redirect and NotFound's noindex tag are both client-side only (useEffect / React Router), so a non-JS crawler sees a real 200 page, not a redirect or a 404. This is a soft-404/soft-privatepage problem: Google Search Console will report these as indexable 200s with content identical to the homepage, and nothing in the server response tells any crawler to skip them. | lh-seo-web | 3 |
| BR-003 |  | fixed | `production web (www.louisianahelpr.com) — /assets/*.js.map` | The production website serves complete, un-stripped sourcemaps publicly. 376 .map files totalling 22MB ship alongside 6.2MB of JS/CSS, and any anonymous visitor can download the app's full original TypeScript source — including the code comments that explain security and money logic. The iOS half of this is handled (scripts/strip-ios-sourcemaps.mjs prunes maps from the .ipa) but nothing strips them from the Vercel deployment. vite.config.ts:398 sets sourcemap:'hidden', which only suppresses the //# sourceMappingURL comment; it does not stop the .map being emitted into dist/ and served at a trivially guessable URL. | lh-build-release | 4 |
| SC-006 |  | fixed | `referral_credits · enforce_referral_cap() vs the $10 subscription_bonus` | The 5-credit anti-fraud cap does not bound the LARGEST referral credit the platform mints. enforce_referral_cap() counts only reason IN ('referrer_bonus','first_job_bonus') — both worth $5 — so the $10 'subscription_bonus' minted by check-pro-subscription is completely outside the ceiling. The only control on it is the unique index referral_credits_one_per_reason (user_id, referral_code_id, referred_user_id, reason), which caps it at ONE PER REFERRED PERSON, not per referrer. The loop this leaves open: account A refers account B (self-referral is genuinely blocked at two levels, so B must be a second account), B buys the ONE-TIME Pro pass, A receives $10 in cashable credit. The one-time Pro Price is live at exactly $10.00 (unit_amount 1000, price_1TAZkeKp2H4b7tECnfZ7vF0C, livemode), so the attacker is at break-even on cash while ALSO receiving 30 days of real Pro entitlements on B — a free monthly Job Boost ($3), the 10% commission rate and 10-minute early access. The PLATFORM is net negative on every iteration: it pays out $10, receives $10 less roughly $0.59 of Stripe processing, and grants the perks. It is not runaway — each loop needs a fresh account and a fresh real $10 card charge — but it is uncapped and it scales linearly with accounts, which is precisely what the 5-cap exists to prevent for the $5 credits. Credits are cashable to a Stripe Connect account via cash-out-credits, so this is real money out, not in-app scrip. | lh-subscriptions-credits | 4 |
| TC-004 |  | verified | `e2e/happy-path/ critical-journey coverage` | Two critical journeys from the launch checklist have ZERO E2E coverage anywhere in the Playwright suite: REFUND and ACCOUNT DELETION. Grep for refund/dispute-split/deletion terms across all 22 e2e/happy-path specs + e2e/visual-audit (2 specs) returns no matches for 'refund' and none for account-deletion flows. Accept-a-bid, ban-enforcement and profile-completion are each touched by exactly one spec incidentally (not a dedicated driven flow), which is thinner than the rest of the loop (post-job, browse, apply, message, earnings are each covered by 2+ dedicated specs). Escrow/payment IS well covered (10 specs touch it) and money/authz UNIT coverage is solid at the edge-function level: src/test/edge/ has auto-release-payment.test.ts, auto-resolve-disputes.test.ts, execute-dispute-split.test.ts, release-payout.test.ts, stripe-webhook.test.ts, verification-webhook.test.ts, and subscriptionTiers.test.ts / mutationRowGuard.test.ts / migrationVersions.test.ts all exist as required -- so this is specifically an E2E (UI-driven) gap, not an untested-logic gap. A dispute SPLIT can be unit-verified correct while the UI flow that fires it (DisputeDialog -> rpc_open_dispute -> resolution) is never driven end to end, and an account-deletion request could regress its UI entirely with CI staying green. | lh-test-ci | 2 |
| CS-001 |  | filed | `Sentry Session Replay / /legal?tab=privacy` | Sentry Session Replay records 10% of ALL production sessions (and 100% of sessions with an error) with blockAllMedia:false, so on-screen images are captured; no legal page discloses session recording or screen capture anywhere | lh-compliance-store | 3 |
| DR-005 |  | fixed | `Restore rehearsal — documented procedure and practice environment` | No restore procedure exists and no restore has ever been rehearsed, and there is nowhere to rehearse one. A repo-wide search finds no disaster-recovery runbook, no pg_dump/pg_restore invocation, and no scheduled logical export anywhere in docs/, scripts/ or .github/workflows/ — the only hits for 'restore' are incidental prose plus SI-010's passing mention. docs/qa/CRASH_RECOVERY.md is an APP force-quit test plan, not a database one. Staging cannot serve as the rehearsal environment: per SI-007 it is 65 migrations behind prod and still holds nine tables deliberately dropped from prod, so a restore rehearsed there would not exercise prod's schema — and staging itself reports the identical zero-backup payload, so it has nothing to restore from either. An untested backup is a hypothesis; here there is neither the backup (DR-001) nor the test nor the venue for one. | lh-data-recovery | 4 |
| CS-002 |  | filed | `/legal?tab=privacy — processor disclosure` | Privacy policy omits two real data processors: Apple MapKit JS (receives typed address text and user lat/lng for autocomplete, geocoding and map tiles) and Resend (receives every user email address plus notification body content) | lh-compliance-store | 3 |
| CS-003 |  | filed | `Account deletion — helper_w9_records` | helper_w9_records survives account deletion untouched: it has NO foreign key to auth.users and purge_user_data() never references it, so a deleted helper's typed legal-name e-signature and signing IP address persist indefinitely. Currently 0 rows in prod, so there is no live exposure today, but the path is live. | lh-compliance-store | 1 |
| DR-006 |  | filed | `Money-record divergence — what would be WRONG after a restore to yesterday` | If the database were rolled back, some money records could be rebuilt from Stripe and some could not, and the platform has no way to notice the difference. RECONSTRUCTIBLE (carry a Stripe id): jobs.stripe_payment_intent_id/stripe_session_id, tips.stripe_payment_intent_id, payout_transfers.stripe_transfer_id/stripe_account_id, gift_cards.stripe_payment_intent_id. NOT RECONSTRUCTIBLE: referral_credits has NO Stripe column of any kind — it is pure platform currency with zero external record, so a rolled-back referral credit is simply gone or duplicated with nothing to reconcile against. Dispute OUTCOMES are likewise Postgres-only: disputes carries only execution_transfer_id, so Stripe records that a transfer happened but not who won, who decided it, or why — a restore to yesterday would silently reopen settled disputes with the money already moved. Job escrow STATE (28 rows currently payment_status='escrow', 11 'released') diverges the same way: Stripe would still show the release, the restored row would not. The reconciliation path that would catch this is money-reconciliation, already filed by another lane as having never completed a run — I confirm it is scheduled '20 8 * * *' and has only 8 job_run_details rows ever, last 2026-09-02 08:20. | lh-data-recovery | 1 |
| ST-007 |  | filed | `?tab=str_settings → str-ical-sync dedup ordering` | The STR sync creates the job BEFORE writing the dedup guard, so two overlapping syncs of one connection produce two cleaning jobs for one guest checkout. The guard table str_processed_events does carry a UNIQUE index on (connection_id, event_uid) — confirmed on prod — but the code checks it with a non-locking .maybeSingle() read at index.ts:186-194, then inserts the JOB at :204-224, and only then inserts the guard row at :232-236. Two concurrent runs both pass the read, both create a job, and only the second one's guard insert trips the unique index; that failure is recorded as a defect but the duplicate job is already committed and is not rolled back. The race is reachable because the feature has two independent callers of the same connection: the every-6-hours cron (jobid 57, '44 */6 * * *') and the host's own 'Sync now' button, which passes a user JWT for a single connection. Correct order is guard-row first, letting the unique index reject the duplicate before any job exists. | lh-scheduling-time | 1 |
| CS-004 |  | filed | `ios/App/App/PrivacyInfo.xcprivacy` | Privacy manifest declares ProductInteraction and CrashData as NSPrivacyCollectedDataTypeLinked=false, but the app links BOTH to identity: PostHog identify() and Sentry setUser() each send the Supabase user id together with the user's email address | lh-compliance-store | 4 |
| BR-004 |  | fixed | `npm run dev:staging / vite --mode staging` | There is no client-side staging environment at all: 'npm run dev:staging' runs against PRODUCTION. The script is 'vite --mode staging', which makes Vite look for .env.staging -- a file that does not exist in the repo and is not mentioned in .env.example -- so resolution falls through to plain .env, which holds the prod project. A staging Supabase project does exist (okpxtpfvwtmbuxugqsws) and supabase/.temp/project-ref points the CLI at it, so the DATABASE tooling is on staging while any build a developer makes is on prod. That split is the trap: 'I am on staging' can be true of the CLI and false of the app in the same terminal. Mitigation that does exist: src/integrations/supabase/client.ts:17-23 emits a DEV console.warn when a dev build points at prod, so it is not completely silent -- but it is a DevTools warning against a script whose name asserts the opposite. | lh-build-release | 4 |
| CS-007 |  | filed | `supabase/functions/send-marketing-blast — CAN-SPAM` | Commercial email ships without the physical postal address CAN-SPAM 15 USC 7704(a)(5) requires. This is an explicitly owner-accepted risk (2026-08-31), not an oversight. Current exposure is ZERO: 0 of 39 prod profiles have marketing_consent=true, so no recipient can currently be selected. Remediation is one env var. | lh-compliance-store | 2 |
| ST-008 |  | filed | `jobs.start_time NULL on a non-flexible job → auto_start_due_jobs / sweep_dayof_confirm_reminders / sweep_job_start_reminders / job_expires_at_for_schedule / cancellation ladder` | A non-flexible job with a NULL start_time is an unconstrained state that exists in prod, and the platform holds FOUR mutually inconsistent opinions about when such a job starts — spanning almost 24 hours. Nothing prevents the state: there is no CHECK on public.jobs tying start_time to is_flexible_schedule, and prod holds 3 rows with is_flexible_schedule=false AND start_time IS NULL. Each consumer picks its own default: auto_start_due_jobs COALESCEs to '00:00' and so flips the job to in_progress at local midnight; sweep_dayof_confirm_reminders COALESCEs to '09:00'; job_expires_at_for_schedule COALESCEs to '23:59:59'; job_hours_until_start (the cancellation-fee ladder) ignores start_time entirely, i.e. midnight; and sweep_job_start_reminders requires start_time IS NOT NULL, so the 'starts in about 30 minutes' notification NEVER fires for these jobs for either party. One canonical helper should answer 'when does this job start', the way jobDate.ts already did for 'which day is this job on'. | lh-scheduling-time | 10 |
| VC-004 |  | fixed | `supabase/functions/verification-webhook (deployed)` | verification-webhook is unreachable by every vendor that is supposed to call it. It has NO verify_jwt=false entry in supabase/config.toml, so the gateway default (true) applies and every callback is rejected with 401 UNAUTHORIZED_NO_AUTH_HEADER before the handler runs. Checkr, Certificial and Stripe Identity do not send a Supabase JWT. The function's own signature verification is genuinely well built - fail-closed per vendor, constant-time HMAC compare, content-hash dedupe, idempotency rollback - and none of it has ever executed in prod. This is fail-closed and harmless TODAY (no screening vendor is live and create-bgc-payment is hard-disabled at BGC_PURCHASE_ENABLED=false), but it is a latent trap: the day a vendor is switched on, every callback 401s at the gateway, verification_checks never updates, sync_credential_from_check never fires, and helpers who paid for a background check sit at 'in progress' forever with no result and no refund - the exact outcome create-bgc-payment's own header comment says it is guarding against. This repo has the identical bug on record: health-check's missing declaration made its CI smoke test fail every run from 2026-04-26 (config.toml:108-119). Sibling stripe-idv-webhook IS declared verify_jwt=false (config.toml:70-71) and works, so the Stripe Identity path is unaffected. | lh-verification-credentials | 1 |
| OBS-002 |  | fixed | `src/main.tsx:212-219 identify()/setSentryUser() -> raw email to PostHog + Sentry` | On every login/session-restore, main.tsx calls identifyUser(userId, { email: session.user.email }) (PostHog) and setSentryUser({ id, email }) (Sentry) with the real user email, and both src/lib/posthog.ts identifyUser() and src/lib/sentry.ts setSentryUser() forward whatever they are given -- zero redaction, zero hashing, no allowlist of properties. Contrast with src/lib/errorLogger.ts, which explicitly redacts bearer tokens/JWTs/one-time codes before anything reaches error_logs/Sentry/PostHog -- the identify() path has no equivalent guard. PrivacySection.tsx:108-115 discloses PostHog and Sentry as vendors and frames it as 'each only receives the data it needs for its function' -- product analytics does not need email to identify a user, a stable id does; Sentry's use is more defensible (support/correlation) but still ships PII to a third party by default with no opt-out. This is exactly the check the observability brief called out by name ('check the identify call and every event property'). FIX: drop email from the PostHog identify() call entirely (userId alone is sufficient to stitch events); for Sentry, either keep email (arguably legitimate for crash-triage correlation) or switch to a non-reversible identifier if the team wants to tighten it further -- flagging both for a product/privacy decision rather than fixing unilaterally since it touches what support workflows expect to see in Sentry. | lh-observability | 5 |
| VC-005 |  | filed | `/signup -> profiles.approval_status; /admin?view=people approval queue` | RESOLVES RW-005 (co-owned with lh-account-lifecycle): the 'approved on a fresh account' behaviour is INTENTIONAL, not an approval-gate bypass. supabase/functions/complete-signup/index.ts:465-470 sets approval_status:'approved' unconditionally once signup validation passes, with the comment 'Auto-approve - there is no manual admin review step anymore.' handle_new_user() inserts the profile with no approval_status, so the column default 'pending' is only ever the pre-completion state, and the profiles INSERT policy hard-pins approval_status='pending' in its WITH CHECK so it cannot be self-forged. The finding that remains is the contradiction, not a hole: (a) the column default 'pending' and (b) an entire surviving admin approval queue - the pending/approved/denied tabs in useAdminUsersFilter.ts:47-48, the Approve action in useAdminUserActions.ts:38, DenyUserDialog.tsx:61, the pending badge in adminUserHelpers.tsx:19-21 - both advertise a review step that no longer gates anything. ProtectedRoute.tsx:334 still bounces approval_status='pending' to /account-pending, a state now only reachable by abandoning signup midway. An operator reading the admin queue would reasonably believe they are gating who gets on the platform; they are not. | lh-verification-credentials | 1 |
| ST-009 |  | fixed | `Direct offer expiry → expire_pending_direct_offers poster notification` | When a direct offer expires the poster is almost never told, because the notification window is 5 minutes wide but the sweep that opens it runs once an hour. expire_pending_direct_offers flips direct_offer_status to 'expired' in a CTE, then — instead of notifying from that CTE's RETURNING rows — re-queries public.jobs with 'direct_offer_expires_at > now() - interval 5 minutes'. Its only caller is the auto-expire-jobs edge function, on cron jobid 16 schedule '0 * * * *'. So of the 60 minutes in which an offer can lapse, only the last 5 before a run land inside the notify window: roughly 55 of every 60 expirations set the status and send NOTHING. The poster is left waiting on an offer the platform has already killed, with no signal that it is now open to everyone. The fix is to notify from the CTE that did the update rather than re-deriving the set from a time window — the RETURNING clause already selects id, customer_id, offered_to_helper_id and title, and is discarded. | lh-scheduling-time | 1 |
| VC-006 |  | filed | `helper_w9_records -> purge_user_data() / account deletion` | A deleted helper's typed legal signature and IP address survive account deletion forever, fully attributable. helper_w9_records holds typed_signature (the person's full legal name, typed as a signature) and ip (a literal IP address), keyed by a bare helper_id uuid. Its ONLY foreign key is helper_w9_records_job_id_fkey -> jobs(id) ON DELETE SET NULL; there is NO FK on helper_id, so nothing cascades when the auth user is deleted. purge_user_data() does not touch the table - it is absent from all 28 counters the function returns. This is the exact class that function's own header documents finding by seeding rows rather than by census, and it is the same PII shape as legal_acceptances, which purge_user_data explicitly anonymises by nulling user_id, ip_address and user_agent. The table currently has 0 rows only because no W-9-requiring job has completed yet; the writer is live at src/components/W9CollectionDialog.tsx:63, reached from Activity.tsx:713-720 when a poster sets requires_w9. NOTE FOR THE OWNER: a W-9 is a tax record and there is a legitimate IRS-retention argument for keeping the row. If that is the intent, the fix is still not 'do nothing' - it is to null  (which no tax rule needs) and mark the retention deliberately, the way purge_user_data does for payout_transfers via helper_redacted_at. Right now the survival is accidental, not chosen. | lh-verification-credentials | 1 |
| VC-008 |  | filed | `public.claim_idv_attempt (2 overloads) -> stripe-idv-start` | claim_idv_attempt exists TWICE in prod and any call that does not name p_skip_fee_gate fails outright with 42725 'function is not unique'. Migration 20260829031405 added claim_idv_attempt(uuid,integer,boolean) but the superseded claim_idv_attempt(uuid,integer) was never dropped, and because BOTH have defaults on p_max_attempts, a 1-arg or 2-arg call matches both candidates and Postgres refuses to resolve it. The live path survives only by accident of style: stripe-idv-start/index.ts:127-130 calls it with NAMED arguments {p_user_id, p_skip_fee_gate}, and only the 3-arg overload has that parameter, so PostgREST resolves it uniquely. That is a coincidence, not a guard. The blast radius if anyone ever simplifies that call is total and quiet: stripe-idv-start's claimErr branch at :131-135 fails CLOSED and returns a generic 503 'We could not start verification just now', so identity verification would stop working for every user on the platform while the logs show only a 503. The old overload is dead code on the identity path and dropping it is a one-line replay-safe migration. | lh-verification-credentials | 6 |
| CC-002#lh-concurrency-cache |  | filed | `/post-job — entry:draft (src/hooks/useDraftJob.ts)` | The post-a-job draft is one unscoped localStorage key ('helpr_draft_job') with no cross-tab coordination, no 'storage' event listener and no merge, producing two user-visible defects. (a) SILENT CLOBBER: two tabs on /post-job each autosave the WHOLE draft object on a 5s debounce, so the later writer overwrites the earlier tab's work entirely. The losing tab keeps showing the user's own text on screen, so nothing signals the loss until they reload and find the other tab's job. (b) DRAFT RESURRECTION AFTER A SUCCESSFUL POST — the sharper one. clearDraft() (useDraftJob.ts:141-150) removes the key and resets THIS tab's pendingDraft ref, but a second tab still holds the same content in its own pendingDraft.current, and its visibilitychange->hidden / beforeunload / unmount handler (useDraftJob.ts:104-118) calls flushDraft(), which writes it straight back. So the user pays for and posts a job in tab A, backgrounds tab B, and /post-job then offers 'Pick up your draft' for the job that is already live — an invitation to post and pay for it twice. On mobile visibilitychange->hidden fires on every app-background, so this is the common path, not an edge case. Co-owned with lh-input-boundary per the lane brief. | lh-concurrency-cache | 3 |
| A-001 |  | fixed | `email → BrandButton CTA (all transactional mail)` | Email CTA href and label are interpolated RAW into an HTML attribute inside dangerouslySetInnerHTML (components.tsx:85,91). sanitizeSameOriginLink (safe-strings.ts:32-43) blocks schemes/whitespace/backslash but NOT double-quote or angle brackets, so a slash-separated payload escapes the href attribute and injects an element with a live event handler into another user's Helpr-branded email. Reachable by any authenticated user who has merely APPLIED to the victim's job (create-notification/index.ts:100-153 permits notifying a job counterparty or applicant), whose caller-supplied link is forwarded to send-notification-email (index.ts:68). Impact is capped by mail-client sanitizers, which strip onload/onerror - what survives is injected phishing anchors and remote tracking images inside a genuine transactional email, not script execution. | lh-appsec | 2 |
| A-002 |  | filed | `/post-job → AI Job Builder (ai-job-builder edge function)` | ai-job-builder/index.ts spreads the client-supplied 'messages' array VERBATIM into the Gemini call (…messages) with Array.isArray() as the ONLY validation - element roles and content are unchecked, so an authenticated caller can send arbitrary system/assistant/user turns and use the endpoint as a general-purpose LLM proxy billed to the platform's GEMINI_API_KEY. Separately the model's tool-call output is returned unvalidated (JSON.parse(toolCall.function.arguments) returned verbatim) and the client applies it with a bare cast - AiJobBuilder.tsx:73 onGenerated(data as AiGeneratedJob) → useJobEntry.ts:88-99 setCategory/setBudget/setHelpersNeeded - so the declared enum and additionalProperties:false in the tool schema are never enforced anywhere on the path into the priced form. | lh-appsec | 3 |
| A-003 |  | fixed | `storage: job-photos bucket (PUBLIC)` | Verified against LIVE prod: storage.buckets shows job-photos public=true with file_size_limit=NULL and allowed_mime_types=NULL. Every MIME and size gate on the ~13 client upload sites is therefore client-side only (the picker's accept= is a hint a direct storage API call ignores), and storage.objects.metadata->>'mimetype' is populated per-object from the client's content-type. An authenticated user can therefore store arbitrary attacker-controlled content - including text/html - in a PUBLIC, anonymously fetchable bucket served from the Supabase project origin. Not app-origin XSS (different origin), but a durable phishing/malware host on a domain associated with the app. proof-photos, id-documents, user-documents and application-attachments are private but ALSO have NULL size limits (unbounded upload). | lh-appsec | 2 |
| AM-002 |  | filed | `supabase/functions/stripe-webhook/handlers/chargeDisputeCreated.ts / chargeDisputeClosed.ts → /admin` | Chargeback evidence is never collected or submitted to Stripe. The entire strategy is 'alert ops, a human responds in the Stripe Dashboard before the evidence due date'. Three things weaken that: (a) dispute.evidence_details.due_by is rendered into a Slack field and stored NOWHERE, so no query, queue or reminder can ever surface an approaching deadline; (b) there is no chargeback view in /admin — VIEW_LABELS has 24 entries and none of them is chargebacks, and the admin notification links to bare '/admin'; (c) the admin in-app notification is a bare 'await supabase.from("notifications").insert({...})' with the result discarded (chargeDisputeCreated.ts:139, chargeDisputeClosed.ts:151 and :207) — a dropped insert is silent, which is the exact pattern admin-user-actions/index.ts:86-96 was already fixed for. If the Slack webhook is also down, a chargeback is filed and nobody is told at all. | lh-admin-moderation | 2 |
| A-006 |  | filed | `served CSP → script-src` | The served CSP uses script-src 'self' 'unsafe-inline' with no nonce or hash strategy, which removes CSP's ability to mitigate HTML injection: any future injection sink becomes directly executable, and it is what makes a javascript: URL evaluable at all. This is posture/defense-in-depth, stated honestly - I found NO reachable HTML-injection sink in the SPA to pair it with (every UGC render is a React text node; the only client innerHTML, mapMarkers.ts:170, interpolates solely internal category colors and CSS token values, and the job title reaches it via setAttribute on the accessible name, not markup). The three JSON-LD dangerouslySetInnerHTML blocks in Index.tsx:149-157 stringify module-level constants with no user input. | lh-appsec | 3 |
| AM-003 |  | fixed | `public.purge_user_data() → user_violations / user_strikes / user_bans / helper_shadowbans` | The moderation ladder tables survive account deletion with the departed user's id attached, and user_violations carries their VERBATIM message text. purge_user_data() handles 27 tables (jobs, messages, notifications, push_tokens, referrals, legal_acceptances, reports, job_tracking, favorite_helpers, ...) but touches none of user_violations, user_strikes, user_bans or helper_shadowbans, and none of the four has a FK to auth.users, so nothing cascades either. apply_message_violation_consequence() writes description = '<reason> \| Message: "<first 300 chars of the user's message>"' — so a right-to-be-forgotten deletion leaves the user's own quoted words in a table nothing anonymises. Contrast: reports.reporter_id IS anonymised at 4k and legal_acceptances has user_id/ip_address/user_agent nulled, so the retention policy elsewhere in this same function is 'keep the row, drop the identity'. These four just have no policy at all. | lh-admin-moderation | 1 |
| AM-006 |  | fixed | `public.auto_restrict_repeat_violators() — trigger on user_violations` | The automatic suspension ladder fails silently. Its whole body is wrapped in 'EXCEPTION WHEN OTHERS THEN RAISE NOTICE ...' with no log_cron_defect call, no admin notification and no re-raise. So if the profiles UPDATE that sets ban_status='temp_banned' + auto_suspended_until is refused or errors, the user_violations INSERT still commits, the trigger returns NEW, and a repeat violator who should now be suspended for 7 or 30 days stays fully active with nothing anywhere recording that the suspension was skipped. The asymmetry is the tell: its sibling sweep_expired_auto_bans() — which does the reverse, LIFTING suspensions — calls public.log_cron_defect() in both of its exception handlers. Enforcement fails quieter than release. | lh-admin-moderation | 2 |
| AL-003 |  | fixed | `/admin?view=people — user detail panel (reviews)` | One deleted reviewer silently degrades the entire admin user-detail review panel. useOpenProfile.ts:89 adds r.reviewer_id to relatedUserIds with no null guard — the null guard IS applied to r.job_id on the same line — and reviews.reviewer_id is SET NULL on deletion. The resulting .in('user_id', [...null]) returns HTTP 400; the error is only console.error'd at :101 so nameMap stays empty and EVERY reviewer name (:108) AND every reviewee name (:115) collapses to the literal 'User' and every job_title disappears. An admin reads that as data about the users rather than as a failed fetch. | lh-account-lifecycle | 5 |
| OA-004 |  | verified | `native session storage — src/integrations/supabase/keychainStorageAdapter.ts` | The Supabase session is NOT in the iOS Keychain, despite the file, the export and the call site all being named 'keychainStorageAdapter'. It is in NSUserDefaults. client.ts:36 reads 'storage: Capacitor.isNativePlatform() ? keychainStorageAdapter : localStorage', which reads as 'Keychain on device, localStorage on web'. The adapter actually writes through @capacitor/preferences, and that plugin's own iOS source is unambiguous: Preferences.swift:19-20 'private var defaults: UserDefaults { return UserDefaults.standard }', with get/set/remove going straight to it (:39-48). So the access token AND the long-lived refresh token are written to (a) localStorage, readable by any script that executes in the WKWebView, and (b) an unencrypted plist in the app container, which is included in unencrypted iTunes/Finder and iCloud backups and is NOT protected by device-passcode-gated Keychain accessibility. The adapter's own header comment says 'Mirror auth-token writes to NSUserDefaults' and cites 'NSUserDefaults survives Offload App + reinstall' as a FEATURE — that same property means uninstalling the app does not clear the refresh token. I am reporting the naming as the primary defect rather than the storage choice: localStorage is Supabase's documented default and the mirror exists for a real, well-documented reason (WebKit evicting localStorage under memory pressure). The problem is that the name asserts a security property the code does not have, which is precisely what stops anyone noticing. Scope note, stated rather than assumed: I inventoried what is stored and found the session tokens only — no card, bank or government-ID material is written to either store by this adapter. | lh-onboarding-auth | 5 |
| OA-005 |  | fixed | `/reset-password` | The reset screen enforces 3 of the 5 password rules the signup screen enforces, and its hint copy understates the policy — reviving a bug class this repo has already fixed once. ResetPassword.tsx:66 computes passwordValid as 'password.length >= 8 && /[A-Z]/ && /[0-9]/' and the submit guard repeats exactly those three (:106-111). Signup.tsx:228-233 checks FIVE — length, lowercase, uppercase, digit AND symbol — and its inline comment records why: the project's Supabase password policy requires one of each of lowercase, uppercase, digit and symbol, so a password missing any of them came back as a 422 whose raw body was thrown into a toast. That is the exact failure the reset screen is still open to. The hint under the field (:199) reads 'At least 8 characters, 1 uppercase, 1 number', so the UI actively tells the user a policy that is narrower than the one the server enforces. On rejection the error goes to friendlyAuthError (:116), which has no password-policy case, so it falls through to its default 'Couldn't sign you in — give it another try?' (authErrors.ts:23) — wrong copy in this context, since the user is resetting a password, not signing in. Two screens in one app set the same credential against the same policy and disagree about what the policy is. | lh-onboarding-auth | 4 |
| OA-006 |  | filed | `/reset-password → success panel` | The reset screen makes a security promise that nothing in this codebase keeps, and the recovery token is left in the URL. (a) ResetPassword.tsx:174 tells the user 'Anywhere else you were signed in will ask for the new password next time.' No code in src/ or supabase/functions/ revokes other sessions on a password change: a repo-wide grep for signOut scope turns up exactly one global sign-out, the user-initiated 'sign out everywhere' button at SecurityTab.tsx:189, which this flow never calls. Whether the promise happens to hold therefore depends entirely on GoTrue's own session-revocation-on-password-update behaviour, which is project configuration this codebase neither sets nor checks. Stating it as a guarantee to the user is a promise the app does not control. (b) The recovery token is never stripped from the address bar: there is no history.replaceState anywhere in ResetPassword.tsx (grep count 0), so the type=recovery hash survives in the URL and in browser history after the reset completes. | lh-onboarding-auth | 2 |
| AL-006 |  | filed | `profiles.anonymized_at / get_safe_profiles / /user/:id` | The purge writes profiles.anonymized_at as its idempotency guard and NOTHING ever reads it — grep over src/ returns 3 hits, all in generated types (src/integrations/supabase/types.ts:2749, :2848, :2947). There is no RLS policy, view, RPC, hook or component that branches on it. Confirmed at the database level too: get_safe_profiles does not filter or expose it, so anonymised profiles are returned as ordinary profiles everywhere. The visible consequence is /user/:id — UserProfile.tsx renders a full live profile page for a deleted person: name 'A neighbor' (:130), initials '?' (:360-367), gradient avatar, 'Since May 2026' still shown, their received reviews still listed, and the Save-as-preferred-Helpr, Report and Block actions all still live (:463). It is reachable from ChatView.tsx:312, deepLinkRoute.ts:123, ApplicantsPanel.tsx:351/383 and SavedHelperCard.tsx:83. This single gap is also why no surface anywhere can distinguish 'deleted account' from 'name lookup failed' — see AL-009. | lh-account-lifecycle | 3 |
| AL-007 |  | filed | `Disputes — the surviving party is locked out when the opener deletes` | disputes.opener_id is ON DELETE SET NULL. The live RLS UPDATE policy is 'disputes opener update while open' USING ((SELECT auth.uid()) = opener_id AND status = 'open') — with opener_id NULL that predicate is NULL for every caller, so the row becomes permanently un-updatable by any non-admin. The UI arrives at the same place independently: DisputeTimelineDialog.tsx:249 computes openerId = dispute?.opener_id ?? legacy?.disputed_by ?? null, :258 isOpener = openerId === userId (false), and :270-273 sets canAddEvidence=false / blockedFromEvidence=true. The surviving counterparty is told the other side filed this and they cannot respond, on a dispute that now nobody can act on, while escrow is held. | lh-account-lifecycle | 3 |
| OA-007 |  | filed | `/signup → profiles.approval_status (RESOLVES RW-005) + /account-pending copy` | RW-005 RESOLVED: a fresh signup arriving as approval_status='approved' is INTENTIONAL, not an approval-queue bypass. complete-signup sets approval_status:'approved' unconditionally on every success path (index.ts:464-465) under an explicit design comment: 'Auto-approve — there is no manual admin review step anymore... Identity verification + payout setup happen later as separate Stripe-gated steps.' The column default 'pending' and the RLS INSERT policy (profiles 'Users can insert their own profile', WITH CHECK includes approval_status='pending') are the trigger-inserted starting state; the edge function is what flips it. So the gate is real and it is auto-approve by design. BUT the more useful statement is the one RW-005 and OA-001 only make together, and it is bigger than either: APPROVAL IS A SIDE EFFECT OF AN EDGE FUNCTION THAT CAN SILENTLY NOT RUN. There is no reconciliation sweep, no retry and no alert behind it — when complete-signup does not execute, the account simply stays at the default forever and ProtectedRoute.tsx:334 bounces it. That is the same single point of failure as the missing legal consent in OA-001. SECOND, DEPENDENT FINDING: because auto-approve is the design, /account-pending's copy is fiction for essentially every account that reaches it. It says 'We are verifying your details' and 'Hang tight, {name}. Our team is reviewing your credentials' (AccountPending.tsx:270,278-279), renders a 'Final admin review' checklist step (:198), and quotes a staffed SLA 'during business hours' (:328-329). No such review exists. A user who lands there — which now only happens when complete-signup silently failed — is told to wait for a team that will never look at their account, so the one screen that should surface the OA-001 breakage instead reassures them into waiting indefinitely. | lh-onboarding-auth | 4 |
| TS-008 |  | fixed | `reviews — blind period` | The 14-day double-blind review hold is client-overridable. set_review_visibility returns early when NEW.feedback_visible_at IS NOT NULL, and feedback_visible_at carries an INSERT grant to authenticated. A client that supplies its own past timestamp publishes instantly, defeating the reciprocity incentive the hold exists to create (the counterparty can then read the review before writing their own). | lh-trust-safety | 1 |
| TS-009 |  | fixed | `reviews — ban enforcement gap` | enforce_ban_gate is attached to jobs, messages and applications but NOT to reviews. A banned or suspended user can still write a retaliatory review on any job they completed within the 30-day window - the single most damaging thing a just-banned user can do to the counterparty who reported them. | lh-trust-safety | 1 |
| TS-010 |  | filed | `job posts, bios, applications, reviews, disputes — moderation coverage` | The off-platform-contact scanner covers in-app chat ONLY. A user who cannot type 'venmo me' in chat can post the identical text as a job description broadcast to every browsing helper, an application cover letter, a review, a dispute reason, or a permanent public profile bio - with no client warning, no fraud_flags row, no strike and no suspension. PrivacySection.tsx:144 and CommunitySection.tsx:321 imply platform-wide enforcement. | lh-trust-safety | 1 |
| TS-011 |  | filed | `block enforcement surface (whole app)` | Blocking is enforced server-side in exactly ONE place — the messages INSERT trigger. Everything else BlockUserDialog promises is a client-side filter or absent entirely: the blocked user's profile still renders at /user/:blockedId, they remain in your Saved Helprs list, and browse filtering is client-only. A blocked person a user believes is gone is still fully present to anyone who bypasses or simply does not run the client filter. | lh-trust-safety | 1 |
| TS-012 |  | filed | `reports intake` | Report intake has no rate limit and near-zero notification. public.reports carries exactly one trigger (auto_escalate_reports) and no rate-limit guard, so report spam is unthrottled. auto_escalate_reports returns early unless reported_type='user' AND there are 3+ open reports in 90 days, so a report of a job, message or review — and the first two reports of any user — notifies nobody and sits in a pull-only queue. Shake-to-report and both support forms land in AdminSupport, not the trust and safety queue (AdminReports.tsx:99 filters .neq('reported_type','support')). | lh-trust-safety | 2 |
| S-003 |  | filed | `/profile?tab=subscription → create-pro-checkout, billing_cycle=one_time` | The 'Once' membership option charges three LIVE Stripe Prices whose amounts nothing in the repo asserts, while the UI displays the MONTHLY price next to it. proTiers.ts:112 states outright that one_time is excluded from PRO_RECURRING_AMOUNT_CENTS because 'subscriptionTiers.ts defines no one-time amount' - so the drift guard that protects monthly/annual does not cover the one path whose displayed price is an assumption. tierConfig.tsx:61 renders oneTime as . If the live one_time Prices are not 500/1000/2000 cents the user is charged an amount that disagrees with what was shown. | lh-suggester | 3 |
| S-004 |  | filed | `/dashboard (every authed load) - BroadcastBanner + /admin?view=broadcasts` | Broadcast messages were confirmed REMOVED by the owner on 2026-09-01 (PROTOCOL 6d), but the feature is still fully wired and still costs the primary screen. BroadcastBanner runs two Supabase queries (broadcast_messages, then broadcast_dismissals) on every Dashboard render, and AdminBroadcasts is still a live admin view that can create rows in a dead table. Both prod tables hold 0 rows, so the queries are pure latency on the app's most-loaded screen for a product that no longer exists. | lh-suggester | 4 |
| S-005 |  | filed | `supabase/functions/helpr-pass-wallet (deployed edge function)` | helpr-pass-wallet is a deployed, invocable edge function with ZERO callers anywhere in the repo. A grep across all of src/, supabase/ and e2e/ for the function name, and for every plausible client hook (addToWallet, PassKit, pkpass), returns exactly one hit: a comment listing it in _shared/rate-limit.ts. There is no add-to-wallet affordance in any component. The lh-audit standard still lists 'Helpr Pass wallet' as a must-drive cell with tier-refresh requirements, so a whole audit sub-checklist exists for a feature with no UI. It reads profiles and reviews, so it is also live attack surface with no product owner. | lh-suggester | 1 |
| AL-008 |  | filed | `RW-005 RESOLVED — the approval gate does not gate, and /account-pending's copy describes a review that never happens` | RW-005 asked whether a fresh signup coming back approval_status='approved' against the column's 'pending' default is intended. Answer, from live prod: it is a real auto-approval on TWO independent paths, and no human review exists on the standard path. (1) handle_new_user() gives EVERY new auth user the role 'customer' — its own comment says 'Always customer'. (2) sync_email_verified() (trigger sync_email_verified_trigger, AFTER UPDATE OF email_confirmed_at ON auth.users, enabled) sets approval_status='approved' for any user who has_role('customer') and is currently 'pending'. Since every user is 'customer', the 'Helpers keep whatever approval_status they have' branch in that function is dead code. (3) sync_email_verified_on_insert() does the same on INSERT. (4) Independently, complete-signup/index.ts:470 sets approval_status:'approved' unconditionally. So 'approved' means 'email verified', not 'reviewed'. The product consequence is the copy: AccountPending.tsx's verification-centre variant says 'We're verifying your details' and 'Our team is reviewing your credentials' with a progress bar and a 'Final admin review' checklist row — describing a review that does not exist for the standard path. ProtectedRoute.tsx's own comment already concedes this ('about a review that does not exist'). | lh-account-lifecycle | 1 |
| AL-009 |  | filed | `Activity cards — dead Message links and a dead Review action once the counterparty deletes` | Four sibling-inconsistent spots where the counterparty being NULL produces a dead control. All four have a CORRECTLY GUARDED sibling in the same file or folder, so these are misses, not design. (1) DisputedSection.tsx:257 navigates to /messages?jobId=..&userId=${job.customer_id} unguarded — its siblings ConfirmedSection.tsx:125 and ActiveJobSection.tsx:407 both use the job.customer_id ? .. : '/messages' form. (2)+(3) PostedJobActions.tsx:614 (in_progress / revision_requested branch) and :1057 (disputed branch) navigate to userId=${job.helper_id} unguarded, while the SAME FILE guards correctly at :388, :657, :676, :814 and :1017. Both produce a literal userId=null in the URL. (4) PostedJobActions.tsx canReview (:725-726) is 'payment_status === released \|\| payout_pending' with NO job.helper_id test, so the poster of a completed job whose helper has deleted sees a live 'Review' chip; tapping it calls openReviewForPosted, whose first line is 'if (!job.helper_id) return;' (useLifecycleHandlers.ts:457) — a silent no-op with no toast, no error and no log. The helper side of the same pairing IS gated (AppliedJobCard.tsx:512 gates on && posterId, with a comment explaining why). The adjacent Tip chip (:765-770) has the same shape and its own in-file comment at :749 already concedes it is not gated on payability. | lh-account-lifecycle | 3 |
| AL-010 |  | filed | `Cron notification inserts against ownerless jobs — retried forever or silently dropped` | notifications.user_id is NOT NULL, and jobs.customer_id / jobs.helper_id are now nullable. Two crons insert a notification keyed on an ownerless job's customer_id and treat the failure as retryable, so the job is retried on EVERY run indefinitely and permanently pollutes the run result: expiring-jobs-push/index.ts:82 (its fetch at :62 filters .is('helper_id', null) but not customer_id; the insert error is rethrown at :93, caught per-job, and markFailures means the processed flag is never set) and payment-confirm-reminder/index.ts:251 (same shape, every 6h). A larger set inserts the same way with no .select() and no error check at all, so the notification is silently lost: auto-expire-jobs/index.ts:101 and :184, auto-release-payment/index.ts:299,312, auto-resolve-disputes/index.ts:342,353, execute-dispute-split/index.ts:1285,1310, void-cancelled-payments/index.ts:373, create-payment/index.ts (12 sites), stripe-webhook handlers, auto-tip-charge/index.ts:259, saved-helper-availability-push/index.ts:124, charge-recurring-visits/index.ts:993,1002,1121. The guarded precedent already exists in the same codebase — review-nag-cron/index.ts:265-269 'if (!party.user_id) continue;' and auto-expire-jobs/index.ts:112 'if (job.helper_id)'. Cheapest correct fix is one shared notifyIfPresent(userId, ..) in _shared/ rather than 30 individual guards. | lh-account-lifecycle | 4 |
| AM-009 |  | fixed | `public.scan_message_content() vs public.apply_message_violation_consequence()` | One offence, two independent consequence ladders that do not know about each other and disagree on every parameter. (A) The server trigger scan_message_content writes a fraud_flags row and escalates on a 24-HOUR rolling count, 2 rungs, ending in an auto_suspended_until write that does nothing (see AM-008). (B) The client-invoked RPC apply_message_violation_consequence writes a user_violations row and escalates through apply_consequence_ladder on an ALL-TIME count, 3 rungs (warning / final_warning / pending_ban_review + 7-day suspension), and does set ban_status. Both fire for the SAME blocked message: sendHandlers.ts:200 calls logViolation() which invokes (B), while the INSERT that follows trips (A). So a single message costs the user one fraud flag AND one violation, counted in two different windows toward two different ceilings. The lh-audit standard calls a duplicated ladder a drift hazard even when the copies match; these do not match. Note this also DISPROVES the obvious worry: the scanner is NOT client-only — scan_message_content is a genuine BEFORE INSERT trigger, so a user calling the messages API directly is still flagged and hidden. What they escape is only ladder (B). | lh-admin-moderation | 1 |
| EJ-003 |  | fixed | `/signup — failure toast` | Signup shows the raw Supabase auth string 'email rate limit exceeded' to the user. Signup.tsx:400 routes the catch through userFacingError(), which by design trusts any short prose-shaped message; GoTrue's auth errors are lowercase, terse, and match none of its INTERNAL_PATTERNS, so they pass through verbatim. friendlyAuthError() already maps this exact case to 'Too many attempts just now. Give it a moment and try again.' but is only used by Login. The user is told their EMAIL is rate limited, with no next step, on the most important screen in the funnel — and per EJ-001 this is the failure they are most likely to hit at launch. | lh-e2e-journeys | 1 |
| OA-008 |  | verified | `iOS app-switcher snapshot redaction — AppLockGate.tsx / appLock.ts` | App-switcher snapshot redaction is coupled to an OPT-IN setting that is not about snapshots, and that setting is OFF by default — so for the default user the iOS task-switcher preview captures whatever is on screen: an open chat thread, a checkout/payment screen, a profile, a payout account. AppLockGate DOES implement a wordless privacy shield and correctly binds it to Capacitor's appStateChange (iOS willResignActive), which is the right event — it fires before the system snapshot, and the file's comments show real care in separating it from the 'pause' event that arms the actual lock (AppLockGate.tsx:239-255). The gate is also mounted above every route (App.tsx:645-676), so nothing escapes it. The defect is the guard on the last line of that handler: 'if (isAppLockEnabled() && userRef.current) setCovered(true)' (:253). isAppLockEnabled() reads safeStorage 'helpr_app_lock_enabled' === '1' and returns false when the key is ABSENT, and false again on any storage error (appLock.ts:147-155) — so it is default-OFF and fails to OFF. App Lock is separately opt-in behind a biometric confirmation (SecurityTab.tsx:290). These are two different threat models being served by one flag: App Lock answers 'someone picked up my unlocked phone', snapshot redaction answers 'what is visible in the app switcher and written to the snapshot cache on disk'. A user who never enables App Lock has no reason to expect the second is disabled too, and the snapshot persists in the container. Apps handling messaging, payment or identity data normally redact unconditionally. RECOMMEND: raise the shield on resign-active whenever a user session exists, independent of isAppLockEnabled() — the isBiometricPromptOpen() carve-out at :253 already prevents the flash-over-own-dialog bug that guard was partly protecting. | lh-onboarding-auth | 4 |
| EJ-004 |  | fixed | `any supabase.functions.invoke failure surfaced via userFacingError` | 'Edge Function returned a non-2xx status code' is shown verbatim to users. It is supabase-js's FunctionsHttpError message — machine text that names an implementation detail and gives no next step — but it matches none of userFacingError's 16 INTERNAL_PATTERNS (44 chars, prose-shaped, no 'supabase'/'postgres'/stack frame), so it is trusted and passed through. userFacingError has 30 call sites and the app has 71 functions.invoke calls; the toasting overlap includes TipDialog, JobBoostDialog, ReferralSection, AdminDisputes and profile SecurityTab — i.e. money and trust surfaces, not just signup. | lh-e2e-journeys | 1 |
| VC-009 |  | fixed | `Desktop web nav rail (DesktopSidebarNav) — every authed screen at 1440` | One of the six items in the desktop primary navigation is set in a different typeface, size, slant and weight from the other five. Home / Posts / Jobs / Messages / Profile are Bodoni Moda 16px italic (weight 500, 700 when active) via 'font-display italic text-ds-16' at DesktopSidebarNav.tsx:346. 'Admin' is Montserrat 17px upright weight 600 via 'flex-1 text-ds-14 font-semibold' at DesktopSidebarNav.tsx:372 — the only hand-written label in the list, and the only one that does not go through the rail's own label class. It is also larger than its siblings while claiming a smaller token (text-ds-14 computes 17px next to text-ds-16's 16px), so the label is inheriting a size from somewhere other than the rail's scale. Identical in light and dark. This is the most-seen chrome on the desktop surface and it is where a reader can tell a different person built one row. | lh-visual-critic | 2 |
| EJ-005 |  | fixed | `/post-job entry — 'Offer It to a Saved Helpr'` | One of the six post-a-job entry paths disappears out from under the user's finger. OfferToSavedHelpr is meant to self-hide for a poster with no saved helprs ('sees nothing rather than an empty picker inviting them to choose from nobody' — its own docstring), but the implementation inverts that intent: the get_my_saved_helpers query is gated on , so it does not run until the card is OPENED. The card therefore renders, the poster taps it, the query resolves [],  fires, and the card unmounts with no empty state, no message, and no replacement. The sibling self-hiding card (Repost) gets this right — it fetches on mount and reserves a skeleton row so it is simply absent. | lh-e2e-journeys | 1 |
| EJ-006 |  | fixed | `/post-job?rebook= — Repost entry path` | Repost prefills a title longer than the field's own limit and then renders the result as VALID. TITLE_MAX is 32 and maxLength only constrains typing, not a programmatic prefill, so reposting any job whose title predates the limit lands a 45-character value in #title. The counter reads '45/32' in muted grey while the field shows the green valid check and the Details section header shows a green tick and the word DONE. Three affirmative signals against one quiet numeric one. At least 8 jobs in prod carry titles over 32 chars (max 45), so this is reachable, not hypothetical, and it is the one entry path whose whole promise is 'quickest way to ask for the same help again'. | lh-e2e-journeys | 1 |
| VC-010 |  | fixed | `Segmented 'pick one of N' controls across the Profile tab set (?tab=accessibility, ?tab=earnings x2, ?tab=analytics)` | The app has three different visual languages for the SAME control, three different corner radii, and three different label type scales — and two of them sit on one screen. Measured selected-state on four segmented controls, all reachable within the Profile tab set: (1) Accessibility > Color mode 'Light' = backgroundColor rgba(95,101,67,0.12), a 12% olive tint, backgroundImage none, borderRadius 0px, label 16px/400; (2) Earnings > 'Money' = rgb(240,242,244) which is the measured page canvas colour on every screen in the app, backgroundImage none, borderRadius 8px, label 15px/600; (3) Earnings > 'Lifetime', 140px below (2) = radial-gradient(125% 125% at 32% 22%) olive gloss, borderRadius 9999px, label 14px/600; (4) Analytics > '12 months' = the same gloss, 9999px, 14px/600. So the canonical treatment (gloss + pill) exists and is used by two of the four; the other two invent their own, and the Earnings tab ships both languages within one scroll. A user cannot learn one rule for 'this is the one that is on'. | lh-visual-critic | 5 |
| BR-007 |  | filed | `https://www.louisianahelpr.com/tools/apple-jwt.html (public/tools/apple-jwt.html, shipped in dist/)` | An internal developer utility that solicits an Apple Sign In .p8 PRIVATE KEY is publicly reachable and crawlable on the production marketing domain. public/tools/apple-jwt.html is copied verbatim into dist/ by the build and is live at https://www.louisianahelpr.com/tools/apple-jwt.html (HTTP 200, 10415 bytes). It renders a form whose fields are Team ID, Key ID, Services ID and a textarea labelled 'Private Key (.p8 contents)' with the instruction 'Paste the entire contents including BEGIN/END lines', then produces a client-secret JWT for Supabase Auth. IMPORTANT SCOPE LIMIT, verified not assumed: the page does NOT exfiltrate. It contains ZERO network calls — no fetch, no XMLHttpRequest, no sendBeacon, no <form action>, no dynamic .src — the signing is done entirely in-browser with WebCrypto. So this is not a live credential leak and I am not filing it as one. The real harms are (a) robots.txt does not Disallow /tools, so a page on the company's own domain reading 'paste your Apple private key here' is indexable, and (b) it hands a phisher a ready-made harvester: clone the page to a lookalike domain, add one fetch(), and it inherits the provenance of having genuinely been served from louisianahelpr.com. An App Review or security-conscious visitor finding it also reads badly on a marketplace asking users to trust it with money. It has no reason to be on the production web surface at all — it is a one-time setup tool. | lh-build-release | 4 |
| BR-008 |  | filed | `.github/workflows/bundle-size.yml — 'Check production bundle sizes' vs vite.config.ts:401` | The bundle-size gate weighs 38 KB of a 913 KB first-paint graph and reports 'within budget'. It has been measuring 0.6% of the shipped JavaScript. Its extraction is INITIAL_CHUNKS=grep -oE 'assets/[^"]+\.js' dist/index.html, documented in its own comment as 'entry + modulepreload'. But dist/index.html contains ZERO modulepreload links, because vite.config.ts:401-403 sets modulePreload: { resolveDependencies: () => [] } — a deliberate, reasonable perf choice (documented at vite.config.ts:398-403) that returns an empty dependency list so Vite emits no <link rel=modulepreload> at all. The two files are in direct conflict and neither references the other. Result: the grep matches exactly ONE file, the entry chunk at 38 KB. The author anticipated this exact failure and wrote an EMPTY-GREP GUARD (bundle-size.yml:113-131) whose comment even names the cause — 'a plugin rewrites the script tags (this repo already has THREE transformIndexHtml plugins in vite.config.ts rewriting the head)' — but the guard only fires on ZERO matches. One match satisfies it, so the check passes having weighed almost nothing. This is the guard-that-cannot-fail shape: MAX_INITIAL_KB=1500 is compared against 38, and MAX_CHUNK_KB=400 is applied to that single file only, so a 390 KB chunk (jspdf.es.min-DKS1p-*.js is 390 KB today) entering the initial graph would never be flagged. NOT CURRENTLY OVER BUDGET — the true graph is 913 KB against a 1500 KB budget, so nothing is broken for users today; the defect is that the gate would keep saying 'within budget' all the way past 1500 KB. Fix is to derive the initial graph from the entry chunk's static-import closure rather than from index.html markup, or to assert a minimum plausible chunk COUNT rather than merely non-zero. | lh-build-release | 6 |
| OA-010 |  | filed | `src/lib/socialAuth.ts:87-96 — friendlyProviderError` | Recognition is inferred by comparing a returned string against a COPY LITERAL, so rewording one sentence silently breaks the social sign-in error path with nothing failing. friendlyProviderError needs to know whether friendlyAuthError actually matched the raw provider error, or whether it fell through to its generic default. friendlyAuthError cannot answer that — it must return a string, so it reports 'here is something to show' and 'I recognised this' with the same value. The workaround is an equality test against the fallback SENTENCE: 'if (mapped === "Couldn't sign you in — give it another try?")'. The day anyone rewords that fallback — a copy pass, a tone change, swapping the em dash, fixing the curly apostrophe (the lh-audit standard explicitly calls for curly quotes, so this string is a live target) — the comparison stops matching. Nothing throws, no test fails, no type error: the branch simply never fires again, and every UNRECOGNISED Apple/Google failure silently starts showing the bare 'Couldn't sign you in' instead of the provider-named line the function exists to produce. It fails by going quiet, which is the same class as everything else this sweep has surfaced. THE FIX IS NOW TRIVIAL and that is why this is filed rather than left as a comment: matchAuthError(raw): string \| null landed in authErrors.ts as part of the OA-003 fix (ffbb94f49) and separates recognition from presentation. friendlyProviderError becomes 'return matchAuthError(raw) ?? providerFallback', deleting the literal comparison entirely. NOT FIXED BY ME: the social flow is outside the scope the orchestrator approved for the OA-003 fix; queued for the fix phase. | lh-onboarding-auth | 1 |
| OA-014 |  | filed | `/reset-password (link state detection)` | /reset-password decides the password form is usable from a URL FRAGMENT alone, with no verified session, and the dead end it produces is mislabelled as a sign-in failure.

ResetPassword.tsx:79-80 does: parse window.location.hash, and if type === "recovery" then setReady(true). That is a synchronous check on an attacker- or user-supplied fragment; it does not wait for, or consult, the token exchange. Every real recovery hash contains type=recovery alongside the access_token, so this fires BEFORE supabase-js has established the session - and it stays fired if the exchange then fails.

Consequence, driven: the full New password / Confirm password form renders and accepts input, and on submit supabase.auth.updateUser correctly fails server-side (no session). The error is routed through friendlyAuthError (:116), which has no case for a missing session, so it falls through to its default at authErrors.ts:23 and the user is told "Couldn't sign you in - give it another try?". On a screen titled Set New Password, having just typed a new password twice, the user is told a SIGN-IN failed, is not told the link is the problem, and is given no path to request a fresh link - even though this exact screen renders that path correctly for every other bad-link state.

Realistic reach: a stale or already-consumed recovery URL. The redirect GoTrue issues carries expires_in=3600, so a link a user bookmarks, mails to themselves, or reopens from browser history an hour later still carries type=recovery with a dead access_token - which lands exactly here. Notably it is also the ONE bad-link case the screen mishandles: the comment at :82-90 states the intent to distinguish the three not-ready cases, and the other two are correct. | lh-onboarding-auth | 2 |
| OA-015 |  | filed | `Profile -> Security -> two-factor enrolment (TwoFactorCard)` | The TOTP shared secret is written to the system clipboard and nothing ever clears it. TwoFactorCard.tsx:288 does navigator.clipboard.writeText(data.secret) from the setup-key button.

What makes this worth raising rather than a routine copy affordance is that it is no longer a fallback. The comment at :270-276 records that the owner removed every QR code from the app on 2026-08-25 and that the setup key is now "the primary control ... rather than a fallback under the image". So copy-to-clipboard is the main enrolment path, and essentially every user who turns on 2FA puts their TOTP seed on the pasteboard.

A TOTP seed is not a one-time code - it is a permanent credential that mints valid codes forever. On iOS the general pasteboard persists until something overwrites it, is readable by the app the user switches to next, and syncs to their other Apple devices over Universal Clipboard. So the secret that exists to be the second factor sits in the least private buffer on the device, indefinitely, immediately after the user was told to paste it somewhere. Anyone who reads that pasteboard later - another app, another synced device - holds a permanent bypass of the 2FA the user just enabled.

Proportionate fix, in preference order: keep the copy affordance (it IS the enrolment path now) but bound its lifetime - on native, write through Capacitor Clipboard and schedule a clear, since UIPasteboard supports an expirationDate and the app already owns the native layer; at minimum clear the clipboard when enrolment completes or the card unmounts, and say in the copy that the key was copied and will be cleared. Offering a tap-to-reveal-and-type path alongside it would remove the need entirely for users who want it.

Also inventoried while here and NOT a concern: the app three other clipboard writes are a support case number (ReportDialog.tsx:259), a referral link (CompletionPrompts.tsx:113) and the user own message text (messages/MessageActionSheet.tsx:85). None is a credential. | lh-onboarding-auth | 5 |
| AM-011 |  | filed | `/admin (all 24 views) on the desktop website · src/pages/Admin.tsx:535 · src/components/admin/AdminSidebar.tsx:82,122` | The admin queue badges — the only thing that tells an admin a queue has work while they are not on the dashboard — do not render at all on the desktop website. getBadge() is computed in Admin.tsx:408 and is non-zero in prod right now, but it is passed ONLY to <AdminSidebar>, which is gated behind '{!isWebDesktop && <AdminSidebar ... getBadge={getBadge} />}' at Admin.tsx:535. useIsWebDesktop() is true for any non-native browser at >=900px, so on every desktop-web admin session that component never mounts. Its replacement — the global DesktopSidebarNav that App.tsx mounts, which carries the admin sections nested under 'Admin' by the owner's own request — has no badge support. So an admin working in a browser (the normal way this console is used) sees a queue count in exactly one place, the dashboard's Priority Alerts card, and nothing anywhere else in the console. | lh-admin-moderation | 4 |
| AM-012 |  | filed | `/admin?view=home vs /admin?view=disputes vs the sidebar badge` | The admin console disagrees with itself about whether seed rows exist, and produces THREE different counts for the same queue at the same moment. The dashboard stat queries in Admin.tsx all carry .eq('is_seed', false); the queue components and the unread-badge counter do not. Measured simultaneously in one session: dashboard 'Pending Disputes' card = 0, sidebar Disputes badge = 2, and the Disputes view header = 'Open (2)' listing both jobs with live 'Quick: Release to Helpr' and 'Quick: Refund Poster' controls on them. The same split hits approvals: dashboard 'Pending Helpr approvals' = 1, the Users view tab = 'Pending 2', and the database has 3. An admin reading the dashboard concludes there is nothing to do while the queue behind it holds items wired to money buttons. This matters more at launch, not less: seed_jobs_hidden_publicly() is the launch switch and CLAUDE.md already records four browse surfaces that do not share a seed definition — the admin console is a fifth, and it is the one place where acting on a seed row moves real money. | lh-admin-moderation | 3 |
| OA-018 |  | filed | `social login -> existing email-account collision` | The email-to-social identity-linking path has NEVER been exercised, on a launch where both providers are live and prominent. Across all 40 prod auth users there are ZERO accounts with more than one identity (auth.identities grouped by user_id, max = 1), and the only providers that have ever produced an identity are apple and email - Google has produced none, despite being offered as one of two buttons on both /login and /signup.

So the collision this lane brief asks about - "an existing email-account collision is handled rather than creating a duplicate profile" - has no evidence either way in production. The scenario is ordinary: a user signs up with email and password as alice@example.com, comes back later and taps Sign in with Apple, and Apple returns the same address. Whether GoTrue links that identity to the existing user or creates a SECOND user with the same email is decided by project-level auth configuration, which this codebase neither sets nor reads and which is not exposed through the Supabase MCP tools available to me. It is the same shape as OA-006: a behaviour the app depends on, promises nothing about, and does not control.

The consequence if it links WRONG is not cosmetic on this app specifically, because of how signup is built. A second auth user means a second handle_new_user profile row that never went through complete-signup - so it lands with approval_status pending, terms_version_accepted empty and zero legal_acceptances, which is precisely the stranded state of OA-001/OA-009, and it would then be bounced into the /account-pending dead end filed as OA-017. Today prod is clean (0 duplicate lower(email) in auth.users, 40 users to 40 profiles, 1:1), so nothing is broken right now - this is an untested path, not an observed defect, and I am filing it as a pre-launch gap rather than a bug.

What IS verified working, so it is not re-audited: both providers are genuinely enabled and configured at GoTrue - authorize?provider=google returns 302 to accounts.google.com with a real client_id and the correct supabase /auth/v1/callback redirect_uri, and provider=apple returns 302 to appleid.apple.com with client_id com.Helpr.sign... - and Sign in with Apple is present and rendered FIRST on both screens, which is what Apple HIG asks for (SocialAuthButtons.tsx:55-79). Cancel is handled as a distinct outcome rather than an error (socialAuth.ts:28,66-82,146). | lh-onboarding-auth | 6 |
| AM-013 |  | fixed | `src/components/admin/AdminBroadcasts.tsx:165 · /admin?view=broadcasts` | The only unguarded zero-row write in the entire admin console, on a LIVE product surface. remove() runs supabase.from('broadcast_messages').delete().eq('id', id), inspects ONLY error, and then unconditionally splices the row out of the React Query cache with qc.setQueryData. A delete that matched zero rows returns { data: [], error: null } — indistinguishable from success — so the admin watches the broadcast vanish from the list while it is still live in the database and still being served to every signed-in user by BroadcastBanner. The UI confirms the lie. Fix is the house pattern already used 8 lines up in this same file: .select('id') passed through unwrapMutation() from src/lib/mutationResult.ts, which AdminBroadcasts.tsx:104 (cancel) already does. | lh-admin-moderation | 2 |
| EJ-007 |  | fixed | `/my-posts -> Applicants panel (ApplicantsPanel)` | The Hire button renders ON TOP of the verification chip that exists to justify it. ApplicantsPanel deliberately leaves Hire enabled for a helper who cannot be hired, and its own comments give the reason twice: the chip is shown BEFORE the poster taps Hire because it is a safety signal about letting a stranger into your home and it stops the card offering a Hire the database will refuse; the Button comment adds that a dead control explaining nothing would be worse. That reasoning depends entirely on the chip being readable, and at the audit standards own 375px floor it is not - the amber No payout account pill is overlapped by the Hire button and renders as No payout accoun. So the poster sees a full-strength primary CTA and a truncated warning, taps Hire, picks a response deadline, sends the offer, and only THEN is refused. | lh-e2e-journeys | 2 |
| VC-011 |  | filed | `/profile?tab= — the 7 tabs converted from routes on 2026-09-02` | Seven of 23 Profile tabs paint a browser-tab title in a different shape from their 16 siblings, and one has no product name at all. The 16 canonical tabs read 'X — My Profile — Helpr'; pets/work_record/home_history/str_settings/auto_tip/wrapped read 'X — Helpr' (no 'My Profile'), and analytics reads the bare word 'Analytics'. Cause: each converted page kept the usePageTitle() call it had as a standalone route (PetProfiles.tsx:40, WorkRecord.tsx:98, HomeHistory.tsx:115, StrSettings.tsx:41, AutoTip.tsx:76, HelprWrapped.tsx:257, HelperAnalytics.tsx:104). React runs child effects before parent effects, so Profile.tsx:131's TAB_TITLES title lands first and the child's overwrites it. TAB_TITLES has correct entries for all seven; they are simply never used. This is the exact defect the Record<Tab,string> guard in profile/types.ts was written to make impossible — the guard cannot fail, because the value it guards is discarded downstream.  [RE-FILED from VC-001, which collided head-on with lh-verification-credentials' VC-001; see the note on that id.] | lh-visual-critic | 1 |
| VC-012 |  | fixed | `/profile?tab=legal and /profile?tab=wrapped` | Two Profile tabs call the same screen two different names in the same breath. tab=legal paints the h1 'Legal & Policies' (LegalTab.tsx:323) while its browser tab says 'Legal — My Profile — Helpr' (TAB_TITLES.legal='Legal'). tab=wrapped paints the h1 'Your 2026 so far' (HelprWrapped.tsx:432, from wrappedSeasonLabel() in lib/format.ts:152-162) while TAB_TITLES.wrapped says 'Helpr Wrapped' — so the feature has three names depending on where you look. profile/types.ts states in its own comment that each TAB_TITLES value 'matches the h1 its screen actually paints... Measured against the rendered h1s 2026-08-24'; both have drifted since, and nothing detects it because the comment is the only check.  [RE-FILED from VC-002, which collided head-on with lh-verification-credentials' VC-002; see the note on that id.] | lh-visual-critic | 2 |
| VC-013 |  | fixed | `/profile?tab=earnings — EarningsViewSwitcher vs the range control 140px below it` | One screen carries two segmented controls with two different selected-state languages, and the off-system one is painted the page-canvas colour so it does not read as selected at all. The Money/History/Insights/Payouts switcher (EarningsViewSwitcher.tsx:70) sets its active segment with an inline background SHORTHAND — background: hsl(var(--parchment)) — which computes to rgb(240,242,244), identical to the measured page background on every screen in the app, with background-image: none. 140px below it the Lifetime/This Week/This Month/This Year control, and the range control on ?tab=analytics, and every chip in the Browse filter sheet, all paint the canonical radial-gradient(125% 125% at 32% 22%) olive gloss. Same gesture, same screen, two answers. The shorthand is also the second of the two documented gloss killers: it resets background-image, so adding btn-grad-primary to this control would silently do nothing.  [RE-FILED from VC-003, which collided head-on with lh-verification-credentials' VC-003; see the note on that id.] | lh-visual-critic | 2 |
| VC-016 |  | fixed | `AlertDialog / BrandConfirmDialog — the shared close X on every confirmation in the app` | The close X was made 44x44 on DialogContent on 2026-09-02 and its twin was left on the old geometry, so the app now has two different shared close buttons. Measured live on the 'Log Out?' confirmation: the AlertDialog X is a 32 x 44 box (alert-dialog.tsx:191 is right-3 ... w-8, with no width/height style, so 32px wide and 44px tall only because index.css sets a bare button{min-height:44px}). DialogContent's X is dialog.tsx:350-352, right-1.5, with an explicit style width/height/minWidth/minHeight 44px. dialog.tsx's own comment records that a compactClose prop shrinking its X to 32x32 was DELETED the same day because '32x32 is below the 44x44 HIG floor, so the prop's only possible effect was to make the one control every dialog shares harder to hit' — and it names 'the old right-3 w-8 geometry' as the control it A/B'd against. That control geometry is still shipping in alert-dialog.tsx, which is the primitive behind the ~28 confirmations that render through BrandConfirmDialog, i.e. every destructive confirm in the app. A 32px-wide target is 12px under the HIG floor on exactly the dialogs where a mis-tap matters most.  [RE-FILED from VC-006, which collided head-on with lh-verification-credentials' VC-006; see the note on that id.] | lh-visual-critic | 2 |
| V-007 |  | fixed | `/browse as a guest — src/pages/dashboard/useDashboardFilters.ts:230-245 — Nearby radius filter` | The guest Nearby radius filter is a total no-op while the UI reports it as applied. The precise-haversine branch requires typeof job.latitude === 'number' and the open_jobs_browse view does not project coordinates at all; the string fallback needs profile, which is null for a guest. So both branches fall through and every job passes, at every radius, behind a 'Filtered Results' badge. The app's own mapFilter.ts:16-18 states the rule this violates. | lh-verifier | 1 |
| V-008 |  | filed | `get_public_open_jobs + notify_saved_searches_on_new_job — the paid early-access perk` | The early-access perk leaks through two paths that migration 20260901022522 closed only for /jobs. get_public_open_jobs is the one anon-callable open-jobs RPC with NO early_access_cutoff() predicate, and it orders created_at DESC LIMIT 6 — precisely the newest jobs the paid perk exists to withhold. notify_saved_searches_on_new_job has the same gap, so a free-tier user with a saved search is notified at INSERT time, ahead of the cutoff. Separately worth noting: get_public_open_jobs has no caller in src/ at all and is still anon-EXECUTE-able. | lh-verifier | 1 |
| V-009 |  | fixed | `WCAG AA contrast — StatusPill.tsx:7, helpCenterContent.ts:66, HeroSection.tsx:229-233, /help FAQ chips` | Four AA contrast failures on live surfaces, all invisible to CI for the reason filed separately as the axe-incomplete gap. (1) giftCards/StatusPill.tsx:7 uses color:hsl(var(--amber-tint)) — a 12% tint used as text — at 2.49:1; index.css:403-404 explicitly splits --amber-tint (fills/borders) from --amber-ink (label & body text), and statusColors.ts:92-94 records this exact fix verbatim for a DIFFERENT map. The fix was applied to the members of one map rather than to every call site of the token. (2) helpCenterContent.ts:66 'Membership & Billing': hsl(var(--sage)) = 2.79:1 at 12px/600, while the other six topics pass — added by a commit that fixed a colour-fallback drift and introduced a contrast failure. (3) HeroSection.tsx:229-233 'Browse Jobs' CTA fails in BOTH themes at every width: light 1440 3.75:1, dark 1440 3.78:1, dark 375 4.07:1, dark 320 4.03:1 at 17px/600. (4) /help FAQ count chips 3.66-3.69:1, six instances. | lh-verifier | 1 |
| N-010 |  | fixed | `notify_helper_application_viewed — supabase/migrations/20260612540000_notify_on_application_viewed.sql` | A producer deliberately picked an unmapped notification type in order to AVOID sending a push, but unmapped types are precisely the ones that push UNCONDITIONALLY — so the choice guarantees the push it was meant to prevent. The migration header states: "type = 'info' is used instead of 'application' because type 'application' maps to the job_applications push preference in notification_type_pref_map and would trigger a push notification. 'Poster viewed your application' is informational, not urgent enough to interrupt with a push." That reasoning is inverted: a map row GATES a push, it does not cause one. The author's own NOTE four lines later concedes the real behaviour ('info does not appear in notification_type_pref_map, so it falls through to the push fan-out unless the user's master push_enabled toggle is off') without noticing it contradicts the decision above it. Net effect: an intentionally low-priority in-app-only notice is the one class of notification that ignores every category preference. This is NT-001's root cause being used as a feature, and it is why the hole cannot simply be closed by making unmapped types fail closed — a producer depends on the current behaviour. Schema gap behind it: there is no 'in-app only' channel, which is what this call site actually wanted. | lh-notifications | 1 |
| AS-002 |  | fixed | `src/components/HelprMark.tsx — the '· LA' wordmark suffix, DARK MODE, 12 public screens` | The '· LA' tail of the Helpr wordmark is painted hsl(var(--burnt-sienna)), whose dark-mode value (19 65% 52%) measures 3.84:1 against the dark header surface. It is 15.2px text, so WCAG AA requires 4.5:1. It appears on /, /signup-pending, /legal (3 tabs), /help, /support, /privacy, /terms, /rules and the 404 — twelve public screens. | lh-a11y-sensory | 5 |
| AS-003 |  | fixed | `src/pages/Login.tsx + src/pages/signup/SignupStep1.tsx — the 'or' auth divider, DARK MODE` | The 'or' rule between the credentials and social-login columns is hsl(var(--burnt-sienna) / 0.9), which measures 4.27:1 on the dark auth canvas at 11px — under the 4.5:1 AA needs. The in-code comment justifying the 0.9 alpha cites a measurement of 4.86:1 that was taken in LIGHT MODE ONLY ('composited over --parchment'); dark mode was never measured. Reachable anonymously on /login, /signup, /account-pending, /account-denied, /account-banned and /data-rights. | lh-a11y-sensory | 3 |
| AS-004 |  | fixed | `/help — src/pages/helpCenter/helpCenterContent.ts SECTION_ACCENTS + src/pages/HelpCenter.tsx` | Two AA failures in the Help Center FAQ headers, light mode. (1) The 'Membership & Billing' topic accent is hsl(var(--sage)); --sage (78 9% 53%) is a surface colour, not a text colour, and the topic label is 12px/600, so it measures 2.75:1 — the worst contrast on the public surface. It was introduced by the fix for a missing map key, which added the seventh accent without checking it as text. (2) The per-topic article-count badge is hsl(var(--olivewood) / 0.6) at 11px and measures 3.64:1 on all seven rows. It is aria-hidden, which hides it from a screen reader but does nothing for a sighted reader. | lh-a11y-sensory | 6 |
| AS-006 |  | fixed | `AUTHED surface (light mode) — /payment-success, /u/:id profile, /profile?tab=schedule` | Four more AA colour-contrast failures on the SIGNED-IN surface, found by the tightened gate and outside this lane's public-surface fixes. NOT FIXED — reported so the gate's first red run is expected rather than a surprise. (1) /payment-success: the 'Payment not confirmed' line is 3.33:1 at 11px — the one sentence on that screen that tells a paying customer something went wrong. (2) public profile: the 'First Job' milestone badge is 2.12:1 (gold-warm used as 12px/600 text, which the 'GOLD IS PRESTIGE ONLY, --gold-ink for anything you read' rule in index.css already forbids). (3) public profile: 'Tap a highlighted figure to see what is behind it' is 4.4:1, a 0.1 miss. (4) /profile?tab=schedule and /schedule: the blocked-day numerals are 2.97:1. | lh-a11y-sensory | 5 |
| GD-009 |  | fixed | `scripts/check-types-fresh.mjs` | The types-freshness guard emits a false positive on EVERY run: it reports 'RPCs in the types, gone from prod (1): graphql() — callers are already failing'. graphql() lives in the committed file's graphql_public schema block, which the documented --schema public generation does not emit; the guard's function regex is not scoped to the public block. A gate that cries wolf on every run is the gate people learn to ignore — the exact GD-005 noise problem now inside the guard itself. Separately the step is gated on steps.precheck.outputs.skip != 'true', so a skipped precheck silently skips the guard. | lh-generated-drift | 2 |
| NB-019 |  | filed | `Chat composer voice note — microphone hardware cleanup` | The microphone MediaStream is never released when the chat composer unmounts mid-recording, leaving the iOS orange mic indicator lit and the mic capturing until the app is killed. src/hooks/useVoiceRecorder.ts imports only { useCallback, useRef, useState } — it contains ZERO useEffect, therefore no unmount teardown. cleanupStream() (which calls streamRef.current.getTracks().forEach(t => t.stop())) is reachable only from recorder.onstop (requires an explicit stop()) or discard(). Its sole consumer is src/components/RichMessageInput.tsx:107, the chat composer, which itself contains ZERO useEffect and no cleanup. So: tap record in a thread, then leave the thread by any route change — back button, bottom nav, a push tap, a helpr:// deep link — and getUserMedia's stream stays open for the life of the WebView. This is an SPA, so nothing tears it down. Positive control that this is an omission and not the house pattern: the sibling hook mounted in the very same component, src/hooks/useVoiceDictation.ts, DOES clean up on unmount at line 216 (return () => { ... }). Verified against origin/main, not the divergent local tree. | lh-native-bridge | 1 |
| GD-011 |  | fixed | `supabase/migrations/20260902161042_*.sql, 20260902163216_*.sql` | Two migration FILENAMES carry different content on the two sides of the shared-tree fork, and prod has already recorded both versions — so the local variants can never apply. origin/main holds the FIXED files (replay-safety DROP FUNCTION added 2026-09-03, the change that un-redded db-smoke); the shared tree holds the older unfixed variants under identical names. git merge-tree reports add/add CONFLICT on both. Whoever resolves that merge must take the origin side; taking local would re-introduce a migration that db-smoke fails on and that supabase db push would skip anyway, since the version string is already in schema_migrations. Routing to lh-schema-integrity — not my lane to fix. | lh-generated-drift | 2 |
| PD-008 |  | filed | `src/components/dashboard/jobDetailDialog/useJobDetailData.ts (every job-dialog open, signed-in)` | CONFIRMS the cross-lane lead, with one correction. posterCancelRate fires FOUR count:exact HEAD queries on public.jobs whose result is set into state and read by NOBODY - JobDetailDialog.tsx:73-83 does not destructure it and grep -rn 'posterCancelRate' outside the hook returns zero hits. applicationCount is likewise never destructured, BUT its query is NOT waste: the same effect derives viewerAppPosition/viewerUserId from that list, which ARE used. The bigger cost the lead missed: supabase.auth.getUser() is a NETWORK GET /auth/v1/user in auth-js 2.112.3 (no local path - see GoTrueClient._getUser), and this one hook calls it 4 times (3 per open + 1 in the cached tier query) while a module-level already-resolved user snapshot exists in useAuthReady.ts. Per dialog open a signed-in helper pays ~10 round-trips: 3x getUser, record_job_view, applications list, 4x dead jobs count, repeatJobs count. repeatJobs is a SERIAL 2-RTT chain (getUser then count). App-wide there are 30 getUser() call sites vs 18 getSession(); this file is the worst at 4. | lh-perf-deps | 1 |
| PD-009 |  | filed | `SPA route changes, all public routes` | Route changes retain DOM nodes and listeners that three forced GCs do not reclaim - a long session degrades. Returning to the SAME route (/) after 40 SPA navigations across 10 public routes: live elements identical (195 -> 197) but renderer-known nodes 347 -> 1365, i.e. retained-detached nodes ~152 -> ~1168 (7.7x); JSEventListeners 197 -> 296; JS heap 4.5MB -> 10.0MB. Growth is monotonic and continues on already-visited routes (30 navs: 240 listeners/896 nodes; 40 navs: 296/1983), so it is retention, not first-load chunk cost. Rate approx 25 retained detached nodes and 2.5 listeners per navigation. Documents stays 1, so it is not detached documents. Also: window.setInterval is called once and clearInterval never. | lh-perf-deps | 3 |
| BR-020 |  | fixed | `src/hooks/useMapKitJs.ts:96,124 — build-time env inlining into the public client bundle` | The ENTIRE import.meta.env object is inlined verbatim into the shipped bundle, so EVERY VITE_-prefixed variable present in the build environment ships to users whether or not any code reads it. Two sites cast import.meta before reading .env — 'const env = (import.meta as { env?: Record<string,string\|undefined> }).env' — which defeats Vite's per-key static replacement, so instead of substituting one value Vite emits the whole env literal. Today the blast radius is benign (only the four expected public vars exist), but the rule this repo relies on is 'a VITE_ var is public BY DESIGN because we choose what to reference'; this pattern silently changes it to 'every VITE_ var in the environment is published automatically'. Anyone who adds a VITE_-prefixed value to Vercel or a workflow for an unrelated purpose ships it to every user with no code change and no review. It is also the mechanism that makes chunk hashes env-sensitive and so contributes to BR-002: adding or removing a single unreferenced var (VITE_SUPABASE_PROJECT_ID, which no source file reads) renames 36 of 396 chunks. | lh-build-release | 1 |
| ME-041 |  | fixed | `/payment-success?job_id= → replayed / bookmarked return after escrow has moved on` | Re-opening the Stripe return URL for a job that HAS been paid — but whose payment_status has since moved to released/refunded/chargeback — prints 'Your payment hasn't been confirmed on our side yet. It usually lands within a minute.' That sentence is false: the payment was confirmed and the money has already moved. PaymentSuccess.tsx deliberately routes those states to 'unknown' (correct — the escrow copy would be false for them), but 'unknown' then picks the 'pending' body, which asserts a specific untruth instead of the intended 'we can't confirm that here'. The page's own truthfulness contract says unknown must 'not imply the money was taken OR that it wasn't'; the pending wording implies it was not yet. | lh-money-escrow | 1 |
| ME-043 |  | filed | `create-payment automatic_tax → Stripe Tax calculation for Louisiana` | Stripe Tax returns $0.00 even on the ONE category the code declares taxable. An 'assembly' job priced against a real Baton Rouge LA billing address — automatic_tax enabled, labor line correctly carrying tax_code txcd_20030000, and Stripe's own checkout page printing 'Secure escrow payment for taxable labor (assembly)' — computed Tax $0.00 and a total equal to subtotal. So NO Louisiana sales tax is being collected on any category today, including the two (assembly, handyman) that _shared/salesTax.ts exists to tax. The tax codes, the category list and the parity tests are all correct; the calculation itself comes back zero, which is the signature of Stripe Tax having no LOUISIANA REGISTRATION on the account. HONEST CAVEAT I could not resolve without Stripe account access: Stripe Tax registrations are per-MODE, and this was measured in the sandbox, so live mode may have a registration the sandbox lacks. Either way the app has NO signal that distinguishes 'correctly $0' from 'Stripe Tax silently not registered' — it just records whatever comes back into jobs.sales_tax_amount — so a missing live registration would be invisible. Owner action: confirm an active Louisiana registration under Stripe Tax > Registrations in BOTH modes before launch. | lh-money-escrow | 1 |
| DH-017 |  | fixed | `Every ProtectedRoute route → /account-banned (ProtectedRoute.tsx:288-289)` | A lapsed 7-day suspension keeps the user locked out of the entire app for up to 60 minutes AFTER the app itself says the suspension ended. Three components implement the same fact and only one forgot the clock: is_caller_banned() carves out a lapsed temp_banned ('rights come back on the clock' — its own comment) so the server restores ALL write rights immediately at auto_suspended_until; StrikeBanner.tsx:46-47 gates on 'until > new Date()' so the explanatory red banner DISAPPEARS at expiry; but ProtectedRoute.tsx:288-289 tests ban_status membership alone, with no auto_suspended_until check, so it keeps redirecting every protected route to /account-banned until the hourly sweep-expired-auto-bans cron rewrites the column. The refusal outlives the state that justified it. This is not defence-in-depth: the client is STRICTER than the server, and it is strictly wrong. AccountBanned.tsx makes the broken promise explicit — line 140 renders 'You will regain access once the suspension period ends' and line 235 renders 'Suspended until <timestamp>' — so the user sits on a dead-end screen quoting a deadline that has already passed, with no banner left to explain it and no self-service exit. Fix belongs upstream in the redirect predicate, not in new copy: ProtectedRoute must mirror is_caller_banned()'s carve-out (treat temp_banned with a non-null auto_suspended_until <= now() as NOT banned), exactly as StrikeBanner already does. The server-side enforce_ban_gate triggers stay untouched. | lh-design-holes | 2 |
| SF-017 |  | fixed | `src/hooks/useCurrentUser.ts:169-197 (every signed-in session) + src/components/admin/AdminNotificationLogs.tsx:204-205` | Two postgres_changes channels subscribe to tables that are in NO publication in prod, so they have never delivered a single event and never will. (1) useCurrentUser's 'profile-self-<uid>' channel binds UPDATE on public.profiles — profiles was DROPPED from supabase_realtime by migration 20260423164103 (a deliberate PII-broadcast fix), so the binding has been dead since 2026-04. Its job is to invalidate the currentUser cache when an admin flips approval_status, idv_status, subscription_tier or a ban; none of that reaches an open client in realtime. The 30s staleTime + refetchOnWindowFocus in the same hook is what actually delivers those changes, so the user-visible effect is a delay rather than a permanent stale gate — but the realtime path the comment at :239-241 describes does not exist. (2) AdminNotificationLogs binds INSERT on notification_logs, which is in no publication either; the admin log view never live-updates. Second-order cost: subscribeWithRecovery classifies CHANNEL_ERROR as dead, so 'profile-self-*' is added to the downChannels health registry permanently for EVERY signed-in user and retried forever on backoff, plus one Sentry warning per session. That makes downChannels non-empty by construction and therefore useless as a realtime-health signal — and realtimeRecovery.ts:41 claims OfflineBanner reads it, which nothing in src/ does (grep for subscribeRealtimeHealth/downChannels outside that file returns zero consumers). This is documented ONLY inside the KNOWN_UNPUBLISHED_BINDINGS allowlist in src/test/realtimePublication.test.ts, which suppresses the guard rather than filing the defect — the guard reads as passing while the bindings stay dead. Note the allowlist also names 'referrals' and a second 'profiles' site in Admin.tsx: those are false positives, Admin.tsx:148-154 is a plain count query, not a postgres_changes binding. Fix is NOT to re-publish profiles (that reopens the PII broadcast 20260423164103 closed) — it is to delete the two dead bindings and let the existing staleTime/focus refetch own it, then shrink the allowlist to empty. | lh-silent-failure | 2 |
| AR-012 |  | fixed | `recurring_visit_releases INSERT (helper releases a recurring visit date)` | The INSERT policy's WITH CHECK subquery is uncorrelated to the inserted row, so the feature is dead for every user. It reads EXISTS (SELECT 1 FROM jobs j WHERE j.id = j.parent_job_id AND j.recurring_helper_id = auth.uid()) -- alias j shadows the target table, so j.parent_job_id is the JOBS column, not recurring_visit_releases.parent_job_id. The intended predicate was j.id = recurring_visit_releases.parent_job_id. j.id = j.parent_job_id is a self-parent row, of which prod has ZERO, so the predicate is always false: silent denial, not a leak. | lh-authz-rls | 2 |
| EF-025 |  | fixed | `supabase/config.toml + deployed admin-update-email` | config.toml carries a six-line comment stating admin-update-email 'deliberately has NO verify_jwt override' so the gateway JWT check remains as 'defence-in-depth on an account-takeover-grade action'. Production contradicts it: the deployed function has verify_jwt=false, so that second layer does not exist. NOT exploitable - the in-function getClaims + has_role gate is sound and fails closed (503 on RPC error, 403 on non-admin) - but the file documents a guarantee prod does not provide, on the one endpoint that can change a user's login identity. Same class as the config comments that made other silent no-ops read as impossible. | lh-edge-functions | 1 |
| EF-027 |  | fixed | `pg_cron -> edge function invocation (all cron-invoked functions)` | No cron.job command sets timeout_milliseconds, so every cron-invoked edge function runs under pg_net's 5000ms default deadline. 79 timeouts in 30 days, concentrated on the email and MONEY crons: process-email-queue 37, auto-expire-jobs 17, auto-release-payment 14, void-cancelled-payments 2, process-scheduled-payouts 1, money-reconciliation 1. Scoped claim: a pg_net timeout does NOT mean the function failed - the request was delivered and the edge runtime keeps executing - so this is an OBSERVABILITY defect, not a processing outage. The consequence is that those runs write no cron_run_log row, so sweep_silent_cron_failures cannot grade them and a genuine failure inside a timed-out run of auto-release-payment or process-scheduled-payouts is invisible. That is the 'you cannot fix what you cannot see' class on the escrow-release path. | lh-edge-functions | 1 |
| AR-014 |  | fixed | `applications INSERT / POST /rest/v1/applications` | Four more job-state conditions that every discovery surface filters are not refused at the apply path: a job whose status is no longer 'open' (proved with 'completed'), a fixture row (is_seed), a job whose date_needed has already passed, and a job past its expires_at. A helper can attach an application to a finished, expired, past-dated or fixture job; the poster then sees an applicant on a job that is closed, and the seed case lets a fixture row accrue real applications the launch switch was written to prevent. | lh-authz-rls | 1 |
| AR-015 |  | fixed | `applications INSERT on an ownerless (anonymized-poster) job` | Applying to a job whose customer_id is NULL -- the state a deleted account leaves behind, per the anonymise-on-delete path -- fails CLOSED, but by accident rather than by a guard. Nothing in the policy or the gate triggers checks customer_id; the write dies deeper in the AFTER trigger notify_on_application, which tries to insert a notification addressed to the (NULL) poster. The user sees a raw NOT NULL constraint message, and the protection evaporates the moment that notify path is made null-tolerant. | lh-authz-rls | 3 |
| SI-031 |  | fixed | `/admin?view=jobs — job list + job detail payment badge` | jobs.payment_status CHECK allows 10 values; the admin colour map defines 5. A job in cancelled/abandoned/failed/chargeback/cancelling renders its money-status badge with NO tone colour at all (paymentColors[...] \|\| ""), on the surface an operator uses to triage money problems. The text label map degrades via humanize(); the colour map has no fallback. | lh-schema-integrity | 10 |
| CC-020 |  | fixed | `42 user-facing strings across 17 files (toasts, empty states, onboarding tour, Activity counters, review 'For: a task')` | Canonical noun is 'job' (744 string sites; ActivityEmptyState.tsx:118 documents the rename) but 42 live user-facing strings still say 'task' — e.g. QuickApplyHandler.tsx:205 toast 'We can't open this task right now', OnboardingTour.tsx:59 'Post a Task', PostedJobsTab.tsx:400 'Post a Task' button, Activity counters '0 tasks', review fallback 'a task', JobDetail.tsx:156 'This task isn't available.' | lh-copy-content | 3 |
| SM-002 |  | fixed | `scripts/audit-capture.mjs:57, scripts/probe-state-matrix.mjs:34 (audit harness)` | Both audit harnesses hard-code the seeded helper's auth id 6bdc1f67-…a6147, but eli.test.helper@ was re-created as f6cc3ebb-9478-473c-8eb8-62b406f0734f (GoTrue created_at 2026-09-07T06:11Z; old id → 404 user_not_found). snapshotAccountState() therefore snapshots ZERO rows and restoreAccountState() is a no-op — the mandatory mutation guard for the state-matrix lane silently does nothing — and the minted session injects user_id=undefined so every authed route renders 'We couldn't load your account' (PostgREST 22P02 invalid uuid 'undefined'). | lh-state-matrix | 2 |
| SI-034 |  | fixed | `src/integrations/supabase/types.ts (generated) vs prod schema` | The committed types.ts is STALE against prod: 6 columns added by migrations already deployed are missing from it (applications.flag_reason, applications.flagged_hidden, louisiana_zip_parishes.latitude/longitude, notification_preferences.email_enabled, profiles.parish_source, profiles.subscription_source). Regenerating it — which any dev running 
> vite_react_shadcn_ts@0.0.0 db:types
> npx supabase gen types typescript --project-id fncmgoasalhdgfwzhsqa --schema public > src/integrations/supabase/types.ts && echo '✓ Regenerated src/integrations/supabase/types.ts (needs SUPABASE_ACCESS_TOKEN env var or supabase login)'

✓ Regenerated src/integrations/supabase/types.ts (needs SUPABASE_ACCESS_TOKEN env var or supabase login) will do — turns the build RED with 1 error: the synthetic direct-offer application literal at useActivityData.ts:389 omits the two new NOT-NULL-ish applications columns. The staleness is the only thing currently hiding the break. No runtime defect today; the synthetic row is a stand-in that nothing reads for those fields yet. | lh-schema-integrity | 6 |
| CC-022 |  | fixed | `Terminology: 27 user-facing strings across 19 files` | Canonical nouns are Helpr (652 sites) and poster (443); 27 live strings still say lowercase 'helper(s)', 'helprs', 'customer(s)' or 'client(s)' — e.g. Pets empty state 'so helpers know their needs' (rendered, screenshot), ReviewsTab 'your customer's words', ProfileEditForm 'Customers read this', AtAGlanceCard 'Clients who rebooked', ApplyEarningsBreakdown '÷ 3 helprs', LogisticsSection 'Number of helpers needed', OnboardingTour 'posters and helpers'. Compound: helperMilestones.ts:73 at 25 jobs says 'You're an Elite-tier helper now' — Elite is the PAID tier, the earned tier is 'Top Rated' and also needs 4.8 rating + 10 reviews, so the toast is a false claim. | lh-copy-content | 4 |
| SM-003 |  | fixed | `EmptyState.tsx dock variant — every full-surface empty state (/dashboard, /browse guest+authed, /my-posts, /my-jobs, /messages) at 375, light+dark` | The 08df4cda0 'delete the inner box' fix removed the dock's liquid-glass fill and border but left its inline boxShadow (inset white highlight + -1px/1px/0 -1px olivewood side and top shadows, EmptyState.tsx:102-106). That shadow still paints a faint second rectangle ~12px inside the panel border with SQUARE top corners — the owner's nested-box defect is reduced, not gone, on every empty state. Visible in a screenshot in one second at 375; also present on the guest /browse. | lh-state-matrix | 3 |
| BD-006 |  | fixed | `src/components/dashboard/FilterSheet.tsx showNearby doc + useDashboardJobsCount.ts` | Two code comments assert schema facts that prod contradicts, and both would misdirect a fix. (1) FilterSheet.tsx:502-510 justifies hiding the radius chip from guests: 'the guest browse feed comes from get_ranked_open_jobs, whose rows carry no latitude/longitude'. Wrong twice — the guest feed reads the open_jobs_browse VIEW (DashboardGuest.tsx:204), and that view DOES expose latitude/longitude, which DashboardGuest.tsx:213 explicitly projects (masked 2dp ~1.1km, migration 20260903031231). Radius is a real client-side haversine for a guest via browser geolocation; useUserLocation needs no account. App.tsx:258 and JobPosterCard.tsx:27 repeat the same wrong attribution. (2) useDashboardJobsCount.ts calls its missing radius predicate a 'KNOWN GAP (documented, not a bug)' that 'can run slightly HIGH', which is how the 3-vs-1 count defect below stayed unfixed. | lh-browse-discovery | 9 |
| SI-035 |  | fixed | `reviews table — poster-reviews-helper vs helper-reviews-poster` | CONFIRMED: the two review forms render DIFFERENT questions over the SAME column. reviews.punctuality receives 'Showed up on time' when a poster reviews a helper and 'Confirmed the work and released payment quickly' when a helper reviews a poster. There is no direction column, so nothing on the row distinguishes them and no reader can. quality is clean (poster form omits it entirely -> persists NULL) and communication is consistent across both. Latent today ONLY because every sub-criteria display surface was removed 2026-08-30 (ReviewsTab.tsx:47-50, RatingBreakdown.tsx:17-18) -- the column is still written by both directions and still returned by get_public_profile_reviews, so the first surface that renders it again averages two different questions. SEPARATE, larger issue: the overall star rating is direction-blind (aggregated purely on reviewee_id), and since every account both posts and works, a user's as-a-helper and as-a-poster reputations are averaged into ONE number and one review count. NOT SHIPPING A FIX -- product call, per orchestrator. | lh-schema-integrity | 5 |
| SM-004 |  | fixed | `/profile?tab=availability at 375 (light+dark) — every enabled day row` | The hours pill truncates its own default value: '9 AM – 5 PM' renders as '9 AM – 5 …' on all seven rows at 375. TimeRangeField.tsx:31-34 carries a comment saying the compact format was added so the range 'fits the narrow pill at 375px instead of truncating' — measured: the pill gets 128px, the text needs 89px inside 128-2*12-16-6 = 82px, 7px short, so the fix shipped and did nothing. Any non-:00 time ('10:30 AM – 5:30 PM') is far worse. This is the screen every helper edits on day one. | lh-state-matrix | 2 |
| SC-014 |  | fixed | `supabase/functions/helpr-pass-wallet — Helpr Pass refused to a comped Elite member` | helpr-pass-wallet/index.ts:69 resolves `const subActive = subExp ? subExp > new Date() : false` — a NULL subscription_expires_at is treated as INACTIVE. That is the opposite of the house convention, which every other gate uses (tierFeePercent, feePercentForTier, early_access_cutoff's SQL, instant-payout, EarningsTab): a null expiry on a paid tier means 'no scheduled end' and counts as ACTIVE. So an Elite member on a comped or lifetime grant — exactly the accounts most likely to have a null expiry — is refused their Helpr Pass with an HTTP 402 reading 'Helpr Pass is an Elite perk.' NOT FIXED: this is outside my lane's files. I fixed the identical inversion in create-boost-payment/index.ts:104 and JobBoostDialog.tsx:40, where it was costing an Elite member their included boost. | lh-subscriptions-credits | 2 |
| PD-019 |  | fixed | `/dashboard, /browse map, any MapKit surface` | MapKit's token round-trip ran strictly AFTER Apple's CDN round-trip. resolveToken() lived inside authorizationCallback, which MapKit invokes only after its script has downloaded, parsed and init() has run — so a Supabase edge function (cold-start prone) was serialized behind a CDN fetch it has no relationship to. | lh-perf-deps | 1 |
| EJ-020 |  | fixed | `/my-posts and /my-jobs → ActivityEmptyState 'Show <bucket>' pointer` | When the default bucket is empty the tab bar is not rendered at all, and the single control offered points at the FULLEST other bucket rather than the most urgent — routing the poster of a live job away from it. ActivityEmptyState computes jumpTo by sorting the other non-empty filters on count descending and taking [0] ('the fullest one is the single best place to send someone'), treating volume as a proxy for urgency. Volume is not urgency: Waiting holds passive listings nobody applied to, while Scheduled can hold a job whose helper has ALREADY ARRIVED ON SITE and is blocked awaiting the poster's 'Confirm They Arrived', with escrow held. Measured at 375 with the push prompt dismissed, the poster's entire page carries THREE controls — 'Skip to content', 'Show Waiting (4)', and the profile avatar — with documentElement.scrollHeight === clientHeight === 812, so nothing is below the fold; 'Scheduled' appears only inside a non-clickable <p>. The item is reachable only by taking the wrong turn first: tapping 'Show Waiting (4)' makes the tab bar appear, and Scheduled is one more tap on. So the empty state names the urgent job, renders no control that reaches it, and hands the user a button going the other way — the exact failure its own comment says it was written to fix ('Naming a place is not the same as offering it'). | lh-e2e-journeys | 3 |
| EJ-022 |  | fixed | `Cancellation confirmation toast (CancellationDialog) on a never-funded job` | Cancelling a job that was abandoned at checkout told the poster money was coming back that was never taken: 'Job cancelled — the full amount returns to your card.' Nothing had been charged (payment_status stayed 'unpaid', no escrow ever held), so no refund will ever appear on a statement — and this lands on the one screen where the poster is already unsure what happened to their payment. The toast branched only on whether a cancellation fee applied, which silently assumes a payment happened. Fixed in b3b225bd4: the funded state is passed from the card (which already reads payment_status) and the unfunded case now reads 'Job cancelled. Your checkout was never completed, so you were never charged — there's nothing to refund.' | lh-e2e-journeys | 2 |
| VD-002 |  | fixed | `NavQuickMenu (bottom nav long-press, 375)` | Long-press quick menu was not centred on its tab: framer-motion's inline transform ends as 'none' and wipes Tailwind -translate-x-1/2, so the panel's left edge sat on the tab centre; Messages menu spanned x 206-430 in a 375 viewport (55px clipped). Menu also took no focus, so Tab walked the page behind it. | visual-dialogs | 1 |
| VD-004 |  | fixed | `Chat photo message (MessageAttachment)` | Sending a photo in chat rendered 'Couldn't Load Photo' on the sender's own message until reload: the first single-object createSignedUrl right after a 200 upload returned 404 NoSuchKey (storage read-after-write lag), and the component treated one null as terminal. | visual-dialogs | 1 |
| VD-005 |  | fixed | `Photo viewers (dashboard PhotoLightbox, MessageAttachment lightbox)` | Both hand-rolled full-screen photo viewers opened without taking focus and did not trap Tab: focus stayed on the thumbnail behind, and inside JobDetailDialog Radix's FocusScope kept Tab cycling on 'View photos' underneath, so the viewer's Close was unreachable by keyboard and never announced. | visual-dialogs | 2 |
| BR-024 |  | filed | `Supabase Management API — version/ezbr_sha256 fields for ~40 edge functions` | version and ezbr_sha256 are NOT deploy evidence for the ~40 functions whose entrypoint_path the platform has rewritten to /app/... They churn continuously with no deploy: measured health-check 1305->1307 and money-reconciliation 986->988 inside 9 minutes with no CI run, both landing on the identical updated_at 1790097325917. That is how create-payment reached version 2191. Any staleness audit keyed on updated_at/version will report those 40 as fresh regardless of the truth; only a content read is decisive for them. | lh-build-release | 1 |
| BR-025 |  | filed | `prod edge-function deploy pipeline` | Supabase accepts and discards a fraction of edge-function uploads. Measured twice today: run 35756822730 lost 8 of 64 uploads, run 35761183146 lost 3 of 58, and the two sets do not overlap — it is intermittent, not a per-function property (create-pro-checkout, lost twice in a row, landed on the third attempt unchanged). Root cause is inside the CLI or the platform, not this repo: the CLI itself printed 'Deployed Functions on project ***: X' for every lost function, so the workflow's exit-code branch was not the swallower. Mitigated, not cured, by the retry in 3470ad103. | lh-build-release | 1 |

## LOW (77)

| ID | Blocker | Status | Surface | Claim | Agent | Evidence |
|---|---|---|---|---|---|---|
| RW-002 |  | verified | `retired marketing routes` | 10 retired routes (/enterprise, /how-it-works, /parishes, /parish/:slug, /impact, /local-guide, /community, /browse-jobs, /evacuation, /become-a-partner) have no redirect stub and fall through to NotFound. Intentional per code comments, but any external link, sitemap entry or indexed URL still pointing at them now 404s. | lh-route-walker | 1 |
| SI-010 |  | fixed | `NOT VALID CHECK constraints on jobs and tips` | Eight CHECK constraints on money/lifecycle tables are marked NOT VALID, meaning Postgres never verified the rows that existed when they were added. New writes ARE checked, so the exposure is historical only -- and I verified the historical rows actually conform, so these can be VALIDATEd with no data cleanup. Leaving them NOT VALID means the constraint reads as weaker than it is and a future pg_dump/restore or a replay onto a fresh environment carries the same unvalidated state. | lh-schema-integrity | 2 |
| SI-011 |  | fixed | `PROTOCOL.md section 6d/6c accuracy (fleet input data)` | Two facts the whole fleet is working from are wrong, and both were derived from migration history rather than live state -- the exact failure mode the lane definition warns about. (1) PROTOCOL.md 6d lists worker_protection_credits under 'Confirmed LIVE (do not treat as dead just because they look quiet)' and builds finding SI-001 on it as a launch blocker; the table does not exist in prod and was dropped 2026-08-30. (2) PROTOCOL.md 6c states '254 database functions of which 218 are SECURITY DEFINER'; the live prod count is materially lower. Any lane sizing its sampling strategy off 6c is sampling against a number that includes dropped objects. | lh-schema-integrity | 1 |
| AR-003 |  | filed | `jobs / applications / messages — approval_status server enforcement (prod)` | approval_status ('pending' -> 'approved') is enforced only in discovery RPCs and the client ProtectedRoute, never as a write gate. Zero RLS policies and zero write-path functions reference approval_status. A pending-approval account (redirected to /account-pending in the UI) can, via direct PostgREST, POST a job, apply to a job, and send messages. This is arguably by-design (the app is role-less; approval gates helper discovery/quality, not posting), so flagging for a product decision rather than asserting a breach: if 'pending' is meant to block marketplace participation it must be enforced server-side, not only by the route guard. | lh-authz-rls | 1 |
| AR-004 |  | filed | `public.open_jobs_browse view (prod) + Supabase security advisor` | Supabase security linter reports open_jobs_browse as security_definer (its ONLY ERROR-level lint) because the view is security_invoker=false and anon-selectable, so it runs with the owner's rights and bypasses the jobs RLS. VERIFIED this is intentional and safe, not a leak: the view already applies mask_job_location() (street address -> 'City, LA'; confirmed '1234 Rue Beauregard Apt 5B, Lafayette, LA 70501' -> 'Lafayette, LA'), withholds lat/long/zip/parish/stripe_payment_intent_id and every fee column, filters to open+funded jobs past early_access_cutoff(), and the exposed customer_id is inert because profiles has no anon SELECT policy. It is the deliberate public guest browse feed. Recommend adding security_invoker=on (defence-in-depth) so the linter is clean and the view can never over-return if a future column is added; product behaviour is unchanged since anon already has the intended read. | lh-authz-rls | 1 |
| AR-005 |  | fixed | `3 functions with mutable search_path (prod)` | Supabase advisor flags set_profile_view_hour_bucket, redact_audit_snapshot, profiles_locked_update_columns as function_search_path_mutable. Verified live these three are SECURITY INVOKER (prosecdef=false), so the search_path risk is materially lower than for a definer function (they run with the caller's privileges, not the owner's) — this is why my definer-only sweep of 202 SECURITY DEFINER functions correctly found ZERO with an unpinned search_path. Still worth pinning (SET search_path TO '' or 'public') for hygiene and a clean linter. Not a privilege-escalation primitive on their own. | lh-authz-rls | 2 |
| ME-003 |  | fixed | `stripe-webhook checkout.session.completed → job boost activation` | Boost activation write (checkoutSessionCompleted.ts:266-273) has no .select('id'): a zero-row match (job deleted/purged after boost checkout opened) reads as success — poster paid for a boost that never activates, no alert, dedupe row committed so Stripe retry can't repair it. | lh-money-escrow | 1 |
| ME-012 |  | fixed | `supabase/config.toml → auto-tip-charge verify_jwt drift` | auto-tip-charge is the only cron money function with NO verify_jwt=false entry in config.toml (0 grep hits vs 36 entries; the file's own comment explains sb_secret_* bearers are rejected at the gateway when verify_jwt=true). It WORKS today — live prod net._http_response shows its own 200 body ({ok:true,fn:'auto-tip-charge'} latest 2026-09-02 04:07) — but a redeploy that applies config.toml verbatim or a vault key rotation to sb_secret_* kills it silently: the cron's job_run_details would stay green while every tip charge 401s at the gateway. | lh-money-escrow | 2 |
| ME-017 |  | verified | `assorted low-severity money items (batched)` | 1) CancellationDialog.tsx:491 CTA prints unformatted money ('Cancel · pay $18.7'). 2) TipDialog has no local $1-$1000 bound; invalid amounts round-trip to a server toast. 3) CancellationFeePill.tsx:31-35 optional fallbackFeePercent defaults to 10 — latent over-promise for a 12% helper if a future call site omits the prop. 4) instant-payout notifications insert drops its error entirely (index.ts:364-370). 5) reap alerts say 'within the hour' but worst-case reap latency is ~89min (cron :34 + 30min age gate). 6) auto_tip_candidates RPC unpaged (db-max-rows=1000 silent truncation; self-healing). 7) pay-onboarding-fee reads platform_settings with limit(1) and no order (non-deterministic if the table ever grows a second row; currently 1 row live). 8) create-boost-payment idempotency key boost:user:job stable across tier changes for 24h — retry after upgrade hits Stripe idempotency-mismatch surfaced as a generic error; 24h boost duration sits exactly on the key TTL boundary. | lh-money-escrow | 6 |
| CJ-003 |  | verified | `db: retention coverage for append-only tables` | Four append-only tables have no retention sweep of any kind — not an unscheduled one, none written at all: job_views, profile_views, notification_logs, login_history. The only function that deletes from notification_logs or login_history is purge_user_data(), which is the per-user GDPR delete path, not retention. Free tier, so these grow to the 500MB cap unattended. | lh-cron-jobs | 1 |
| CJ-005 |  | fixed | `db: cron_work_expectations coverage` | extend-boosts-hourly is the only one of the 42 scheduled pg_cron jobs with no row in cron_work_expectations, so sweep_dead_crons() — which iterates the expectations table, not cron.job — cannot see it. If it stops firing or starts raising, nothing notices. It has run 2902 times since 2026-05-04 and calls extend_boosts_with_no_applications(), which mutates paid job boosts. | lh-cron-jobs | 1 |
| CJ-006 |  | fixed | `email pipeline: pgmq dead-letter queues` | The dead-letter path records but never drains, and its only monitor is a passive admin tile. process-email-queue moves TTL-expired and max-retry messages into the pgmq queues auth_emails_dlq / transactional_emails_dlq via move_to_dlq(). Nothing anywhere reads those two queues — no cron, no edge function, no script, no admin action — so a message that lands there is lost silently and permanently. The only surfacing is a count of email_send_log rows with status='dlq' rendered on /admin?view=health (useHealthData.ts:27); there is no Slack alert and no error_logs row, so it is only seen if a human opens that page. Currently latent, not active: both DLQs have received zero messages. | lh-cron-jobs | 1 |
| GD-005 |  | fixed | `package.json db:types vs the committed types.ts` | The committed types.ts was NOT produced by the documented command. It carries a graphql_public schema block (repo lines 14-38 plus a Constants entry at ~5788) that 'npm run db:types' - which passes --schema public - does not emit, and its generic signatures lack a parenthesisation the current CLI emits (5 hunks). So the next person to run the documented regeneration command gets ~40 lines of spurious diff unrelated to any schema change, which is exactly the noise that makes a real drift hunk easy to wave past. Nothing in src/, supabase/ or scripts/ consumes graphql_public, so removing it is safe. | lh-generated-drift | 3 |
| NB-003 |  | filed | `ios/App/App/Info.plist UIBackgroundModes` | Info.plist declares UIBackgroundModes: remote-notification, but nothing in the app can service a background push: AppDelegate.swift does not implement application(_:didReceiveRemoteNotification:fetchCompletionHandler:), and neither does Capacitor core (grep over node_modules/@capacitor/ios/Sources for didReceiveRemoteNotification / fetchCompletionHandler returns zero hits). The declaration is therefore inert. It is not currently harmful because no sender emits content-available, but it is a false capability claim in the binary and it would silently swallow the first silent push anyone adds. | lh-native-bridge | 3 |
| NB-005 |  | filed | `Universal Links — /warnings notification deep link` | /warnings is a real route (App.tsx:273, redirects to /profile?tab=warnings) and prod notifications link to it, but it is not claimed in public/.well-known/apple-app-site-association. Tapping one of those notifications opens Safari instead of the app. It is the ONLY unmatched link shape in the entire live census — every other shape either matches a claimed path or is deliberately excluded — so this is the last hole in an otherwise complete AASA. | lh-native-bridge | 3 |
| CJ-010 |  | filed | `cron.job: sweep-pending-broadcast-fan-outs (every 60 seconds)` | THE HIGHEST-FREQUENCY CRON IN THE PRODUCT SERVES A FEATURE THE OWNER CONFIRMED DEAD. sweep-pending-broadcast-fan-outs is scheduled '* * * * *' — once per minute, 1,440 firings a day, 171,495 recorded runs, more than the other 43 jobs combined — and it scans public.broadcast_messages, which has ZERO rows and belongs to the broadcast-messages feature PROTOCOL.md 6d lists as removed (confirmed dead by the product owner 2026-09-01). Every firing opens a transaction, takes a SECURITY DEFINER function's plan, scans an empty table and returns 0. On the Supabase FREE TIER that is the single largest source of scheduler load in the product, spent entirely on a product that no longer exists. It also inflates the denominator of any cron health measurement: sweep_cron_blackouts differences the aggregate cron.job_run_details stream to find dispatch holes, and a per-minute job is what makes that stream dense enough for a 15-minute gap to read as a blackout — so deleting this job without adjusting that sweep would change the blackout detector's sensitivity. SCHEDULE OWNERSHIP IS MINE; the underlying objects (broadcast_messages, broadcast_dismissals, fan_out_broadcast_to_notifications, set_broadcast_pending_fan_out) are removal findings owned by lh-schema-integrity per PROTOCOL 6d, and the unschedule must land in the same migration as the DROP or the cron will error every minute against a missing table. | lh-cron-jobs | 3 |
| NB-009 |  | filed | `Cold launch — splash safety net vs the keychain hydrate cap` | The splash-screen safety net is armed downstream of the very thing it exists to protect against, so its stated guarantee does not hold. nativeInit.ts ends with a module-scope setTimeout(hide, 1500) commented 'if something hangs in initNative(), force-hide the splash after 1.5s so the app can never be stuck'. But main.tsx imports App.tsx on line 2 and nativeInit on line 4, and App.tsx pulls in integrations/supabase/client.ts, which does a TOP-LEVEL 'await hydratePromise' on native (client.ts:28) worth up to HYDRATE_TIMEOUT_MS = 2000ms of Capacitor Preferences bridge I/O. ES module evaluation therefore completes that 2s await BEFORE nativeInit.ts evaluates and arms the timer. Two consequences: (a) worst-case time from launch to a GUARANTEED splash hide is ~3.5s (2000 hydrate + 1500 net), not the 1.5s the comment implies; (b) the net provides no cover at all for a hang during hydration, which keychainStorageAdapter.ts:22-26 explicitly names as 'a real state' since these are the earliest bridge calls in the WebView lifecycle. The 2000ms internal cap on hydrate is what actually bounds that window; the splash net contributes nothing to it. Bounded and non-fatal, but the invariant asserted in the comment is not the one the code provides. | lh-native-bridge | 4 |
| DH-005 |  | filed | `/admin?view=people → user detail → Actions tab; /admin?view=settings → admin list` | The admin console renders two self-destructive controls on the admin's OWN row and refuses both only at the server, after the confirmation. (1) DELETE ACCOUNT: ActionsTab.tsx:168 renders Delete Account unconditionally for whatever profile is open, the admin's own row is not excluded from the people list, and DeleteUserDialog.tsx:44 then gates the submit behind requireBiometric('Confirm permanently deleting this account') — so the admin is asked to authenticate with Face ID / Touch ID for an action supabase/functions/admin-delete-user/index.ts:77 was always going to reject with 400 'Cannot delete your own account'. (2) REMOVE ADMIN: loadAdmins (AdminSettings.tsx:234-240) selects every user_roles row where role='admin' with no self-exclusion, and the Trash2 'Remove admin' button at AdminSettings.tsx:647-655 renders for every row with no self check, opening the full destructive 'Remove This Admin?' confirm before AdminSettings.tsx:333 finally says 'You can't remove yourself as admin.' Both server refusals are correct and must stay — this is defence in depth, not duplication. The missing filters belong upstream: exclude the caller in loadAdmins (or hide/disable the button with the reason), and branch ActionsTab.tsx:168 on viewProfile.user_id === caller so the biometric prompt is never reached. | lh-design-holes | 4 |
| EF-005 |  | filed | `stripe-webhook → Supabase edge function logs` | STRIPE_WEBHOOK_SECRET is written to the function log on every invocation, and every configured signing secret is written on every signature failure. index.ts:64 logs webhookSecret.slice(0,8) plus its exact length unconditionally at the top of the handler; index.ts:153 logs, for EACH comma-separated secret, slice(0,8) and exact length. Stripe signing secrets are prefixed 'whsec_' (6 chars), so slice(0,8) publishes 2 characters of real entropy plus the exact length of each secret - not enough to forge a signature on its own (90+ bits remain), which is why this is LOW and not higher. It is still a signing secret written to a log on every request: Supabase function logs are visible to anyone with dashboard access and are shipped to any configured log drain, so the secret's blast radius becomes 'everyone who can read logs' rather than 'the function runtime'. index.ts:61 logs stripeKey.slice(0,8), which is harmless by comparison - 'sk_live_' / 'sk_test_' is exactly 8 characters, so it reveals only the mode the line says it is revealing, and is worth keeping. Fix is confined to my lane and to one file: drop the prefix/length from :64 and :153, keep the count and the mode. Everything else about this function's verification is correct - it fails closed (a forged or unsigned event returns 200 to stop Stripe retries but never reaches a handler), it supports multiple endpoint secrets, and it has an idempotency guard. | lh-edge-functions | 3 |
| NB-012 |  | filed | `RETRACTION — docs/audit/OPEN_ITEMS.md, two stale claims about src/lib/deepLinkRoute.ts` | RETRACTION, filed at the orchestrator's request. TWO claims in OPEN_ITEMS.md about deepLinkRoute.ts are stale — both were true when written and both were fixed by c538e318, and each is stated as a VERIFIED negative, which is the shape most likely to be believed without rechecking. (1) §1 item 7 (line 129) says 'normalizeDeepLinkUrl drops url.hash ... every branch returns /Users/lexilombas/.deno/bin /Users/lexilombas/.npm-global/bin /Users/lexilombas/.local/bin /opt/homebrew/opt/ruby/bin /opt/homebrew/bin /opt/homebrew/sbin /usr/local/bin /System/Cryptexes/App/usr/bin /usr/bin /bin /usr/sbin /sbin /var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin /var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin /var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin /opt/pmk/env/global/bin /Library/Apple/usr/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/supabase/0.1.6/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/vercel/0.43.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/claude-code-setup/1.0.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/claude-md-management/1.0.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/code-review/unknown/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/code-simplifier/1.0.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/frontend-design/unknown/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/github/unknown/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/sentry/1.0.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/stripe/0.1.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/swift-lsp/1.0.0/bin /Users/lexilombas/.claude/plugins/cache/claude-plugins-official/typescript-lsp/1.0.0/bin' and closes with 'Verified: url.hash appears nowhere in the module.' It appears twice, and every one of the six return branches now appends it: lines 123, 130, 146, 156, 163, 169. git diff origin/main on that file is empty, so this is not a local edit. (2) The UNVERIFIED table item 4 (line 184) says the helpr:// custom-scheme branch 'has ZERO test coverage — deepLinkRoute.test.ts never constructs a helpr: URL.' It does, at line 207: 'keeps the fragment on the helpr:// native return scheme', asserting normalizeDeepLinkUrl('helpr:///payment-success?session_id=cs_1#done'). NOTE the same document's summary table at line 91 already carries the correct conclusion for (1) — 'Real, but not the unblocker for claiming /reset-password' — so the document currently contradicts itself, with the stale detail row outliving the corrected summary row. The real, still-live blocker is refiled separately. | lh-native-bridge | 5 |
| DH-008 |  | filed | `All four browse surfaces — own-resource sweep (the lane's named priority)` | SWEEP RESULT, mostly clean, recorded so it is not re-derived. All four browse definitions checked INDEPENDENTLY against live prod for own-resource exclusion. (1) get_ranked_open_jobs (/jobs) and (2) get_open_jobs_for_map (map): BOTH now exclude own jobs, and both do it correctly — the predicate is 'AND ((SELECT auth.uid()) IS NULL OR j.customer_id <> (SELECT auth.uid()))', which explicitly guards the x <> NULL trap that would otherwise drop EVERY row for a logged-out visitor. The Wave-1 fix anticipated it and says so in a comment. Clean. (3) get_public_open_jobs (landing teaser): no exclusion, deliberate per the owner — a logged-out visitor has no own jobs. Correct. (4) open_jobs_browse (view): NO server-side own-job exclusion — it actively whitelists customer_id = auth.uid(). Its five consumers were checked one by one: useDashboardData.ts:257 (feed, compensated by a client display filter at useDashboardFilters.ts:211), useDashboardJobsCount.ts:77 (compensated, .neq at :99), DashboardGuest.tsx:204 (guests own nothing), JobDetail.tsx:53 and QuickApplyHandler.tsx:108 (NOT compensated — filed as DH-001). So the divergence between the four surfaces converges on exactly one uncovered path, already filed. The residual structural risk is that surface 4's compensation is client-side and per-consumer rather than in the view, so a new consumer inherits the hole by default — see the DH-002 note. | lh-design-holes | 2 |
| NB-014 |  | filed | `helpr:// custom scheme — the stated trust boundary in Info.plist and deepLinkRoute.ts` | The app is SAFE here today, but for reasons other than the ones written down, and the written reason would license an unsafe change. Info.plist says of the helpr scheme: 'Registering it here is also the trust boundary: this scheme is only deliverable to this bundle, which is why src/lib/deepLinkRoute.ts is allowed to route it without the https host allowlist.' deepLinkRoute.ts:80-86 repeats it: 'the scheme is only deliverable to us because it is registered in our own Info.plist, which is the same trust boundary the allowlist provides for https links.' That is not what a custom URL scheme is. Registration controls who RECEIVES helpr:// — it places no restriction whatsoever on who may SEND one. Any installed app can call UIApplication.open on helpr:///..., and any web page in any browser can navigate to it. So it is emphatically NOT equivalent to the https host allowlist, which constrains the sender. What actually keeps this safe is two things neither comment mentions: (a) the /auth/* and /admin path guards at deepLinkRoute.ts:118-120 run AFTER the scheme branch, so they still apply to helpr URLs, and (b) every reachable destination re-reads server state under RLS rather than trusting the URL — I verified the highest-value one, PaymentSuccess.tsx, which ignores params for its verdict entirely and polls jobs.payment_status, resolving to held / not_held / unknown, firing its success haptic only on a confirmed 'held' and performing no writes. A hostile helpr:///payment-success?job_id=<someone else's> therefore hits RLS, reads nothing, and lands on 'unknown'. FILED BECAUSE the stated boundary is the kind a future change leans on: anyone adding a helpr:// route that trusts a query parameter would be doing so on the authority of a comment that is wrong. Fix is to correct both comments to name (a) and (b) as the real controls. | lh-native-bridge | 4 |
| ME-019 |  | filed | `sales tax - service-fee taxability reversed, unresolved` | LA sales-tax treatment of the poster service fee reversed and the current position is self-described as provisional. In April BOTH line items were taxed at the Delcambre rate ~11.2 pct; today create-payment marks the service fee txcd_00000000 with the comment 'Non-taxable until LDR clarifies B2C SaaS treatment post-Act 470'. One of the two was wrong. Exposure is roughly 1.3 pct of every job budget in potentially uncollected LA tax, accruing from launch and payable by the platform if the determination goes the other way. Needs a CPA determination, not a code change - filed because it is a live money path with real remittance exposure and no named owner. | lh-money-escrow | 4 |
| EF-011 |  | fixed | `cron.job 'extend-boosts-hourly' → the one cron with no dead-cron monitoring` | extend-boosts-hourly is the ONLY active cron in prod - of 44 total, 22 function-backed and 22 SQL-only - that is not registered in public.cron_work_expectations, so sweep_dead_crons cannot ever flag it. That function only considers jobs present in cron_work_expectations, so an unregistered cron is not 'healthy', it is unwatched: if it stops firing or starts erroring, nothing notices and nothing is written to error_logs. It runs SELECT public.extend_boosts_with_no_applications() hourly and touches a PAID feature - job boosts are one of the four live money systems per PROTOCOL 6d - so a silent death means posters who paid for a boost stop getting the extension they bought, with no signal to anyone. It is currently healthy (2916 runs, 0 non-succeeded, last fired 2026-09-02 18:00:00Z), which is exactly why this is LOW and worth fixing cheaply now rather than discovering later: the fix is one INSERT into cron_work_expectations with an expected_max_gap of about 03:00:00, matching the sibling hourly jobs sweep-expired-auto-bans and reap-stranded-instant-payouts. Every one of the other 21 SQL-only crons is registered, so this is a one-off omission and not a design gap. NOTE the monitoring it would join is currently only half-effective anyway - see EF-008: the error_logs row would be written but the Slack page would be discarded, because sweep_dead_crons alerts via the slack-ops-alert edge function whose SLACK_API_KEY is unprovisioned. Both should be fixed; neither substitutes for the other. | lh-edge-functions | 1 |
| CV-002 |  | fixed | `src/pages/postjob/EntryChoice.tsx` | EntryChoice's docblock describes 'three calm, liquid-glass cards' and enumerates three options (start fresh / draft / template) while SIX ship: those three plus Repost a Recent Job, the AI Job Builder and Offer to a Saved Helpr. The numbered section comments also label two different sections '5'. A reader taking the docblock at face value audits half the screen — which is close to what happened, since none of the six was in any lane's scope. | main | 2 |
| N-008 |  | fixed | `Notification centre → tapping a notification for a deleted job` | A third of all notifications point at a job that no longer exists, and the copy shown tells the user to come back and try again — permanently false. 581 of 1773 prod rows (33%) carry a job-shaped link whose job row is gone; 474 of those are job_match rows linking to /dashboard?quickApply=<id>. QuickApplyHandler resolves the miss and toasts 'This task isn't available to open YET — if you just got the alert, try again in a few minutes.' That copy was deliberately written for the Early-Access delay case, but it is also the only copy a permanently-deleted job produces. | lh-notifications | 2 |
| LTF-002 |  | verified | `Scope correction — job_milestones / job_scope_items` | The launch-audit brief for this lane names job_milestones, auto_approve_milestone, job_scope_items and set_revision_deadline as live surfaces to audit. None exist in prod. This is the same shape as the retracted SI-001/003/004/005 (a lead built from names in a brief, not from live state) and is filed so it is not re-derived by a future pass. | lh-long-tail-features | 2 |
| LTF-003 |  | fixed | `pet_report_cards table — orphaned by today's client-side removal` | The pet report card feature was removed from src/ today (2026-09-02), including its public-profile trust badge. pet_report_cards still exists as a live table in prod with no client reference anywhere (grep -rln pet_report_cards src/ matches only the generated types.ts). Relay to lh-schema-integrity for a removal/drop decision — outside this lane's territory to fix. | lh-long-tail-features | 1 |
| PD-003 |  | filed | `npm audit (production dependency tree)` | npm audit reports 6 vulnerabilities (1 low, 4 moderate, 1 high) but all 4 flagged packages (fast-uri, xmldom, postcss-selector-parser, uuid) are transitive BUILD-TIME-ONLY tooling deps (Capacitor CLI/xcode, tailwindcss/postcss, vite-plugin-pwa/workbox-build) -- none ship in the client bundle. fast-uri/xmldom/postcss-selector-parser are fixable non-breaking via 'npm audit fix'; uuid requires --force which bumps @capacitor/cli to 8.4.3 (a breaking change) -- reporting only per standing instruction not to bump deps this run. | lh-perf-deps | 2 |
| PD-004 |  | filed | `Dead/duplicate exports (knip)` | npm run deadcode (knip) reports zero unused FILES and zero unused/unlisted dependencies (clean), but a real unused-EXPORTS backlog: 90 unused exported values/functions, 11 unused exported types, and 31 files with a duplicate default+named export of the same symbol (e.g. HelprMark\|default, ProfileEditForm\|default, BrandConfirmDialog\|default). These are warnings-only in CI per CLAUDE.md and don't affect bundle size (tree-shaking removes them), but the duplicate-export pattern is a maintenance/clarity smell worth a cleanup pass. npm run deadcode:functions is separately clean: 67 edge functions checked, 1 known-unreferenced (already accounted for), 0 new. | lh-perf-deps | 4 |
| SW-002 |  | verified | `index.html canonical link + robots.txt` | Canonical href for every non-share-rewritten route (as served, pre-JS) points at the bare homepage (https://www.louisianahelpr.com), not the requested URL — this is worse than 'no route-specific canonical', it is an actively wrong self-referencing signal telling Google every one of these pages is a duplicate of the homepage. Combined with SW-001, a crawler that does not run JS (or crawls before JS executes) can be told to canonicalize /legal, /help, /support etc. into the homepage. | lh-seo-web | 1 |
| SW-004 |  | fixed | `Index.tsx (homepage) BreadcrumbList JSON-LD` | The homepage ('/') injects a schema.org BreadcrumbList claiming Home -> Legal, which is fabricated: the homepage has no breadcrumb trail to /legal, and no other page defines or reuses this schema. It is the only JSON-LD injected client-side anywhere in src/ (grep for application/ld+json under src/ matches only Index.tsx). Reads as copy-pasted boilerplate never adapted to the page it landed on. | lh-seo-web | 2 |
| CS-005 |  | filed | `ios/App/App/Info.plist — NSContactsUsageDescription` | NSContactsUsageDescription is declared for an API the app never calls and cannot call: no contacts plugin is installed, and no CNContactStore reference exists in any of the 16 linked plugins or the 13 transitive SPM checkouts. The string promises a contacts-based invite feature that does not exist. | lh-compliance-store | 2 |
| CS-006 |  | filed | `ios/App/App/Info.plist — NSFaceIDUsageDescription` | NSFaceIDUsageDescription is narrower than actual use: it says Face ID confirms identity 'before cashing out your earnings', but the same biometric gate also unlocks the whole app after inactivity and gates admin actions (ban user, delete user, change login email) that have nothing to do with payouts | lh-compliance-store | 3 |
| CS-008 |  | fixed | `docs/audit/launch-2026-09/PROTOCOL.md §6d — scope correction` | PROTOCOL §6d lists 'Broadcast messages' (broadcast_messages, broadcast_dismissals, fan_out_broadcast_to_notifications, send-marketing-blast) as a REMOVED feature whose objects are removal findings. That is wrong: all four exist in LIVE PROD and the feature is fully wired. Lanes acting on §6d would file false removal findings or delete a live feature. | lh-compliance-store | 2 |
| CS-009 |  | filed | `ios/App/App/Info.plist — UIBackgroundModes` | UIBackgroundModes declares 'remote-notification' but nothing implements application(_:didReceiveRemoteNotification:fetchCompletionHandler:) — neither AppDelegate.swift nor the Capacitor push plugin — so the background-fetch capability the mode grants is unused. Low rejection risk (near-universal for push apps) but it is a declared capability with no backing code. | lh-compliance-store | 2 |
| OBS-004 |  | fixed | `vite.config.ts:399-403 -- comment claims oxc strips console in prod; it does not` | vite.config.ts:401-402 says 'Console stripping is handled by oxc automatically in production mode.' This is not true of oxc's default minify behavior (minify: "oxc" with no drop_console/pure_funcs equivalent configured) -- oxc, like esbuild and terser, does NOT strip console.* calls unless explicitly told to. Verified by building dist/ and finding console.error/console.warn calls throughout first-party app chunks (AdminUsers-*.js: 22 console.error calls, e.g. useAdminUserSummaries logging raw Supabase error objects; similar in Messages, UserProfile, AdminSettings, PostedJobsTab, TrackingMap chunks) -- if oxc were dropping console output, none of these would survive. The reason the app is NOT currently shipping verbose console.log/debug/info noise is a different, real mechanism: every first-party console.log call found in src/ (useAuthReady.ts, ProtectedRoute.tsx, useCurrentUser.ts, Jobs.tsx) is gated behind a DEBUG_AUTH = import.meta.env.DEV constant, which Vite's define/dead-code-elimination removes at build time -- verified zero occurrences of those call sites in dist/. So the practical outcome today is clean (no first-party console.log/debug/info reaches production), but it is clean by developer discipline (DEV-gating), not by the minifier config the comment credits, and any FUTURE unguarded console.log added to the codebase would ship straight to production with nothing to catch it. Recommend either fixing the comment to describe the DEV-gating mechanism accurately, or adding an explicit oxc/esbuild drop_console-equivalent config for defense-in-depth (belt-and-suspenders against a comment-invariant class of bug this same audit standard warns about by name). Also worth a look separately: console.error calls in AdminUsers/admin chunks print raw Supabase error objects with internal query/table/column names to the browser console on an admin-only page -- low risk since it requires admin access already, noted for completeness rather than filed as its own finding. | lh-observability | 4 |
| VC-007 |  | filed | `/admin?view=exceptions -> verification_exceptions` | Granting a verification exception - a manual override on the safety gate - is audit-logged only by the CLIENT, so the log is advisory rather than binding. AdminExceptionQueue.tsx:151-165 resolves an exception by UPDATEing verification_exceptions directly through PostgREST, then separately calls logAdminAction() at :166. The RLS policy 'Admins can manage verification exceptions' is ALL / has_role(admin) with no predicate tying a write to an audit row, so any admin token can PATCH the table directly and leave no admin_audit_log entry at all. Contrast review_credential(), the sibling override for license and insurance, which is a SECURITY DEFINER RPC that writes the admin_audit_log row inside the same transaction as the decision - that one cannot be bypassed. This is the weaker half of an otherwise consistent pattern. Note it is currently unexercised: verification_exceptions has 0 rows in prod and its only writer is verification-webhook, which is unreachable (see VC-004). | lh-verification-credentials | 1 |
| ST-011 |  | filed | `saved_searches → notify_saved_searches_on_new_job digest path` | The saved-search anti-spam throttle is applied to the DIGEST path as well as the immediate-notification path, so a digest-mode helper can receive at most one matching job per saved search per hour in a digest that is only sent once a day. The matching query filters on 'AND (s.last_notified_at IS NULL OR s.last_notified_at < now() - interval 1 hour)', and the loop body stamps last_notified_at = now() for every contributing search BEFORE branching on digest_mode. On the digest branch the helper is not notified at all — the match is parked in match_digest_queue for daily-match-digest to drain — yet the hour-long throttle is spent all the same. A second matching job posted 10 minutes later does not match the search, so it never reaches the queue and never appears in the digest. Throttling exists to stop notification spam; a daily digest is already the batching mechanism, so applying the same throttle to it silently drops matches the helper explicitly asked to be told about. The stamp belongs on the immediate branch only. | lh-scheduling-time | 4 |
| A-004 |  | filed | `public/_headers (dead config)` | public/_headers declares a full security header set including its own Content-Security-Policy, X-Frame-Options and frame-ancestors, but Vercel does NOT read _headers (that is a Netlify/Cloudflare Pages convention) - the file is inert. PROVEN by diffing the live response against both candidates: the served CSP matches vercel.json BYTE-FOR-BYTE, and uniquely lacks api.resend.com while carrying cdn.apple-mapkit.com and nominatim.openstreetmap.org - i.e. exactly vercel.json's list, not _headers'. The two policies have already drifted. Risk is a future hardening edit made in _headers that silently changes nothing in production. | lh-appsec | 3 |
| A-005 |  | filed | `served CSP → connect-src` | The live CSP's connect-src allows https://api.openai.com, but the browser never contacts OpenAI: the only AI path is the ai-job-builder EDGE function, which calls https://generativelanguage.googleapis.com server-side with a server-held GEMINI_API_KEY. The allowance is stale (a comment in vite.config.ts still cites 'OpenAI' as a reason the meta CSP is stripped on web) and needlessly widens the policy - an exfiltration destination granted for a client call that does not exist. | lh-appsec | 3 |
| A-007 |  | filed | `admin → user detail; helper public profile` | profiles.portfolio_urls and avatar_url are client-writable free-form text (verified on LIVE prod: authenticated holds UPDATE on both, and prevent_self_escalation does NOT pin them), and 14 render sites emit them into <img src={url}> with no scheme or host validation. This is NOT stored XSS - see the retraction note - but an attacker-controlled absolute URL to a host they own is loaded automatically whenever a victim (notably an ADMIN opening the user-detail row) renders the record. That is a tracking pixel: it leaks the admin's IP and User-Agent and discloses exactly when their account is under review. The served CSP does not stop it - img-src allows the https: wildcard. | lh-appsec | 3 |
| A-008 |  | filed | `send-marketing-blast → marketing-blast email template` | marketing-blast.tsx:20-36 carries an explicit comment asserting its dangerouslySetInnerHTML is safe because bodyHtml is admin-authored, 'HERE AND NOWHERE ELSE'. That invariant is false: send-marketing-blast/index.ts:449 performs rawHtml.replaceAll('{{name}}', fullName \|\| 'neighbor') BEFORE handing the string to the template, and fullName is profiles.full_name - user-set, client-writable, and unescaped. A display name containing markup is spliced raw into the campaign HTML. Impact is confined to the attacker's own inbox (the blast personalises per recipient), so this is low severity - but the file's stated safety argument no longer holds and the fix is to escape the substituted value, not the body. | lh-appsec | 2 |
| AM-004 |  | filed | `/admin?view=audit (AdminAuditLog) · public.audit_role_changes()` | The Audit Log view is 44% non-admin noise. 89 of 202 admin_audit_log rows in prod are action='role_granted' written by the audit_role_changes trigger on every ordinary signup's default role grant — not an admin action by anyone. When auth.uid() is null those rows are attributed to the sentinel admin_id 00000000-0000-0000-0000-000000000000. Add role_revoked (39) and the trigger accounts for 128 of 202 rows, so the surface an admin opens to answer 'who did what to whom' is dominated by automated signup bookkeeping. | lh-admin-moderation | 2 |
| AM-007 |  | fixed | `public.enforce_audit_log_self_attribution() — trigger on admin_audit_log` | The audit-log self-attribution guard is a no-op on exactly the writes it looks like it protects. Its body is 'IF auth.uid() IS NOT NULL AND NEW.admin_id IS DISTINCT FROM auth.uid() THEN RAISE ...', so it only bites when a session uid is present. Every admin EDGE FUNCTION (admin-user-actions, admin-delete-user, admin-update-email, admin-resend-verification, admin-test-push, execute-dispute-split) writes its audit row through the service-role client, where auth.uid() is NULL — so for all of those the trigger passes unconditionally and admin_id is whatever the function chose to put there. The same is true of audit_money_table_change() and audit_role_changes(), which is why 89 prod rows are attributed to the 00000000-… sentinel. NOT currently exploitable: the RLS INSERT policy on admin_audit_log requires has_role(auth.uid(),'admin'), so a non-admin cannot insert at all, and forging requires the service-role key. Filed so the guard is not mistaken for stronger than it is. | lh-admin-moderation | 1 |
| TS-013 |  | filed | `apply rate limit` | The apply rate limit that shipped is a single 15-per-24h cap with NO burst limit, not the 10/min + 50/hr + 200/day ladder recorded as the 2026-06-09 product decision. Net daily allowance is far stricter than decided, so this is not an abuse hole, but a helper can still fire all 15 applications within seconds and the decision and the implementation do not describe the same control. | lh-trust-safety | 1 |
| S-006 |  | filed | `prod platform_settings.feature_flags (fncmgoasalhdgfwzhsqa)` | Four kill-switches sit in prod feature_flags reading false - boosts_enabled, referrals_enabled, subscriptions_enabled, ai_helpr_assistant - and NOTHING in src/ or supabase/functions/ reads any of them. The code side was cleaned up (AdminSettings KNOWN_FEATURE_FLAGS is down to idv_requirement_paused alone, with a comment explaining why the other four were deleted rather than wired), but the stale keys were never removed from the live row. The row was last updated 2026-05-02. Anyone reading prod config to answer 'are subscriptions on?' gets false, while subscriptions are in fact fully sellable against live Stripe Price IDs. Low severity because nothing reads them; filed because the misleading direction is the dangerous one. | lh-suggester | 1 |
| CC-004 |  | filed | `app boot with IndexedDB blocked (src/lib/queryPersister.ts:30-41)` | When IndexedDB is unavailable (Safari private browsing, site data blocked, some embedded webviews) the React Query persister raises an UNCAUGHT error on every cold load. idbAsyncStorage.getItem/setItem/removeItem each call idb-keyval's get/set/del with no try/catch, and nothing upstream absorbs it. The app still boots and works — persistence is a nice-to-have — but every affected session emits an unhandled rejection that Sentry will capture, so a cosmetic degradation is indistinguishable in the dashboard from a real crash. Contrast the sibling removePersistedClient() at :120-136, which DOES try/catch for exactly this reason and says so in its comment; the three storage adapter methods just never got the same treatment. | lh-concurrency-cache | 3 |
| CC-005 |  | filed | `React Query retry policy (src/lib/queryClient.ts:33-42)` | Query retries are exponential but have NO JITTER, so a Supabase blip makes every client in the field retry in lockstep. The QueryClient sets a custom retry() predicate but never sets retryDelay, so TanStack v5's default applies: Math.min(1000 * 2**attemptIndex, 30000) — deterministic 1s then 2s, identical on every device. Combined with refetchOnWindowFocus:true, a brief outage produces a synchronised 3x request spike aimed at the service that just failed. Bounded rather than dangerous (retry caps at 2 attempts and mutations are explicitly retry:0), which is why this is LOW and not a blocker — but the app already knows the right shape: src/lib/realtimeRecovery.ts:238-247 jitters its own backoff with 'base + Math.floor(Math.random() * 400)' and the file's header comment calls jitter out by name as a requirement. The two backoff policies in the codebase disagree with each other, and the React Query one is the one facing the whole user base. | lh-concurrency-cache | 3 |
| AL-011 |  | filed | `Deleted-user fallback copy — 16 different strings, and an internal token shown to users` | Two cohesion defects in how a departed user is named. (a) There are 16 distinct fallback strings for the same state. Consumer surfaces say 'A neighbor' / 'a neighbor' (a case split), except Messages+Activity which say 'Helpr', Group Jobs which alone says 'Former Helpr' (GroupJobHelpers.tsx:20 DEPARTED_HELPER_LABEL — the only consumer string that names the state honestly), and the exported home record which says 'Name not on file' (HomeHistory.tsx:208). Admin is worse: six labels for the same state ('Unknown', 'Unknown Helpr', 'User', 'em-dash', 'Unnamed', 'No name') plus two that DO name deletion ('Deleted user' AdminJobs.tsx:191, 'Deleted account' AdminReferrals.tsx:129) — and eight admin dialogs silently inherit the CONSUMER default 'A neighbor' by calling formatName() without a second argument. An admin cannot tell 'deleted account' from 'lookup failed' from 'never set a name'. This is the direct downstream cost of AL-006 (nothing reads anonymized_at). (b) Two concrete render bugs: giftCards/CreditCard.tsx:17 does (credit.donor?.full_name ?? 'A neighbor').split(' ')[0] so a gift card from a deleted donor literally reads 'from A'; and AdminReferrals.tsx:121 falls through full_name then email — both NULL after anonymisation — to p.user_id.slice(0,8), rendering an 8-character raw UUID as a person's name. | lh-account-lifecycle | 2 |
| AL-012 |  | filed | `Redacted job description — the internal token '[removed at account deletion]' is user-visible copy` | purge_user_data rewrites a retained job's description to the literal '[removed at account deletion]'. Nothing in src/ special-cases that string — grep returns zero hits outside SQL — so it is printed raw wherever a redacted job renders, and the participants (the assigned helper, applicants) still see those jobs even though browse hides them. Render sites: AppliedJobCard.tsx:150, PostedJobCard.tsx:86, JobDetailDialog.tsx:427,432, JobStatTiles.tsx:63 (.slice(0,200)), and EditJobDialog.tsx:71,127 which PRE-FILLS the edit textarea with the token. JobDetail.tsx:93 feeds it to usePageMeta ogDescription, so a shared link's social preview would read '[removed at account deletion]' (not reachable today — JobDetail reads open_jobs_browse, which excludes ownerless jobs). adminJobsHelpers.ts:43,72,73 additionally runs the spam heuristic's all-caps ratio over the token. A bracketed internal marker is not consumer copy; the surface should render something like 'This job's details were removed when the poster closed their account.' | lh-account-lifecycle | 3 |
| BR-009 |  | filed | `VITE_APPLE_MAPKIT_TOKEN — build-time MapKit fallback token baked into every bundle` | The MapKit fallback token compiled into the client bundle expires 2027-02-14 and nothing watches it. useMapKitJs asks the mapkit-token edge function for a short-lived origin-locked token and falls back to the build-time VITE_APPLE_MAPKIT_TOKEN. The primary path is currently HEALTHY — POST to the mapkit-token function returns HTTP 200, so the 2026-08-27 '503 not_configured' defect is fixed — which means this is a fallback and not a live outage. But the fallback is a static ES256 JWT inlined at build time with iat 2026-08-18 and exp 2027-02-14T04:43:25Z (165 days from today). When it lapses the fallback silently stops working, and because it is baked in at build time, restoring it requires rotating the repo secret AND cutting a new build — for iOS that means an App Review cycle. No CI check, alarm or test asserts remaining validity. Worth either a expiry assertion in CI or dropping the build-time fallback now that the edge function is healthy. Filed LOW deliberately: nothing is broken today and the primary path works. | lh-build-release | 4 |
| OA-016 |  | filed | `/reset-password (already-used link copy)` | The "this link has already been used" message can never be shown. ResetPassword.tsx:93-101 branches on the error_description GoTrue puts in the hash: it tests /expired/i FIRST and only falls to the /invalid\|used/i branch if that misses. GoTrue emits ONE description for both cases - "Email link is invalid or has expired" - and that string contains the word expired, so the first test always wins and linkError is always "expired". The distinct copy at :196-198, "This password-reset link has already been used. Request a new one if you still need to change your password.", is unreachable dead code.

Graded LOW deliberately, not inflated: the expired copy that does render - "Reset links are single-use and time-limited - request a fresh one below" - already tells the user both things and offers the right button, so real user harm is close to zero. It is filed because the comment at :82-90 explicitly claims the screen distinguishes three cases when it distinguishes two, which is the kind of confident-but-wrong comment that misleads the next reader. Either drop the used branch and its comment, or branch on error_code (which IS distinct enough to matter only if GoTrue ever separates them - today it sends otp_expired for both). | lh-onboarding-auth | 2 |
| EJ-009 |  | fixed | `/post-job form — draft autosave under interruption` | The flush-on-teardown safety net has a hole exactly where its own comment says there is none. useDraftJob registers beforeunload + visibilitychange:hidden handlers and explains them as: 'without these, the last few seconds of typing inside the debounce window are lost.' But flushDraft can only write what pendingDraft holds, and pendingDraft is populated by saveDraft - which the form does not call for a further 2 seconds (useJobFormEffects.ts:352-355 wraps autoSave in its own setTimeout(2000), on top of useDraftJob's SAVE_DEBOUNCE_MS of 5000). So the two debounces stack to ~7s to durability, and for the first ~2s of typing the flush runs with savedAt === 0 and returns early having saved nothing. Anything typed is lost if the app is torn down inside that window. | lh-e2e-journeys | 1 |
| VC-014 |  | fixed | `/dashboard vs /my-jobs, /my-posts, /messages (the four PageScaffold siblings)` | The screen every session opens on is the only primary tab with no visible page title, and the heading it does have is off-system in family, size and weight. My Jobs, My Posts and Messages each paint a real h1 at 20px Bodoni Moda, weight 700, letter-spacing -0.4px, 77-88px wide at x=41 y=33. The Dashboard's h1 is 16px Montserrat weight 400 in a 1x1px box — a visually-hidden heading, not a painted one — and it reads 'Browse Jobs', a name that appears nowhere on the screen, does not match the bottom-nav label ('Home'), and does not match the route (/dashboard). So the home screen has three names and no visible one, while its three siblings each state theirs in the same type at the same coordinates.  [RE-FILED from VC-004, which collided head-on with lh-verification-credentials' VC-004; see the note on that id.] | lh-visual-critic | 3 |
| VC-015 |  | filed | `/my-jobs (empty state) vs /my-posts, /messages` | My Jobs stacks three glossy CTAs in one column while its two canonical siblings show exactly one. Measured on the empty state: 'Enable' (push-permission banner, linear-gradient olive), 'Show Waiting (3)' (empty-state CTA, radial-gradient olive) and 'Post a new job' (FAB, radial-gradient olive) all carry the app's primary gloss simultaneously, two of them ~460px apart in the same vertical column. /my-posts and /messages each measure exactly one glossy control (the FAB). The standard is one unambiguous primary action per screen with the eye landing on it first; here the topmost and most saturated of the three is a notification-permission ask, not the task action, so the screen's actual next step ('Show Waiting (3)') is the second thing the eye finds.  [RE-FILED from VC-005, which collided head-on with lh-verification-credentials' VC-005; see the note on that id.] | lh-visual-critic | 2 |
| VC-017 |  | fixed | `The three shared popup close (X) controls: DialogContent vs AlertDialogContent vs SheetCloseButton` | The one control every popup in the app shares is three different controls. Box: Dialog 44x44 (explicit inline style, dialog.tsx:352), AlertDialog 32x44 (w-8 + the global button min-height, alert-dialog.tsx:191 — filed as VC-006), Sheet 40x40 (h-10 w-10, sheet.tsx:147). Glyph: Dialog 18px alone / 16px when it sits beside other chrome icons, AlertDialog 18px always, Sheet 20px always — so a sheet's X draws 25% larger than a dialog's X in the icon-row case, which is the same 12.5%-heavier complaint that was just fixed on the dialog side. Colour: Dialog and AlertDialog use text-muted-foreground; Sheet hardcodes hsl(var(--olivewood)). Focus: Dialog uses focus-visible:ring (dialog.tsx's comment records this as an explicit owner instruction on 2026-08-31, 'there shouldn't be a box around it when it's clicked'), AlertDialog uses focus-visible:ring, and SheetCloseButton still uses plain focus:ring — so mouse-clicking a sheet's X draws the ring the owner asked to be removed, while mouse-clicking a dialog's X does not. Press feedback also differs: btn-press on both dialogs, active:scale-[0.94] on the sheet.  [RE-FILED from VC-007, which collided head-on with lh-verification-credentials' VC-007; see the note on that id.] | lh-visual-critic | 3 |
| AS-005 |  | fixed | `/signup-pending — src/pages/SignupPending.tsx step-number badge, DARK MODE` | The numbered step badge paints its numeral hsl(var(--bark)) inside a hsl(var(--bark) / 0.1) tint. In dark mode that 10% wash darkens the ground under the numeral enough to take it from 4.95:1 on the bare card to 4.21:1 inside the badge, under the 4.5:1 this 15px/700 text needs. A badge's own tint darkening its own label is only visible to a measurement of the composited pixels. | lh-a11y-sensory | 4 |
| GD-010 |  | filed | `src/pages/Support.tsx:54, supabase/functions/contact-support/index.ts:7-8,313` | Three comments justify the guest-report gate with 'reports.reporter_id is a NOT NULL uuid'. reporter_id is nullable in prod as of 20260902051631. The CONCLUSION still holds — anon does hold table INSERT on reports, but the only INSERT policy is WITH CHECK (auth.uid() = reporter_id) and for anon auth.uid() is NULL, so the check evaluates NULL and fails — but the stated reason is now false, and the next reader who trusts it will reason from a guarantee the schema withdrew. Not a security hole; verified 0 null reporter_id rows in prod. | lh-generated-drift | 1 |
| NB-020 |  | filed | `push_tokens.app_version / window.HELPR_BUILD` | push_tokens.app_version can never identify the build that registered a device, and the comment above it claims it can. src/main.tsx:24 sets window.HELPR_BUILD = "2026-05-04-editorial-brand-polish" — a string literal, not a build-time injection. src/lib/nativePush.ts:90-96 reads it into every push_tokens row under the comment 'This avoids the previous hardcoded 1.0.4 which would silently lie after every iOS rebuild.' It replaced one hardcode with another four months older, so the comment asserts the opposite of what the code does. Consequence: the first token that ever lands will be stamped with a May-2026 string, and the exact question the push investigation needs — which build registered this device — cannot be answered from the table. error_logs has no app_version column either (verified via information_schema), so there is currently NO version telemetry anywhere in prod. Fix: inject at build time via Vite define from the git SHA or CURRENT_PROJECT_VERSION. | lh-native-bridge | 1 |
| PD-007 |  | filed | `vite.config.ts:445-464 (retraction of my own first-pass fix proposal)` | RETRACTION of PD-001's fix option (a). My first pass proposed narrowing resolveDependencies so the entry's static graph keeps its modulepreload hints. Measurement says that would be a large NET REGRESSION: injecting the 77 modulepreload tags for the true closure moves FCP 0.70s -> 3.22s and LCP 1.14s -> 5.29s on Slow 4G, because 77 highest-priority preloads saturate a 1.6Mbps pipe and starve the first paint. Only appReady improves (3.74s -> 3.26s) and serial request depth (11 -> 6). Tested with the tags both BEFORE the module script and at the very end of <head> (after the CSS links) - same result, so it is not an injection-order artefact. The current setting is correct in effect; only its comment and the CI grep need fixing. | lh-perf-deps | 1 |
| PD-011 |  | filed | `src/lib/socialAuth.ts:15 -> src/lib/nativeInit.ts:5 -> src/main.tsx (cold start, every user, web + native)` | @capgo/capacitor-social-login ships 41.2KB raw / 11.1KB gz in the cold-start critical path via a fully STATIC import chain: main.tsx imports initNative from lib/nativeInit, which statically imports lib/socialLogin -> lib/socialAuth, which statically imports SocialLogin from @capgo/capacitor-social-login. It emits as twitter-provider-*.js (31.0KB/8.0KB gz) + web-*.js (10.2KB/3.1KB gz), both inside the entry's static-import closure. On the WEB surface it is 100% dead weight - initSocialLogin() returns immediately on !Capacitor.isNativePlatform(); on native it is only needed when the user taps Apple/Google sign-in. Making that one import dynamic inside initSocialLogin removes it from every cold start. | lh-perf-deps | 1 |
| PD-012 |  | filed | `src/components/mobileNav/useNavUnreadCount.ts:49-61 and src/components/DesktopSidebarNav.tsx:134` | The nav unread badge fetches EVERY unread message ROW for the user (select job_id, sender_id, created_at) with no .limit() and counts them client-side, and the same unbounded query is duplicated in two components that both mount on authed screens. Harmless today (prod message volume is tiny) but it is unbounded by construction: an inactive user with thousands of unread messages pays a proportional payload + JSON parse on the main thread every time the badge refreshes. Same shape as PD-002 from the first pass (useActivityData with no .limit()). | lh-perf-deps | 2 |
| PD-013 |  | filed | `package-lock.json / npm audit` | Dependencies: 4 moderate vulnerabilities (down from 6 last pass), 0 high/critical. All four are build-time-only paths: @capacitor/cli (direct) -> xcode -> uuid@7.0.3 (missing buffer bounds check), and fflate. CORRECTION to my first pass, which said 'none reach the client bundle': fflate DOES ship, via jspdf (dist/assets/jspdf.es.min-*.js) and via posthog-js's pinned fflate@0.4.8. The advisory is an infinite loop in unzipSync on malformed ZIP64, and grep for unzipSync in the emitted jspdf chunk returns 0 - jspdf uses fflate for deflate only - so the vulnerable function is not reachable. Drift: 26 packages behind lock, 10 of them a major version (react/react-dom 18.3.1 -> 19.2.8, tailwindcss 3.4.19 -> 4.3.3, typescript 5.9.3 -> 7.0.2, vitest 4.1.11 -> 5.0.0, framer-motion 12.43 -> 13.2, react-leaflet 4.2.1 -> 5.0.0). The uuid fix requires a semver-major @capacitor/cli 8.4.3 bump. Also note jsdom is locked at 30.0.1 while the registry latest is 29.1.1 - the lock is AHEAD of latest, worth a look. | lh-perf-deps | 1 |
| BR-021 |  | filed | `public/app-icon-1024.png, public/app-icon-1024-dark.png — deployed to the web origin` | 1.3 MB of iOS-only marketing icons are deployed to the public web origin referenced by nothing. app-icon-1024.png (645 KB) and app-icon-1024-dark.png (651 KB) are copied from public/ into dist/ by every build and uploaded to Vercel, but zero built files reference them. They cost deploy storage and bandwidth-on-discovery, not page weight (no page fetches them), and they cannot simply be deleted: scripts/verify-ios-bundle.mjs fails the iOS build when the marketing icon is absent or under 100 KB. Filed LOW because nothing is broken for users; the fix is to source the iOS icon from a path the web build does not copy, rather than to remove the file. | lh-build-release | 2 |
| DH-018 |  | fixed | `/post-job — 3-step wizard → submit (useJobSubmit.ts:333-339)` | The 5-open-job cap is enforced only at SUBMIT, after the poster has completed the entire three-step wizard (Details, Logistics, Budget). Nothing upstream tells a poster who is already at the cap that they cannot post: the Post CTA renders normally, the wizard opens, every field accepts input, and the refusal arrives at the end. The guard itself is correct and well built — it pre-checks the count client-side BEFORE the jobs INSERT and BEFORE create-payment, so no money is at risk and the copy is human ('You can have a maximum of 5 open jobs at a time. Close or wait for existing jobs first.'). The defect is purely where it fires: the state is knowable at wizard entry, so the cost of being at the cap should be a disabled CTA with the reason, not a discarded form. Fix belongs upstream at the Post entry point (same count query, run on mount), leaving the submit-time check and the server trigger in place as defence in depth. | lh-design-holes | 5 |
| DH-019 |  | fixed | `helper_preferred_parishes — a ranking input with no way to set it` | The table helper_preferred_parishes is READ by job ranking but is writable by nothing, so it is permanently empty and the ranking path that depends on it is dead for every user. There is no client write site (zero references in src/ outside the generated types.ts and one admin health check that only counts rows), and no edge function writes it. Its guard trigger enforce_parish_limit ('You can select up to 5 home parishes') therefore protects a table no user can add a first parish to, let alone a sixth — a refusal nothing can reach, which reads as a live feature to the next reader. Meanwhile get_ranked_open_jobs consults it and falls back to the single profile parish (migration 20260506020000_get_ranked_open_jobs_fallback_to_profile_parish), so in practice EVERY helper is permanently on the fallback path and the multi-parish ranking branch has never executed in production. Two separable outcomes, neither of which is mine to choose: either the 'home parishes' picker was never built (a product gap — helpers in this state cannot express coverage across the parish lines they actually work) or the feature was withdrawn and the table, trigger and ranking branch are removal findings. Filing so it is decided rather than inherited again. | lh-design-holes | 2 |
| EF-026 |  | fixed | `supabase/config.toml + deployed auto-tip-charge` | auto-tip-charge (a money path - charges saved cards off-session) is deployed verify_jwt=false but has no [functions.auto-tip-charge] entry in config.toml, while all 12 of its cron siblings do. config.toml is the DEPLOY-TIME source of truth for the gate (functions-deploy.yml applies it), so the current open gate is unreproducible from the repo and depends on deploy history. Rated LOW, not MEDIUM: I checked whether the next redeploy would flip it to true and kill the cron, and it would NOT - cron.job builds its header from vault service_role_key, which is the legacy JWT and passes verify_jwt=true. Currently healthy in prod. | lh-edge-functions | 1 |
| EF-028 |  | filed | `.github/workflows/edge-function-smoke.yml` | The daily edge-function smoke test probes exactly ONE of 71 deployed functions (health-check) and asserts only that it returns 200. Its name and daily cadence read as broad coverage of the edge surface; it is 1/71 = 1.4%. No money endpoint (create-payment, release-payout, instant-payout), no webhook (stripe-webhook, resend-webhook, stripe-idv-webhook) and no admin endpoint is probed. This is why EF-023 (resend-webhook 503 on every delivery) has survived three audit passes with CI fully green - nothing in CI would ever notice. NOT filed as a workflow defect: the assertion it does make is now correct and strict (hard-fails on a missing CRON_SECRET, asserts ==200, and its own header comment documents the earlier '<500 on a 404' weakness that was fixed). The gap is coverage, not correctness. | lh-edge-functions | 1 |
| SI-032 |  | fixed | `RLS predicate columns across 8 tables` | 8 columns used in an RLS qual have no index with them as the leading key, so every policy check on them is a sequential scan. All 8 tables are near-empty today, so this is latent, not live — but job_tracking.helper_id and job_checkins.user_id are written once per location ping / arrival on an ACTIVE job, so they are the two that grow fastest once real jobs run. Free tier makes the cliff sharper. | lh-schema-integrity | 3 |
| SI-033 |  | filed | `prod public schema — B2B removal residue` | The B2B tier removal is COMPLETE in every way that matters: zero business* tables, zero business* functions, and create_business_api_key does NOT exist (to_regprocedure IS NULL). What survives is cosmetic only: 3 orphan columns with no FK and no referent (jobs.business_id, helper_w9_records.business_id, favorite_helpers.business_account_id) plus 3 partial indexes on them. No attack surface, no SECURITY DEFINER residue. Filed so the next census reads this as measured-and-decided rather than re-deriving it. NOTE: profiles.business_name is NOT residue — it serves sole-proprietor helpers. | lh-schema-integrity | 8 |
| SC-015 |  | filed | `live Stripe — three seat products still active for a deleted backend` | The live Stripe account acct_1RQbAfKp2H4b7tEC still carries three ACTIVE recurring products for the business/seat ladder that was deleted from this codebase by migrations 20260828004538 / 20260828011811: Crew (2 seats) $20/mo + $200/yr (prod_UP8XCpifCuHO1y), Team (3 seats) $30/mo + $300/yr (prod_UP8Xdu0Z55uyyZ), Enterprise (4+ seats) $40/mo + $400/yr (prod_UP8XIjp23K25YG). Nothing in the app can sell them and PRODUCT_TO_TIER maps none of them, so a checkout against one would complete and grant NO entitlement — the exact hole the webhook's Slack alert exists to shout about. No customer can currently reach them, which is why this is LOW and not a blocker; it is a hygiene item and an owner action (archiving a Stripe product is a dashboard/API write I did not make). | lh-subscriptions-credits | 2 |
| PD-021 |  | filed | `/my-posts boot (app-wide hooks)` | Exact-duplicate concurrent Supabase requests fire on every authed page load — the same URL issued 2-3x in the same millisecond by different hooks, so React Query cannot dedupe them (different query keys, or no key at all). | lh-perf-deps | 2 |
| VD-003 |  | fixed | `Popover primitive (date picker, time range)` | Radix collisionPadding default 0: PostJob date picker popover sat flush against the right viewport edge (x 41-375) and Availability time-range popover at x 5-305 at 375. Date-picker dialog also had no accessible name. | visual-dialogs | 1 |
| VD-006 |  | filed | `Sheet primitive (CompletionChoiceSheet and every SheetHero sheet)` | SheetHero reserved a pr-12 lane beside the close X but not its height: the X is a 44px box at top 1rem (reaches 60px) while a one-line title is 24px, so full-width content directly under the title ran under the X. Completion sheet at 375: X box 306-350, first option 330-403 (20px overlap, glyph on the card border). | visual-dialogs | 2 |
| VD-007 |  | fixed | `BirthdayPopup (dashboard, on the user's birthday)` | Bespoke birthday card opened with a visible focus ring boxed around its X (Radix autofocused the first tabbable; the shared DialogContent parks focus on the container for this reason), the X was a bare 16px glyph that was also the whole tap target, and at 375 the content-hugging card measured 188px wide so 'Happy Birthday, Audit' broke into three lines. | visual-dialogs | 2 |
| VD-008 |  | fixed | `Dashboard push banner (PushNotificationPrompt) at 375` | The one-line notify banner truncated its own label ('Notify me about new j…'): label got 147px, needed 160, because the global 44px control floor inflates its h-6 Enable and w-10 dismiss past the sizes the row was designed around. | visual-dialogs | 2 |

## POLISH (2)

| ID | Blocker | Status | Surface | Claim | Agent | Evidence |
|---|---|---|---|---|---|---|
| NB-011 |  | filed | `Permission-denied copy across native surfaces` | One OS event — the user denied a permission — is described three different ways, and only one of them tells the user what to do about it. RichMessageInput.tsx:254 gets it right: 'Location access denied — allow it in Settings to share your location.' useUserLocation.ts:67 names the state but offers no recovery: 'Location permission denied.' The three photo call sites (see NB-007) are actively misleading: 'Couldn't open your photos. Please try again.' iOS prompts once per install, so 'try again' can never work and the Settings path is the only recovery. The correct pattern already exists in the codebase; it just is not the one most surfaces use. Cohesion lens: a user who denies location in chat and denies photos in a review gets two different classes of advice for the same kind of event. | lh-native-bridge | 4 |
| SW-005 |  | fixed | `sitemap.xml + /legal ?tab= variants` | The three /legal tabs (terms, community, privacy) each get their own real, distinct canonical URL (PAGE_CANONICALS in legalSections.ts) but none is listed in sitemap.xml — only the bare /legal is. generate-sitemap.mjs derives URLs from <Route path> in App.tsx and has no concept of query-param tab variants, so these three indexable, distinctly-canonicalized pages are undiscoverable except by crawl. | lh-seo-web | 2 |
