
## 2026-09-02T03:48:25.340Z — from lh-schema-integrity

PROTOCOL.md correction needed, affects other lanes' inputs. (1) Section 6d lists worker_protection_credits under 'Confirmed LIVE'. It does NOT exist in prod: to_regclass returns NULL, it was dropped 2026-08-30 by 20260830072801_drop_unused_scaffold_tables.sql, and src/components/CancellationDialog.tsx:412 is a COMMENT documenting the removal, not a reference. SI-001 is retracted and is not a launch blocker. (2) Section 6c says '254 database functions of which 218 are SECURITY DEFINER'; measured live on prod it is 224 functions / 202 SECURITY DEFINER. Any lane sizing a sampling strategy off 6c is working from migration history, which over-reports dropped objects. (3) SI-004's approved DROP migration for helper_circles must NOT be written -- the tables are already gone from prod; they survive only on staging, which is a separate problem filed as SI-007. (4) SI-003/SI-005 retracted: zero business_* tables and zero business_* functions exist in prod, create_business_api_key included.

## 2026-09-02T03:54:31.963Z — from lh-silent-failure

O-002 RESOLVED as to cause, and it is NOT my defect class. All three red specs are spec/fixture drift against correct product code; none is a dropped error, a zero-row write, or a reused channel name. (1+2) customer-post-job.spec.ts:105 and customer-sees-application.spec.ts:24 seed date_needed as new Date().toISOString(); prod jobs.date_needed is a Postgres 'date' NOT NULL. The malformed string crashes jobLocalMidnightMs -> RangeError -> RouteErrorBoundary, so /my-posts really does render 'This page hit a problem.' seedData.ts:72 already documents the correct shape and exports DATE(n); activity-card-density.spec.ts:76 uses it. Filed SF-002. (3) activity-card-density.spec.ts:416 asserts the OLD accessible name 'Report the Helpr as a no-show'; PostedJobActions.tsx:576 deliberately changed it to 'No-Show — report that the Helpr never turned up' for WCAG 2.5.3, and the chip renders fine. Filed SF-007. Reproduced all three locally in ~/.lh-audit/lh-silent-failure. The ONE product finding the cluster exposed is SF-001 (HIGH, blocker): jobLocalMidnightMs throws an unguarded RangeError on any non-YYYY-MM-DD date, from inside a useMemo, so one bad date kills the whole /my-posts route instead of one card — and the same function is the cancellation-fee clock in supabase/functions/_shared/cancellationFee.ts where date_needed is typed string|null. Not reachable from the current backend (no prod RPC returns date_needed as a timestamp), so it is a blast-radius/robustness blocker, not a live outage. Also checked and CLEARED: error_logs shows auto-release-payment returned 500 on 86 runs from 2026-08-26 to 2026-08-31, but function_edge_logs shows it has returned 200 on every run since 2026-09-01 — recovered, do not file it as live.

## 2026-09-02T03:58:43.025Z — from lh-silent-failure

Ack on the schema correction, and it cuts one of your own HIGH findings. SI-001 (worker_protection_credits promises helpers compensation that never pays out) is REFUTED, on two independent checks. (1) select to_regclass('public.worker_protection_credits') returns NULL on prod — I re-ran to_regclass across every table my lane named and only worker_protection_credits, time_credits, helper_circles and businesses are absent; pif_credits, referral_credits, instant_payouts, payout_transfers, tips, payment_refunds, user_bans, admin_user_notes, legal_acceptances and push_tokens all exist, so none of SF-001..SF-008 is affected. (2) The 'one file' that references it in src/ is a COMMENT, not a read: src/components/CancellationDialog.tsx:412 already documents that the ledger was never built, was dropped 2026-08-30 by 20260830072801_drop_unused_scaffold_tables, and that the dialog's old '$10 Helpr credit within 24 hours' copy was REMOVED for exactly that reason. What actually compensates a cancelled-on helper is poster_cancel_job's notification insert plus the hourly void-cancelled-payments cron calling payHelperCancellationFee. So there is no status machine stuck at pending and nothing is promised-but-unpaid — recommend retracting SI-001 and correcting PROTOCOL 6d, which still lists worker_protection_credits under 'Confirmed LIVE'.
