# Morning report — 2026-09-21 (run 10:30 AM)

**11 things need attention**

1. **Main's Test workflow is red on every push since about 9:20 AM.** Lint fails on irregular whitespace in `src/test/helpers/blankNonCode.ts` (lines 101, 139, 140). Latest red: run 35631446646.
2. **The Vacuity gate is red on main.** `src/test/vacuity.baseline.json` still lists `src/lib/deepLinkRoute.test.ts`, which is now registered. The baseline can only shrink, so that line must be removed. Run 35631446559.
3. **The nightly DB drift check flagged a possible security hole.** `public.rpc_add_dispute_evidence` is client-callable, SECURITY DEFINER, and writes to `jobs` from the caller's `_evidence_urls` argument. It also found `src/integrations/supabase/types.ts` out of date against the live schema. Run 35590661433.
4. **Four nightly-red issues have been open for more than a day,** so "Nightly reds nobody read" fails on every push: #1582 press-every-control (204h), #1595 e2e-journeys (195h), #1618 prod-audit (103h), #1626 nightly-webkit (27h). None is acknowledged.
5. **Press-every-control (nightly, 1:54 AM) is red:** 80 presses failed across 4 shards (7, 11, 2 and 60). One sample: on /support, "Other" logged "admin role lookup failed … Profile request timed out".
6. **Prod audit failed all 3 runs in the window.** The admin-referrals form did not render at `/admin?view=referrals`, so no text field was found. The coverage test then failed because that inventory file was left unaccounted for. Latest red: run 35559129731.
7. **The E2E real backend scheduled run at 10:05 AM failed in its pre-sweep.** A stranded E2E job (`e7e09075-a8c6-44b0-9c11-4ee42b8849ea`) is under dispute, and cancel_escrow returned 409, so the sweeper can't clear it. It needs an admin dispute decision or a sweeper change. The push run at 10:22 AM went green, but this will recur on every scheduled run. The sweeper also counts 11 stranded "[E2E DO NOT ACCEPT]" jobs.
8. **Nightly WebKit and E2E user journeys have produced no result since yesterday.** Their only runs in the window, manually dispatched at 9:55 PM, were cancelled, and nothing has run since.
9. **Write-contract snapshot refresh's last run (Sept 19) failed** and it has not run since.
10. **20 "URGENT: main broken" issues are still open,** dated June 27 through Sept 7 (#922 … #1557). They look stale and should be triaged or closed.
11. **Sentry: one regressed issue.** JAVASCRIPT-1H "socket closed: 1006" (Supabase realtime socket) regressed 14 minutes ago: 2 events, 1 user. There are no brand-new issues in the last 24h.

## Production
- Production is **current.** It serves `864fbc144` (9:37 AM today), which includes the newest shipping commit `0ed9d5976` ("fix(money): whole-cent parity").

## Actions (since yesterday 7:30 AM)
- Green or recovered: Vitest, E2E happy-path smoke (earlier browse-feed-completeness reds are now green), UI sweep, mobile spot-check, bundle size, prod freshness, prod errors, race runner, A11y WebKit (prod), DB deploy, edge-functions deploy, DB backup, security audit, uptime, Stripe webhook guard.
- Many runs were cancelled because newer pushes superseded them; each of those workflows later completed a run, except for the items above.
- E2E abuse & notifications has not run in the window (last run Sept 15, green).
- The run list is capped at 1,000, so the scan is per-workflow for the named workflows.

## Issues
- nightly-red: #1626 nightly-webkit, #1618 prod-audit (stale), #1595 e2e-journeys, #1582 press-every-control (stale).
- prod-errors: none open.
- URGENT: 20 open (see item 10).

## Cloud routines
- None of the listed routines is enabled or fired in the last 24h. They are all used-up one-off check-ins from Sept 15–16.
- The list API returned the same first page when given the next-page cursor, so the rest of the list was not readable. The daily 6 AM routines may exist but could not be checked.

## Pull requests
Needing the owner (not dependabot):
- #1607 role-neutral-copy: checks green; waiting on a human look at 375 and copy calls (since Sept 15).
- #1529 fix(e2e) Playwright fixes: 6 failing checks; open since Sept 3.
- #1624, #1623 package-lock resyncs: 2 failing each.
- #1622 docs drift fix, #1621 perf lazy-sentry: failing checks, but under 3 days old.

Dependabot: #1630, #1620, #1599, #1550, #1549, all with failing checks.

No PRs merged in the last day; work lands directly on main.

**Stale routine PRs closed: none.** No PR met every rule. #1621 and #1622 match the routine patterns but are under 3 days old.

## OPEN.md
- 186 unchecked, 257 checked.
- About 13 new items in the last 24h, mostly from the guard-vacuity work:
  - 49 guards delete the code they inspect and should move to `blankNonCode`.
  - No guard asserts a Stripe outflow has a ledger row (`cash-out-credits`).
  - The edge mock never matches chained filters.
  - `verification-webhook` has untested stripe_identity and certificial branches.
  - The iPad "0 jobs" header disagrees with the cards below it.
  - The ipad-13 browse shot is flaky.
  - `RUN_APPSTORE_SHOTS` is in no workflow.
  - `TrustRow`'s separator never renders.
  - The inventory scanner misses Tailwind slash-opacity.
  - Three live AA failures.
  - Four changed sites were never photographed.

## Main (last 24h)
- 171 commits, almost all test infrastructure: vacuity guard proofs (~70), burn-down chart updates (22), other test and guard work (~35), OPEN.md updates (13), merges (10), and a few a11y, smoke, press and vercel commits.
- One money fix: whole-cent parity for the gift-card $10.555 bug.
