# Owner-report repros

One spec per owner report, generated by `npm run repro` BEFORE the fix.
Never commit a spec that still has its placeholder assertion: it fails on
purpose, and the happy-path CI project runs this directory.
